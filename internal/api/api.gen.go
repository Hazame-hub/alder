// Package api provides primitives to interact with the openapi HTTP API.
//
// Code generated by github.com/oapi-codegen/oapi-codegen/v2 version v2.8.0 DO NOT EDIT.
package api

import (
	"bytes"
	"compress/flate"
	"context"
	"encoding/base64"
	"encoding/json"
	"fmt"
	"io"
	"net/http"
	"net/url"
	"path"
	"strings"
	"time"

	"github.com/getkin/kin-openapi/openapi3"
	"github.com/gofiber/fiber/v2"
	"github.com/oapi-codegen/runtime"
)

// Defines values for AttributeComparisonStatus.
const (
	Differs      AttributeComparisonStatus = "differs"
	LeftOnly     AttributeComparisonStatus = "leftOnly"
	RightOnly    AttributeComparisonStatus = "rightOnly"
	Same         AttributeComparisonStatus = "same"
	Undetermined AttributeComparisonStatus = "undetermined"
	Withheld     AttributeComparisonStatus = "withheld"
)

// Valid indicates whether the value is a known member of the AttributeComparisonStatus enum.
func (e AttributeComparisonStatus) Valid() bool {
	switch e {
	case Differs:
		return true
	case LeftOnly:
		return true
	case RightOnly:
		return true
	case Same:
		return true
	case Undetermined:
		return true
	case Withheld:
		return true
	default:
		return false
	}
}

// Defines values for AttributeKindKind.
const (
	Binary      AttributeKindKind = "binary"
	Boolean     AttributeKindKind = "boolean"
	Certificate AttributeKindKind = "certificate"
	Dn          AttributeKindKind = "dn"
	Image       AttributeKindKind = "image"
	Integer     AttributeKindKind = "integer"
	Oid         AttributeKindKind = "oid"
	Password    AttributeKindKind = "password"
	String      AttributeKindKind = "string"
	Text        AttributeKindKind = "text"
	Time        AttributeKindKind = "time"
)

// Valid indicates whether the value is a known member of the AttributeKindKind enum.
func (e AttributeKindKind) Valid() bool {
	switch e {
	case Binary:
		return true
	case Boolean:
		return true
	case Certificate:
		return true
	case Dn:
		return true
	case Image:
		return true
	case Integer:
		return true
	case Oid:
		return true
	case Password:
		return true
	case String:
		return true
	case Text:
		return true
	case Time:
		return true
	default:
		return false
	}
}

// Defines values for AttributeTypeInputUsage.
const (
	UsageDSAOperation         AttributeTypeInputUsage = "dSAOperation"
	UsageDirectoryOperation   AttributeTypeInputUsage = "directoryOperation"
	UsageDistributedOperation AttributeTypeInputUsage = "distributedOperation"
	UsageUserApplications     AttributeTypeInputUsage = "userApplications"
)

// Valid indicates whether the value is a known member of the AttributeTypeInputUsage enum.
func (e AttributeTypeInputUsage) Valid() bool {
	switch e {
	case UsageDSAOperation:
		return true
	case UsageDirectoryOperation:
		return true
	case UsageDistributedOperation:
		return true
	case UsageUserApplications:
		return true
	default:
		return false
	}
}

// Defines values for ChangeModOp.
const (
	ChangeModOpAdd     ChangeModOp = "add"
	ChangeModOpDelete  ChangeModOp = "delete"
	ChangeModOpReplace ChangeModOp = "replace"
)

// Valid indicates whether the value is a known member of the ChangeModOp enum.
func (e ChangeModOp) Valid() bool {
	switch e {
	case ChangeModOpAdd:
		return true
	case ChangeModOpDelete:
		return true
	case ChangeModOpReplace:
		return true
	default:
		return false
	}
}

// Defines values for ChangePackageFormat.
const (
	PackageFormatAlderChangePackage ChangePackageFormat = "alder-change-package"
)

// Valid indicates whether the value is a known member of the ChangePackageFormat enum.
func (e ChangePackageFormat) Valid() bool {
	switch e {
	case PackageFormatAlderChangePackage:
		return true
	default:
		return false
	}
}

// Defines values for ChangeRequestType.
const (
	ChangeRequestTypeAdd         ChangeRequestType = "add"
	ChangeRequestTypeDelete      ChangeRequestType = "delete"
	ChangeRequestTypeModify      ChangeRequestType = "modify"
	ChangeRequestTypeModrdn      ChangeRequestType = "modrdn"
	ChangeRequestTypeSetpassword ChangeRequestType = "setpassword"
)

// Valid indicates whether the value is a known member of the ChangeRequestType enum.
func (e ChangeRequestType) Valid() bool {
	switch e {
	case ChangeRequestTypeAdd:
		return true
	case ChangeRequestTypeDelete:
		return true
	case ChangeRequestTypeModify:
		return true
	case ChangeRequestTypeModrdn:
		return true
	case ChangeRequestTypeSetpassword:
		return true
	default:
		return false
	}
}

// Defines values for ConfigActionable.
const (
	ConfigActionableReadOnly ConfigActionable = "read_only"
	ConfigActionableUnknown  ConfigActionable = "unknown"
	ConfigActionableWritable ConfigActionable = "writable"
)

// Valid indicates whether the value is a known member of the ConfigActionable enum.
func (e ConfigActionable) Valid() bool {
	switch e {
	case ConfigActionableReadOnly:
		return true
	case ConfigActionableUnknown:
		return true
	case ConfigActionableWritable:
		return true
	default:
		return false
	}
}

// Defines values for ConfigMutability.
const (
	ConfigMutabilityReadOnly ConfigMutability = "read_only"
	ConfigMutabilityUnknown  ConfigMutability = "unknown"
	ConfigMutabilityWritable ConfigMutability = "writable"
)

// Valid indicates whether the value is a known member of the ConfigMutability enum.
func (e ConfigMutability) Valid() bool {
	switch e {
	case ConfigMutabilityReadOnly:
		return true
	case ConfigMutabilityUnknown:
		return true
	case ConfigMutabilityWritable:
		return true
	default:
		return false
	}
}

// Defines values for ConfigProvider.
const (
	ConfigProvider389DS    ConfigProvider = "389ds"
	ConfigProviderOpenLDAP ConfigProvider = "openldap"
)

// Valid indicates whether the value is a known member of the ConfigProvider enum.
func (e ConfigProvider) Valid() bool {
	switch e {
	case ConfigProvider389DS:
		return true
	case ConfigProviderOpenLDAP:
		return true
	default:
		return false
	}
}

// Defines values for ConfigSnapshotCompleteness.
const (
	ConfigSnapshotCompletenessComplete ConfigSnapshotCompleteness = "complete"
	ConfigSnapshotCompletenessPartial  ConfigSnapshotCompleteness = "partial"
)

// Valid indicates whether the value is a known member of the ConfigSnapshotCompleteness enum.
func (e ConfigSnapshotCompleteness) Valid() bool {
	switch e {
	case ConfigSnapshotCompletenessComplete:
		return true
	case ConfigSnapshotCompletenessPartial:
		return true
	default:
		return false
	}
}

// Defines values for ConfigSnapshotFormat.
const (
	ConfigSnapshotFormatAlderSnapshot ConfigSnapshotFormat = "alder-snapshot"
)

// Valid indicates whether the value is a known member of the ConfigSnapshotFormat enum.
func (e ConfigSnapshotFormat) Valid() bool {
	switch e {
	case ConfigSnapshotFormatAlderSnapshot:
		return true
	default:
		return false
	}
}

// Defines values for ConfigSnapshotKind.
const (
	ConfigSnapshotKindConfig ConfigSnapshotKind = "config"
)

// Valid indicates whether the value is a known member of the ConfigSnapshotKind enum.
func (e ConfigSnapshotKind) Valid() bool {
	switch e {
	case ConfigSnapshotKindConfig:
		return true
	default:
		return false
	}
}

// Defines values for ConfigSnapshotIncompleteReason.
const (
	InsufficientAccess ConfigSnapshotIncompleteReason = "insufficient_access"
	ReadFailed         ConfigSnapshotIncompleteReason = "read_failed"
	ReadLimitReached   ConfigSnapshotIncompleteReason = "read_limit_reached"
	UnsupportedSection ConfigSnapshotIncompleteReason = "unsupported_section"
)

// Valid indicates whether the value is a known member of the ConfigSnapshotIncompleteReason enum.
func (e ConfigSnapshotIncompleteReason) Valid() bool {
	switch e {
	case InsufficientAccess:
		return true
	case ReadFailed:
		return true
	case ReadLimitReached:
		return true
	case UnsupportedSection:
		return true
	default:
		return false
	}
}

// Defines values for ConnectRequestTls.
const (
	Ldaps     ConnectRequestTls = "ldaps"
	Plaintext ConnectRequestTls = "plaintext"
	Starttls  ConnectRequestTls = "starttls"
)

// Valid indicates whether the value is a known member of the ConnectRequestTls enum.
func (e ConnectRequestTls) Valid() bool {
	switch e {
	case Ldaps:
		return true
	case Plaintext:
		return true
	case Starttls:
		return true
	default:
		return false
	}
}

// Defines values for DestinationKind.
const (
	Entry  DestinationKind = "entry"
	Search DestinationKind = "search"
)

// Valid indicates whether the value is a known member of the DestinationKind enum.
func (e DestinationKind) Valid() bool {
	switch e {
	case Entry:
		return true
	case Search:
		return true
	default:
		return false
	}
}

// Defines values for DiffCandidateBlocked.
const (
	DiffBlockedDependencyCycle            DiffCandidateBlocked = "dependency_cycle"
	DiffBlockedIncompleteComparison       DiffCandidateBlocked = "incomplete_comparison"
	DiffBlockedMetadataOnly               DiffCandidateBlocked = "metadata_only"
	DiffBlockedNonNumericOID              DiffCandidateBlocked = "non_numeric_oid"
	DiffBlockedNothingToChange            DiffCandidateBlocked = "nothing_to_change"
	DiffBlockedOnlyUnchangeableAttributes DiffCandidateBlocked = "only_unchangeable_attributes"
	DiffBlockedSchemaNotEditable          DiffCandidateBlocked = "schema_not_editable"
	DiffBlockedSchemaTargetRequired       DiffCandidateBlocked = "schema_target_required"
	DiffBlockedServerDefined              DiffCandidateBlocked = "server_defined"
	DiffBlockedSourceNotLive              DiffCandidateBlocked = "source_not_live"
	DiffBlockedUnbuildableDefinition      DiffCandidateBlocked = "unbuildable_definition"
	DiffBlockedUnknownDifference          DiffCandidateBlocked = "unknown_difference"
	DiffBlockedUnresolvableRename         DiffCandidateBlocked = "unresolvable_rename"
	DiffBlockedUnresolvedReference        DiffCandidateBlocked = "unresolved_reference"
)

// Valid indicates whether the value is a known member of the DiffCandidateBlocked enum.
func (e DiffCandidateBlocked) Valid() bool {
	switch e {
	case DiffBlockedDependencyCycle:
		return true
	case DiffBlockedIncompleteComparison:
		return true
	case DiffBlockedMetadataOnly:
		return true
	case DiffBlockedNonNumericOID:
		return true
	case DiffBlockedNothingToChange:
		return true
	case DiffBlockedOnlyUnchangeableAttributes:
		return true
	case DiffBlockedSchemaNotEditable:
		return true
	case DiffBlockedSchemaTargetRequired:
		return true
	case DiffBlockedServerDefined:
		return true
	case DiffBlockedSourceNotLive:
		return true
	case DiffBlockedUnbuildableDefinition:
		return true
	case DiffBlockedUnknownDifference:
		return true
	case DiffBlockedUnresolvableRename:
		return true
	case DiffBlockedUnresolvedReference:
		return true
	default:
		return false
	}
}

// Defines values for DiffKind.
const (
	DiffKindAdded        DiffKind = "added"
	DiffKindMetadataOnly DiffKind = "metadata_only"
	DiffKindModified     DiffKind = "modified"
	DiffKindRemoved      DiffKind = "removed"
	DiffKindRenamed      DiffKind = "renamed"
	DiffKindUnchanged    DiffKind = "unchanged"
	DiffKindUnknown      DiffKind = "unknown"
)

// Valid indicates whether the value is a known member of the DiffKind enum.
func (e DiffKind) Valid() bool {
	switch e {
	case DiffKindAdded:
		return true
	case DiffKindMetadataOnly:
		return true
	case DiffKindModified:
		return true
	case DiffKindRemoved:
		return true
	case DiffKindRenamed:
		return true
	case DiffKindUnchanged:
		return true
	case DiffKindUnknown:
		return true
	default:
		return false
	}
}

// Defines values for DiffReasonCode.
const (
	DiffReasonAccessNotVerified  DiffReasonCode = "access_not_verified"
	DiffReasonInsufficientAccess DiffReasonCode = "insufficient_access"
	DiffReasonSchemaPartial      DiffReasonCode = "schema_partial"
	DiffReasonSchemaUnavailable  DiffReasonCode = "schema_unavailable"
	DiffReasonScopeMismatch      DiffReasonCode = "scope_mismatch"
	DiffReasonSearchLimitReached DiffReasonCode = "search_limit_reached"
)

// Valid indicates whether the value is a known member of the DiffReasonCode enum.
func (e DiffReasonCode) Valid() bool {
	switch e {
	case DiffReasonAccessNotVerified:
		return true
	case DiffReasonInsufficientAccess:
		return true
	case DiffReasonSchemaPartial:
		return true
	case DiffReasonSchemaUnavailable:
		return true
	case DiffReasonScopeMismatch:
		return true
	case DiffReasonSearchLimitReached:
		return true
	default:
		return false
	}
}

// Defines values for DiffSideKind.
const (
	DiffSideKindLive     DiffSideKind = "live"
	DiffSideKindSnapshot DiffSideKind = "snapshot"
)

// Valid indicates whether the value is a known member of the DiffSideKind enum.
func (e DiffSideKind) Valid() bool {
	switch e {
	case DiffSideKindLive:
		return true
	case DiffSideKindSnapshot:
		return true
	default:
		return false
	}
}

// Defines values for ErrorCause.
const (
	ErrorCausePlanStale ErrorCause = "plan_stale"
)

// Valid indicates whether the value is a known member of the ErrorCause enum.
func (e ErrorCause) Valid() bool {
	switch e {
	case ErrorCausePlanStale:
		return true
	default:
		return false
	}
}

// Defines values for ErrorError.
const (
	ErrorErrorBadRequest                   ErrorError = "bad_request"
	ErrorErrorConfigModelUnavailable       ErrorError = "config_model_unavailable"
	ErrorErrorConflict                     ErrorError = "conflict"
	ErrorErrorConstraintViolation          ErrorError = "constraint_violation"
	ErrorErrorForbidden                    ErrorError = "forbidden"
	ErrorErrorInternal                     ErrorError = "internal"
	ErrorErrorLdifModeMismatch             ErrorError = "ldif_mode_mismatch"
	ErrorErrorNotFound                     ErrorError = "not_found"
	ErrorErrorPackageChecksumMismatch      ErrorError = "package_checksum_mismatch"
	ErrorErrorPackageInvalid               ErrorError = "package_invalid"
	ErrorErrorPackageTooLarge              ErrorError = "package_too_large"
	ErrorErrorPackageUnsupportedVersion    ErrorError = "package_unsupported_version"
	ErrorErrorPlanMismatch                 ErrorError = "plan_mismatch"
	ErrorErrorPreflightArtifactUnsupported ErrorError = "preflight_artifact_unsupported"
	ErrorErrorRecoveryChecksumMismatch     ErrorError = "recovery_checksum_mismatch"
	ErrorErrorRecoveryInvalid              ErrorError = "recovery_invalid"
	ErrorErrorRecoveryTooLarge             ErrorError = "recovery_too_large"
	ErrorErrorRecoveryUnsupportedVersion   ErrorError = "recovery_unsupported_version"
	ErrorErrorSnapshotChecksumMismatch     ErrorError = "snapshot_checksum_mismatch"
	ErrorErrorSnapshotInvalid              ErrorError = "snapshot_invalid"
	ErrorErrorSnapshotScopeUnsupported     ErrorError = "snapshot_scope_unsupported"
	ErrorErrorSnapshotTooLarge             ErrorError = "snapshot_too_large"
	ErrorErrorSnapshotUnsupportedVersion   ErrorError = "snapshot_unsupported_version"
	ErrorErrorTargetNotAllowed             ErrorError = "target_not_allowed"
	ErrorErrorUnauthorized                 ErrorError = "unauthorized"
	ErrorErrorUpstream                     ErrorError = "upstream"
)

// Valid indicates whether the value is a known member of the ErrorError enum.
func (e ErrorError) Valid() bool {
	switch e {
	case ErrorErrorBadRequest:
		return true
	case ErrorErrorConfigModelUnavailable:
		return true
	case ErrorErrorConflict:
		return true
	case ErrorErrorConstraintViolation:
		return true
	case ErrorErrorForbidden:
		return true
	case ErrorErrorInternal:
		return true
	case ErrorErrorLdifModeMismatch:
		return true
	case ErrorErrorNotFound:
		return true
	case ErrorErrorPackageChecksumMismatch:
		return true
	case ErrorErrorPackageInvalid:
		return true
	case ErrorErrorPackageTooLarge:
		return true
	case ErrorErrorPackageUnsupportedVersion:
		return true
	case ErrorErrorPlanMismatch:
		return true
	case ErrorErrorPreflightArtifactUnsupported:
		return true
	case ErrorErrorRecoveryChecksumMismatch:
		return true
	case ErrorErrorRecoveryInvalid:
		return true
	case ErrorErrorRecoveryTooLarge:
		return true
	case ErrorErrorRecoveryUnsupportedVersion:
		return true
	case ErrorErrorSnapshotChecksumMismatch:
		return true
	case ErrorErrorSnapshotInvalid:
		return true
	case ErrorErrorSnapshotScopeUnsupported:
		return true
	case ErrorErrorSnapshotTooLarge:
		return true
	case ErrorErrorSnapshotUnsupportedVersion:
		return true
	case ErrorErrorTargetNotAllowed:
		return true
	case ErrorErrorUnauthorized:
		return true
	case ErrorErrorUpstream:
		return true
	default:
		return false
	}
}

// Defines values for InventoryRequestScope.
const (
	InventoryRequestScopeBase InventoryRequestScope = "base"
	InventoryRequestScopeOne  InventoryRequestScope = "one"
	InventoryRequestScopeSub  InventoryRequestScope = "sub"
)

// Valid indicates whether the value is a known member of the InventoryRequestScope enum.
func (e InventoryRequestScope) Valid() bool {
	switch e {
	case InventoryRequestScopeBase:
		return true
	case InventoryRequestScopeOne:
		return true
	case InventoryRequestScopeSub:
		return true
	default:
		return false
	}
}

// Defines values for ObjectClassInputKind.
const (
	KindAbstract   ObjectClassInputKind = "ABSTRACT"
	KindAuxiliary  ObjectClassInputKind = "AUXILIARY"
	KindStructural ObjectClassInputKind = "STRUCTURAL"
)

// Valid indicates whether the value is a known member of the ObjectClassInputKind enum.
func (e ObjectClassInputKind) Valid() bool {
	switch e {
	case KindAbstract:
		return true
	case KindAuxiliary:
		return true
	case KindStructural:
		return true
	default:
		return false
	}
}

// Defines values for ObjectClassSummaryKind.
const (
	ABSTRACT   ObjectClassSummaryKind = "ABSTRACT"
	AUXILIARY  ObjectClassSummaryKind = "AUXILIARY"
	STRUCTURAL ObjectClassSummaryKind = "STRUCTURAL"
)

// Valid indicates whether the value is a known member of the ObjectClassSummaryKind enum.
func (e ObjectClassSummaryKind) Valid() bool {
	switch e {
	case ABSTRACT:
		return true
	case AUXILIARY:
		return true
	case STRUCTURAL:
		return true
	default:
		return false
	}
}

// Defines values for ObjectViewId.
const (
	ViewGroups              ObjectViewId = "groups"
	ViewOrganizationalUnits ObjectViewId = "organizationalUnits"
	ViewUsers               ObjectViewId = "users"
)

// Valid indicates whether the value is a known member of the ObjectViewId enum.
func (e ObjectViewId) Valid() bool {
	switch e {
	case ViewGroups:
		return true
	case ViewOrganizationalUnits:
		return true
	case ViewUsers:
		return true
	default:
		return false
	}
}

// Defines values for PackageBuildRequestMethod.
const (
	PackageBuildCLI        PackageBuildRequestMethod = "cli"
	PackageBuildChangeset  PackageBuildRequestMethod = "changeset"
	PackageBuildDataDiff   PackageBuildRequestMethod = "data_diff"
	PackageBuildSchemaDiff PackageBuildRequestMethod = "schema_diff"
)

// Valid indicates whether the value is a known member of the PackageBuildRequestMethod enum.
func (e PackageBuildRequestMethod) Valid() bool {
	switch e {
	case PackageBuildCLI:
		return true
	case PackageBuildChangeset:
		return true
	case PackageBuildDataDiff:
		return true
	case PackageBuildSchemaDiff:
		return true
	default:
		return false
	}
}

// Defines values for PackageChangeIntent.
const (
	PackageIntentDesired PackageChangeIntent = "desired"
	PackageIntentExact   PackageChangeIntent = "exact"
)

// Valid indicates whether the value is a known member of the PackageChangeIntent enum.
func (e PackageChangeIntent) Valid() bool {
	switch e {
	case PackageIntentDesired:
		return true
	case PackageIntentExact:
		return true
	default:
		return false
	}
}

// Defines values for PackageChangeKind.
const (
	PackageKindData   PackageChangeKind = "data"
	PackageKindSchema PackageChangeKind = "schema"
)

// Valid indicates whether the value is a known member of the PackageChangeKind enum.
func (e PackageChangeKind) Valid() bool {
	switch e {
	case PackageKindData:
		return true
	case PackageKindSchema:
		return true
	default:
		return false
	}
}

// Defines values for PackageDataChangeType.
const (
	PackageOpAdd    PackageDataChangeType = "add"
	PackageOpDelete PackageDataChangeType = "delete"
	PackageOpModify PackageDataChangeType = "modify"
	PackageOpRename PackageDataChangeType = "rename"
)

// Valid indicates whether the value is a known member of the PackageDataChangeType enum.
func (e PackageDataChangeType) Valid() bool {
	switch e {
	case PackageOpAdd:
		return true
	case PackageOpDelete:
		return true
	case PackageOpModify:
		return true
	case PackageOpRename:
		return true
	default:
		return false
	}
}

// Defines values for PackageItemStatus.
const (
	PackageStatusAlreadySatisfied   PackageItemStatus = "already_satisfied"
	PackageStatusConflict           PackageItemStatus = "conflict"
	PackageStatusDependencyMissing  PackageItemStatus = "dependency_missing"
	PackageStatusNoOp               PackageItemStatus = "no_op"
	PackageStatusReady              PackageItemStatus = "ready"
	PackageStatusTargetIncompatible PackageItemStatus = "target_incompatible"
	PackageStatusUnknown            PackageItemStatus = "unknown"
	PackageStatusUnsupported        PackageItemStatus = "unsupported"
)

// Valid indicates whether the value is a known member of the PackageItemStatus enum.
func (e PackageItemStatus) Valid() bool {
	switch e {
	case PackageStatusAlreadySatisfied:
		return true
	case PackageStatusConflict:
		return true
	case PackageStatusDependencyMissing:
		return true
	case PackageStatusNoOp:
		return true
	case PackageStatusReady:
		return true
	case PackageStatusTargetIncompatible:
		return true
	case PackageStatusUnknown:
		return true
	case PackageStatusUnsupported:
		return true
	default:
		return false
	}
}

// Defines values for PackageModOp.
const (
	PackageModAdd     PackageModOp = "add"
	PackageModDelete  PackageModOp = "delete"
	PackageModReplace PackageModOp = "replace"
)

// Valid indicates whether the value is a known member of the PackageModOp enum.
func (e PackageModOp) Valid() bool {
	switch e {
	case PackageModAdd:
		return true
	case PackageModDelete:
		return true
	case PackageModReplace:
		return true
	default:
		return false
	}
}

// Defines values for PackageOmittedReason.
const (
	PackageOmittedSecret         PackageOmittedReason = "secret_not_portable"
	PackageOmittedServerSpecific PackageOmittedReason = "server_specific"
	PackageOmittedUnsupported    PackageOmittedReason = "unsupported_change"
)

// Valid indicates whether the value is a known member of the PackageOmittedReason enum.
func (e PackageOmittedReason) Valid() bool {
	switch e {
	case PackageOmittedSecret:
		return true
	case PackageOmittedServerSpecific:
		return true
	case PackageOmittedUnsupported:
		return true
	default:
		return false
	}
}

// Defines values for PackageProblemCode.
const (
	PackageProblemAssumptionUnmet       PackageProblemCode = "assumption_unmet"
	PackageProblemAttributeUndefined    PackageProblemCode = "attribute_undefined"
	PackageProblemDefinitionDiffers     PackageProblemCode = "definition_differs"
	PackageProblemDefinitionMissing     PackageProblemCode = "definition_missing"
	PackageProblemDependencyRequired    PackageProblemCode = "dependency_required"
	PackageProblemEntryExists           PackageProblemCode = "entry_exists"
	PackageProblemEntryMissing          PackageProblemCode = "entry_missing"
	PackageProblemObjectClassUndefined  PackageProblemCode = "object_class_undefined"
	PackageProblemOutsideNamingContexts PackageProblemCode = "outside_naming_contexts"
	PackageProblemParentMissing         PackageProblemCode = "parent_missing"
	PackageProblemReadFailed            PackageProblemCode = "read_failed"
	PackageProblemReferencedBySchema    PackageProblemCode = "referenced_by_schema"
	PackageProblemRenameTargetExists    PackageProblemCode = "rename_target_exists"
	PackageProblemSchemaNotEditable     PackageProblemCode = "schema_not_editable"
	PackageProblemSchemaTargetRequired  PackageProblemCode = "schema_target_required"
	PackageProblemServerDefined         PackageProblemCode = "server_defined"
	PackageProblemUnbuildable           PackageProblemCode = "unbuildable_change"
)

// Valid indicates whether the value is a known member of the PackageProblemCode enum.
func (e PackageProblemCode) Valid() bool {
	switch e {
	case PackageProblemAssumptionUnmet:
		return true
	case PackageProblemAttributeUndefined:
		return true
	case PackageProblemDefinitionDiffers:
		return true
	case PackageProblemDefinitionMissing:
		return true
	case PackageProblemDependencyRequired:
		return true
	case PackageProblemEntryExists:
		return true
	case PackageProblemEntryMissing:
		return true
	case PackageProblemObjectClassUndefined:
		return true
	case PackageProblemOutsideNamingContexts:
		return true
	case PackageProblemParentMissing:
		return true
	case PackageProblemReadFailed:
		return true
	case PackageProblemReferencedBySchema:
		return true
	case PackageProblemRenameTargetExists:
		return true
	case PackageProblemSchemaNotEditable:
		return true
	case PackageProblemSchemaTargetRequired:
		return true
	case PackageProblemServerDefined:
		return true
	case PackageProblemUnbuildable:
		return true
	default:
		return false
	}
}

// Defines values for PackageProvenanceMethod.
const (
	PackageMethodCLI        PackageProvenanceMethod = "cli"
	PackageMethodChangeset  PackageProvenanceMethod = "changeset"
	PackageMethodDataDiff   PackageProvenanceMethod = "data_diff"
	PackageMethodSchemaDiff PackageProvenanceMethod = "schema_diff"
)

// Valid indicates whether the value is a known member of the PackageProvenanceMethod enum.
func (e PackageProvenanceMethod) Valid() bool {
	switch e {
	case PackageMethodCLI:
		return true
	case PackageMethodChangeset:
		return true
	case PackageMethodDataDiff:
		return true
	case PackageMethodSchemaDiff:
		return true
	default:
		return false
	}
}

// Defines values for PackageSchemaChangeElement.
const (
	PackageElementAttributeType PackageSchemaChangeElement = "attributeType"
	PackageElementObjectClass   PackageSchemaChangeElement = "objectClass"
)

// Valid indicates whether the value is a known member of the PackageSchemaChangeElement enum.
func (e PackageSchemaChangeElement) Valid() bool {
	switch e {
	case PackageElementAttributeType:
		return true
	case PackageElementObjectClass:
		return true
	default:
		return false
	}
}

// Defines values for PackageSchemaChangeOp.
const (
	PackageSchemaAdd     PackageSchemaChangeOp = "add"
	PackageSchemaDelete  PackageSchemaChangeOp = "delete"
	PackageSchemaReplace PackageSchemaChangeOp = "replace"
)

// Valid indicates whether the value is a known member of the PackageSchemaChangeOp enum.
func (e PackageSchemaChangeOp) Valid() bool {
	switch e {
	case PackageSchemaAdd:
		return true
	case PackageSchemaDelete:
		return true
	case PackageSchemaReplace:
		return true
	default:
		return false
	}
}

// Defines values for PlanAction.
const (
	PlanActionAdd         PlanAction = "add"
	PlanActionConflict    PlanAction = "conflict"
	PlanActionDelete      PlanAction = "delete"
	PlanActionInvalid     PlanAction = "invalid"
	PlanActionModify      PlanAction = "modify"
	PlanActionRename      PlanAction = "rename"
	PlanActionSetPassword PlanAction = "set_password"
	PlanActionUnchanged   PlanAction = "unchanged"
)

// Valid indicates whether the value is a known member of the PlanAction enum.
func (e PlanAction) Valid() bool {
	switch e {
	case PlanActionAdd:
		return true
	case PlanActionConflict:
		return true
	case PlanActionDelete:
		return true
	case PlanActionInvalid:
		return true
	case PlanActionModify:
		return true
	case PlanActionRename:
		return true
	case PlanActionSetPassword:
		return true
	case PlanActionUnchanged:
		return true
	default:
		return false
	}
}

// Defines values for PlanIntent.
const (
	PlanIntentDesired PlanIntent = "desired"
	PlanIntentExact   PlanIntent = "exact"
)

// Valid indicates whether the value is a known member of the PlanIntent enum.
func (e PlanIntent) Valid() bool {
	switch e {
	case PlanIntentDesired:
		return true
	case PlanIntentExact:
		return true
	default:
		return false
	}
}

// Defines values for PlanLdifMode.
const (
	PlanLdifModeChanges PlanLdifMode = "changes"
	PlanLdifModeDesired PlanLdifMode = "desired"
)

// Valid indicates whether the value is a known member of the PlanLdifMode enum.
func (e PlanLdifMode) Valid() bool {
	switch e {
	case PlanLdifModeChanges:
		return true
	case PlanLdifModeDesired:
		return true
	default:
		return false
	}
}

// Defines values for PlanProblemCode.
const (
	PlanProblemAttributeNotPermitted    PlanProblemCode = "attribute_not_permitted"
	PlanProblemAttributeUndefined       PlanProblemCode = "attribute_undefined"
	PlanProblemDependencyRequired       PlanProblemCode = "dependency_required"
	PlanProblemEntryExists              PlanProblemCode = "entry_exists"
	PlanProblemEntryMissing             PlanProblemCode = "entry_missing"
	PlanProblemExpectedStateDiffers     PlanProblemCode = "expected_state_differs"
	PlanProblemHasChildren              PlanProblemCode = "has_children"
	PlanProblemMissingRequiredAttribute PlanProblemCode = "missing_required_attribute"
	PlanProblemObjectClassUndefined     PlanProblemCode = "object_class_undefined"
	PlanProblemReferencedBySchema       PlanProblemCode = "referenced_by_schema"
	PlanProblemRenameTargetExists       PlanProblemCode = "rename_target_exists"
	PlanProblemSingleValueViolation     PlanProblemCode = "single_value_violation"
)

// Valid indicates whether the value is a known member of the PlanProblemCode enum.
func (e PlanProblemCode) Valid() bool {
	switch e {
	case PlanProblemAttributeNotPermitted:
		return true
	case PlanProblemAttributeUndefined:
		return true
	case PlanProblemDependencyRequired:
		return true
	case PlanProblemEntryExists:
		return true
	case PlanProblemEntryMissing:
		return true
	case PlanProblemExpectedStateDiffers:
		return true
	case PlanProblemHasChildren:
		return true
	case PlanProblemMissingRequiredAttribute:
		return true
	case PlanProblemObjectClassUndefined:
		return true
	case PlanProblemReferencedBySchema:
		return true
	case PlanProblemRenameTargetExists:
		return true
	case PlanProblemSingleValueViolation:
		return true
	default:
		return false
	}
}

// Defines values for PlanTargetKind.
const (
	PlanTargetConfig PlanTargetKind = "config"
	PlanTargetData   PlanTargetKind = "data"
	PlanTargetSchema PlanTargetKind = "schema"
)

// Valid indicates whether the value is a known member of the PlanTargetKind enum.
func (e PlanTargetKind) Valid() bool {
	switch e {
	case PlanTargetConfig:
		return true
	case PlanTargetData:
		return true
	case PlanTargetSchema:
		return true
	default:
		return false
	}
}

// Defines values for PreflightCategory.
const (
	PreflightCategoryArtifact     PreflightCategory = "artifact"
	PreflightCategoryCapabilities PreflightCategory = "capabilities"
	PreflightCategoryEntries      PreflightCategory = "entries"
	PreflightCategoryNaming       PreflightCategory = "naming"
	PreflightCategoryOperational  PreflightCategory = "operational"
	PreflightCategoryReferences   PreflightCategory = "references"
	PreflightCategorySchema       PreflightCategory = "schema"
	PreflightCategorySensitive    PreflightCategory = "sensitive"
)

// Valid indicates whether the value is a known member of the PreflightCategory enum.
func (e PreflightCategory) Valid() bool {
	switch e {
	case PreflightCategoryArtifact:
		return true
	case PreflightCategoryCapabilities:
		return true
	case PreflightCategoryEntries:
		return true
	case PreflightCategoryNaming:
		return true
	case PreflightCategoryOperational:
		return true
	case PreflightCategoryReferences:
		return true
	case PreflightCategorySchema:
		return true
	case PreflightCategorySensitive:
		return true
	default:
		return false
	}
}

// Defines values for PreflightClassification.
const (
	PreflightAlreadySatisfied     PreflightClassification = "already_satisfied"
	PreflightExcluded             PreflightClassification = "excluded"
	PreflightIncompatible         PreflightClassification = "incompatible"
	PreflightPortable             PreflightClassification = "portable"
	PreflightPrerequisiteRequired PreflightClassification = "prerequisite_required"
	PreflightUnknown              PreflightClassification = "unknown"
	PreflightUnsupported          PreflightClassification = "unsupported"
)

// Valid indicates whether the value is a known member of the PreflightClassification enum.
func (e PreflightClassification) Valid() bool {
	switch e {
	case PreflightAlreadySatisfied:
		return true
	case PreflightExcluded:
		return true
	case PreflightIncompatible:
		return true
	case PreflightPortable:
		return true
	case PreflightPrerequisiteRequired:
		return true
	case PreflightUnknown:
		return true
	case PreflightUnsupported:
		return true
	default:
		return false
	}
}

// Defines values for PreflightOverall.
const (
	PreflightCompatible                  PreflightOverall = "compatible"
	PreflightCompatibleWithPrerequisites PreflightOverall = "compatible_with_prerequisites"
	PreflightIncomplete                  PreflightOverall = "incomplete"
	PreflightNotCompatible               PreflightOverall = "incompatible"
)

// Valid indicates whether the value is a known member of the PreflightOverall enum.
func (e PreflightOverall) Valid() bool {
	switch e {
	case PreflightCompatible:
		return true
	case PreflightCompatibleWithPrerequisites:
		return true
	case PreflightIncomplete:
		return true
	case PreflightNotCompatible:
		return true
	default:
		return false
	}
}

// Defines values for PreflightScope.
const (
	PreflightScopeArtifact   PreflightScope = "artifact"
	PreflightScopeAttribute  PreflightScope = "attribute"
	PreflightScopeDefinition PreflightScope = "definition"
	PreflightScopeEntry      PreflightScope = "entry"
	PreflightScopeItem       PreflightScope = "item"
	PreflightScopeValue      PreflightScope = "value"
)

// Valid indicates whether the value is a known member of the PreflightScope enum.
func (e PreflightScope) Valid() bool {
	switch e {
	case PreflightScopeArtifact:
		return true
	case PreflightScopeAttribute:
		return true
	case PreflightScopeDefinition:
		return true
	case PreflightScopeEntry:
		return true
	case PreflightScopeItem:
		return true
	case PreflightScopeValue:
		return true
	default:
		return false
	}
}

// Defines values for PreflightSourceType.
const (
	PreflightSourceChangePackage  PreflightSourceType = "change_package"
	PreflightSourceDataSnapshot   PreflightSourceType = "data_snapshot"
	PreflightSourceSchemaSnapshot PreflightSourceType = "schema_snapshot"
)

// Valid indicates whether the value is a known member of the PreflightSourceType enum.
func (e PreflightSourceType) Valid() bool {
	switch e {
	case PreflightSourceChangePackage:
		return true
	case PreflightSourceDataSnapshot:
		return true
	case PreflightSourceSchemaSnapshot:
		return true
	default:
		return false
	}
}

// Defines values for RecoveryBundleFormat.
const (
	RecoveryFormatAlderRecovery RecoveryBundleFormat = "alder-recovery"
)

// Valid indicates whether the value is a known member of the RecoveryBundleFormat enum.
func (e RecoveryBundleFormat) Valid() bool {
	switch e {
	case RecoveryFormatAlderRecovery:
		return true
	default:
		return false
	}
}

// Defines values for RecoveryChangeType.
const (
	RecoveryChangeTypeAdd    RecoveryChangeType = "add"
	RecoveryChangeTypeDelete RecoveryChangeType = "delete"
	RecoveryChangeTypeModify RecoveryChangeType = "modify"
	RecoveryChangeTypeModrdn RecoveryChangeType = "modrdn"
)

// Valid indicates whether the value is a known member of the RecoveryChangeType enum.
func (e RecoveryChangeType) Valid() bool {
	switch e {
	case RecoveryChangeTypeAdd:
		return true
	case RecoveryChangeTypeDelete:
		return true
	case RecoveryChangeTypeModify:
		return true
	case RecoveryChangeTypeModrdn:
		return true
	default:
		return false
	}
}

// Defines values for RecoveryDriftState.
const (
	RecoveryDriftAlreadyRecovered RecoveryDriftState = "already_recovered"
	RecoveryDriftBlocked          RecoveryDriftState = "blocked"
	RecoveryDriftDrifted          RecoveryDriftState = "drifted"
	RecoveryDriftReady            RecoveryDriftState = "ready"
)

// Valid indicates whether the value is a known member of the RecoveryDriftState enum.
func (e RecoveryDriftState) Valid() bool {
	switch e {
	case RecoveryDriftAlreadyRecovered:
		return true
	case RecoveryDriftBlocked:
		return true
	case RecoveryDriftDrifted:
		return true
	case RecoveryDriftReady:
		return true
	default:
		return false
	}
}

// Defines values for RecoveryModOp.
const (
	RecoveryModOpAdd     RecoveryModOp = "add"
	RecoveryModOpDelete  RecoveryModOp = "delete"
	RecoveryModOpReplace RecoveryModOp = "replace"
)

// Valid indicates whether the value is a known member of the RecoveryModOp enum.
func (e RecoveryModOp) Valid() bool {
	switch e {
	case RecoveryModOpAdd:
		return true
	case RecoveryModOpDelete:
		return true
	case RecoveryModOpReplace:
		return true
	default:
		return false
	}
}

// Defines values for RecoveryReasonCode.
const (
	RecoveryReasonHiddenAttributesUnknown    RecoveryReasonCode = "hidden_attributes_unknown"
	RecoveryReasonIdentityRegenerated        RecoveryReasonCode = "identity_regenerated"
	RecoveryReasonPasswordNotCaptured        RecoveryReasonCode = "password_not_captured"
	RecoveryReasonPreStateUnavailable        RecoveryReasonCode = "pre_state_unavailable"
	RecoveryReasonSchemaOrConfigNotSupported RecoveryReasonCode = "schema_or_config_not_supported"
	RecoveryReasonSensitiveValueNotCaptured  RecoveryReasonCode = "sensitive_value_not_captured"
	RecoveryReasonSensitiveValuesNotRestored RecoveryReasonCode = "sensitive_values_not_restored"
	RecoveryReasonServerOwnedAttribute       RecoveryReasonCode = "server_owned_attribute"
)

// Valid indicates whether the value is a known member of the RecoveryReasonCode enum.
func (e RecoveryReasonCode) Valid() bool {
	switch e {
	case RecoveryReasonHiddenAttributesUnknown:
		return true
	case RecoveryReasonIdentityRegenerated:
		return true
	case RecoveryReasonPasswordNotCaptured:
		return true
	case RecoveryReasonPreStateUnavailable:
		return true
	case RecoveryReasonSchemaOrConfigNotSupported:
		return true
	case RecoveryReasonSensitiveValueNotCaptured:
		return true
	case RecoveryReasonSensitiveValuesNotRestored:
		return true
	case RecoveryReasonServerOwnedAttribute:
		return true
	default:
		return false
	}
}

// Defines values for RecoveryRecoverability.
const (
	RecoverabilityExact       RecoveryRecoverability = "exact"
	RecoverabilityPartial     RecoveryRecoverability = "partial"
	RecoverabilityUnavailable RecoveryRecoverability = "unavailable"
)

// Valid indicates whether the value is a known member of the RecoveryRecoverability enum.
func (e RecoveryRecoverability) Valid() bool {
	switch e {
	case RecoverabilityExact:
		return true
	case RecoverabilityPartial:
		return true
	case RecoverabilityUnavailable:
		return true
	default:
		return false
	}
}

// Defines values for SchemaChangeRequestKind.
const (
	AttributeType SchemaChangeRequestKind = "attributeType"
	ObjectClass   SchemaChangeRequestKind = "objectClass"
)

// Valid indicates whether the value is a known member of the SchemaChangeRequestKind enum.
func (e SchemaChangeRequestKind) Valid() bool {
	switch e {
	case AttributeType:
		return true
	case ObjectClass:
		return true
	default:
		return false
	}
}

// Defines values for SchemaChangeRequestOp.
const (
	SchemaChangeRequestOpAdd     SchemaChangeRequestOp = "add"
	SchemaChangeRequestOpDelete  SchemaChangeRequestOp = "delete"
	SchemaChangeRequestOpReplace SchemaChangeRequestOp = "replace"
)

// Valid indicates whether the value is a known member of the SchemaChangeRequestOp enum.
func (e SchemaChangeRequestOp) Valid() bool {
	switch e {
	case SchemaChangeRequestOpAdd:
		return true
	case SchemaChangeRequestOpDelete:
		return true
	case SchemaChangeRequestOpReplace:
		return true
	default:
		return false
	}
}

// Defines values for SchemaElementKind.
const (
	SchemaElementAttributeType SchemaElementKind = "attributeType"
	SchemaElementObjectClass   SchemaElementKind = "objectClass"
)

// Valid indicates whether the value is a known member of the SchemaElementKind enum.
func (e SchemaElementKind) Valid() bool {
	switch e {
	case SchemaElementAttributeType:
		return true
	case SchemaElementObjectClass:
		return true
	default:
		return false
	}
}

// Defines values for SchemaFieldCategory.
const (
	SchemaFieldCore        SchemaFieldCategory = "core"
	SchemaFieldDescription SchemaFieldCategory = "description"
	SchemaFieldExtension   SchemaFieldCategory = "extension"
)

// Valid indicates whether the value is a known member of the SchemaFieldCategory enum.
func (e SchemaFieldCategory) Valid() bool {
	switch e {
	case SchemaFieldCore:
		return true
	case SchemaFieldDescription:
		return true
	case SchemaFieldExtension:
		return true
	default:
		return false
	}
}

// Defines values for SchemaProblem.
const (
	SchemaProblemAmbiguousName       SchemaProblem = "ambiguous_name"
	SchemaProblemDependencyCycle     SchemaProblem = "dependency_cycle"
	SchemaProblemNonNumericOID       SchemaProblem = "non_numeric_oid"
	SchemaProblemReferencedBySchema  SchemaProblem = "referenced_by_schema"
	SchemaProblemServerDefined       SchemaProblem = "server_defined"
	SchemaProblemUnparsed            SchemaProblem = "unparsed_definition"
	SchemaProblemUnrecognizedKeyword SchemaProblem = "unrecognized_keyword"
	SchemaProblemUnresolvedReference SchemaProblem = "unresolved_reference"
	SchemaProblemUsageUnknown        SchemaProblem = "usage_unknown"
	SchemaProblemUsedByEntries       SchemaProblem = "used_by_entries"
)

// Valid indicates whether the value is a known member of the SchemaProblem enum.
func (e SchemaProblem) Valid() bool {
	switch e {
	case SchemaProblemAmbiguousName:
		return true
	case SchemaProblemDependencyCycle:
		return true
	case SchemaProblemNonNumericOID:
		return true
	case SchemaProblemReferencedBySchema:
		return true
	case SchemaProblemServerDefined:
		return true
	case SchemaProblemUnparsed:
		return true
	case SchemaProblemUnrecognizedKeyword:
		return true
	case SchemaProblemUnresolvedReference:
		return true
	case SchemaProblemUsageUnknown:
		return true
	case SchemaProblemUsedByEntries:
		return true
	default:
		return false
	}
}

// Defines values for SchemaReferenceRelation.
const (
	SchemaRelationMay  SchemaReferenceRelation = "may"
	SchemaRelationMust SchemaReferenceRelation = "must"
	SchemaRelationSup  SchemaReferenceRelation = "sup"
)

// Valid indicates whether the value is a known member of the SchemaReferenceRelation enum.
func (e SchemaReferenceRelation) Valid() bool {
	switch e {
	case SchemaRelationMay:
		return true
	case SchemaRelationMust:
		return true
	case SchemaRelationSup:
		return true
	default:
		return false
	}
}

// Defines values for SchemaSnapshotCompleteness.
const (
	SchemaSnapshotComplete SchemaSnapshotCompleteness = "complete"
	SchemaSnapshotPartial  SchemaSnapshotCompleteness = "partial"
)

// Valid indicates whether the value is a known member of the SchemaSnapshotCompleteness enum.
func (e SchemaSnapshotCompleteness) Valid() bool {
	switch e {
	case SchemaSnapshotComplete:
		return true
	case SchemaSnapshotPartial:
		return true
	default:
		return false
	}
}

// Defines values for SchemaSnapshotFormat.
const (
	SchemaSnapshotFormatAlderSnapshot SchemaSnapshotFormat = "alder-snapshot"
)

// Valid indicates whether the value is a known member of the SchemaSnapshotFormat enum.
func (e SchemaSnapshotFormat) Valid() bool {
	switch e {
	case SchemaSnapshotFormatAlderSnapshot:
		return true
	default:
		return false
	}
}

// Defines values for SchemaSnapshotKind.
const (
	SchemaSnapshotKindSchema SchemaSnapshotKind = "schema"
)

// Valid indicates whether the value is a known member of the SchemaSnapshotKind enum.
func (e SchemaSnapshotKind) Valid() bool {
	switch e {
	case SchemaSnapshotKindSchema:
		return true
	default:
		return false
	}
}

// Defines values for SchemaSnapshotSummaryCompleteness.
const (
	SchemaSummaryComplete SchemaSnapshotSummaryCompleteness = "complete"
	SchemaSummaryPartial  SchemaSnapshotSummaryCompleteness = "partial"
)

// Valid indicates whether the value is a known member of the SchemaSnapshotSummaryCompleteness enum.
func (e SchemaSnapshotSummaryCompleteness) Valid() bool {
	switch e {
	case SchemaSummaryComplete:
		return true
	case SchemaSummaryPartial:
		return true
	default:
		return false
	}
}

// Defines values for SchemaWriteStyle.
const (
	SchemaStyleConfig    SchemaWriteStyle = "config"
	SchemaStyleNone      SchemaWriteStyle = "none"
	SchemaStyleSubschema SchemaWriteStyle = "subschema"
)

// Valid indicates whether the value is a known member of the SchemaWriteStyle enum.
func (e SchemaWriteStyle) Valid() bool {
	switch e {
	case SchemaStyleConfig:
		return true
	case SchemaStyleNone:
		return true
	case SchemaStyleSubschema:
		return true
	default:
		return false
	}
}

// Defines values for SearchRequestScope.
const (
	SearchRequestScopeBase SearchRequestScope = "base"
	SearchRequestScopeOne  SearchRequestScope = "one"
	SearchRequestScopeSub  SearchRequestScope = "sub"
)

// Valid indicates whether the value is a known member of the SearchRequestScope enum.
func (e SearchRequestScope) Valid() bool {
	switch e {
	case SearchRequestScopeBase:
		return true
	case SearchRequestScopeOne:
		return true
	case SearchRequestScopeSub:
		return true
	default:
		return false
	}
}

// Defines values for SnapshotCompleteness.
const (
	SnapshotCompletenessComplete SnapshotCompleteness = "complete"
)

// Valid indicates whether the value is a known member of the SnapshotCompleteness enum.
func (e SnapshotCompleteness) Valid() bool {
	switch e {
	case SnapshotCompletenessComplete:
		return true
	default:
		return false
	}
}

// Defines values for SnapshotFormat.
const (
	SnapshotFormatAlderSnapshot SnapshotFormat = "alder-snapshot"
)

// Valid indicates whether the value is a known member of the SnapshotFormat enum.
func (e SnapshotFormat) Valid() bool {
	switch e {
	case SnapshotFormatAlderSnapshot:
		return true
	default:
		return false
	}
}

// Defines values for SnapshotKind.
const (
	SnapshotKindData SnapshotKind = "data"
)

// Valid indicates whether the value is a known member of the SnapshotKind enum.
func (e SnapshotKind) Valid() bool {
	switch e {
	case SnapshotKindData:
		return true
	default:
		return false
	}
}

// Defines values for SnapshotExcluded.
const (
	SnapshotExcludedOperationalAttributes SnapshotExcluded = "operational-attributes"
	SnapshotExcludedSensitiveValues       SnapshotExcluded = "sensitive-values"
)

// Valid indicates whether the value is a known member of the SnapshotExcluded enum.
func (e SnapshotExcluded) Valid() bool {
	switch e {
	case SnapshotExcludedOperationalAttributes:
		return true
	case SnapshotExcludedSensitiveValues:
		return true
	default:
		return false
	}
}

// Defines values for SnapshotIntegrity.
const (
	SnapshotIntegrityUnverified SnapshotIntegrity = "unverified"
	SnapshotIntegrityVerified   SnapshotIntegrity = "verified"
)

// Valid indicates whether the value is a known member of the SnapshotIntegrity enum.
func (e SnapshotIntegrity) Valid() bool {
	switch e {
	case SnapshotIntegrityUnverified:
		return true
	case SnapshotIntegrityVerified:
		return true
	default:
		return false
	}
}

// Defines values for SnapshotScope.
const (
	SnapshotScopeBase SnapshotScope = "base"
	SnapshotScopeOne  SnapshotScope = "one"
	SnapshotScopeSub  SnapshotScope = "sub"
)

// Valid indicates whether the value is a known member of the SnapshotScope enum.
func (e SnapshotScope) Valid() bool {
	switch e {
	case SnapshotScopeBase:
		return true
	case SnapshotScopeOne:
		return true
	case SnapshotScopeSub:
		return true
	default:
		return false
	}
}

// Defines values for StateKind.
const (
	StateKindConfig StateKind = "config"
	StateKindData   StateKind = "data"
	StateKindSchema StateKind = "schema"
)

// Valid indicates whether the value is a known member of the StateKind enum.
func (e StateKind) Valid() bool {
	switch e {
	case StateKindConfig:
		return true
	case StateKindData:
		return true
	case StateKindSchema:
		return true
	default:
		return false
	}
}

// Defines values for ValueComparisonSide.
const (
	Both  ValueComparisonSide = "both"
	Left  ValueComparisonSide = "left"
	Right ValueComparisonSide = "right"
)

// Valid indicates whether the value is a known member of the ValueComparisonSide enum.
func (e ValueComparisonSide) Valid() bool {
	switch e {
	case Both:
		return true
	case Left:
		return true
	case Right:
		return true
	default:
		return false
	}
}

// Defines values for ExportAnsibleParamsScope.
const (
	ExportAnsibleParamsScopeBase ExportAnsibleParamsScope = "base"
	ExportAnsibleParamsScopeOne  ExportAnsibleParamsScope = "one"
	ExportAnsibleParamsScopeSub  ExportAnsibleParamsScope = "sub"
)

// Valid indicates whether the value is a known member of the ExportAnsibleParamsScope enum.
func (e ExportAnsibleParamsScope) Valid() bool {
	switch e {
	case ExportAnsibleParamsScopeBase:
		return true
	case ExportAnsibleParamsScopeOne:
		return true
	case ExportAnsibleParamsScopeSub:
		return true
	default:
		return false
	}
}

// Defines values for ExportLdifParamsScope.
const (
	ExportLdifParamsScopeBase ExportLdifParamsScope = "base"
	ExportLdifParamsScopeOne  ExportLdifParamsScope = "one"
	ExportLdifParamsScopeSub  ExportLdifParamsScope = "sub"
)

// Valid indicates whether the value is a known member of the ExportLdifParamsScope enum.
func (e ExportLdifParamsScope) Valid() bool {
	switch e {
	case ExportLdifParamsScopeBase:
		return true
	case ExportLdifParamsScopeOne:
		return true
	case ExportLdifParamsScopeSub:
		return true
	default:
		return false
	}
}

// Defines values for ExportOutlineParamsScope.
const (
	ExportOutlineParamsScopeBase ExportOutlineParamsScope = "base"
	ExportOutlineParamsScopeOne  ExportOutlineParamsScope = "one"
	ExportOutlineParamsScopeSub  ExportOutlineParamsScope = "sub"
)

// Valid indicates whether the value is a known member of the ExportOutlineParamsScope enum.
func (e ExportOutlineParamsScope) Valid() bool {
	switch e {
	case ExportOutlineParamsScopeBase:
		return true
	case ExportOutlineParamsScopeOne:
		return true
	case ExportOutlineParamsScopeSub:
		return true
	default:
		return false
	}
}

// Defines values for ExportYamlParamsScope.
const (
	ExportYamlParamsScopeBase ExportYamlParamsScope = "base"
	ExportYamlParamsScopeOne  ExportYamlParamsScope = "one"
	ExportYamlParamsScopeSub  ExportYamlParamsScope = "sub"
)

// Valid indicates whether the value is a known member of the ExportYamlParamsScope enum.
func (e ExportYamlParamsScope) Valid() bool {
	switch e {
	case ExportYamlParamsScopeBase:
		return true
	case ExportYamlParamsScopeOne:
		return true
	case ExportYamlParamsScopeSub:
		return true
	default:
		return false
	}
}

// ApplyResult defines model for ApplyResult.
type ApplyResult struct {
	Applied bool `json:"applied"`

	// Dn The entry's DN after the change, which differs for a rename.
	Dn   string  `json:"dn"`
	Ldif *string `json:"ldif,omitempty"`

	// Note Present when the change was accepted but Alder could not confirm
	// where the entry ended up.
	Note *string `json:"note,omitempty"`

	// Recovery The recovery bundle, when one was asked for. Added in 1.9.
	Recovery *json.RawMessage `json:"recovery,omitempty"`

	// StoredDn Where the entry actually came to rest, when that is not the DN the
	// change was addressed to.
	//
	// A directory may store an added entry under a different name: where
	// an entry's position among its siblings forms part of that name, the
	// server assigns the position and rewrites the RDN. Reporting the
	// requested DN in that case sends the caller to an entry that is not
	// there.
	StoredDn *string `json:"storedDn,omitempty"`
	Summary  *string `json:"summary,omitempty"`
}

// AttributeComparison defines model for AttributeComparison.
type AttributeComparison struct {
	Left ComparedSide `json:"left"`

	// Name The attribute description, options included. `userCertificate;binary`
	// and `userCertificate` are two rows, not one.
	Name  string       `json:"name"`
	Right ComparedSide `json:"right"`

	// Status `withheld` means both entries hold the attribute and it was not
	// compared, because it is sensitive. It is a status of its own rather
	// than `same` with a flag beside it: a client reading only this field
	// must not be able to conclude that two passwords match.
	//
	// `undetermined` means the access rules stopped the comparison. The
	// attribute is on one side and was not returned for the other, and
	// asking the server directly said the bind may not look rather than
	// that the entry lacks it. Reporting that as `leftOnly` would be a
	// statement about the directory that this session has no basis for.
	// The side that could not be read carries `denied`.
	Status    AttributeComparisonStatus `json:"status"`
	Truncated *bool                     `json:"truncated,omitempty"`

	// Values Absent for a withheld attribute.
	Values *[]ValueComparison `json:"values,omitempty"`

	// Withheld Sensitive, so no value crosses the wire from either side. Presence
	// is still reported on each side, because `/entry` already says whether
	// a password is set. Equality is not: reporting whether two entries
	// hold the same password hash would be an oracle about password
	// material the product offers nowhere else.
	Withheld *bool `json:"withheld,omitempty"`
}

// AttributeComparisonStatus `withheld` means both entries hold the attribute and it was not
// compared, because it is sensitive. It is a status of its own rather
// than `same` with a flag beside it: a client reading only this field
// must not be able to conclude that two passwords match.
//
// `undetermined` means the access rules stopped the comparison. The
// attribute is on one side and was not returned for the other, and
// asking the server directly said the bind may not look rather than
// that the entry lacks it. Reporting that as `leftOnly` would be a
// statement about the directory that this session has no basis for.
// The side that could not be read carries `denied`.
type AttributeComparisonStatus string

// AttributeKind The schema's opinion about one attribute, for the editor.
type AttributeKind struct {
	Desc *string           `json:"desc,omitempty"`
	Kind AttributeKindKind `json:"kind"`

	// Known False when the schema does not define this attribute.
	Known       bool    `json:"known"`
	MaxLength   *int    `json:"maxLength,omitempty"`
	Name        string  `json:"name"`
	Obsolete    *bool   `json:"obsolete,omitempty"`
	Oid         *string `json:"oid,omitempty"`
	Operational *bool   `json:"operational,omitempty"`

	// ReadOnly NO-USER-MODIFICATION; the server owns this value.
	ReadOnly *bool `json:"readOnly,omitempty"`

	// Sensitive A secret. Values are withheld and never logged.
	Sensitive   *bool   `json:"sensitive,omitempty"`
	SingleValue *bool   `json:"singleValue,omitempty"`
	Syntax      *string `json:"syntax,omitempty"`
	SyntaxLabel *string `json:"syntaxLabel,omitempty"`
}

// AttributeKindKind defines model for AttributeKind.Kind.
type AttributeKindKind string

// AttributeTypeDetail defines model for AttributeTypeDetail.
type AttributeTypeDetail struct {
	// Edit An attribute type as it declares itself, in the shape a change posts
	// back.
	//
	// The detail responses return one of these so an edit starts from what the
	// definition actually says. The summary alongside it is for reading: it
	// reports effective values, resolved through SUP, which is what somebody
	// looking at an attribute wants to know and exactly what an editor must not
	// write back — a value inherited from a superior would become one declared
	// here.
	Edit *AttributeTypeInput `json:"edit,omitempty"`

	// Kind The schema's opinion about one attribute, for the editor.
	Kind *AttributeKind `json:"kind,omitempty"`

	// OptionalIn Object classes that name this attribute in MAY.
	OptionalIn *[]string `json:"optionalIn,omitempty"`
	Raw        *string   `json:"raw,omitempty"`

	// RequiredBy Object classes that name this attribute in MUST.
	RequiredBy    *[]string            `json:"requiredBy,omitempty"`
	Summary       AttributeTypeSummary `json:"summary"`
	SuperiorChain *[]string            `json:"superiorChain,omitempty"`
}

// AttributeTypeInput An attribute type as it declares itself, in the shape a change posts
// back.
//
// The detail responses return one of these so an edit starts from what the
// definition actually says. The summary alongside it is for reading: it
// reports effective values, resolved through SUP, which is what somebody
// looking at an attribute wants to know and exactly what an editor must not
// write back — a value inherited from a superior would become one declared
// here.
type AttributeTypeInput struct {
	Collective         *bool                    `json:"collective,omitempty"`
	Desc               *string                  `json:"desc,omitempty"`
	Equality           *string                  `json:"equality,omitempty"`
	Extensions         *map[string][]string     `json:"extensions,omitempty"`
	Names              []string                 `json:"names"`
	NoUserModification *bool                    `json:"noUserModification,omitempty"`
	Obsolete           *bool                    `json:"obsolete,omitempty"`
	Oid                string                   `json:"oid"`
	Ordering           *string                  `json:"ordering,omitempty"`
	SingleValue        *bool                    `json:"singleValue,omitempty"`
	Substr             *string                  `json:"substr,omitempty"`
	SuperName          *string                  `json:"superName,omitempty"`
	Syntax             *string                  `json:"syntax,omitempty"`
	SyntaxLen          *int                     `json:"syntaxLen,omitempty"`
	Usage              *AttributeTypeInputUsage `json:"usage,omitempty"`
}

// AttributeTypeInputUsage defines model for AttributeTypeInput.Usage.
type AttributeTypeInputUsage string

// AttributeTypeSummary defines model for AttributeTypeSummary.
type AttributeTypeSummary struct {
	Desc        *string   `json:"desc,omitempty"`
	Equality    *string   `json:"equality,omitempty"`
	Name        string    `json:"name"`
	Names       *[]string `json:"names,omitempty"`
	Obsolete    *bool     `json:"obsolete,omitempty"`
	Oid         string    `json:"oid"`
	Operational *bool     `json:"operational,omitempty"`

	// Origin Where this definition came from, in the server's own words. The
	// X-ORIGIN extension where the server keeps one, and otherwise the
	// schema collection holding it on a server that stores its schema in
	// configuration entries.
	//
	// Deliberately not a "shipped or custom" flag. One of the two servers
	// Alder targets discards X-ORIGIN when it loads a schema file, so on
	// that server such a flag would be a guess. This reports what was
	// actually published, and is absent where nothing was.
	Origin      *string `json:"origin,omitempty"`
	SingleValue *bool   `json:"singleValue,omitempty"`
	Superior    *string `json:"superior,omitempty"`
	Syntax      *string `json:"syntax,omitempty"`
	SyntaxLabel *string `json:"syntaxLabel,omitempty"`
}

// AttributeValue Exactly one of `text` or `base64` is present. `text` carries a value
// that is valid UTF-8 and belongs to a textual syntax; everything else,
// including anything with a `;binary` option, arrives as `base64`.
type AttributeValue struct {
	Base64 *string `json:"base64,omitempty"`

	// Size The value's length in bytes.
	Size *int    `json:"size,omitempty"`
	Text *string `json:"text,omitempty"`
}

// Capabilities defines model for Capabilities.
type Capabilities struct {
	// Config This session's reach into the server's own configuration tree. The DN
	// is preferred from what the server announces and otherwise found by
	// trying the conventional location and believing only what the server
	// answers.
	Config *ConfigAccess `json:"config,omitempty"`

	// ConfigContext The DN of the server's own configuration tree, when it publishes
	// one. Its presence is how Alder knows the subschema subentry is a
	// generated view rather than the schema itself.
	ConfigContext *string `json:"configContext,omitempty"`

	// Monitor A server's monitoring entry, where it publishes one this session can
	// read. Found by reading the conventional location and believing only what
	// the server answers, exactly as the configuration tree is.
	//
	// Absent is an ordinary answer. One of the two servers Alder targets
	// publishes such an entry and the other does not, and a directory with
	// none gets no monitoring section rather than an empty one.
	Monitor        *MonitorAccess `json:"monitor,omitempty"`
	NamingContexts []string       `json:"namingContexts"`
	Paging         bool           `json:"paging"`

	// PasswordModify RFC 3062 Password Modify. Where a server offers it, Alder sets
	// passwords with it rather than writing a hash, so the server chooses
	// the scheme and applies its own policy.
	PasswordModify *bool `json:"passwordModify,omitempty"`

	// SchemaWrite Where, and whether, this session can write schema definitions. Style is
	// derived from what the server announced, never from its name: a server
	// that publishes a configContext keeps its schema in configuration
	// entries, and one that does not lets its subschema subentry be modified.
	SchemaWrite             *SchemaWrite `json:"schemaWrite,omitempty"`
	ServerSort              *bool        `json:"serverSort,omitempty"`
	SubschemaSubentry       string       `json:"subschemaSubentry"`
	SupportedControls       *[]string    `json:"supportedControls,omitempty"`
	SupportedExtensions     *[]string    `json:"supportedExtensions,omitempty"`
	SupportedSaslMechanisms *[]string    `json:"supportedSaslMechanisms,omitempty"`
	WhoAmI                  *bool        `json:"whoAmI,omitempty"`
}

// ChangeAttribute defines model for ChangeAttribute.
type ChangeAttribute struct {
	Name   string           `json:"name"`
	Values []AttributeValue `json:"values"`
}

// ChangeExpectation defines model for ChangeExpectation.
type ChangeExpectation struct {
	// Attributes Each attribute must hold exactly these values, compared by its
	// equality rule where Alder models it and byte for byte otherwise.
	// No values means the attribute must be absent. Sensitive attributes
	// are refused.
	Attributes []ChangeAttribute `json:"attributes"`

	// Exhaustive Absent means false. The entry must hold no other user attribute. `objectClass`,
	// operational and identity attributes, and sensitive attributes are
	// not counted.
	Exhaustive *bool `json:"exhaustive,omitempty"`
}

// ChangeMod defines model for ChangeMod.
type ChangeMod struct {
	Name string      `json:"name"`
	Op   ChangeModOp `json:"op"`

	// Values A delete with no values removes the whole attribute; a delete with
	// values removes exactly those.
	Values *[]AttributeValue `json:"values,omitempty"`
}

// ChangeModOp defines model for ChangeMod.Op.
type ChangeModOp string

// ChangePackage An Alder change package: format `alder-change-package`, version 1 (1.11).
// A portable description of intended changes. It holds no plan token, no
// baseline, no session, no credential and no secret, and it is never
// applied: a target validates it and makes its own plan.
type ChangePackage struct {
	// Assumptions What the package expects of a target, checked at validation. About
	// directory semantics, never about a host: the same package should suit
	// dev, test and production when their directories are compatible.
	Assumptions PackageAssumptions `json:"assumptions"`

	// Changes In the package's canonical order: a dependency before what depends
	// on it, ties broken by identifier. The order a target applies them in
	// comes from the graph.
	Changes []PackageChange `json:"changes"`

	// Checksum SHA-256 over the canonical content without `createdAt` and without
	// the checksum itself, so the same intent captured twice has the same
	// checksum. Integrity, not authentication: anyone who edits a package
	// can recompute it. Packages are not signed.
	Checksum    *string             `json:"checksum,omitempty"`
	Counts      PackageCounts       `json:"counts"`
	CreatedAt   time.Time           `json:"createdAt"`
	Description *string             `json:"description,omitempty"`
	Format      ChangePackageFormat `json:"format"`

	// Id A generated UUID, stable across copies and independent of the
	// filename. Provenance only: it authorises nothing and Alder keeps no
	// record of it.
	Id      string           `json:"id"`
	Omitted []PackageOmitted `json:"omitted"`

	// Source Where a package came from. Informational: a package proves nothing about
	// its origin, and nothing here grants any authority. No host, no bind DN,
	// no path, no credential.
	Source  PackageProvenance `json:"source"`
	Title   *string           `json:"title,omitempty"`
	Version int               `json:"version"`
}

// ChangePackageFormat defines model for ChangePackage.Format.
type ChangePackageFormat string

// ChangePreview defines model for ChangePreview.
type ChangePreview struct {
	AffectedAttributes *[]string `json:"affectedAttributes,omitempty"`
	Ansible            string    `json:"ansible"`
	AnsiblePlaybook    *string   `json:"ansiblePlaybook,omitempty"`

	// Ldif Unfolded, because the point of the preview is that a person reads
	// it and a value broken at column 76 is harder to check.
	Ldif string `json:"ldif"`

	// LdifFolded Folded at 76 columns, for download.
	LdifFolded *string `json:"ldifFolded,omitempty"`
	Summary    string  `json:"summary"`

	// Warnings Schema findings that do not block the change, such as setting an
	// attribute no object class on the entry permits.
	Warnings *[]string `json:"warnings,omitempty"`
}

// ChangeRequest defines model for ChangeRequest.
type ChangeRequest struct {
	Attributes *[]ChangeAttribute `json:"attributes,omitempty"`

	// Baseline The `baseline` this change was planned against, from
	// `POST /plan`. Optional: a change without one is applied exactly as
	// it was before this field existed.
	//
	// When present, the server re-reads the entry before applying and
	// refuses with `409` if what the plan depended on has changed since.
	// It is checked against a fingerprint the server recomputes; the value
	// a client sends is never trusted as a description of anything.
	Baseline     *string            `json:"baseline,omitempty"`
	DeleteOldRdn *bool              `json:"deleteOldRdn,omitempty"`
	Dn           string             `json:"dn"`
	Expect       *ChangeExpectation `json:"expect,omitempty"`
	Mods         *[]ChangeMod       `json:"mods,omitempty"`

	// NewPassword The new password, for `setpassword`. Write-only: it is never echoed
	// by the preview, never rendered into the LDIF or the Ansible task,
	// and never logged. A `setpassword` change is an RFC 3062 extended
	// operation rather than a modification, so it has no LDIF form; the
	// preview says so and shows the equivalent `ldappasswd` invocation.
	NewPassword *string           `json:"newPassword,omitempty"`
	NewRdn      *string           `json:"newRdn,omitempty"`
	NewSuperior *string           `json:"newSuperior,omitempty"`
	Type        ChangeRequestType `json:"type"`
}

// ChangeRequestType defines model for ChangeRequest.Type.
type ChangeRequestType string

// ChangesetOutcome defines model for ChangesetOutcome.
type ChangesetOutcome struct {
	Applied bool   `json:"applied"`
	Dn      string `json:"dn"`
	Error   *Error `json:"error,omitempty"`

	// Index Position in the submitted list.
	Index   int     `json:"index"`
	Summary *string `json:"summary,omitempty"`
}

// ChangesetPreview defines model for ChangesetPreview.
type ChangesetPreview struct {
	// AnsiblePlaybook One playbook containing every task, in the same order.
	AnsiblePlaybook *string         `json:"ansiblePlaybook,omitempty"`
	Changes         []ChangePreview `json:"changes"`

	// Ldif Every change as one RFC 2849 document, in order. A password change
	// appears as the notice explaining it has no LDIF form, so the
	// document describes the run rather than silently omitting a step.
	Ldif string `json:"ldif"`

	// Warnings Findings about the set as a whole, such as an entry created before
	// its parent, or the same DN changed twice.
	Warnings *[]string `json:"warnings,omitempty"`
}

// ChangesetRequest defines model for ChangesetRequest.
type ChangesetRequest struct {
	// Changes The changes, in the order they should be applied. A cap exists
	// because the whole set is rendered and applied in one request.
	Changes []ChangeRequest `json:"changes"`

	// Recovery Return a recovery bundle for the changes that were applied. Absent
	// means false. Read
	// by `POST /changeset/apply` only. Added in 1.9.
	Recovery *bool `json:"recovery,omitempty"`
}

// ChangesetResult defines model for ChangesetResult.
type ChangesetResult struct {
	AppliedCount int `json:"appliedCount"`

	// FailedIndex The index that failed, absent when every change succeeded.
	FailedIndex *int `json:"failedIndex,omitempty"`

	// Outcomes One per submitted change, in order. Entries after a failure are
	// reported as not applied rather than omitted, so the caller can see
	// what remains.
	Outcomes []ChangesetOutcome `json:"outcomes"`

	// Recovery The recovery bundle, when one was asked for and at least one change
	// was applied. It covers the applied changes only. Added in 1.9.
	Recovery *json.RawMessage `json:"recovery,omitempty"`
}

// ComparedEntry defines model for ComparedEntry.
type ComparedEntry struct {
	Dn            string    `json:"dn"`
	ObjectClasses *[]string `json:"objectClasses,omitempty"`
	Rdn           *string   `json:"rdn,omitempty"`

	// Structural The single structural class. A difference here explains most of the
	// rows below it and should be read first.
	Structural *string `json:"structural,omitempty"`
}

// ComparedSide defines model for ComparedSide.
type ComparedSide struct {
	// Denied The access rules forbid this bind from reading the attribute on this
	// side, so its absence here says nothing about the entry. Set only
	// after the server was asked directly; a plain read cannot tell this
	// from an attribute the entry does not hold.
	Denied *bool `json:"denied,omitempty"`

	// Permitted Permitted by this entry's own classes. Present but not permitted is
	// reachable through an extensibleObject or a DIT content rule, and is
	// a finding rather than a curiosity.
	Permitted bool `json:"permitted"`
	Present   bool `json:"present"`

	// Required Required by *this* entry's own classes. Required and absent is
	// sometimes the whole answer, and is invisible to a diff of what is
	// present.
	Required   bool `json:"required"`
	ValueCount int  `json:"valueCount"`

	// ValueSchemes The storage scheme of each withheld value, never the value.
	ValueSchemes *[]string `json:"valueSchemes,omitempty"`
}

// ComparisonCounts One bucket per status, and every attribute lands in exactly one, so the
// six sum to the length of `attributes`.
type ComparisonCounts struct {
	Differs   int `json:"differs"`
	LeftOnly  int `json:"leftOnly"`
	RightOnly int `json:"rightOnly"`
	Same      int `json:"same"`

	// Undetermined One-sided, and the missing side could not be read. Counted apart
	// from `leftOnly` and `rightOnly` because those name a difference
	// between two entries and this names a limit on what the session may
	// see.
	Undetermined int `json:"undetermined"`

	// Withheld Held by both and not compared. Counted separately rather than folded
	// into `same`, which would be a claim about password material.
	Withheld int `json:"withheld"`
}

// ConfigAccess This session's reach into the server's own configuration tree. The DN
// is preferred from what the server announces and otherwise found by
// trying the conventional location and believing only what the server
// answers.
type ConfigAccess struct {
	// BoundAs The identity the configuration tree is read as.
	BoundAs *string `json:"boundAs,omitempty"`

	// Dn The configuration tree's root. Absent where there is none.
	Dn *string `json:"dn,omitempty"`

	// Readable Whether this session can actually read it. A server may announce a
	// tree the bound identity has no rights in.
	Readable bool `json:"readable"`

	// Reason Why it is unreachable, in a sentence meant to be read.
	Reason *string `json:"reason,omitempty"`

	// SeparateBind Whether a second identity is being used for it.
	SeparateBind bool `json:"separateBind"`
}

// ConfigActionable What Alder could do about a difference. `writable` means Alder already
// changes that exact setting through the ordinary plan; a comparison
// never creates a new way to write configuration, so everything else is
// reported and never staged.
type ConfigActionable string

// ConfigDiff A configuration comparison. When `providerMismatch` is true the two
// sides are different providers' configuration: nothing was compared
// setting by setting, `items` is empty, and `source` and `target` say what
// each side holds. Alder does not translate configuration between
// providers and does not synthesise added and removed settings.
type ConfigDiff struct {
	Complete    bool             `json:"complete"`
	Counts      ConfigDiffCounts `json:"counts"`
	CrossVendor bool             `json:"crossVendor"`
	Items       []ConfigDiffItem `json:"items"`

	// Provider The configuration model a snapshot belongs to. Configuration is
	// provider-specific: two providers' settings are not the same settings,
	// and Alder does not translate between them.
	Provider         *ConfigProvider       `json:"provider,omitempty"`
	ProviderMismatch bool                  `json:"providerMismatch"`
	Sections         []ConfigSectionCounts `json:"sections"`

	// Source What one side holds, in the terms that survive a provider mismatch:
	// which sections it has and how much is in them. When the two sides are
	// different providers this is the whole answer.
	Source ConfigProviderSummary `json:"source"`

	// Target What one side holds, in the terms that survive a provider mismatch:
	// which sections it has and how much is in them. When the two sides are
	// different providers this is the whole answer.
	Target ConfigProviderSummary `json:"target"`
}

// ConfigDiffCounts defines model for ConfigDiffCounts.
type ConfigDiffCounts struct {
	// Actionable How many differences Alder could change through the ordinary plan.
	Actionable int `json:"actionable"`
	Added      int `json:"added"`
	Compared   int `json:"compared"`
	Modified   int `json:"modified"`
	Removed    int `json:"removed"`
	Unchanged  int `json:"unchanged"`
	Unknown    int `json:"unknown"`
}

// ConfigDiffItem defines model for ConfigDiffItem.
type ConfigDiffItem struct {
	// Actionable What Alder could do about a difference. `writable` means Alder already
	// changes that exact setting through the ordinary plan; a comparison
	// never creates a new way to write configuration, so everything else is
	// reported and never staged.
	Actionable ConfigActionable `json:"actionable"`

	// Candidate Change requests that would move the live source toward the target: input
	// for `POST /plan`, never applied from here.
	Candidate *DiffCandidate `json:"candidate,omitempty"`

	// Comparison `normalised` when the model understands the value, `raw` when it is
	// compared as text because nothing here parses it.
	Comparison *string `json:"comparison,omitempty"`

	// Dn The configuration entry the setting lives on.
	Dn *string `json:"dn,omitempty"`

	// Id The setting's identity within the provider's model: section,
	// resource and key. Stable across captures, and never a position.
	Id string `json:"id"`

	// Key The setting's name in the provider's own vocabulary.
	Key string `json:"key"`

	// Kind `metadata_only` (1.10) is a schema definition whose meaning is equal and whose X- extensions differ.
	Kind DiffKind `json:"kind"`

	// Mutability What the provider's model says about a setting: whether it is something
	// an administrator sets. It is not a statement about this session's
	// rights, and not a promise that Alder can change it.
	Mutability *ConfigMutability `json:"mutability,omitempty"`

	// Operational The value names something about the machine -- a path, a host, a
	// port, a file. Not a secret, and not something to publish carelessly.
	Operational *bool `json:"operational,omitempty"`

	// Ordered The order of the values is part of the setting, so a comparison keeps it.
	Ordered  *bool     `json:"ordered,omitempty"`
	Problems *[]string `json:"problems,omitempty"`

	// Resource The resource the setting belongs to
	Resource      *string `json:"resource,omitempty"`
	ResourceLabel *string `json:"resourceLabel,omitempty"`
	Section       string  `json:"section"`

	// Sensitive The value is a secret and was never recorded; only whether it is present.
	Sensitive *bool `json:"sensitive,omitempty"`

	// Source The values the source holds. A withheld setting has none.
	Source *[]string `json:"source,omitempty"`
	Target *[]string `json:"target,omitempty"`

	// Type string, int, bool, dn, path, url, structured or binary.
	Type *string `json:"type,omitempty"`
}

// ConfigMutability What the provider's model says about a setting: whether it is something
// an administrator sets. It is not a statement about this session's
// rights, and not a promise that Alder can change it.
type ConfigMutability string

// ConfigProvider The configuration model a snapshot belongs to. Configuration is
// provider-specific: two providers' settings are not the same settings,
// and Alder does not translate between them.
type ConfigProvider string

// ConfigProviderSummary What one side holds, in the terms that survive a provider mismatch:
// which sections it has and how much is in them. When the two sides are
// different providers this is the whole answer.
type ConfigProviderSummary struct {
	// Completeness complete or partial.
	Completeness string `json:"completeness"`

	// Provider The configuration model a snapshot belongs to. Configuration is
	// provider-specific: two providers' settings are not the same settings,
	// and Alder does not translate between them.
	Provider  ConfigProvider `json:"provider"`
	Resources int            `json:"resources"`

	// Root The configuration tree the server announced or answered at.
	Root          *string               `json:"root,omitempty"`
	Sections      []ConfigSectionTotals `json:"sections"`
	Settings      int                   `json:"settings"`
	Vendor        *string               `json:"vendor,omitempty"`
	VendorVersion *string               `json:"vendorVersion,omitempty"`
}

// ConfigResource One configuration object a provider repeats: a database, an overlay, a
// module, a backend, a plugin. Identified by what the provider names it
// by -- a suffix, an overlay's name -- never by its position.
type ConfigResource struct {
	Dn *string `json:"dn,omitempty"`

	// Kind global, database, overlay, module, backend, plugin, encryption, mapping-tree, replication or area.
	Kind    string  `json:"kind"`
	Label   *string `json:"label,omitempty"`
	Name    string  `json:"name"`
	Section string  `json:"section"`
}

// ConfigSectionCounts defines model for ConfigSectionCounts.
type ConfigSectionCounts struct {
	Counts  ConfigDiffCounts `json:"counts"`
	Section string           `json:"section"`
}

// ConfigSectionTotals defines model for ConfigSectionTotals.
type ConfigSectionTotals struct {
	Section  string `json:"section"`
	Settings int    `json:"settings"`
}

// ConfigSetting One attribute of one configuration entry, normalised by the provider's model.
type ConfigSetting struct {
	// Comparison normalised or raw.
	Comparison string  `json:"comparison"`
	Dn         *string `json:"dn,omitempty"`
	Key        string  `json:"key"`

	// Mutability What the provider's model says about a setting: whether it is something
	// an administrator sets. It is not a statement about this session's
	// rights, and not a promise that Alder can change it.
	Mutability  ConfigMutability `json:"mutability"`
	Operational *bool            `json:"operational,omitempty"`

	// Ordered The order of the values carries meaning.
	Ordered *bool `json:"ordered,omitempty"`

	// Resource The resource's identity; absent for a provider's global settings.
	Resource  *string `json:"resource,omitempty"`
	Section   string  `json:"section"`
	Sensitive *bool   `json:"sensitive,omitempty"`
	Type      string  `json:"type"`

	// Values The values, canonicalised. A withheld setting has none.
	Values *[]string `json:"values,omitempty"`

	// Withheld How many values a sensitive setting had.
	Withheld *int `json:"withheld,omitempty"`
}

// ConfigSnapshot A configuration snapshot document: `alder-snapshot` version 1 with kind
// `config`, introduced in Alder 1.13. Releases before 1.13 refuse it as a
// kind they do not know. It holds one server's persistent configuration
// as that server's software arranges it; it is not portable to another
// product, and Alder never translates one into another.
type ConfigSnapshot struct {
	Checksum     *string                    `json:"checksum,omitempty"`
	Completeness ConfigSnapshotCompleteness `json:"completeness"`
	Counts       ConfigSnapshotCounts       `json:"counts"`
	Coverage     ConfigSnapshotCoverage     `json:"coverage"`
	CreatedAt    time.Time                  `json:"createdAt"`
	Format       ConfigSnapshotFormat       `json:"format"`
	Incomplete   []ConfigSnapshotIncomplete `json:"incomplete"`
	Kind         ConfigSnapshotKind         `json:"kind"`
	Resources    []ConfigResource           `json:"resources"`
	Settings     []ConfigSetting            `json:"settings"`
	Source       ConfigSnapshotSource       `json:"source"`
	Version      int                        `json:"version"`
}

// ConfigSnapshotCompleteness defines model for ConfigSnapshot.Completeness.
type ConfigSnapshotCompleteness string

// ConfigSnapshotFormat defines model for ConfigSnapshot.Format.
type ConfigSnapshotFormat string

// ConfigSnapshotKind defines model for ConfigSnapshot.Kind.
type ConfigSnapshotKind string

// ConfigSnapshotCounts defines model for ConfigSnapshotCounts.
type ConfigSnapshotCounts struct {
	// Operational Settings whose values name something about the machine.
	Operational int `json:"operational"`
	ReadOnly    int `json:"readOnly"`
	Resources   int `json:"resources"`
	Settings    int `json:"settings"`

	// Withheld Settings whose values are secrets and were never recorded.
	Withheld int `json:"withheld"`
}

// ConfigSnapshotCoverage defines model for ConfigSnapshotCoverage.
type ConfigSnapshotCoverage struct {
	// Excluded What is left out on purpose, as stable identifiers: `runtime-state`,
	// `schema-definitions`, `sensitive-values` and
	// `server-maintained-attributes`.
	Excluded []string `json:"excluded"`

	// Sections The categories this provider's model covers.
	Sections []string `json:"sections"`
}

// ConfigSnapshotIncomplete One reason a capture did not cover everything.
type ConfigSnapshotIncomplete struct {
	Detail *string                        `json:"detail,omitempty"`
	Reason ConfigSnapshotIncompleteReason `json:"reason"`
	Scope  *string                        `json:"scope,omitempty"`
}

// ConfigSnapshotIncompleteReason defines model for ConfigSnapshotIncomplete.Reason.
type ConfigSnapshotIncompleteReason string

// ConfigSnapshotSource defines model for ConfigSnapshotSource.
type ConfigSnapshotSource struct {
	// Provider The configuration model a snapshot belongs to. Configuration is
	// provider-specific: two providers' settings are not the same settings,
	// and Alder does not translate between them.
	Provider ConfigProvider `json:"provider"`

	// Root The configuration tree the server announced or answered at.
	Root          string  `json:"root"`
	Vendor        *string `json:"vendor,omitempty"`
	VendorVersion *string `json:"vendorVersion,omitempty"`
}

// ConnectRequest defines model for ConnectRequest.
type ConnectRequest struct {
	// BindDn Omit for an anonymous bind.
	BindDn *string `json:"bindDn,omitempty"`

	// BindPassword Held in memory for the life of the session and nowhere else.
	BindPassword *string `json:"bindPassword,omitempty"`

	// CaCertificate A PEM bundle to verify against instead of the system roots.
	CaCertificate *string `json:"caCertificate,omitempty"`

	// ConfigBindDn An optional second identity, used only for the server's own
	// configuration tree.
	//
	// A directory keeps its configuration beside its data, and the account
	// that administers a suffix normally has no rights there; they are
	// separate administrative domains on purpose. Without this, reaching
	// the configuration means connecting as the configuration
	// administrator and losing access to the data — which on a server that
	// stores its schema in its configuration makes schema editing and
	// entry browsing mutually exclusive. Operations are routed by DN, so
	// one session does both.
	ConfigBindDn *string `json:"configBindDn,omitempty"`

	// ConfigBindPassword Held in the server's memory for the life of the session, exactly
	// like the bind password. Never written to disk, never returned.
	ConfigBindPassword *string `json:"configBindPassword,omitempty"`

	// Host A host name or an IP literal. Where the operator has configured an
	// allowlist, a host and port outside it is refused with `403` and
	// `target_not_allowed` before any connection is opened.
	Host string `json:"host"`

	// InsecureSkipVerify Skip certificate verification for this connection. Per-connection
	// and never a default; a session using it is marked unverified.
	InsecureSkipVerify *bool `json:"insecureSkipVerify,omitempty"`
	Port               int   `json:"port"`

	// ServerName Override the name checked against the certificate.
	ServerName *string `json:"serverName,omitempty"`

	// Tls `plaintext` is refused unless the server was started with
	// `--i-know-this-is-insecure`.
	Tls ConnectRequestTls `json:"tls"`
}

// ConnectRequestTls `plaintext` is refused unless the server was started with
// `--i-know-this-is-insecure`.
type ConnectRequestTls string

// CountResult defines model for CountResult.
type CountResult struct {
	Count int     `json:"count"`
	Took  *string `json:"took,omitempty"`

	// Truncated True when the limit was reached, so the real total is higher and the
	// count must be reported as "at least".
	Truncated bool `json:"truncated"`
}

// Destination defines model for Destination.
type Destination struct {
	Base *string `json:"base,omitempty"`

	// Dn Set for an entry destination; the DN, canonically rendered.
	Dn *string `json:"dn,omitempty"`

	// Filter Set for a search destination, built by the filter package and
	// escaped — the text came from a box somebody typed in.
	Filter *string         `json:"filter,omitempty"`
	Kind   DestinationKind `json:"kind"`
	Label  string          `json:"label"`
}

// DestinationKind defines model for Destination.Kind.
type DestinationKind string

// Diff defines model for Diff.
type Diff struct {
	ComparedByBytes *[]string `json:"comparedByBytes,omitempty"`
	Complete        bool      `json:"complete"`

	// Config A configuration comparison. When `providerMismatch` is true the two
	// sides are different providers' configuration: nothing was compared
	// setting by setting, `items` is empty, and `source` and `target` say what
	// each side holds. Alder does not translate configuration between
	// providers and does not synthesise added and removed settings.
	Config      *ConfigDiff `json:"config,omitempty"`
	Counts      DiffCounts  `json:"counts"`
	CrossVendor bool        `json:"crossVendor"`
	Items       []DiffItem  `json:"items"`

	// Kind What a snapshot or comparison is of: directory data, the published
	// schema (1.10), or the server's own configuration (1.13). A release
	// before the one that added a kind refuses a snapshot of it rather than
	// misreading it.
	Kind               StateKind       `json:"kind"`
	OperationalIgnored bool            `json:"operationalIgnored"`
	Reasons            *[]DiffReason   `json:"reasons,omitempty"`
	RuleDifferences    *[]string       `json:"ruleDifferences,omitempty"`
	Schema             *SchemaDiff     `json:"schema,omitempty"`
	Source             DiffSideSummary `json:"source"`
	Target             DiffSideSummary `json:"target"`
}

// DiffAttributeChange defines model for DiffAttributeChange.
type DiffAttributeChange struct {
	Added *[]SnapshotValue `json:"added,omitempty"`

	// AddedOmitted Values in the target and not the source beyond the 1
	AddedOmitted    *int  `json:"addedOmitted,omitempty"`
	ComparedByBytes *bool `json:"comparedByBytes,omitempty"`

	// Kind `metadata_only` (1.10) is a schema definition whose meaning is equal and whose X- extensions differ.
	Kind        DiffKind         `json:"kind"`
	Name        string           `json:"name"`
	Operational *bool            `json:"operational,omitempty"`
	Removed     *[]SnapshotValue `json:"removed,omitempty"`

	// RemovedOmitted Values in the source and not the target beyond the 1
	RemovedOmitted *int `json:"removedOmitted,omitempty"`

	// Sensitive Compared by withheld count only; no values are ever listed.
	Sensitive      *bool           `json:"sensitive,omitempty"`
	UnknownReason  *DiffReasonCode `json:"unknownReason,omitempty"`
	WithheldSource *int            `json:"withheldSource,omitempty"`
	WithheldTarget *int            `json:"withheldTarget,omitempty"`
}

// DiffCandidate Change requests that would move the live source toward the target: input
// for `POST /plan`, never applied from here.
type DiffCandidate struct {
	Blocked *DiffCandidateBlocked `json:"blocked,omitempty"`
	Changes []ChangeRequest       `json:"changes"`

	// Destructive The candidate deletes an entry. Never selected for the caller.
	Destructive bool `json:"destructive"`
}

// DiffCandidateBlocked defines model for DiffCandidateBlocked.
type DiffCandidateBlocked string

// DiffCounts defines model for DiffCounts.
type DiffCounts struct {
	Added     int `json:"added"`
	Compared  int `json:"compared"`
	Modified  int `json:"modified"`
	Removed   int `json:"removed"`
	Renamed   int `json:"renamed"`
	Unchanged int `json:"unchanged"`
	Unknown   int `json:"unknown"`
}

// DiffItem defines model for DiffItem.
type DiffItem struct {
	Attributes *[]DiffAttributeChange `json:"attributes,omitempty"`

	// Candidate Change requests that would move the live source toward the target: input
	// for `POST /plan`, never applied from here.
	Candidate *DiffCandidate `json:"candidate,omitempty"`

	// Kind `metadata_only` (1.10) is a schema definition whose meaning is equal and whose X- extensions differ.
	Kind     DiffKind        `json:"kind"`
	Reason   *DiffReasonCode `json:"reason,omitempty"`
	SourceDn *string         `json:"sourceDn,omitempty"`
	TargetDn *string         `json:"targetDn,omitempty"`
}

// DiffKind `metadata_only` (1.10) is a schema definition whose meaning is equal and whose X- extensions differ.
type DiffKind string

// DiffLiveSide The live directory. Omitted fields are taken from the other side's
// snapshot, so `{}` means "the same subtree, as it is now".
type DiffLiveSide struct {
	Base   *string `json:"base,omitempty"`
	Filter *string `json:"filter,omitempty"`

	// Kind What a snapshot or comparison is of: directory data, the published
	// schema (1.10), or the server's own configuration (1.13). A release
	// before the one that added a kind refuses a snapshot of it rather than
	// misreading it.
	Kind                  *StateKind `json:"kind,omitempty"`
	OperationalAttributes *bool      `json:"operationalAttributes,omitempty"`

	// SchemaTarget For a schema comparison with the live schema as source: the schema entry
	// an added definition would be written to, where the server keeps schema
	// in several collections. Without it such additions are
	// `schema_target_required`.
	SchemaTarget *string        `json:"schemaTarget,omitempty"`
	Scope        *SnapshotScope `json:"scope,omitempty"`
}

// DiffReason defines model for DiffReason.
type DiffReason struct {
	Code   DiffReasonCode `json:"code"`
	Detail string         `json:"detail"`
}

// DiffReasonCode defines model for DiffReasonCode.
type DiffReasonCode string

// DiffRequest defines model for DiffRequest.
type DiffRequest struct {
	// IncludeUnchanged List unchanged entries as items rather than only counting them.
	IncludeUnchanged *bool `json:"includeUnchanged,omitempty"`

	// Source Exactly one of `snapshot` or `live`.
	Source DiffSide `json:"source"`

	// Target Exactly one of `snapshot` or `live`.
	Target DiffSide `json:"target"`
}

// DiffSide Exactly one of `snapshot` or `live`.
type DiffSide struct {
	// Live The live directory. Omitted fields are taken from the other side's
	// snapshot, so `{}` means "the same subtree, as it is now".
	Live     *DiffLiveSide      `json:"live,omitempty"`
	Snapshot *DiffSide_Snapshot `json:"snapshot,omitempty"`
}

// DiffSide_Snapshot defines model for DiffSide.Snapshot.
type DiffSide_Snapshot struct {
	union json.RawMessage
}

// DiffSideKind defines model for DiffSideKind.
type DiffSideKind string

// DiffSideSummary defines model for DiffSideSummary.
type DiffSideSummary struct {
	Base      string  `json:"base"`
	Checksum  *string `json:"checksum,omitempty"`
	CreatedAt *string `json:"createdAt,omitempty"`

	// DefinitionCount For a schema side, how many definitions it holds, parsed or not. Added in 1.10.
	DefinitionCount       *int               `json:"definitionCount,omitempty"`
	EntryCount            int                `json:"entryCount"`
	Filter                string             `json:"filter"`
	Integrity             *SnapshotIntegrity `json:"integrity,omitempty"`
	Kind                  DiffSideKind       `json:"kind"`
	OperationalAttributes bool               `json:"operationalAttributes"`
	Scope                 SnapshotScope      `json:"scope"`
	Vendor                *string            `json:"vendor,omitempty"`
}

// EntryAttribute defines model for EntryAttribute.
type EntryAttribute struct {
	// Kind The schema's opinion about one attribute, for the editor.
	Kind AttributeKind `json:"kind"`
	Name string        `json:"name"`

	// Required In the MUST set of this entry's object classes.
	Required   *bool `json:"required,omitempty"`
	ValueCount *int  `json:"valueCount,omitempty"`

	// ValueSchemes For a withheld attribute, the storage scheme of each value — the
	// `{SSHA}` or `{PBKDF2-SHA512}` prefix RFC 2307 puts in front of a
	// hash — where the value carries one.
	//
	// Only the label crosses the wire. The hash itself is never sent, and
	// a value that does not begin with `{` reports nothing rather than
	// having its first characters guessed at.
	//
	// It is worth reporting because the scheme is the one thing about a
	// stored password an administrator needs to see: it is what says
	// whether the directory is still handing out crypt(3) hashes.
	ValueSchemes *[]string        `json:"valueSchemes,omitempty"`
	Values       []AttributeValue `json:"values"`

	// Withheld The values were withheld because the attribute is sensitive. The
	// count is still reported so the UI can say the attribute is set.
	Withheld *bool `json:"withheld,omitempty"`
}

// EntryComparison defines model for EntryComparison.
type EntryComparison struct {
	Attributes []AttributeComparison `json:"attributes"`

	// Counts One bucket per status, and every attribute lands in exactly one, so the
	// six sum to the length of `attributes`.
	Counts ComparisonCounts `json:"counts"`
	Left   ComparedEntry    `json:"left"`
	Right  ComparedEntry    `json:"right"`

	// Truncated Some attribute held more values than the limit reports.
	Truncated bool `json:"truncated"`
}

// EntryView defines model for EntryView.
type EntryView struct {
	Attributes []EntryAttribute `json:"attributes"`

	// CandidateKinds The schema's opinion about the attributes this entry's classes
	// permit but which it does not currently carry.
	//
	// Without it the editor knows nothing about an attribute until the
	// entry already has one, so adding an attribute produced a bare text
	// box labelled "not in the schema" — no description, no syntax, and a
	// text box for something the schema calls a Boolean.
	CandidateKinds *[]AttributeKind `json:"candidateKinds,omitempty"`
	Dn             string           `json:"dn"`
	HasChildren    *bool            `json:"hasChildren,omitempty"`

	// Ldif The entry as an LDIF content record, ready to copy.
	Ldif *string `json:"ldif,omitempty"`

	// MembershipAttributes The attributes this entry's object classes use to hold members,
	// canonically spelled, whether the entry currently has values in them
	// or not. Empty for an entry that is not a group.
	//
	// Present so adding a member can be one action rather than an edit of
	// a list: replacing a fifty-member list to add one person removes
	// anybody another administrator added since the entry was read.
	MembershipAttributes *[]string `json:"membershipAttributes,omitempty"`
	ObjectClasses        *[]string `json:"objectClasses,omitempty"`
	ParentDn             *string   `json:"parentDn,omitempty"`
	Rdn                  *string   `json:"rdn,omitempty"`

	// ReferencedByFilter An RFC 4515 filter matching every entry that names this one — the
	// answer to "which groups is this person in", and to "what would
	// break if I deleted this".
	//
	// Built here rather than in the browser, with the filter builder, so
	// the DN is escaped once by the code that owns that rule. It asserts
	// only the attributes this server actually defines, and only ones a
	// user may modify, so it is the same question phrased in whatever
	// vocabulary the connected directory has.
	//
	// Absent when the server defines none of them, or when the entry has
	// no DN to search for.
	ReferencedByFilter *string       `json:"referencedByFilter,omitempty"`
	Requirements       *Requirements `json:"requirements,omitempty"`
}

// Error defines model for Error.
type Error struct {
	// Affected The changes or records an error is about, by position in what was
	// sent and by DN: every stale change, not just the first. Added in
	// 1.5.
	Affected *[]ErrorAffected `json:"affected,omitempty"`

	// Cause A more precise reason beside `error`, where the error code has to
	// stay what an earlier release sent. `plan_stale` accompanies
	// `error: conflict` when a planned change's baseline no longer matches
	// the directory: 1.4 answered that case with `conflict`, and a client
	// switching on that must keep working, so the code is unchanged and the
	// precision is added here. Added in 1.5.
	Cause *ErrorCause `json:"cause,omitempty"`

	// Detail Extra context, such as the LDIF line a parse failed on.
	Detail *string `json:"detail,omitempty"`

	// Error A stable machine-readable code.
	Error ErrorError `json:"error"`

	// Hint What that result code usually means for the operation that was
	// attempted, and what to do about it.
	//
	// Written by Alder from the standard result code and the change being
	// made. It never repeats the server's own diagnostic text, which is
	// withheld on purpose: some servers name entries the caller may not be
	// allowed to know exist, and some echo part of the request back. A
	// result code alone is rarely enough to act on, which is what this is
	// for.
	Hint *string `json:"hint,omitempty"`

	// LdapCode The LDAP result code, where the failure came from the directory.
	LdapCode *int `json:"ldapCode,omitempty"`

	// Message A human-readable explanation. Never contains a credential.
	Message string `json:"message"`
}

// ErrorCause A more precise reason beside `error`, where the error code has to
// stay what an earlier release sent. `plan_stale` accompanies
// `error: conflict` when a planned change's baseline no longer matches
// the directory: 1.4 answered that case with `conflict`, and a client
// switching on that must keep working, so the code is unchanged and the
// precision is added here. Added in 1.5.
type ErrorCause string

// ErrorError A stable machine-readable code.
type ErrorError string

// ErrorAffected defines model for ErrorAffected.
type ErrorAffected struct {
	Dn    string `json:"dn"`
	Index int    `json:"index"`
}

// ExpandedMember defines model for ExpandedMember.
type ExpandedMember struct {
	// Direct Whether the group names this member itself.
	Direct bool   `json:"direct"`
	Dn     string `json:"dn"`

	// Group Whether this member is itself a group that was expanded.
	Group bool    `json:"group"`
	Rdn   *string `json:"rdn,omitempty"`

	// Through The membership attribute that names this member.
	Through *string `json:"through,omitempty"`

	// Via The chain of groups that brought this member in, outermost first.
	// Empty for a direct member. This is the part a flat list cannot say,
	// and the part somebody needs in order to remove an unwanted member
	// from the right group.
	Via *[]string `json:"via,omitempty"`
}

// ImportRequest defines model for ImportRequest.
type ImportRequest struct {
	Ldif string `json:"ldif"`

	// Reconcile Turn a content record whose entry already exists into a modify that
	// brings that entry to what the record describes.
	//
	// Without it such a record is an add, and a directory refuses an add
	// for an entry that exists — which is correct, and makes the second
	// half of an export/edit/import loop fail on every record.
	//
	// It replaces the attributes the record names and leaves every other
	// attribute exactly as it is. The document is not treated as the whole
	// truth about the entry: an export omits `userPassword` always and
	// operational attributes by default, and a file that does not mention
	// an attribute is not a file asking for it to be removed.
	Reconcile *bool `json:"reconcile,omitempty"`
}

// ImportResult defines model for ImportResult.
type ImportResult struct {
	Changes []ChangePreview `json:"changes"`

	// Reconciled How many records were turned into a modify because their entry
	// already exists. Only set when `reconcile` was asked for.
	Reconciled *int `json:"reconciled,omitempty"`

	// Requests The parsed changes, ready to post back to /changes/apply.
	Requests *[]ChangeRequest `json:"requests,omitempty"`

	// SkippedAttributes Attributes left out of a reconciliation because the directory owns
	// them — operational or `NO-USER-MODIFICATION`. Enforcing one fails
	// the whole record, and dropping it silently would let the file mean
	// something other than it says.
	SkippedAttributes *[]string `json:"skippedAttributes,omitempty"`

	// Unchanged The DNs whose entry already matches the document, and which are
	// therefore absent from `changes`. Reported rather than shown as
	// no-op modifications: a change that does nothing still has to be
	// read and confirmed, and there is nothing there to confirm.
	Unchanged *[]string `json:"unchanged,omitempty"`
}

// InventoryRequest defines model for InventoryRequest.
type InventoryRequest struct {
	// Attribute The attribute to tally. Attribute options are folded together:
	// `alderTeam` and `alderTeam;lang-fr` are one attribute.
	Attribute string `json:"attribute"`
	BaseDn    string `json:"baseDn"`

	// Filter An RFC 4515 filter narrowing what is examined. Parsed, never pasted.
	Filter *string `json:"filter,omitempty"`

	// Limit Entries examined. The tally is folded a page at a time, so this
	// bounds how long it takes rather than how much it holds — which is
	// why it can be far larger than the other limits here.
	Limit *int `json:"limit,omitempty"`

	// MaxValues Rows returned. The counts behind them stay exact — the tail is
	// reported as a remainder rather than dropped.
	MaxValues *int                  `json:"maxValues,omitempty"`
	Scope     InventoryRequestScope `json:"scope"`
}

// InventoryRequestScope defines model for InventoryRequest.Scope.
type InventoryRequestScope string

// InventoryResponse defines model for InventoryResponse.
type InventoryResponse struct {
	Attribute string `json:"attribute"`

	// DistinctValues Exact across the examined entries, even when fewer rows are returned.
	DistinctValues int `json:"distinctValues"`

	// Examined Entries the search returned. Every other number here is about these
	// entries, not about the directory.
	Examined int `json:"examined"`
	Limit    int `json:"limit"`

	// OtherEntries Entries carrying one of those values.
	OtherEntries int `json:"otherEntries"`

	// OtherValues Distinct values beyond `maxValues`, counted but not listed.
	OtherValues int `json:"otherValues"`

	// SingletonValues Distinct values held by exactly one entry. The typo signal: a value
	// one entry holds among three hundred is usually a misspelling of one
	// that ninety hold.
	SingletonValues int `json:"singletonValues"`

	// Truncated The search reached its limit, so entries exist that were not
	// examined and the tally is partial.
	Truncated    bool           `json:"truncated"`
	Values       []InventoryRow `json:"values"`
	WithValue    int            `json:"withValue"`
	WithoutValue int            `json:"withoutValue"`
}

// InventoryRow defines model for InventoryRow.
type InventoryRow struct {
	// Entries Entries carrying this value, not occurrences of it.
	Entries int `json:"entries"`

	// Value Exactly one of `text` or `base64` is present. `text` carries a value
	// that is valid UTF-8 and belongs to a textual syntax; everything else,
	// including anything with a `;binary` option, arrives as `base64`.
	Value AttributeValue `json:"value"`
}

// MatchingRuleSummary defines model for MatchingRuleSummary.
type MatchingRuleSummary struct {
	Desc     *string `json:"desc,omitempty"`
	Name     string  `json:"name"`
	Obsolete *bool   `json:"obsolete,omitempty"`
	Oid      string  `json:"oid"`
	Syntax   *string `json:"syntax,omitempty"`
}

// MemberList defines model for MemberList.
type MemberList struct {
	// Cycles Groups that contain themselves, directly or through others. A
	// configuration error somebody should hear about, not a condition to
	// survive silently.
	Cycles *[]string `json:"cycles,omitempty"`

	// Dangling Member values whose entry could not be read. Two things produce
	// this and nothing distinguishes them: the entry was deleted while the
	// group went on naming it, or it exists and this bind may not see it.
	// A server answers "no such object" either way, deliberately, so that
	// refusing access does not disclose what is there.
	//
	// So this is a list of members whose status is unknown, not a list of
	// broken references. Reporting it as the latter tells an operator
	// auditing who can reach something that a member is gone when they may
	// still be in the group.
	Dangling *[]string        `json:"dangling,omitempty"`
	Members  []ExpandedMember `json:"members"`

	// Truncated True when the walk stopped at its bound. A membership list that
	// quietly omits people is worse than none, since the question being
	// asked is usually "who can get in".
	Truncated bool `json:"truncated"`

	// Unresolvable Membership values that are not DNs, and so cannot be followed to an
	// entry: `memberUid` holds a login name, `memberURL` a search.
	Unresolvable *[]string `json:"unresolvable,omitempty"`
}

// MonitorAccess A server's monitoring entry, where it publishes one this session can
// read. Found by reading the conventional location and believing only what
// the server answers, exactly as the configuration tree is.
//
// Absent is an ordinary answer. One of the two servers Alder targets
// publishes such an entry and the other does not, and a directory with
// none gets no monitoring section rather than an empty one.
type MonitorAccess struct {
	Dn       *string `json:"dn,omitempty"`
	Readable bool    `json:"readable"`
}

// ObjectClassDetail defines model for ObjectClassDetail.
type ObjectClassDetail struct {
	// Edit An object class as it declares itself, in the shape a change posts back.
	// Returned by the detail response so an edit starts from the definition
	// rather than from the summary, which resolves inherited attributes.
	Edit          *ObjectClassInput `json:"edit,omitempty"`
	InheritedMay  *[]string         `json:"inheritedMay,omitempty"`
	InheritedMust *[]string         `json:"inheritedMust,omitempty"`
	May           *[]string         `json:"may,omitempty"`
	Must          *[]string         `json:"must,omitempty"`

	// Raw The definition exactly as the server published it.
	Raw        *string            `json:"raw,omitempty"`
	Subclasses *[]string          `json:"subclasses,omitempty"`
	Summary    ObjectClassSummary `json:"summary"`

	// SuperiorChain Transitive superiors, nearest first.
	SuperiorChain *[]string `json:"superiorChain,omitempty"`
}

// ObjectClassInput An object class as it declares itself, in the shape a change posts back.
// Returned by the detail response so an edit starts from the definition
// rather than from the summary, which resolves inherited attributes.
type ObjectClassInput struct {
	Desc       *string               `json:"desc,omitempty"`
	Extensions *map[string][]string  `json:"extensions,omitempty"`
	Kind       *ObjectClassInputKind `json:"kind,omitempty"`
	May        *[]string             `json:"may,omitempty"`
	Must       *[]string             `json:"must,omitempty"`
	Names      []string              `json:"names"`
	Obsolete   *bool                 `json:"obsolete,omitempty"`
	Oid        string                `json:"oid"`
	SuperNames *[]string             `json:"superNames,omitempty"`
}

// ObjectClassInputKind defines model for ObjectClassInput.Kind.
type ObjectClassInputKind string

// ObjectClassSummary defines model for ObjectClassSummary.
type ObjectClassSummary struct {
	Desc     *string                `json:"desc,omitempty"`
	Kind     ObjectClassSummaryKind `json:"kind"`
	Name     string                 `json:"name"`
	Names    *[]string              `json:"names,omitempty"`
	Obsolete *bool                  `json:"obsolete,omitempty"`
	Oid      string                 `json:"oid"`

	// Origin Where this definition came from, in the server's own words. The
	// X-ORIGIN extension where the server keeps one, and otherwise the
	// schema collection holding it on a server that stores its schema in
	// configuration entries.
	//
	// Deliberately not a "shipped or custom" flag. One of the two servers
	// Alder targets discards X-ORIGIN when it loads a schema file, so on
	// that server such a flag would be a guess. This reports what was
	// actually published, and is absent where nothing was.
	Origin    *string   `json:"origin,omitempty"`
	Superiors *[]string `json:"superiors,omitempty"`
}

// ObjectClassSummaryKind defines model for ObjectClassSummary.Kind.
type ObjectClassSummaryKind string

// ObjectView defines model for ObjectView.
type ObjectView struct {
	// Anchors The object classes the filter matches on. Shown to the user, so the
	// view can say what it means by "user" rather than ask to be trusted.
	Anchors []string           `json:"anchors"`
	Columns []ObjectViewColumn `json:"columns"`

	// CreateClasses The structural classes this view matches, which are the classes a
	// new one could be created as. A server publishes hundreds of
	// structural classes and a list of all of them is not a choice anybody
	// can make; this is the handful that would produce an entry this view
	// would then show.
	CreateClasses *[]string `json:"createClasses,omitempty"`

	// Description One line on what this view is showing, in the UI's words.
	Description *string `json:"description,omitempty"`

	// Filter An RFC 4515 filter over the anchor classes this server defines,
	// rendered by the filter builder. It is posted to /search as-is.
	Filter string       `json:"filter"`
	Id     ObjectViewId `json:"id"`
	Label  string       `json:"label"`

	// PermittedColumns Every attribute an entry this view matches could hold, as candidates
	// for the columns. `columns` is the default subset of this.
	//
	// Computed over every class the view's filter matches, not just the
	// classes it anchors on — walking the superior chain alone would drop
	// `mail` from Users, because `person` does not permit it and
	// `inetOrgPerson` does. That is the bug the object views were fixed
	// for once already, and repeating the walk in the browser is how the
	// two copies would drift apart again.
	PermittedColumns *[]ObjectViewColumn `json:"permittedColumns,omitempty"`
}

// ObjectViewId defines model for ObjectView.Id.
type ObjectViewId string

// ObjectViewColumn defines model for ObjectViewColumn.
type ObjectViewColumn struct {
	// Attribute The attribute type, canonically spelled by the schema.
	Attribute string `json:"attribute"`

	// Desc The schema's own DESC, where the server published one.
	Desc *string `json:"desc,omitempty"`

	// Label A readable heading. Falls back to the attribute name itself, which
	// is always shown alongside it: a friendly label that hides the real
	// name is how somebody learns the wrong thing about their directory.
	Label string `json:"label"`
}

// ObjectViewList defines model for ObjectViewList.
type ObjectViewList struct {
	Views []ObjectView `json:"views"`
}

// PackageAssumptionResult defines model for PackageAssumptionResult.
type PackageAssumptionResult struct {
	Detail    *string `json:"detail,omitempty"`
	Kind      string  `json:"kind"`
	Satisfied bool    `json:"satisfied"`
	Value     string  `json:"value"`
}

// PackageAssumptions What the package expects of a target, checked at validation. About
// directory semantics, never about a host: the same package should suit
// dev, test and production when their directories are compatible.
type PackageAssumptions struct {
	NamingContexts *[]string `json:"namingContexts,omitempty"`
	ObjectClasses  *[]string `json:"objectClasses,omitempty"`
	SchemaOids     *[]string `json:"schemaOids,omitempty"`
}

// PackageAttribute defines model for PackageAttribute.
type PackageAttribute struct {
	Name   string         `json:"name"`
	Values []PackageValue `json:"values"`
}

// PackageBuildRequest defines model for PackageBuildRequest.
type PackageBuildRequest struct {
	// Assumptions What the package expects of a target, checked at validation. About
	// directory semantics, never about a host: the same package should suit
	// dev, test and production when their directories are compatible.
	Assumptions *PackageAssumptions `json:"assumptions,omitempty"`

	// Changes The changes to package. A `baseline` or an `expect` on one is
	// ignored rather than carried: both describe the environment the
	// change was planned in.
	Changes     []ChangeRequest            `json:"changes"`
	Description *string                    `json:"description,omitempty"`
	Method      *PackageBuildRequestMethod `json:"method,omitempty"`

	// RecordSource Record this directory's vendor and naming contexts as provenance.
	// Off by default: provenance is informational, and a package that says
	// nothing about where it was made is still a valid package.
	RecordSource *bool   `json:"recordSource,omitempty"`
	Title        *string `json:"title,omitempty"`
}

// PackageBuildRequestMethod defines model for PackageBuildRequest.Method.
type PackageBuildRequestMethod string

// PackageChange defines model for PackageChange.
type PackageChange struct {
	// Data An intended change to an ordinary entry. The same operations the rest of
	// Alder uses, without `setpassword`, which cannot travel without its
	// secret.
	Data *PackageDataChange `json:"data,omitempty"`

	// DependsOn The changes that must be applied before this one.
	DependsOn *[]string `json:"dependsOn,omitempty"`

	// Destructive The change removes something. Written out rather than inferred.
	Destructive bool `json:"destructive"`

	// Id Identifies this change inside the package. Dependencies name it.
	Id string `json:"id"`

	// Intent How the change is planned. `desired` reconciles an add against the
	// entry that is there, changing only the attributes it names, and only
	// an add may ask for it. Absence never implies deletion: a deletion is
	// only ever an explicit `delete`.
	Intent *PackageChangeIntent `json:"intent,omitempty"`
	Kind   PackageChangeKind    `json:"kind"`

	// Label What the operator was doing when the change was made. Carries no meaning.
	Label *string `json:"label,omitempty"`

	// Schema An intended change to a schema definition, as what it means rather than
	// as one server's modification: the entry and attribute that hold schema
	// differ between products, and each target works that out for itself.
	Schema *PackageSchemaChange `json:"schema,omitempty"`
}

// PackageChangeIntent How the change is planned. `desired` reconciles an add against the
// entry that is there, changing only the attributes it names, and only
// an add may ask for it. Absence never implies deletion: a deletion is
// only ever an explicit `delete`.
type PackageChangeIntent string

// PackageChangeKind defines model for PackageChange.Kind.
type PackageChangeKind string

// PackageCounts defines model for PackageCounts.
type PackageCounts struct {
	Changes     int `json:"changes"`
	Data        int `json:"data"`
	Destructive int `json:"destructive"`
	Omitted     int `json:"omitted"`
	Schema      int `json:"schema"`
}

// PackageDataChange An intended change to an ordinary entry. The same operations the rest of
// Alder uses, without `setpassword`, which cannot travel without its
// secret.
type PackageDataChange struct {
	Attributes   *[]PackageAttribute   `json:"attributes,omitempty"`
	DeleteOldRdn *bool                 `json:"deleteOldRdn,omitempty"`
	Dn           string                `json:"dn"`
	Mods         *[]PackageMod         `json:"mods,omitempty"`
	NewRdn       *string               `json:"newRdn,omitempty"`
	NewSuperior  *string               `json:"newSuperior,omitempty"`
	Type         PackageDataChangeType `json:"type"`
}

// PackageDataChangeType defines model for PackageDataChange.Type.
type PackageDataChangeType string

// PackageInspection defines model for PackageInspection.
type PackageInspection struct {
	// Assumptions What the package expects of a target, checked at validation. About
	// directory semantics, never about a host: the same package should suit
	// dev, test and production when their directories are compatible.
	Assumptions PackageAssumptions `json:"assumptions"`
	Changes     []PackageChange    `json:"changes"`
	Counts      PackageCounts      `json:"counts"`
	CreatedAt   string             `json:"createdAt"`
	Description *string            `json:"description,omitempty"`
	Integrity   SnapshotIntegrity  `json:"integrity"`
	Omitted     []PackageOmitted   `json:"omitted"`

	// Order The changes in the order a target must apply them.
	Order     []string `json:"order"`
	PackageId string   `json:"packageId"`

	// Source Where a package came from. Informational: a package proves nothing about
	// its origin, and nothing here grants any authority. No host, no bind DN,
	// no path, no credential.
	Source  PackageProvenance `json:"source"`
	Title   *string           `json:"title,omitempty"`
	Version int               `json:"version"`
}

// PackageItemStatus What validation concluded about one change against this target.
// `already_satisfied` is a success: a change applied here by hand is not a
// problem to report. `unknown` is never read as ready.
type PackageItemStatus string

// PackageMod defines model for PackageMod.
type PackageMod struct {
	Name string       `json:"name"`
	Op   PackageModOp `json:"op"`

	// Values A delete with no values removes the attribute.
	Values *[]PackageValue `json:"values,omitempty"`
}

// PackageModOp defines model for PackageMod.Op.
type PackageModOp string

// PackageOmitted An intended change the package deliberately does not carry, with the
// reason. Nothing is dropped silently.
type PackageOmitted struct {
	Detail *string              `json:"detail,omitempty"`
	Kind   *string              `json:"kind,omitempty"`
	Reason PackageOmittedReason `json:"reason"`

	// Subject What was left out, named without its values.
	Subject *string `json:"subject,omitempty"`
}

// PackageOmittedReason defines model for PackageOmitted.Reason.
type PackageOmittedReason string

// PackageProblem defines model for PackageProblem.
type PackageProblem struct {
	// Code Why the change is not simply ready, in the vocabulary the plan and
	// the schema comparison already use where the situation is the same.
	Code    PackageProblemCode `json:"code"`
	Detail  *string            `json:"detail,omitempty"`
	Subject *string            `json:"subject,omitempty"`
}

// PackageProblemCode Why the change is not simply ready, in the vocabulary the plan and
// the schema comparison already use where the situation is the same.
type PackageProblemCode string

// PackageProvenance Where a package came from. Informational: a package proves nothing about
// its origin, and nothing here grants any authority. No host, no bind DN,
// no path, no credential.
type PackageProvenance struct {
	AlderVersion           *string                  `json:"alderVersion,omitempty"`
	Method                 *PackageProvenanceMethod `json:"method,omitempty"`
	NamingContexts         *[]string                `json:"namingContexts,omitempty"`
	SchemaSnapshotChecksum *string                  `json:"schemaSnapshotChecksum,omitempty"`
	SnapshotChecksum       *string                  `json:"snapshotChecksum,omitempty"`
	Vendor                 *string                  `json:"vendor,omitempty"`
	VendorVersion          *string                  `json:"vendorVersion,omitempty"`
}

// PackageProvenanceMethod defines model for PackageProvenance.Method.
type PackageProvenanceMethod string

// PackageSchemaChange An intended change to a schema definition, as what it means rather than
// as one server's modification: the entry and attribute that hold schema
// differ between products, and each target works that out for itself.
type PackageSchemaChange struct {
	// Definition The RFC 4512 definition, canonicalised. Required to add or replace.
	Definition *string                    `json:"definition,omitempty"`
	Element    PackageSchemaChangeElement `json:"element"`
	Oid        string                     `json:"oid"`
	Op         PackageSchemaChangeOp      `json:"op"`
}

// PackageSchemaChangeElement defines model for PackageSchemaChange.Element.
type PackageSchemaChangeElement string

// PackageSchemaChangeOp defines model for PackageSchemaChange.Op.
type PackageSchemaChangeOp string

// PackageTarget What the directory a package was validated against announces. Display only.
type PackageTarget struct {
	NamingContexts []string  `json:"namingContexts"`
	SchemaEntries  *[]string `json:"schemaEntries,omitempty"`
	SchemaWritable bool      `json:"schemaWritable"`
	Vendor         *string   `json:"vendor,omitempty"`
	VendorVersion  *string   `json:"vendorVersion,omitempty"`
}

// PackageValidation defines model for PackageValidation.
type PackageValidation struct {
	Assumptions []PackageAssumptionResult `json:"assumptions"`
	Counts      PackageValidationCounts   `json:"counts"`
	Integrity   SnapshotIntegrity         `json:"integrity"`
	Items       []PackageValidationItem   `json:"items"`

	// Order The identifiers of the ready changes, in the order to apply them.
	Order     []string `json:"order"`
	PackageId string   `json:"packageId"`

	// Target What the directory a package was validated against announces. Display only.
	Target PackageTarget `json:"target"`
}

// PackageValidationCounts defines model for PackageValidationCounts.
type PackageValidationCounts struct {
	AlreadySatisfied   int `json:"alreadySatisfied"`
	Conflict           int `json:"conflict"`
	DependencyMissing  int `json:"dependencyMissing"`
	NoOp               int `json:"noOp"`
	Ready              int `json:"ready"`
	TargetIncompatible int `json:"targetIncompatible"`
	Unknown            int `json:"unknown"`
	Unsupported        int `json:"unsupported"`
}

// PackageValidationItem defines model for PackageValidationItem.
type PackageValidationItem struct {
	// Changes For a ready change: the ordinary change requests it means on this
	// target, prepared for `POST /plan` and nothing else. They carry no
	// baseline, because only a plan issues one.
	Changes     *[]ChangeRequest  `json:"changes,omitempty"`
	DependsOn   *[]string         `json:"dependsOn,omitempty"`
	Destructive bool              `json:"destructive"`
	Id          string            `json:"id"`
	Intent      *string           `json:"intent,omitempty"`
	Kind        string            `json:"kind"`
	Label       *string           `json:"label,omitempty"`
	Problems    *[]PackageProblem `json:"problems,omitempty"`

	// Status What validation concluded about one change against this target.
	// `already_satisfied` is a success: a change applied here by hand is not a
	// problem to report. `unknown` is never read as ready.
	Status PackageItemStatus `json:"status"`
}

// PackageValidationRequest defines model for PackageValidationRequest.
type PackageValidationRequest struct {
	// Package An Alder change package: format `alder-change-package`, version 1 (1.11).
	// A portable description of intended changes. It holds no plan token, no
	// baseline, no session, no credential and no secret, and it is never
	// applied: a target validates it and makes its own plan.
	Package ChangePackage `json:"package"`

	// SchemaTarget The schema entry an added definition should be written to, where the
	// server keeps schema in several. Without it such a change is a
	// conflict with `schema_target_required`.
	SchemaTarget *string `json:"schemaTarget,omitempty"`
}

// PackageValue One attribute value: exactly one of text or base64.
type PackageValue struct {
	Base64 *string `json:"base64,omitempty"`
	Text   *string `json:"text,omitempty"`
}

// Plan defines model for Plan.
type Plan struct {
	Counts   PlanCounts     `json:"counts"`
	Impact   *PlanImpact    `json:"impact,omitempty"`
	Items    []PlanItem     `json:"items"`
	Subtrees *[]PlanSubtree `json:"subtrees,omitempty"`

	// Warnings Findings about the set as a whole rather than any one change:
	// an entry created before its parent, an entry acted on after it is
	// deleted, the same entry changed twice. None of them block.
	Warnings *[]string `json:"warnings,omitempty"`
}

// PlanAction What a change would do. A stable identifier: switch on this rather than
// reading `reason`.
//
// `unchanged`, `conflict` and `invalid` apply nothing. `conflict` is a
// change the directory would refuse as things stand; `invalid` is one the
// schema forbids whatever the state. `invalid` was added in 1.5, and a
// client that does not recognise it should treat it as it treats
// `conflict`. `set_password` is separate from `modify` because it is an
// extended operation with no LDIF form, and because a directory cannot be
// asked whether a password is already the one being set, so it is never
// reported as unchanged.
type PlanAction string

// PlanCounts defines model for PlanCounts.
type PlanCounts struct {
	Add      int `json:"add"`
	Conflict int `json:"conflict"`
	Delete   int `json:"delete"`
	Examined int `json:"examined"`

	// Invalid Added in 1.5.
	Invalid     *int `json:"invalid,omitempty"`
	Modify      int  `json:"modify"`
	Rename      int  `json:"rename"`
	SetPassword int  `json:"setPassword"`
	Unchanged   int  `json:"unchanged"`
}

// PlanImpact defines model for PlanImpact.
type PlanImpact struct {
	// Kinds How many applicable changes land in each target area.
	Kinds      PlanImpactKinds      `json:"kinds"`
	Membership PlanImpactMembership `json:"membership"`
	References PlanImpactReferences `json:"references"`
}

// PlanImpactKinds How many applicable changes land in each target area.
type PlanImpactKinds struct {
	Config int `json:"config"`
	Data   int `json:"data"`
	Schema int `json:"schema"`
}

// PlanImpactMembership defines model for PlanImpactMembership.
type PlanImpactMembership struct {
	Gained int `json:"gained"`

	// Groups How many distinct entries have membership changes.
	Groups  int `json:"groups"`
	Removed int `json:"removed"`
}

// PlanImpactReferences defines model for PlanImpactReferences.
type PlanImpactReferences struct {
	// Analysed Whether references were searched for. False when the plan deletes
	// or renames nothing, or when it affects more DNs than a single
	// request will search for; `reason` then says which, and no count is
	// offered in place of one.
	Analysed bool    `json:"analysed"`
	Dangling int     `json:"dangling"`
	Found    int     `json:"found"`
	Reason   *string `json:"reason,omitempty"`

	// Truncated The reference search reached its bound, so there may be more
	// references than counted. Independently of this, the search runs as
	// the session's own bind: an entry the access rules hide is absent
	// from the answer exactly as one that does not exist is.
	Truncated bool `json:"truncated"`
}

// PlanIntent How a proposal was read. `exact` is planned as written and never
// rewritten. `desired` is reconciled against the entry that is there.
type PlanIntent string

// PlanItem defines model for PlanItem.
type PlanItem struct {
	// Action What a change would do. A stable identifier: switch on this rather than
	// reading `reason`.
	//
	// `unchanged`, `conflict` and `invalid` apply nothing. `conflict` is a
	// change the directory would refuse as things stand; `invalid` is one the
	// schema forbids whatever the state. `invalid` was added in 1.5, and a
	// client that does not recognise it should treat it as it treats
	// `conflict`. `set_password` is separate from `modify` because it is an
	// extended operation with no LDIF form, and because a directory cannot be
	// asked whether a password is already the one being set, so it is never
	// reported as unchanged.
	Action PlanAction `json:"action"`

	// Baseline An opaque token binding the operation in `record` and the state it
	// was planned against. Send it back on the matching change when
	// applying. Absent for items that apply nothing.
	Baseline *string `json:"baseline,omitempty"`
	Dn       string  `json:"dn"`

	// Exists Whether the entry was there when the plan was made.
	Exists bool `json:"exists"`

	// Index The change's position in the set that was sent, so a client can line the plan up against its own list.
	Index int `json:"index"`

	// Intent How a proposal was read. `exact` is planned as written and never
	// rewritten. `desired` is reconciled against the entry that is there.
	Intent *PlanIntent `json:"intent,omitempty"`

	// Kind Which area of the server a change lands in. `schema`: the server's
	// schema entry, where a write changes what every entry may hold.
	// `config`: the server's own configuration tree. `data`: everything else.
	// Decided from the locations the server announces, not from a DN pattern.
	Kind       *PlanTargetKind         `json:"kind,omitempty"`
	Membership *[]PlanMembershipChange `json:"membership,omitempty"`
	Preview    *ChangePreview          `json:"preview,omitempty"`
	Problem    *PlanProblem            `json:"problem,omitempty"`

	// Reason Why this is unchanged, why it conflicts, or what the planner
	// rewrote. Prose, for a person to read. Not stable across releases.
	Reason   *string             `json:"reason,omitempty"`
	Record   *ChangeRequest      `json:"record,omitempty"`
	Recovery *RecoveryAssessment `json:"recovery,omitempty"`

	// References Entries naming a DN this change deletes or renames away.
	References *PlanReferenceImpact `json:"references,omitempty"`

	// SkippedAttributes Attributes left out of a reconciled record because the directory
	// owns them and would refuse to be told otherwise. Reported rather
	// than dropped silently: a document carrying `entryUUID` was exported
	// with operational attributes, and the reader should know they are not
	// being enforced.
	SkippedAttributes *[]string `json:"skippedAttributes,omitempty"`
}

// PlanLdifMode How an LDIF document is read. `changes`: every record is the exact
// operation it states, and a content record is an add. `desired`: the
// document is the state entries should be in; only content records are
// accepted, each is reconciled, and absence never means deletion.
type PlanLdifMode string

// PlanMembershipChange One membership attribute's net effect on the entry a change touches:
// the change's modifications replayed in order over the values the entry
// holds, so an add and a delete of the same member is no change. Member
// DNs are compared as DNs.
type PlanMembershipChange struct {
	Attribute string   `json:"attribute"`
	Gained    []string `json:"gained"`
	Removed   []string `json:"removed"`
}

// PlanProblem defines model for PlanProblem.
type PlanProblem struct {
	// Attribute The attribute or object class concerned, where there is one.
	Attribute *string `json:"attribute,omitempty"`

	// Code Why a change would not apply. Stable identifiers.
	//
	// State conflicts (`action: conflict`), which may resolve when the
	// directory changes: `entry_missing`, `entry_exists`, `has_children`,
	// `rename_target_exists`, and `expected_state_differs` (1.9): the entry
	// no longer holds the state a change's `expect` requires -- for a change
	// from a recovery bundle, the directory has drifted since the original
	// apply. `dependency_required` and `referenced_by_schema` (1.10): a schema
	// change names a definition the schema would not define when it runs, or
	// removes one the schema still names, reading the set in order.
	//
	// Schema violations (`action: invalid`), which will not resolve by
	// waiting: `object_class_undefined`, `attribute_undefined`,
	// `attribute_not_permitted`, `single_value_violation`,
	// `missing_required_attribute`. Only rules the schema states outright are
	// checked; anything that depends on server behaviour is left for the
	// directory to refuse.
	Code PlanProblemCode `json:"code"`
}

// PlanProblemCode Why a change would not apply. Stable identifiers.
//
// State conflicts (`action: conflict`), which may resolve when the
// directory changes: `entry_missing`, `entry_exists`, `has_children`,
// `rename_target_exists`, and `expected_state_differs` (1.9): the entry
// no longer holds the state a change's `expect` requires -- for a change
// from a recovery bundle, the directory has drifted since the original
// apply. `dependency_required` and `referenced_by_schema` (1.10): a schema
// change names a definition the schema would not define when it runs, or
// removes one the schema still names, reading the set in order.
//
// Schema violations (`action: invalid`), which will not resolve by
// waiting: `object_class_undefined`, `attribute_undefined`,
// `attribute_not_permitted`, `single_value_violation`,
// `missing_required_attribute`. Only rules the schema states outright are
// checked; anything that depends on server behaviour is left for the
// directory to refuse.
type PlanProblemCode string

// PlanReferenceCount defines model for PlanReferenceCount.
type PlanReferenceCount struct {
	Attribute string `json:"attribute"`
	Count     int    `json:"count"`
}

// PlanReferenceImpact Entries naming a DN this change deletes or renames away.
type PlanReferenceImpact struct {
	ByAttribute []PlanReferenceCount `json:"byAttribute"`

	// Count How many references were found.
	Count int `json:"count"`

	// Dangling How many of them the plan leaves pointing at nothing: not removed
	// by another change in the same plan, and not held by an entry the
	// plan also deletes.
	Dangling int `json:"dangling"`

	// Referrers The referring entries, at most fifty.
	Referrers *[]string `json:"referrers,omitempty"`
}

// PlanRequest Exactly one of `changes` and `ldif`. `changes` was required in 1.4; it
// is now one of two inputs, which loosens the request and breaks no client
// that sends it.
type PlanRequest struct {
	// Changes The changes to plan, in the order they would be applied. Bounded
	// like a changeset, and for the same reason: the whole set is read
	// and classified in one request.
	Changes *[]ChangeRequest `json:"changes,omitempty"`

	// Ldif An LDIF document to plan, bounded like an import. Parsed with the
	// same reader as `/import/ldif`; `attr:< url` references and LDAP
	// controls are refused, as they are there.
	Ldif *string `json:"ldif,omitempty"`

	// Mode How an LDIF document is read. `changes`: every record is the exact
	// operation it states, and a content record is an add. `desired`: the
	// document is the state entries should be in; only content records are
	// accepted, each is reconciled, and absence never means deletion.
	Mode *PlanLdifMode `json:"mode,omitempty"`

	// Reconcile Turn an `add` of an entry that already exists into the modification
	// that makes the attributes it names match, leaving every attribute
	// it does not name alone. This is what closes the export, edit,
	// import loop. Without it such a change is a conflict, because a
	// directory refuses an add for an entry that exists.
	//
	// Applies to `changes` only. For LDIF the same choice is `mode`, made
	// per document from its records rather than by a switch that
	// reinterprets them.
	Reconcile *bool `json:"reconcile,omitempty"`
}

// PlanSubtree Planned deletions that together remove a branch. Reported only for more
// than one entry, so a subtree deletion never reads as a one-entry change.
type PlanSubtree struct {
	Entries int    `json:"entries"`
	Root    string `json:"root"`
}

// PlanTargetKind Which area of the server a change lands in. `schema`: the server's
// schema entry, where a write changes what every entry may hold.
// `config`: the server's own configuration tree. `data`: everything else.
// Decided from the locations the server announces, not from a DN pattern.
type PlanTargetKind string

// PreflightCapability A target capability this source needs. Capabilities it does not need are not listed.
type PreflightCapability struct {
	Available   bool   `json:"available"`
	Capability  string `json:"capability"`
	Explanation string `json:"explanation"`
	RequiredBy  string `json:"requiredBy"`
}

// PreflightCategory defines model for PreflightCategory.
type PreflightCategory string

// PreflightClassification What a finding concludes about one source object. `excluded` is what
// does not travel by design, such as a server-generated value or a withheld
// secret, and does not by itself stop anything else.
type PreflightClassification string

// PreflightCounts defines model for PreflightCounts.
type PreflightCounts struct {
	AlreadySatisfied     int `json:"alreadySatisfied"`
	Excluded             int `json:"excluded"`
	Incompatible         int `json:"incompatible"`
	Portable             int `json:"portable"`
	PrerequisiteRequired int `json:"prerequisiteRequired"`
	Unknown              int `json:"unknown"`
	Unsupported          int `json:"unsupported"`
}

// PreflightFinding defines model for PreflightFinding.
type PreflightFinding struct {
	// BlocksPlan A plan made now for the change this concerns would refuse it.
	BlocksPlan        bool              `json:"blocksPlan"`
	BlocksPortability bool              `json:"blocksPortability"`
	Category          PreflightCategory `json:"category"`

	// Causes Ids of the findings that explain this one.
	Causes *[]string `json:"causes,omitempty"`

	// Classification What a finding concludes about one source object. `excluded` is what
	// does not travel by design, such as a server-generated value or a withheld
	// secret, and does not by itself stop anything else.
	Classification PreflightClassification `json:"classification"`

	// Code A stable identifier to switch on, for example definition_portable,
	// definition_conflict, syntax_unavailable, naming_context_mismatch,
	// parent_missing, parent_unknown, existing_entry_differs,
	// reference_missing, reference_unknown, ignored_operational and
	// sensitive_value_not_migratable. docs/PREFLIGHT.md lists them all.
	Code string `json:"code"`

	// Count How many source objects an aggregated finding stands for.
	Count       *int   `json:"count,omitempty"`
	Explanation string `json:"explanation"`

	// Id Assigned after findings are sorted, so the same source against the same target state gives the same ids.
	Id string `json:"id"`

	// ManualAction An operator has something to do that Alder will not do for them.
	ManualAction  bool                     `json:"manualAction"`
	Prerequisites *[]PreflightPrerequisite `json:"prerequisites,omitempty"`
	Scope         PreflightScope           `json:"scope"`

	// Source The source object a finding is about. Which fields are set depends on the scope.
	Source PreflightSourceRef   `json:"source"`
	Target *PreflightTargetFact `json:"target,omitempty"`

	// ValidationStatus For a package item, what change package validation concluded.
	ValidationStatus *string `json:"validationStatus,omitempty"`
}

// PreflightNotEvaluated defines model for PreflightNotEvaluated.
type PreflightNotEvaluated struct {
	Area   string `json:"area"`
	Reason string `json:"reason"`
}

// PreflightOverall defines model for PreflightOverall.
type PreflightOverall string

// PreflightPrerequisite Something that has to be true first, as data rather than advice.
type PreflightPrerequisite struct {
	Capability *string `json:"capability,omitempty"`
	Dn         *string `json:"dn,omitempty"`
	Element    *string `json:"element,omitempty"`
	Name       *string `json:"name,omitempty"`
	Oid        *string `json:"oid,omitempty"`

	// ProvidedBy The finding about the source object that supplies it, when the same source does.
	ProvidedBy *string `json:"providedBy,omitempty"`

	// Type schema, syntax, matching_rule, entry or capability.
	Type string `json:"type"`
}

// PreflightReport defines model for PreflightReport.
type PreflightReport struct {
	Capabilities []PreflightCapability   `json:"capabilities"`
	Complete     bool                    `json:"complete"`
	Counts       PreflightCounts         `json:"counts"`
	CreatedAt    string                  `json:"createdAt"`
	Findings     []PreflightFinding      `json:"findings"`
	NotEvaluated []PreflightNotEvaluated `json:"notEvaluated"`
	Overall      PreflightOverall        `json:"overall"`

	// Reasons Why the report is not complete, as stable codes.
	Reasons       []string           `json:"reasons"`
	ReportVersion int                `json:"reportVersion"`
	Sections      []PreflightSection `json:"sections"`
	Source        PreflightSource    `json:"source"`
	Target        PreflightTarget    `json:"target"`
	Truncated     bool               `json:"truncated"`
}

// PreflightRequest defines model for PreflightRequest.
type PreflightRequest struct {
	// Artifact The artifact exactly as its file holds it: a change package, a schema
	// snapshot or a data snapshot. It is decoded strictly, as its own
	// endpoints decode it.
	Artifact map[string]interface{} `json:"artifact"`

	// SchemaTarget For a change package, the schema entry an added definition would be
	// written to, where the server keeps schema in several.
	SchemaTarget *string `json:"schemaTarget,omitempty"`
}

// PreflightScope defines model for PreflightScope.
type PreflightScope string

// PreflightSection defines model for PreflightSection.
type PreflightSection struct {
	Category PreflightCategory `json:"category"`
	Counts   PreflightCounts   `json:"counts"`
}

// PreflightSource defines model for PreflightSource.
type PreflightSource struct {
	Checksum *string `json:"checksum,omitempty"`
	Format   string  `json:"format"`
	Id       *string `json:"id,omitempty"`

	// Integrity verified or unverified.
	Integrity     string              `json:"integrity"`
	Kind          *string             `json:"kind,omitempty"`
	Objects       int                 `json:"objects"`
	Title         *string             `json:"title,omitempty"`
	Type          PreflightSourceType `json:"type"`
	Vendor        *string             `json:"vendor,omitempty"`
	VendorVersion *string             `json:"vendorVersion,omitempty"`
	Version       int                 `json:"version"`
}

// PreflightSourceType defines model for PreflightSource.Type.
type PreflightSourceType string

// PreflightSourceRef The source object a finding is about. Which fields are set depends on the scope.
type PreflightSourceRef struct {
	Attribute *string `json:"attribute,omitempty"`
	Dn        *string `json:"dn,omitempty"`

	// Element attributeType or objectClass.
	Element *string `json:"element,omitempty"`

	// Item A change package item id.
	Item  *string `json:"item,omitempty"`
	Name  *string `json:"name,omitempty"`
	Oid   *string `json:"oid,omitempty"`
	Value *string `json:"value,omitempty"`
}

// PreflightTarget defines model for PreflightTarget.
type PreflightTarget struct {
	// CrossVendor Source and target name different products. Metadata only; no finding follows from it.
	CrossVendor    bool     `json:"crossVendor"`
	NamingContexts []string `json:"namingContexts"`
	SchemaEntry    *string  `json:"schemaEntry,omitempty"`
	SchemaWritable bool     `json:"schemaWritable"`
	Vendor         *string  `json:"vendor,omitempty"`
	VendorVersion  *string  `json:"vendorVersion,omitempty"`
}

// PreflightTargetFact defines model for PreflightTargetFact.
type PreflightTargetFact struct {
	Detail *string `json:"detail,omitempty"`

	// Fact A stable identifier for what the target showed, such as absent, present, hidden or defined_differently.
	Fact string `json:"fact"`
}

// RecoveryAssessment defines model for RecoveryAssessment.
type RecoveryAssessment struct {
	Reasons *[]RecoveryReason `json:"reasons,omitempty"`

	// Recoverability How far a change can be compensated. `exact`: Alder can derive
	// compensating changes that restore the ordinary directory state it
	// captured before the change -- the user attributes the change touched,
	// an entry's absence, or its former name -- subject to the plan's drift
	// checks. It does not cover operational attributes such as
	// `modifyTimestamp`, server-generated identifiers such as `entryUUID`,
	// replication metadata, or attributes the bind could not read.
	// `partial`: some of it; `reasons` say what is not. `unavailable`: none of
	// it. Recovery is compensation through a reviewed plan, never a
	// transaction or a guaranteed rollback.
	Recoverability RecoveryRecoverability `json:"recoverability"`
}

// RecoveryAttribute defines model for RecoveryAttribute.
type RecoveryAttribute struct {
	Name   string          `json:"name"`
	Values []SnapshotValue `json:"values"`
}

// RecoveryBundle An Alder recovery bundle, format version 1: a client-held description
// of the compensating changes Alder could derive from the state that
// existed immediately before an apply. Not a transaction, not a rollback,
// not a backup. It holds no credential, password, token or baseline. See
// `docs/RECOVERY.md`.
type RecoveryBundle struct {
	// Checksum `sha256:<hex>` over every field except `createdAt` and `checksum`. An integrity check against corruption, not authentication or a signature.
	Checksum  *string              `json:"checksum,omitempty"`
	CreatedAt time.Time            `json:"createdAt"`
	Format    RecoveryBundleFormat `json:"format"`

	// Origin What the directory announced about itself. Display, not proof of
	// identity. No host, port, bind DN or credential is recorded.
	Origin RecoveryOrigin `json:"origin"`

	// Recoverability How far a change can be compensated. `exact`: Alder can derive
	// compensating changes that restore the ordinary directory state it
	// captured before the change -- the user attributes the change touched,
	// an entry's absence, or its former name -- subject to the plan's drift
	// checks. It does not cover operational attributes such as
	// `modifyTimestamp`, server-generated identifiers such as `entryUUID`,
	// replication metadata, or attributes the bind could not read.
	// `partial`: some of it; `reasons` say what is not. `unavailable`: none of
	// it. Recovery is compensation through a reviewed plan, never a
	// transaction or a guaranteed rollback.
	Recoverability RecoveryRecoverability `json:"recoverability"`

	// Steps In the order the changes were applied.
	Steps []RecoveryStep `json:"steps"`

	// Version The recovery format version. This Alder reads 1.
	Version int `json:"version"`
}

// RecoveryBundleFormat defines model for RecoveryBundle.Format.
type RecoveryBundleFormat string

// RecoveryChange One compensating change. Never a password change.
type RecoveryChange struct {
	Attributes   *[]RecoveryAttribute `json:"attributes,omitempty"`
	DeleteOldRdn *bool                `json:"deleteOldRdn,omitempty"`
	Dn           string               `json:"dn"`
	Expect       *RecoveryExpect      `json:"expect,omitempty"`
	Mods         *[]RecoveryMod       `json:"mods,omitempty"`
	NewRdn       *string              `json:"newRdn,omitempty"`
	NewSuperior  *string              `json:"newSuperior,omitempty"`
	Type         RecoveryChangeType   `json:"type"`
}

// RecoveryChangeType defines model for RecoveryChange.Type.
type RecoveryChangeType string

// RecoveryDrift defines model for RecoveryDrift.
type RecoveryDrift struct {
	Dn string `json:"dn"`

	// Index Position in `changes`.
	Index   int          `json:"index"`
	Problem *PlanProblem `json:"problem,omitempty"`

	// State A compensating change against the directory as it is now. `ready`: it
	// would apply. `drifted`: the entry is not as the original change left it
	// (`problem` says how), so it would not. `already_recovered`: it would do
	// nothing. `blocked`: it is not valid against the schema.
	State RecoveryDriftState `json:"state"`
}

// RecoveryDriftState A compensating change against the directory as it is now. `ready`: it
// would apply. `drifted`: the entry is not as the original change left it
// (`problem` says how), so it would not. `already_recovered`: it would do
// nothing. `blocked`: it is not valid against the schema.
type RecoveryDriftState string

// RecoveryExpect defines model for RecoveryExpect.
type RecoveryExpect struct {
	Attributes []RecoveryAttribute `json:"attributes"`
	Exhaustive *bool               `json:"exhaustive,omitempty"`
}

// RecoveryInspection defines model for RecoveryInspection.
type RecoveryInspection struct {
	// Changes The compensating changes in execution order, each with its `expect`. Input for `POST /plan`.
	Changes   []ChangeRequest   `json:"changes"`
	Checksum  string            `json:"checksum"`
	CreatedAt string            `json:"createdAt"`
	Drift     []RecoveryDrift   `json:"drift"`
	Integrity SnapshotIntegrity `json:"integrity"`

	// Origin What the directory announced about itself. Display, not proof of
	// identity. No host, port, bind DN or credential is recorded.
	Origin            RecoveryOrigin `json:"origin"`
	OriginDifferences *[]string      `json:"originDifferences,omitempty"`

	// OriginMatches Whether the vendor and naming contexts match this session's directory. Not proof either way.
	OriginMatches bool `json:"originMatches"`

	// Recoverability How far a change can be compensated. `exact`: Alder can derive
	// compensating changes that restore the ordinary directory state it
	// captured before the change -- the user attributes the change touched,
	// an entry's absence, or its former name -- subject to the plan's drift
	// checks. It does not cover operational attributes such as
	// `modifyTimestamp`, server-generated identifiers such as `entryUUID`,
	// replication metadata, or attributes the bind could not read.
	// `partial`: some of it; `reasons` say what is not. `unavailable`: none of
	// it. Recovery is compensation through a reviewed plan, never a
	// transaction or a guaranteed rollback.
	Recoverability RecoveryRecoverability `json:"recoverability"`
	Steps          []RecoveryStep         `json:"steps"`
	Version        int                    `json:"version"`
}

// RecoveryMod defines model for RecoveryMod.
type RecoveryMod struct {
	Name   string           `json:"name"`
	Op     RecoveryModOp    `json:"op"`
	Values *[]SnapshotValue `json:"values,omitempty"`
}

// RecoveryModOp defines model for RecoveryMod.Op.
type RecoveryModOp string

// RecoveryOrigin What the directory announced about itself. Display, not proof of
// identity. No host, port, bind DN or credential is recorded.
type RecoveryOrigin struct {
	NamingContexts []string `json:"namingContexts"`
	Vendor         *string  `json:"vendor,omitempty"`
	VendorVersion  *string  `json:"vendorVersion,omitempty"`
}

// RecoveryOriginal The change that was applied, by kind, entry and attribute names. Never its values.
type RecoveryOriginal struct {
	Attributes *[]string `json:"attributes,omitempty"`
	Dn         string    `json:"dn"`
	TargetDn   *string   `json:"targetDn,omitempty"`
	Type       string    `json:"type"`
}

// RecoveryReason defines model for RecoveryReason.
type RecoveryReason struct {
	Attribute *string `json:"attribute,omitempty"`

	// Code Why a change is not exactly recoverable. Stable identifiers; see `docs/RECOVERY.md`.
	Code RecoveryReasonCode `json:"code"`
}

// RecoveryReasonCode Why a change is not exactly recoverable. Stable identifiers; see `docs/RECOVERY.md`.
type RecoveryReasonCode string

// RecoveryRecoverability How far a change can be compensated. `exact`: Alder can derive
// compensating changes that restore the ordinary directory state it
// captured before the change -- the user attributes the change touched,
// an entry's absence, or its former name -- subject to the plan's drift
// checks. It does not cover operational attributes such as
// `modifyTimestamp`, server-generated identifiers such as `entryUUID`,
// replication metadata, or attributes the bind could not read.
// `partial`: some of it; `reasons` say what is not. `unavailable`: none of
// it. Recovery is compensation through a reviewed plan, never a
// transaction or a guaranteed rollback.
type RecoveryRecoverability string

// RecoveryStep defines model for RecoveryStep.
type RecoveryStep struct {
	// Compensation Runs in this order, after the compensation of every later step.
	Compensation []RecoveryChange `json:"compensation"`

	// Index The change's position in the set that was applied.
	Index int `json:"index"`

	// Kind Which area of the server a change lands in. `schema`: the server's
	// schema entry, where a write changes what every entry may hold.
	// `config`: the server's own configuration tree. `data`: everything else.
	// Decided from the locations the server announces, not from a DN pattern.
	Kind PlanTargetKind `json:"kind"`

	// Original The change that was applied, by kind, entry and attribute names. Never its values.
	Original RecoveryOriginal  `json:"original"`
	Reasons  *[]RecoveryReason `json:"reasons,omitempty"`

	// Recoverability How far a change can be compensated. `exact`: Alder can derive
	// compensating changes that restore the ordinary directory state it
	// captured before the change -- the user attributes the change touched,
	// an entry's absence, or its former name -- subject to the plan's drift
	// checks. It does not cover operational attributes such as
	// `modifyTimestamp`, server-generated identifiers such as `entryUUID`,
	// replication metadata, or attributes the bind could not read.
	// `partial`: some of it; `reasons` say what is not. `unavailable`: none of
	// it. Recovery is compensation through a reviewed plan, never a
	// transaction or a guaranteed rollback.
	Recoverability RecoveryRecoverability `json:"recoverability"`
}

// Reference defines model for Reference.
type Reference struct {
	// Attribute The attribute it names this entry by, canonically spelled. Removing
	// the reference is a delete of one value of this attribute on that
	// entry — which is why it is reported rather than inferred.
	Attribute string `json:"attribute"`

	// Dn The entry doing the naming.
	Dn  string  `json:"dn"`
	Rdn *string `json:"rdn,omitempty"`

	// Value The value as the directory stores it, which is not always the
	// subject's own DN: `uniqueMember` may carry a `#uid` suffix, and a
	// server may return a different but equivalent spelling. A delete has
	// to name the stored value — `uniqueMemberMatch` compares the UID
	// part too, so a delete of the bare DN would match nothing and the
	// reference would survive a change that reported success.
	Value string `json:"value"`
}

// ReferenceList defines model for ReferenceList.
type ReferenceList struct {
	References []Reference `json:"references"`

	// SearchedBase The naming context this looked in.
	SearchedBase *string `json:"searchedBase,omitempty"`

	// Truncated True when the search stopped early, so there are references this
	// does not list. A list that quietly omits some is worse than none
	// when the question is "what would break if I deleted this".
	//
	// Note that `false` does not make the list complete. It is the result
	// of a search, so it holds the referring entries *this session can
	// see*; one hidden by the access rules is absent exactly as one that
	// does not exist is, and nothing in the protocol distinguishes them.
	// The answer is "nothing I can see points at this", which is not the
	// same claim as "nothing points at this".
	//
	// That applies to a list with entries in it as much as to an empty
	// one: acting on three references you can see is no safer when there
	// is a fourth you cannot. Unlike the comparison, there is nothing to
	// detect here -- a Compare recovers absent-from-denied for an
	// attribute on an entry you can already read, and no operation says
	// "your search would have matched something you may not see".
	Truncated bool `json:"truncated"`
}

// Requirements defines model for Requirements.
type Requirements struct {
	May  *[]string `json:"may,omitempty"`
	Must *[]string `json:"must,omitempty"`

	// SettableOperational Operational attributes this server says a client may set on an
	// entry: `USAGE directoryOperation` and no `NO-USER-MODIFICATION`.
	//
	// Operational is not the same as read-only, and treating it as such
	// is why an account could not be locked from here — 389 DS keeps
	// `nsAccountLock` itself and still expects you to set it, as OpenLDAP
	// does for a ppolicy `pwdAccountLockedTime`. Only
	// `NO-USER-MODIFICATION` means the directory owns an attribute.
	//
	// These are not in any object class, so they never appear in `must`
	// or `may` and an entry that has never been locked shows no sign that
	// it could be. They are listed separately rather than merged in,
	// because "what this class permits" and "what the server lets you set
	// besides" are different answers to different questions.
	//
	// `dSAOperation` attributes are excluded: `namingContexts` and
	// `supportedControl` are operational and unflagged too, but they
	// belong to the server rather than to an entry.
	SettableOperational *[]string `json:"settableOperational,omitempty"`

	// Structural The single structural class, or empty when it is ambiguous.
	Structural *string `json:"structural,omitempty"`

	// Unknown Object classes on the entry that the schema does not define.
	Unknown *[]string `json:"unknown,omitempty"`
}

// RequirementsView defines model for RequirementsView.
type RequirementsView struct {
	// Kinds One per attribute in the MUST and MAY sets.
	Kinds        []AttributeKind `json:"kinds"`
	Requirements Requirements    `json:"requirements"`
}

// ResolveResult defines model for ResolveResult.
type ResolveResult struct {
	// Destinations Empty when the input is not usable — a half-typed filter, say.
	// More than one when the input is genuinely ambiguous.
	Destinations []Destination `json:"destinations"`
	Query        string        `json:"query"`
}

// SchemaAttributeTypeDefinition defines model for SchemaAttributeTypeDefinition.
type SchemaAttributeTypeDefinition struct {
	Collection *string `json:"collection,omitempty"`
	Collective *bool   `json:"collective,omitempty"`

	// Definition The definition text the server published.
	Definition         string            `json:"definition"`
	Desc               *string           `json:"desc,omitempty"`
	Equality           *string           `json:"equality,omitempty"`
	Extensions         *SchemaExtensions `json:"extensions,omitempty"`
	Names              *[]string         `json:"names,omitempty"`
	NoUserModification *bool             `json:"noUserModification,omitempty"`
	Obsolete           *bool             `json:"obsolete,omitempty"`
	Oid                string            `json:"oid"`
	Ordering           *string           `json:"ordering,omitempty"`
	SingleValue        *bool             `json:"singleValue,omitempty"`
	Substr             *string           `json:"substr,omitempty"`
	Sup                *string           `json:"sup,omitempty"`
	Syntax             *string           `json:"syntax,omitempty"`
	SyntaxLength       *int              `json:"syntaxLength,omitempty"`
	Unrecognized       *[]string         `json:"unrecognized,omitempty"`
	Usage              string            `json:"usage"`
}

// SchemaChangeBuild defines model for SchemaChangeBuild.
type SchemaChangeBuild struct {
	Change ChangeRequest `json:"change"`

	// Definition The RFC 4512 text this change installs. Absent for a delete.
	Definition *string `json:"definition,omitempty"`

	// Removes The definition this change removes, exactly as the server stores it.
	// Shown so a replace can be read as the substitution it is.
	Removes *string `json:"removes,omitempty"`
}

// SchemaChangeRequest defines model for SchemaChangeRequest.
type SchemaChangeRequest struct {
	// AttributeType An attribute type as it declares itself, in the shape a change posts
	// back.
	//
	// The detail responses return one of these so an edit starts from what the
	// definition actually says. The summary alongside it is for reading: it
	// reports effective values, resolved through SUP, which is what somebody
	// looking at an attribute wants to know and exactly what an editor must not
	// write back — a value inherited from a superior would become one declared
	// here.
	AttributeType *AttributeTypeInput `json:"attributeType,omitempty"`

	// Definition A complete RFC 4512 definition, for someone who would rather write
	// one than fill in a form. Takes precedence over the structured input
	// when both are given, and is parsed and checked either way.
	Definition *string                 `json:"definition,omitempty"`
	Kind       SchemaChangeRequestKind `json:"kind"`

	// ObjectClass An object class as it declares itself, in the shape a change posts back.
	// Returned by the detail response so an edit starts from the definition
	// rather than from the summary, which resolves inherited attributes.
	ObjectClass *ObjectClassInput `json:"objectClass,omitempty"`

	// Oid Which definition to replace or remove. Ignored for an add.
	Oid *string               `json:"oid,omitempty"`
	Op  SchemaChangeRequestOp `json:"op"`

	// TargetDn The schema entry to change. It must be one the capabilities listed;
	// any other DN is refused, because accepting one would turn this into
	// a way to aim an arbitrary modification at an arbitrary entry.
	TargetDn string `json:"targetDn"`
}

// SchemaChangeRequestKind defines model for SchemaChangeRequest.Kind.
type SchemaChangeRequestKind string

// SchemaChangeRequestOp defines model for SchemaChangeRequest.Op.
type SchemaChangeRequestOp string

// SchemaContextDefinition defines model for SchemaContextDefinition.
type SchemaContextDefinition struct {
	Definition string    `json:"definition"`
	Desc       *string   `json:"desc,omitempty"`
	Names      *[]string `json:"names,omitempty"`
	Oid        string    `json:"oid"`
	Syntax     *string   `json:"syntax,omitempty"`
}

// SchemaCounts defines model for SchemaCounts.
type SchemaCounts struct {
	AttributeTypes   *int `json:"attributeTypes,omitempty"`
	DitContentRules  *int `json:"ditContentRules,omitempty"`
	Errors           *int `json:"errors,omitempty"`
	MatchingRuleUses *int `json:"matchingRuleUses,omitempty"`
	MatchingRules    *int `json:"matchingRules,omitempty"`
	NameForms        *int `json:"nameForms,omitempty"`
	ObjectClasses    *int `json:"objectClasses,omitempty"`
	Syntaxes         *int `json:"syntaxes,omitempty"`
}

// SchemaDiff defines model for SchemaDiff.
type SchemaDiff struct {
	AttributeTypes SchemaDiffCounts `json:"attributeTypes"`
	Items          []SchemaDiffItem `json:"items"`
	ObjectClasses  SchemaDiffCounts `json:"objectClasses"`

	// Order Keys of the items a change could be derived for, in dependency order:
	// what a definition needs before it for additions and modifications, what
	// names a definition before it for removals. Never lexical.
	Order []string `json:"order"`
}

// SchemaDiffCandidate Change requests that would move the live schema toward the target: the
// schema editor's own modifications of the schema entry, input for
// `POST /plan`, never applied from here.
type SchemaDiffCandidate struct {
	Blocked *DiffCandidateBlocked `json:"blocked,omitempty"`
	Changes []ChangeRequest       `json:"changes"`

	// Destructive The candidate removes a definition. Never selected for the caller.
	Destructive bool             `json:"destructive"`
	Impact      *[]SchemaProblem `json:"impact,omitempty"`

	// Requires Keys of other items whose change must be staged before this one.
	Requires *[]string `json:"requires,omitempty"`
}

// SchemaDiffCounts defines model for SchemaDiffCounts.
type SchemaDiffCounts struct {
	Added        int `json:"added"`
	Compared     int `json:"compared"`
	MetadataOnly int `json:"metadataOnly"`
	Modified     int `json:"modified"`
	Removed      int `json:"removed"`
	Unchanged    int `json:"unchanged"`
	Unknown      int `json:"unknown"`
}

// SchemaDiffItem defines model for SchemaDiffItem.
type SchemaDiffItem struct {
	// Candidate Change requests that would move the live schema toward the target: the
	// schema editor's own modifications of the schema entry, input for
	// `POST /plan`, never applied from here.
	Candidate *SchemaDiffCandidate `json:"candidate,omitempty"`
	Element   SchemaElementKind    `json:"element"`
	Fields    *[]SchemaFieldChange `json:"fields,omitempty"`

	// Key `element:oid`, stable within a comparison.
	Key string `json:"key"`

	// Kind `metadata_only` (1.10) is a schema definition whose meaning is equal and whose X- extensions differ.
	Kind             DiffKind           `json:"kind"`
	Names            *[]string          `json:"names,omitempty"`
	Oid              string             `json:"oid"`
	Problems         *[]SchemaProblem   `json:"problems,omitempty"`
	RequiredBy       *[]SchemaReference `json:"requiredBy,omitempty"`
	Requires         *[]SchemaReference `json:"requires,omitempty"`
	SourceCollection *string            `json:"sourceCollection,omitempty"`
	SourceDefinition *string            `json:"sourceDefinition,omitempty"`
	TargetCollection *string            `json:"targetCollection,omitempty"`
	TargetDefinition *string            `json:"targetDefinition,omitempty"`
}

// SchemaElementKind defines model for SchemaElementKind.
type SchemaElementKind string

// SchemaExtensions defines model for SchemaExtensions.
type SchemaExtensions map[string][]string

// SchemaFieldCategory `core` changes what a definition does; `description` is DESC; `extension` is an X- extension.
type SchemaFieldCategory string

// SchemaFieldChange defines model for SchemaFieldChange.
type SchemaFieldChange struct {
	// Category `core` changes what a definition does; `description` is DESC; `extension` is an X- extension.
	Category SchemaFieldCategory `json:"category"`
	Field    string              `json:"field"`
	Source   *[]string           `json:"source,omitempty"`
	Target   *[]string           `json:"target,omitempty"`
}

// SchemaObjectClassDefinition defines model for SchemaObjectClassDefinition.
type SchemaObjectClassDefinition struct {
	Collection   *string           `json:"collection,omitempty"`
	Definition   string            `json:"definition"`
	Desc         *string           `json:"desc,omitempty"`
	Extensions   *SchemaExtensions `json:"extensions,omitempty"`
	Kind         string            `json:"kind"`
	May          *[]string         `json:"may,omitempty"`
	Must         *[]string         `json:"must,omitempty"`
	Names        *[]string         `json:"names,omitempty"`
	Obsolete     *bool             `json:"obsolete,omitempty"`
	Oid          string            `json:"oid"`
	Sup          *[]string         `json:"sup,omitempty"`
	Unrecognized *[]string         `json:"unrecognized,omitempty"`
}

// SchemaParseError defines model for SchemaParseError.
type SchemaParseError struct {
	Attribute  *string `json:"attribute,omitempty"`
	Definition *string `json:"definition,omitempty"`
	Message    *string `json:"message,omitempty"`
}

// SchemaProblem A fact about a schema difference. `unparsed_definition`, `ambiguous_name`
// and `unrecognized_keyword` make it `unknown`. `non_numeric_oid`,
// `unresolved_reference` and `dependency_cycle` block a change.
// `referenced_by_schema`, `used_by_entries` and `usage_unknown` describe a
// removal's impact; `usage_unknown` is never evidence of no use.
type SchemaProblem string

// SchemaReference defines model for SchemaReference.
type SchemaReference struct {
	Element SchemaElementKind `json:"element"`
	Name    *string           `json:"name,omitempty"`

	// Oid Empty when the reference resolves to nothing on that side.
	Oid      string                  `json:"oid"`
	Relation SchemaReferenceRelation `json:"relation"`
}

// SchemaReferenceRelation defines model for SchemaReference.Relation.
type SchemaReferenceRelation string

// SchemaSnapshot An Alder schema snapshot: format `alder-snapshot`, version 1, kind
// `schema` (1.10). See `docs/SNAPSHOTS.md`. Canonical, checksummed like a
// data snapshot, and holding no server configuration.
type SchemaSnapshot struct {
	AttributeTypes []SchemaAttributeTypeDefinition `json:"attributeTypes"`
	Checksum       *string                         `json:"checksum,omitempty"`
	Completeness   SchemaSnapshotCompleteness      `json:"completeness"`
	Context        SchemaSnapshotContext           `json:"context"`
	Counts         SchemaSnapshotCounts            `json:"counts"`
	Coverage       SchemaSnapshotCoverage          `json:"coverage"`
	CreatedAt      time.Time                       `json:"createdAt"`
	Format         SchemaSnapshotFormat            `json:"format"`
	Kind           SchemaSnapshotKind              `json:"kind"`
	ObjectClasses  []SchemaObjectClassDefinition   `json:"objectClasses"`
	Source         SchemaSnapshotSource            `json:"source"`
	Unparsed       []SchemaUnparsedDefinition      `json:"unparsed"`
	Version        int                             `json:"version"`
}

// SchemaSnapshotCompleteness defines model for SchemaSnapshot.Completeness.
type SchemaSnapshotCompleteness string

// SchemaSnapshotFormat defines model for SchemaSnapshot.Format.
type SchemaSnapshotFormat string

// SchemaSnapshotKind defines model for SchemaSnapshot.Kind.
type SchemaSnapshotKind string

// SchemaSnapshotContext defines model for SchemaSnapshotContext.
type SchemaSnapshotContext struct {
	DitContentRules []SchemaContextDefinition `json:"ditContentRules"`
	LdapSyntaxes    []SchemaContextDefinition `json:"ldapSyntaxes"`
	MatchingRuleUse []SchemaContextDefinition `json:"matchingRuleUse"`
	MatchingRules   []SchemaContextDefinition `json:"matchingRules"`
	NameForms       []SchemaContextDefinition `json:"nameForms"`
}

// SchemaSnapshotCounts defines model for SchemaSnapshotCounts.
type SchemaSnapshotCounts struct {
	AttributeTypes  int `json:"attributeTypes"`
	DitContentRules int `json:"ditContentRules"`
	LdapSyntaxes    int `json:"ldapSyntaxes"`
	MatchingRuleUse int `json:"matchingRuleUse"`
	MatchingRules   int `json:"matchingRules"`
	NameForms       int `json:"nameForms"`
	ObjectClasses   int `json:"objectClasses"`
	Unparsed        int `json:"unparsed"`
}

// SchemaSnapshotCoverage defines model for SchemaSnapshotCoverage.
type SchemaSnapshotCoverage struct {
	Compared    []string `json:"compared"`
	Context     []string `json:"context"`
	NotCaptured []string `json:"notCaptured"`
}

// SchemaSnapshotSource defines model for SchemaSnapshotSource.
type SchemaSnapshotSource struct {
	// Collections The server keeps schema in configuration collections and the capturing
	// session could read them, so definitions record the collection holding
	// them.
	Collections bool `json:"collections"`

	// SubschemaEntry The DN the server publishes its schema at.
	SubschemaEntry string  `json:"subschemaEntry"`
	Vendor         *string `json:"vendor,omitempty"`
	VendorVersion  *string `json:"vendorVersion,omitempty"`
}

// SchemaSnapshotSummary defines model for SchemaSnapshotSummary.
type SchemaSnapshotSummary struct {
	Collections    bool                              `json:"collections"`
	Completeness   SchemaSnapshotSummaryCompleteness `json:"completeness"`
	Counts         SchemaSnapshotCounts              `json:"counts"`
	SubschemaEntry string                            `json:"subschemaEntry"`
}

// SchemaSnapshotSummaryCompleteness defines model for SchemaSnapshotSummary.Completeness.
type SchemaSnapshotSummaryCompleteness string

// SchemaTarget defines model for SchemaTarget.
type SchemaTarget struct {
	AttributeTypes int    `json:"attributeTypes"`
	Dn             string `json:"dn"`
	Name           string `json:"name"`
	ObjectClasses  int    `json:"objectClasses"`
}

// SchemaUnparsedDefinition defines model for SchemaUnparsedDefinition.
type SchemaUnparsedDefinition struct {
	Attribute  string `json:"attribute"`
	Definition string `json:"definition"`
	Error      string `json:"error"`
}

// SchemaView defines model for SchemaView.
type SchemaView struct {
	AttributeTypes *[]AttributeTypeSummary `json:"attributeTypes,omitempty"`
	Counts         SchemaCounts            `json:"counts"`

	// Errors Definitions the server published that this parser could not read.
	// A non-empty list is a partial load, not a failed one, and the UI
	// says so rather than pretending the schema is complete.
	Errors        *[]SchemaParseError    `json:"errors,omitempty"`
	MatchingRules *[]MatchingRuleSummary `json:"matchingRules,omitempty"`
	ObjectClasses *[]ObjectClassSummary  `json:"objectClasses,omitempty"`
	SubschemaDn   string                 `json:"subschemaDn"`
	Syntaxes      *[]SyntaxSummary       `json:"syntaxes,omitempty"`
}

// SchemaWrite Where, and whether, this session can write schema definitions. Style is
// derived from what the server announced, never from its name: a server
// that publishes a configContext keeps its schema in configuration
// entries, and one that does not lets its subschema subentry be modified.
type SchemaWrite struct {
	AttributeTypeAttr *string          `json:"attributeTypeAttr,omitempty"`
	ObjectClassAttr   *string          `json:"objectClassAttr,omitempty"`
	Style             SchemaWriteStyle `json:"style"`

	// Targets The entries a definition may be written to. Exactly one under the
	// subschema style. Under the config style a server holds several, and
	// which one a new definition joins is a choice with consequences,
	// because they load in order.
	Targets *[]SchemaTarget `json:"targets,omitempty"`

	// Unavailable Why there is nowhere to write, in a sentence meant to be read.
	// Present only when style is none.
	Unavailable *string `json:"unavailable,omitempty"`
}

// SchemaWriteStyle defines model for SchemaWrite.Style.
type SchemaWriteStyle string

// SearchRequest defines model for SearchRequest.
type SearchRequest struct {
	Attributes *[]string `json:"attributes,omitempty"`
	BaseDn     string    `json:"baseDn"`
	Cookie     *string   `json:"cookie,omitempty"`

	// Filter A raw RFC 4515 filter. It is parsed, never interpolated.
	Filter   string             `json:"filter"`
	Limit    *int               `json:"limit,omitempty"`
	PageSize *int               `json:"pageSize,omitempty"`
	Scope    SearchRequestScope `json:"scope"`
}

// SearchRequestScope defines model for SearchRequest.Scope.
type SearchRequestScope string

// SearchResponse defines model for SearchResponse.
type SearchResponse struct {
	// Command The `ldapsearch` that would run this same search, for a runbook, a
	// ticket or a script.
	//
	// Rendered here rather than in the browser for the reason the LDIF
	// preview is: the browser holds the filter as it was typed, the
	// server holds the filter it parsed and would send, and normalising
	// it is the point of parsing it. A command built from the typed text
	// would describe a different search from the results beside it.
	//
	// Never contains the bind password. `-W` makes ldapsearch prompt.
	Command   *string             `json:"command,omitempty"`
	Cookie    *string             `json:"cookie,omitempty"`
	Entries   []SearchResultEntry `json:"entries"`
	Referrals *[]string           `json:"referrals,omitempty"`

	// Took How long the search took, as a duration string.
	Took *string `json:"took,omitempty"`

	// Truncated True when results were cut short. A silently truncated search is
	// worse than no search, so this is always surfaced.
	Truncated bool `json:"truncated"`
}

// SearchResultEntry defines model for SearchResultEntry.
type SearchResultEntry struct {
	Attributes *[]EntryAttribute `json:"attributes,omitempty"`
	Dn         string            `json:"dn"`
	Rdn        *string           `json:"rdn,omitempty"`
}

// SessionInfo defines model for SessionInfo.
type SessionInfo struct {
	// BindDn Empty for an anonymous bind.
	BindDn       *string       `json:"bindDn,omitempty"`
	Capabilities *Capabilities `json:"capabilities,omitempty"`
	Connected    bool          `json:"connected"`
	Host         *string       `json:"host,omitempty"`
	Port         *int          `json:"port,omitempty"`

	// ReadOnly True when the server was started with --read-only.
	ReadOnly *bool   `json:"readOnly,omitempty"`
	Tls      *string `json:"tls,omitempty"`

	// VendorName Display only. Nothing branches on it.
	VendorName    *string `json:"vendorName,omitempty"`
	VendorVersion *string `json:"vendorVersion,omitempty"`

	// Verified False when the session skipped certificate verification.
	Verified *bool `json:"verified,omitempty"`
}

// Snapshot An Alder snapshot, format version 1. The document is canonical: see
// `docs/SNAPSHOTS.md` for its ordering and what `checksum` covers.
type Snapshot struct {
	Attributes []SnapshotAttributeInfo `json:"attributes"`

	// Checksum `sha256:<hex>` over the canonical content: every field except `createdAt` and `checksum`. A change to the captured entries or to the covered metadata invalidates it; a change to `createdAt` does not. An integrity check against corruption, not authentication or a signature.
	Checksum              *string              `json:"checksum,omitempty"`
	Completeness          SnapshotCompleteness `json:"completeness"`
	CreatedAt             time.Time            `json:"createdAt"`
	Entries               []SnapshotEntry      `json:"entries"`
	EntryCount            int                  `json:"entryCount"`
	Excluded              []SnapshotExcluded   `json:"excluded"`
	Format                SnapshotFormat       `json:"format"`
	Kind                  SnapshotKind         `json:"kind"`
	OperationalAttributes bool                 `json:"operationalAttributes"`
	SchemaAvailable       bool                 `json:"schemaAvailable"`
	Source                SnapshotSource       `json:"source"`

	// Version The snapshot format version. This Alder reads 1.
	Version int `json:"version"`
}

// SnapshotCompleteness defines model for Snapshot.Completeness.
type SnapshotCompleteness string

// SnapshotFormat defines model for Snapshot.Format.
type SnapshotFormat string

// SnapshotKind defines model for Snapshot.Kind.
type SnapshotKind string

// SnapshotAttribute defines model for SnapshotAttribute.
type SnapshotAttribute struct {
	Name   string           `json:"name"`
	Values *[]SnapshotValue `json:"values,omitempty"`

	// Withheld For a sensitive attribute, how many values it held. It never has values.
	Withheld *int `json:"withheld,omitempty"`
}

// SnapshotAttributeInfo defines model for SnapshotAttributeInfo.
type SnapshotAttributeInfo struct {
	Equality    *string `json:"equality,omitempty"`
	Name        string  `json:"name"`
	Operational *bool   `json:"operational,omitempty"`
	Sensitive   *bool   `json:"sensitive,omitempty"`
	SingleValue *bool   `json:"singleValue,omitempty"`
	Syntax      *string `json:"syntax,omitempty"`
}

// SnapshotCaptureRequest defines model for SnapshotCaptureRequest.
type SnapshotCaptureRequest struct {
	// Base The subtree to capture. Required for kind data.
	Base *string `json:"base,omitempty"`

	// Filter An RFC 4515 filter, defaulting to `(objectClass=*)`. Parsed, never pasted.
	Filter *string `json:"filter,omitempty"`

	// Kind What a snapshot or comparison is of: directory data, the published
	// schema (1.10), or the server's own configuration (1.13). A release
	// before the one that added a kind refuses a snapshot of it rather than
	// misreading it.
	Kind *StateKind `json:"kind,omitempty"`

	// OperationalAttributes Capture operational attributes too. Off by default.
	OperationalAttributes *bool          `json:"operationalAttributes,omitempty"`
	Scope                 *SnapshotScope `json:"scope,omitempty"`
}

// SnapshotEntry defines model for SnapshotEntry.
type SnapshotEntry struct {
	Attributes []SnapshotAttribute `json:"attributes"`
	Dn         string              `json:"dn"`

	// Id The server's stable identity for the entry, as `entryUUID=...` or `nsUniqueId=...`.
	Id *string `json:"id,omitempty"`
}

// SnapshotExcluded defines model for SnapshotExcluded.
type SnapshotExcluded string

// SnapshotInspection defines model for SnapshotInspection.
type SnapshotInspection struct {
	// Checksum The checksum of the canonical content, whether or not the document carried one.
	Checksum              string                 `json:"checksum"`
	CreatedAt             string                 `json:"createdAt"`
	EntryCount            int                    `json:"entryCount"`
	Excluded              []SnapshotExcluded     `json:"excluded"`
	Integrity             SnapshotIntegrity      `json:"integrity"`
	Kind                  string                 `json:"kind"`
	OperationalAttributes bool                   `json:"operationalAttributes"`
	Schema                *SchemaSnapshotSummary `json:"schema,omitempty"`
	SchemaAvailable       bool                   `json:"schemaAvailable"`
	Source                SnapshotSource         `json:"source"`
	Version               int                    `json:"version"`
}

// SnapshotIntegrity defines model for SnapshotIntegrity.
type SnapshotIntegrity string

// SnapshotScope defines model for SnapshotScope.
type SnapshotScope string

// SnapshotSource defines model for SnapshotSource.
type SnapshotSource struct {
	Base          string        `json:"base"`
	Filter        string        `json:"filter"`
	Scope         SnapshotScope `json:"scope"`
	Vendor        *string       `json:"vendor,omitempty"`
	VendorVersion *string       `json:"vendorVersion,omitempty"`
}

// SnapshotValue Exactly one of `text` or `base64`.
type SnapshotValue struct {
	Base64 *string `json:"base64,omitempty"`
	Text   *string `json:"text,omitempty"`
}

// SourceOffer defines model for SourceOffer.
type SourceOffer struct {
	// License The SPDX identifier Alder is licensed under.
	License string `json:"license"`

	// Modified True when the build came from a tree with uncommitted changes, so
	// `revision` alone does not identify the source. Absent means false.
	Modified *bool `json:"modified,omitempty"`

	// Notice The offer in prose, for a person rather than a client.
	Notice string `json:"notice"`

	// Revision The commit this binary was built from, when the build recorded one.
	Revision *string `json:"revision,omitempty"`

	// SourceUrl Where the Corresponding Source of *this* build lives. An operator
	// running a fork sets `--source-url` to their own; leaving it at the
	// default while running a modified build does not comply.
	SourceUrl string `json:"sourceUrl"`
	Version   string `json:"version"`
}

// StateKind What a snapshot or comparison is of: directory data, the published
// schema (1.10), or the server's own configuration (1.13). A release
// before the one that added a kind refuses a snapshot of it rather than
// misreading it.
type StateKind string

// SyntaxSummary defines model for SyntaxSummary.
type SyntaxSummary struct {
	Desc        *string `json:"desc,omitempty"`
	Kind        *string `json:"kind,omitempty"`
	Oid         string  `json:"oid"`
	UsedByCount *int    `json:"usedByCount,omitempty"`
}

// TreeNode defines model for TreeNode.
type TreeNode struct {
	Dn              string    `json:"dn"`
	HasChildren     bool      `json:"hasChildren"`
	IsNamingContext *bool     `json:"isNamingContext,omitempty"`
	ObjectClasses   *[]string `json:"objectClasses,omitempty"`

	// Rdn The leftmost RDN, which is what the tree shows as a label.
	Rdn string `json:"rdn"`

	// Structural The structural class, used to pick an icon.
	Structural *string `json:"structural,omitempty"`
}

// TreePage defines model for TreePage.
type TreePage struct {
	Cookie *string `json:"cookie,omitempty"`

	// HiddenChildren Children this parent has that the session could not see.
	//
	// Some servers publish `numSubordinates`, computed by the server and
	// not filtered by the access rules, and where they do the gap between
	// it and the children returned is exactly what this bind may not see.
	// Absent where the server does not publish it, which is a capability
	// and not a vendor trait — it is read from the entry, never inferred
	// from who the server is. Absent too when the listing was truncated,
	// since then the gap is paging rather than access.
	HiddenChildren *int       `json:"hiddenChildren,omitempty"`
	Nodes          []TreeNode `json:"nodes"`
	Truncated      *bool      `json:"truncated,omitempty"`
}

// ValueComparison defines model for ValueComparison.
type ValueComparison struct {
	Side ValueComparisonSide `json:"side"`

	// Value Exactly one of `text` or `base64` is present. `text` carries a value
	// that is valid UTF-8 and belongs to a textual syntax; everything else,
	// including anything with a `;binary` option, arrives as `base64`.
	Value AttributeValue `json:"value"`
}

// ValueComparisonSide defines model for ValueComparison.Side.
type ValueComparisonSide string

// BadRequest defines model for BadRequest.
type BadRequest = Error

// Forbidden defines model for Forbidden.
type Forbidden = Error

// NotFound defines model for NotFound.
type NotFound = Error

// Unauthorized defines model for Unauthorized.
type Unauthorized = Error

// ApplyChangeParams defines parameters for ApplyChange.
type ApplyChangeParams struct {
	// Recovery Return a recovery bundle for the change once it has been applied. Added in 1.9.
	Recovery *bool `form:"recovery,omitempty" json:"recovery,omitempty"`
}

// CompareEntriesParams defines parameters for CompareEntries.
type CompareEntriesParams struct {
	// Left Usually the entry that works.
	Left  string `form:"left" json:"left"`
	Right string `form:"right" json:"right"`

	// IncludeOperational On by default, unlike the LDIF export. `nsAccountLock` and
	// `pwdAccountLockedTime` are operational on the target servers and are
	// frequently the entire answer.
	IncludeOperational *bool `form:"includeOperational,omitempty" json:"includeOperational,omitempty"`

	// Limit Values reported per attribute.
	Limit *int `form:"limit,omitempty" json:"limit,omitempty"`
}

// CountEntriesParams defines parameters for CountEntries.
type CountEntriesParams struct {
	Dn    string `form:"dn" json:"dn"`
	Limit *int   `form:"limit,omitempty" json:"limit,omitempty"`
}

// GetEntryParams defines parameters for GetEntry.
type GetEntryParams struct {
	Dn                 string `form:"dn" json:"dn"`
	IncludeOperational *bool  `form:"includeOperational,omitempty" json:"includeOperational,omitempty"`
}

// ExportAnsibleParams defines parameters for ExportAnsible.
type ExportAnsibleParams struct {
	Dn    string                    `form:"dn" json:"dn"`
	Scope *ExportAnsibleParamsScope `form:"scope,omitempty" json:"scope,omitempty"`

	// Filter An RFC 4515 filter, defaulting to `(objectClass=*)`, parsed here
	// and never pasted — so a table can export what it is showing.
	Filter *string `form:"filter,omitempty" json:"filter,omitempty"`
	Limit  *int    `form:"limit,omitempty" json:"limit,omitempty"`
}

// ExportAnsibleParamsScope defines parameters for ExportAnsible.
type ExportAnsibleParamsScope string

// ExportLdifParams defines parameters for ExportLdif.
type ExportLdifParams struct {
	Dn    string                 `form:"dn" json:"dn"`
	Scope *ExportLdifParamsScope `form:"scope,omitempty" json:"scope,omitempty"`

	// IncludeOperational Operational attributes cannot be written back, so they are off by default.
	IncludeOperational *bool `form:"includeOperational,omitempty" json:"includeOperational,omitempty"`

	// IncludeSensitive Password hashes and similar are omitted unless this is set. An LDIF
	// export ends up in tickets and repositories.
	IncludeSensitive *bool `form:"includeSensitive,omitempty" json:"includeSensitive,omitempty"`

	// Filter An RFC 4515 filter, defaulting to `(objectClass=*)`. It is parsed
	// here, never pasted, exactly as the search filter is.
	//
	// It exists so a table can export what it is showing. Without it the
	// only exports available are one entry, or a whole subtree, and
	// "the thirty people I just searched for" — which is what somebody
	// actually wants to put in a ticket or a repository — could only be
	// assembled by hand.
	Filter *string `form:"filter,omitempty" json:"filter,omitempty"`

	// Limit The document is streamed and rendered a page at a time, so this
	// bounds how long the export runs rather than how much it holds.
	Limit *int `form:"limit,omitempty" json:"limit,omitempty"`
}

// ExportLdifParamsScope defines parameters for ExportLdif.
type ExportLdifParamsScope string

// ExportOutlineParams defines parameters for ExportOutline.
type ExportOutlineParams struct {
	Dn    string                    `form:"dn" json:"dn"`
	Scope *ExportOutlineParamsScope `form:"scope,omitempty" json:"scope,omitempty"`

	// Filter An RFC 4515 filter, parsed and never pasted.
	Filter *string `form:"filter,omitempty" json:"filter,omitempty"`
	Limit  *int    `form:"limit,omitempty" json:"limit,omitempty"`
}

// ExportOutlineParamsScope defines parameters for ExportOutline.
type ExportOutlineParamsScope string

// ExportYamlParams defines parameters for ExportYaml.
type ExportYamlParams struct {
	Dn    string                 `form:"dn" json:"dn"`
	Scope *ExportYamlParamsScope `form:"scope,omitempty" json:"scope,omitempty"`

	// Filter An RFC 4515 filter, parsed and never pasted.
	Filter             *string `form:"filter,omitempty" json:"filter,omitempty"`
	IncludeOperational *bool   `form:"includeOperational,omitempty" json:"includeOperational,omitempty"`

	// IncludeSensitive Off by default, as for LDIF. A file destined for an editor is a file
	// destined for a repository soon after.
	IncludeSensitive *bool `form:"includeSensitive,omitempty" json:"includeSensitive,omitempty"`
	Limit            *int  `form:"limit,omitempty" json:"limit,omitempty"`
}

// ExportYamlParamsScope defines parameters for ExportYaml.
type ExportYamlParamsScope string

// ExpandMembersParams defines parameters for ExpandMembers.
type ExpandMembersParams struct {
	// Dn The group to expand.
	Dn    string `form:"dn" json:"dn"`
	Limit *int   `form:"limit,omitempty" json:"limit,omitempty"`
}

// ListReferencesParams defines parameters for ListReferences.
type ListReferencesParams struct {
	// Dn The entry being referred to.
	Dn    string `form:"dn" json:"dn"`
	Limit *int   `form:"limit,omitempty" json:"limit,omitempty"`
}

// ResolveParams defines parameters for Resolve.
type ResolveParams struct {
	Q string `form:"q" json:"q"`
}

// GetRequirementsParams defines parameters for GetRequirements.
type GetRequirementsParams struct {
	// Class The object classes, repeated. Superiors are followed, so naming the
	// most specific one is enough.
	Class []string `form:"class" json:"class"`
}

// CaptureSnapshot200JSONResponseBody defines parameters for CaptureSnapshot.
type CaptureSnapshot200JSONResponseBody struct {
	union json.RawMessage
}

// InspectSnapshotJSONBody defines parameters for InspectSnapshot.
type InspectSnapshotJSONBody struct {
	union json.RawMessage
}

// ListChildrenParams defines parameters for ListChildren.
type ListChildrenParams struct {
	// Dn The parent. Omit for the roots.
	Dn    *string `form:"dn,omitempty" json:"dn,omitempty"`
	Limit *int    `form:"limit,omitempty" json:"limit,omitempty"`

	// Cookie Continuation cookie from a previous page.
	Cookie *string `form:"cookie,omitempty" json:"cookie,omitempty"`
}

// ApplyChangeJSONRequestBody defines body for ApplyChange for application/json ContentType.
type ApplyChangeJSONRequestBody = ChangeRequest

// PreviewChangeJSONRequestBody defines body for PreviewChange for application/json ContentType.
//
// Deprecated: this type has been marked as deprecated upstream, but no `x-deprecated-reason` was set
type PreviewChangeJSONRequestBody = ChangeRequest

// ApplyChangesetJSONRequestBody defines body for ApplyChangeset for application/json ContentType.
type ApplyChangesetJSONRequestBody = ChangesetRequest

// PreviewChangesetJSONRequestBody defines body for PreviewChangeset for application/json ContentType.
type PreviewChangesetJSONRequestBody = ChangesetRequest

// DiffStatesJSONRequestBody defines body for DiffStates for application/json ContentType.
type DiffStatesJSONRequestBody = DiffRequest

// ParseLdifJSONRequestBody defines body for ParseLdif for application/json ContentType.
type ParseLdifJSONRequestBody = ImportRequest

// InventoryValuesJSONRequestBody defines body for InventoryValues for application/json ContentType.
type InventoryValuesJSONRequestBody = InventoryRequest

// BuildPackageJSONRequestBody defines body for BuildPackage for application/json ContentType.
type BuildPackageJSONRequestBody = PackageBuildRequest

// InspectPackageJSONRequestBody defines body for InspectPackage for application/json ContentType.
type InspectPackageJSONRequestBody = ChangePackage

// ValidatePackageJSONRequestBody defines body for ValidatePackage for application/json ContentType.
type ValidatePackageJSONRequestBody = PackageValidationRequest

// PlanChangesJSONRequestBody defines body for PlanChanges for application/json ContentType.
type PlanChangesJSONRequestBody = PlanRequest

// PreflightJSONRequestBody defines body for Preflight for application/json ContentType.
type PreflightJSONRequestBody = PreflightRequest

// InspectRecoveryJSONRequestBody defines body for InspectRecovery for application/json ContentType.
type InspectRecoveryJSONRequestBody = RecoveryBundle

// BuildSchemaChangeJSONRequestBody defines body for BuildSchemaChange for application/json ContentType.
type BuildSchemaChangeJSONRequestBody = SchemaChangeRequest

// SearchJSONRequestBody defines body for Search for application/json ContentType.
type SearchJSONRequestBody = SearchRequest

// CreateSessionJSONRequestBody defines body for CreateSession for application/json ContentType.
type CreateSessionJSONRequestBody = ConnectRequest

// CaptureSnapshotJSONRequestBody defines body for CaptureSnapshot for application/json ContentType.
type CaptureSnapshotJSONRequestBody = SnapshotCaptureRequest

// InspectSnapshotJSONRequestBody defines body for InspectSnapshot for application/json ContentType.
type InspectSnapshotJSONRequestBody InspectSnapshotJSONBody

// AsSnapshot returns the union data inside the DiffSide_Snapshot as a Snapshot
func (t DiffSide_Snapshot) AsSnapshot() (Snapshot, error) {
	var body Snapshot
	err := json.Unmarshal(t.union, &body)
	return body, err
}

// FromSnapshot overwrites any union data inside the DiffSide_Snapshot as the provided Snapshot
func (t *DiffSide_Snapshot) FromSnapshot(v Snapshot) error {
	b, err := json.Marshal(v)
	t.union = b
	return err
}

// MergeSnapshot performs a merge with any union data inside the DiffSide_Snapshot, using the provided Snapshot
func (t *DiffSide_Snapshot) MergeSnapshot(v Snapshot) error {
	b, err := json.Marshal(v)
	if err != nil {
		return err
	}

	merged, err := runtime.JSONMerge(t.union, b)
	t.union = merged
	return err
}

// AsSchemaSnapshot returns the union data inside the DiffSide_Snapshot as a SchemaSnapshot
func (t DiffSide_Snapshot) AsSchemaSnapshot() (SchemaSnapshot, error) {
	var body SchemaSnapshot
	err := json.Unmarshal(t.union, &body)
	return body, err
}

// FromSchemaSnapshot overwrites any union data inside the DiffSide_Snapshot as the provided SchemaSnapshot
func (t *DiffSide_Snapshot) FromSchemaSnapshot(v SchemaSnapshot) error {
	b, err := json.Marshal(v)
	t.union = b
	return err
}

// MergeSchemaSnapshot performs a merge with any union data inside the DiffSide_Snapshot, using the provided SchemaSnapshot
func (t *DiffSide_Snapshot) MergeSchemaSnapshot(v SchemaSnapshot) error {
	b, err := json.Marshal(v)
	if err != nil {
		return err
	}

	merged, err := runtime.JSONMerge(t.union, b)
	t.union = merged
	return err
}

// AsConfigSnapshot returns the union data inside the DiffSide_Snapshot as a ConfigSnapshot
func (t DiffSide_Snapshot) AsConfigSnapshot() (ConfigSnapshot, error) {
	var body ConfigSnapshot
	err := json.Unmarshal(t.union, &body)
	return body, err
}

// FromConfigSnapshot overwrites any union data inside the DiffSide_Snapshot as the provided ConfigSnapshot
func (t *DiffSide_Snapshot) FromConfigSnapshot(v ConfigSnapshot) error {
	b, err := json.Marshal(v)
	t.union = b
	return err
}

// MergeConfigSnapshot performs a merge with any union data inside the DiffSide_Snapshot, using the provided ConfigSnapshot
func (t *DiffSide_Snapshot) MergeConfigSnapshot(v ConfigSnapshot) error {
	b, err := json.Marshal(v)
	if err != nil {
		return err
	}

	merged, err := runtime.JSONMerge(t.union, b)
	t.union = merged
	return err
}

func (t DiffSide_Snapshot) MarshalJSON() ([]byte, error) {
	b, err := t.union.MarshalJSON()
	return b, err
}

func (t *DiffSide_Snapshot) UnmarshalJSON(b []byte) error {
	err := t.union.UnmarshalJSON(b)
	return err
}

// AsSnapshot returns the union data inside the CaptureSnapshot200JSONResponseBody as a Snapshot
func (t CaptureSnapshot200JSONResponseBody) AsSnapshot() (Snapshot, error) {
	var body Snapshot
	err := json.Unmarshal(t.union, &body)
	return body, err
}

// FromSnapshot overwrites any union data inside the CaptureSnapshot200JSONResponseBody as the provided Snapshot
func (t *CaptureSnapshot200JSONResponseBody) FromSnapshot(v Snapshot) error {
	b, err := json.Marshal(v)
	t.union = b
	return err
}

// MergeSnapshot performs a merge with any union data inside the CaptureSnapshot200JSONResponseBody, using the provided Snapshot
func (t *CaptureSnapshot200JSONResponseBody) MergeSnapshot(v Snapshot) error {
	b, err := json.Marshal(v)
	if err != nil {
		return err
	}

	merged, err := runtime.JSONMerge(t.union, b)
	t.union = merged
	return err
}

// AsSchemaSnapshot returns the union data inside the CaptureSnapshot200JSONResponseBody as a SchemaSnapshot
func (t CaptureSnapshot200JSONResponseBody) AsSchemaSnapshot() (SchemaSnapshot, error) {
	var body SchemaSnapshot
	err := json.Unmarshal(t.union, &body)
	return body, err
}

// FromSchemaSnapshot overwrites any union data inside the CaptureSnapshot200JSONResponseBody as the provided SchemaSnapshot
func (t *CaptureSnapshot200JSONResponseBody) FromSchemaSnapshot(v SchemaSnapshot) error {
	b, err := json.Marshal(v)
	t.union = b
	return err
}

// MergeSchemaSnapshot performs a merge with any union data inside the CaptureSnapshot200JSONResponseBody, using the provided SchemaSnapshot
func (t *CaptureSnapshot200JSONResponseBody) MergeSchemaSnapshot(v SchemaSnapshot) error {
	b, err := json.Marshal(v)
	if err != nil {
		return err
	}

	merged, err := runtime.JSONMerge(t.union, b)
	t.union = merged
	return err
}

func (t CaptureSnapshot200JSONResponseBody) MarshalJSON() ([]byte, error) {
	b, err := t.union.MarshalJSON()
	return b, err
}

func (t *CaptureSnapshot200JSONResponseBody) UnmarshalJSON(b []byte) error {
	err := t.union.UnmarshalJSON(b)
	return err
}

// AsSnapshot returns the union data inside the InspectSnapshotJSONBody as a Snapshot
func (t InspectSnapshotJSONBody) AsSnapshot() (Snapshot, error) {
	var body Snapshot
	err := json.Unmarshal(t.union, &body)
	return body, err
}

// FromSnapshot overwrites any union data inside the InspectSnapshotJSONBody as the provided Snapshot
func (t *InspectSnapshotJSONBody) FromSnapshot(v Snapshot) error {
	b, err := json.Marshal(v)
	t.union = b
	return err
}

// MergeSnapshot performs a merge with any union data inside the InspectSnapshotJSONBody, using the provided Snapshot
func (t *InspectSnapshotJSONBody) MergeSnapshot(v Snapshot) error {
	b, err := json.Marshal(v)
	if err != nil {
		return err
	}

	merged, err := runtime.JSONMerge(t.union, b)
	t.union = merged
	return err
}

// AsSchemaSnapshot returns the union data inside the InspectSnapshotJSONBody as a SchemaSnapshot
func (t InspectSnapshotJSONBody) AsSchemaSnapshot() (SchemaSnapshot, error) {
	var body SchemaSnapshot
	err := json.Unmarshal(t.union, &body)
	return body, err
}

// FromSchemaSnapshot overwrites any union data inside the InspectSnapshotJSONBody as the provided SchemaSnapshot
func (t *InspectSnapshotJSONBody) FromSchemaSnapshot(v SchemaSnapshot) error {
	b, err := json.Marshal(v)
	t.union = b
	return err
}

// MergeSchemaSnapshot performs a merge with any union data inside the InspectSnapshotJSONBody, using the provided SchemaSnapshot
func (t *InspectSnapshotJSONBody) MergeSchemaSnapshot(v SchemaSnapshot) error {
	b, err := json.Marshal(v)
	if err != nil {
		return err
	}

	merged, err := runtime.JSONMerge(t.union, b)
	t.union = merged
	return err
}

func (t InspectSnapshotJSONBody) MarshalJSON() ([]byte, error) {
	b, err := t.union.MarshalJSON()
	return b, err
}

func (t *InspectSnapshotJSONBody) UnmarshalJSON(b []byte) error {
	err := t.union.UnmarshalJSON(b)
	return err
}

// RequestEditorFn is the function signature for the RequestEditor callback function
type RequestEditorFn func(ctx context.Context, req *http.Request) error

// Doer performs HTTP requests.
//
// The standard http.Client implements this interface.
type HttpRequestDoer interface {
	Do(req *http.Request) (*http.Response, error)
}

// Client which conforms to the OpenAPI3 specification for this service.
type Client struct {
	// The endpoint of the server conforming to this interface, with scheme,
	// https://api.deepmap.com for example. This can contain a path relative
	// to the server, such as https://api.deepmap.com/dev-test, and all the
	// paths in the swagger spec will be appended to the server.
	Server string

	// Doer for performing requests, typically a *http.Client with any
	// customized settings, such as certificate chains.
	Client HttpRequestDoer

	// A list of callbacks for modifying requests which are generated before sending over
	// the network.
	RequestEditors []RequestEditorFn
}

// ClientOption allows setting custom parameters during construction
type ClientOption func(*Client) error

// Creates a new Client, with reasonable defaults
func NewClient(server string, opts ...ClientOption) (*Client, error) {
	// create a client with sane default values
	client := Client{
		Server: server,
	}
	// mutate client and add all optional params
	for _, o := range opts {
		if err := o(&client); err != nil {
			return nil, err
		}
	}
	// ensure the server URL always has a trailing slash
	if !strings.HasSuffix(client.Server, "/") {
		client.Server += "/"
	}
	// create httpClient, if not already present
	if client.Client == nil {
		client.Client = &http.Client{}
	}
	return &client, nil
}

// WithHTTPClient allows overriding the default Doer, which is
// automatically created using http.Client. This is useful for tests.
func WithHTTPClient(doer HttpRequestDoer) ClientOption {
	return func(c *Client) error {
		c.Client = doer
		return nil
	}
}

// WithRequestEditorFn allows setting up a callback function, which will be
// called right before sending the request. This can be used to mutate the request.
func WithRequestEditorFn(fn RequestEditorFn) ClientOption {
	return func(c *Client) error {
		c.RequestEditors = append(c.RequestEditors, fn)
		return nil
	}
}

// The interface specification for the client above.
type ClientInterface interface {

	// ApplyChangeWithBody Apply a change
	//
	// Applies one change. With the `baseline` a plan returned, the server first
	// re-reads the entry and refuses the change if it is not the operation the
	// plan was issued for (`400 plan_mismatch`) or the directory has moved
	// since (`409 conflict`, `cause: plan_stale`). A secret in the change -- a
	// new password, a sensitive attribute value -- is part of that operation:
	// a different value than the one planned is a mismatch.
	//
	// Without a `baseline` the change is applied as it always was. The
	// interface always plans first and always sends one.
	//
	// With `recovery=true` the response carries a recovery bundle for the
	// change once it has succeeded: the compensating change derived from the
	// entry as it was read immediately before the change ran. See
	// `docs/RECOVERY.md`. A bundle is never applied from here or anywhere
	// else; it becomes ordinary changes through `POST /recovery/inspect`.
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with POST /changes/apply (the `ApplyChange` operationId).
	ApplyChangeWithBody(ctx context.Context, params *ApplyChangeParams, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// ApplyChange Apply a change
	//
	// Applies one change. With the `baseline` a plan returned, the server first
	// re-reads the entry and refuses the change if it is not the operation the
	// plan was issued for (`400 plan_mismatch`) or the directory has moved
	// since (`409 conflict`, `cause: plan_stale`). A secret in the change -- a
	// new password, a sensitive attribute value -- is part of that operation:
	// a different value than the one planned is a mismatch.
	//
	// Without a `baseline` the change is applied as it always was. The
	// interface always plans first and always sends one.
	//
	// With `recovery=true` the response carries a recovery bundle for the
	// change once it has succeeded: the compensating change derived from the
	// entry as it was read immediately before the change ran. See
	// `docs/RECOVERY.md`. A bundle is never applied from here or anywhere
	// else; it becomes ordinary changes through `POST /recovery/inspect`.
	//
	// Takes a body of the `application/json` content type.
	//
	// Corresponds with POST /changes/apply (the `ApplyChange` operationId).
	ApplyChange(ctx context.Context, params *ApplyChangeParams, body ApplyChangeJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)

	// PreviewChangeWithBody Render a change as LDIF and as an Ansible task
	//
	// Renders without applying and without reading the directory.
	//
	// Deprecated since 1.6, and kept for 1.x clients. The interface no longer
	// calls it: its confirmation dialog plans the change with `POST /plan`,
	// which returns the same rendering inside the plan item together with
	// what the change would do against the directory as it is, and a token
	// that `/changes/apply` holds the change to. A rendering alone says
	// nothing about whether the change still applies.
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with POST /changes/preview (the `PreviewChange` operationId).
	//
	// Deprecated: this operation has been marked as deprecated upstream, but no `x-deprecated-reason` was set
	PreviewChangeWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// PreviewChange Render a change as LDIF and as an Ansible task
	//
	// Renders without applying and without reading the directory.
	//
	// Deprecated since 1.6, and kept for 1.x clients. The interface no longer
	// calls it: its confirmation dialog plans the change with `POST /plan`,
	// which returns the same rendering inside the plan item together with
	// what the change would do against the directory as it is, and a token
	// that `/changes/apply` holds the change to. A rendering alone says
	// nothing about whether the change still applies.
	//
	// Takes a body of the `application/json` content type.
	//
	// Corresponds with POST /changes/preview (the `PreviewChange` operationId).
	//
	// Deprecated: this operation has been marked as deprecated upstream, but no `x-deprecated-reason` was set
	PreviewChange(ctx context.Context, body PreviewChangeJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)

	// ApplyChangesetWithBody Apply several changes in order
	//
	// Applies each change in the order given and stops at the first failure.
	//
	// A directory has no transaction spanning entries, so a changeset is not
	// atomic and does not pretend to be. The response says exactly which
	// changes were applied and which was not, so the caller can fix the one
	// that failed and resume from there rather than starting again and
	// re-applying what already succeeded.
	//
	// With `recovery: true` the result carries a recovery bundle covering
	// exactly the changes that were applied -- on a run that stopped partway,
	// only those before the failure -- whose compensations run in the reverse
	// order. See `docs/RECOVERY.md`.
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with POST /changeset/apply (the `ApplyChangeset` operationId).
	ApplyChangesetWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// ApplyChangeset Apply several changes in order
	//
	// Applies each change in the order given and stops at the first failure.
	//
	// A directory has no transaction spanning entries, so a changeset is not
	// atomic and does not pretend to be. The response says exactly which
	// changes were applied and which was not, so the caller can fix the one
	// that failed and resume from there rather than starting again and
	// re-applying what already succeeded.
	//
	// With `recovery: true` the result carries a recovery bundle covering
	// exactly the changes that were applied -- on a run that stopped partway,
	// only those before the failure -- whose compensations run in the reverse
	// order. See `docs/RECOVERY.md`.
	//
	// Takes a body of the `application/json` content type.
	//
	// Corresponds with POST /changeset/apply (the `ApplyChangeset` operationId).
	ApplyChangeset(ctx context.Context, body ApplyChangesetJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)

	// PreviewChangesetWithBody Render several changes as one LDIF document and one playbook
	//
	// A changeset is an ordered list of changes reviewed and applied together.
	// It is held by the client, not the server: Alder keeps no per-session
	// state beyond the connection itself, so this endpoint is a pure
	// rendering of whatever it is given.
	//
	// The order is the caller's and is preserved exactly. Alder does not sort
	// the changes to make them work -- an entry created before its parent is
	// reported as a warning so it can be reordered, because guessing the
	// intended order silently is worse than saying what is wrong.
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with POST /changeset/preview (the `PreviewChangeset` operationId).
	PreviewChangesetWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// PreviewChangeset Render several changes as one LDIF document and one playbook
	//
	// A changeset is an ordered list of changes reviewed and applied together.
	// It is held by the client, not the server: Alder keeps no per-session
	// state beyond the connection itself, so this endpoint is a pure
	// rendering of whatever it is given.
	//
	// The order is the caller's and is preserved exactly. Alder does not sort
	// the changes to make them work -- an entry created before its parent is
	// reported as a warning so it can be reordered, because guessing the
	// intended order silently is worse than saying what is wrong.
	//
	// Takes a body of the `application/json` content type.
	//
	// Corresponds with POST /changeset/preview (the `PreviewChangeset` operationId).
	PreviewChangeset(ctx context.Context, body PreviewChangesetJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)

	// CompareEntries What differs between two entries
	//
	// Answers "why does this account work and that one not".
	//
	// A naive diff answers it badly in three ways, and avoiding those is most
	// of what this endpoint is. It compares what is *present*, so an attribute
	// one entry's classes require and it does not hold is invisible — and that
	// absence is frequently the whole answer. It compares bytes, so two DNs
	// naming the same entry in different case read as a difference the
	// directory does not agree with. And its instinct is to show both sides of
	// everything, which for `userPassword` is what rule 6 forbids.
	//
	// So each side is annotated from its *own* object classes, which the two
	// entries need not share; DN-valued attributes are compared as DNs; and a
	// sensitive attribute is compared on presence alone.
	//
	// Corresponds with GET /compare (the `CompareEntries` operationId).
	CompareEntries(ctx context.Context, params *CompareEntriesParams, reqEditors ...RequestEditorFn) (*http.Response, error)

	// CountEntries Count the entries under a base, up to a limit
	//
	// A subtree count, bounded like every other search. It asks the server for
	// no attributes at all, so the cost is the search rather than the values.
	//
	// `truncated` is the important half of the answer: it means the count
	// reached the limit and the real number is higher. The UI reports "at
	// least N" in that case rather than a number that is simply wrong, which
	// is why this is an action somebody asks for rather than something a
	// landing page does to every naming context on load.
	//
	// Corresponds with GET /count (the `CountEntries` operationId).
	CountEntries(ctx context.Context, params *CountEntriesParams, reqEditors ...RequestEditorFn) (*http.Response, error)

	// DiffStatesWithBody Compare two directory states
	//
	// Compares `source` with `target`, each a snapshot or the live directory
	// this session is bound to. The direction is explicit: `added` means in
	// the target and not in the source. See `docs/SNAPSHOTS.md`.
	//
	// - Values compare by the equality rule both sides recorded for the
	//   attribute where Alder models it, and byte for byte otherwise; the
	//   attributes compared by bytes are listed.
	// - A rename is only reported when both sides share an entry's stable
	//   identity. Otherwise a moved entry is removed and added.
	// - What could not be seen is `unknown`, never `removed` or `added`: an
	//   entry beyond a live search's bound, outside the other side's scope, or
	//   an attribute the live directory will not show. Any of those makes the
	//   comparison `complete: false`, with `reasons`.
	// - A live side reads as the session's identity. An entry the access
	//   rules hide is indistinguishable from one that does not exist; see the
	//   limits in `docs/SNAPSHOTS.md`.
	//
	// **Two snapshots need no session.** Comparing two documents the caller
	// sent reads no directory, so the request is answered without a session
	// and opens none. It is still bounded like any request -- the request body
	// limit, the in-flight limit and the request timeout -- and its snapshots
	// are read exactly as strictly. A request with a live side needs a
	// session; without one it is refused with `401` before either snapshot is
	// read.
	//
	// **Schema (1.10).** Two schema snapshots, or a schema snapshot and the
	// live schema (`live: {kind: schema}`, or `live: {}` beside a schema
	// snapshot), compare definitions instead of entries, and the response has
	// `kind: schema` and a `schema` section. Identity is the OID; definitions
	// compare by meaning, not text; a difference only in X- extensions is
	// `metadata_only`; what did not parse is `unknown`. `counts` summarises
	// both element kinds, with `metadata_only` counted as unchanged. A schema
	// and a data side cannot be compared.
	//
	// When the source is live, each item carries a `candidate`: change
	// requests that would move the live directory toward the target. They are
	// input for `POST /plan`, never applied from here. Deleting an entry is a
	// `destructive` candidate, is never selected for the caller, and is
	// withheld entirely when the comparison is partial. A missing entry in the
	// target is never, by itself, a reason to delete.
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with POST /diff (the `DiffStates` operationId).
	DiffStatesWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// DiffStates Compare two directory states
	//
	// Compares `source` with `target`, each a snapshot or the live directory
	// this session is bound to. The direction is explicit: `added` means in
	// the target and not in the source. See `docs/SNAPSHOTS.md`.
	//
	// - Values compare by the equality rule both sides recorded for the
	//   attribute where Alder models it, and byte for byte otherwise; the
	//   attributes compared by bytes are listed.
	// - A rename is only reported when both sides share an entry's stable
	//   identity. Otherwise a moved entry is removed and added.
	// - What could not be seen is `unknown`, never `removed` or `added`: an
	//   entry beyond a live search's bound, outside the other side's scope, or
	//   an attribute the live directory will not show. Any of those makes the
	//   comparison `complete: false`, with `reasons`.
	// - A live side reads as the session's identity. An entry the access
	//   rules hide is indistinguishable from one that does not exist; see the
	//   limits in `docs/SNAPSHOTS.md`.
	//
	// **Two snapshots need no session.** Comparing two documents the caller
	// sent reads no directory, so the request is answered without a session
	// and opens none. It is still bounded like any request -- the request body
	// limit, the in-flight limit and the request timeout -- and its snapshots
	// are read exactly as strictly. A request with a live side needs a
	// session; without one it is refused with `401` before either snapshot is
	// read.
	//
	// **Schema (1.10).** Two schema snapshots, or a schema snapshot and the
	// live schema (`live: {kind: schema}`, or `live: {}` beside a schema
	// snapshot), compare definitions instead of entries, and the response has
	// `kind: schema` and a `schema` section. Identity is the OID; definitions
	// compare by meaning, not text; a difference only in X- extensions is
	// `metadata_only`; what did not parse is `unknown`. `counts` summarises
	// both element kinds, with `metadata_only` counted as unchanged. A schema
	// and a data side cannot be compared.
	//
	// When the source is live, each item carries a `candidate`: change
	// requests that would move the live directory toward the target. They are
	// input for `POST /plan`, never applied from here. Deleting an entry is a
	// `destructive` candidate, is never selected for the caller, and is
	// withheld entirely when the comparison is partial. A missing entry in the
	// target is never, by itself, a reason to delete.
	//
	// Takes a body of the `application/json` content type.
	//
	// Corresponds with POST /diff (the `DiffStates` operationId).
	DiffStates(ctx context.Context, body DiffStatesJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)

	// GetEntry Read one entry
	//
	// Returns the entry with every attribute annotated from the schema: its
	// syntax, whether it is single-valued, whether the server owns it, and
	// which input control the editor should offer. Also returns the MUST and
	// MAY sets computed from the entry's object classes.
	//
	// Corresponds with GET /entry (the `GetEntry` operationId).
	GetEntry(ctx context.Context, params *GetEntryParams, reqEditors ...RequestEditorFn) (*http.Response, error)

	// ExportAnsible Export an entry or a subtree as a playbook that enforces it
	//
	// A playbook that makes a directory match what is here, rather than one
	// that merely reports on it.
	//
	// This is a separate path from `/export/ldif` on purpose. The obvious
	// alternative was `format=ansible` on that operation, which would leave
	// the URL saying "ldif" while returning YAML — and the two differ in more
	// than serialisation. An LDIF export is a transcription of entries; this
	// is an assertion about what they should be, and it is ordered parent
	// first because `ldap_entry` cannot create a child under a parent that
	// does not exist yet.
	//
	// Each entry becomes two tasks: `community.general.ldap_entry` to create
	// it if it is missing, and `community.general.ldap_attrs` with
	// `state: exact` to bring the listed attributes to exactly these values.
	// `ldap_entry` with `state: present` alone would report success against
	// an entry that exists with entirely different attributes, which is the
	// failure this endpoint exists to avoid.
	//
	// Attributes the directory owns — operational, `NO-USER-MODIFICATION` —
	// are omitted, because enforcing them would produce a task that fails on
	// every run against a server doing its job. Sensitive attributes are
	// omitted with no option to include them: a playbook is a file destined
	// for a repository.
	//
	// Corresponds with GET /export/ansible (the `ExportAnsible` operationId).
	ExportAnsible(ctx context.Context, params *ExportAnsibleParams, reqEditors ...RequestEditorFn) (*http.Response, error)

	// ExportLdif Export an entry or a subtree as LDIF
	//
	// Corresponds with GET /export/ldif (the `ExportLdif` operationId).
	ExportLdif(ctx context.Context, params *ExportLdifParams, reqEditors ...RequestEditorFn) (*http.Response, error)

	// ExportOutline The shape of a subtree, as a tree
	//
	// The same entries an LDIF export would give, drawn as the hierarchy they
	// form rather than listed one after another.
	//
	// Asked for by an operator testing Alder: an LDIF export is flat, and a
	// flat list of three hundred records does not tell you the shape of what
	// you exported.
	//
	// **This is not LDIF and cannot be applied.** It could not be: RFC 2849
	// gives a leading space its own meaning -- it continues the line above --
	// so a tree drawn with indentation would stop being a document you can
	// import. Rather than a format that is almost LDIF and quietly broken,
	// this is plainly something else, and says so on its first line.
	//
	// Bounded rather than streamed, unlike the LDIF export: a tree cannot be
	// drawn until its last entry has arrived, because the entry that decides
	// whether a node is a leaf may be the final one to come back.
	//
	// Corresponds with GET /export/outline (the `ExportOutline` operationId).
	ExportOutline(ctx context.Context, params *ExportOutlineParams, reqEditors ...RequestEditorFn) (*http.Response, error)

	// ExportYaml The subtree as nested YAML
	//
	// The entries and what they hold, nested as the tree they form, in a
	// format an editor folds and colours. Asked for so that a subtree can be
	// opened in an editor and read: LDIF is flat and YAML is not.
	//
	// Every attribute is a list, even where the schema says one value. An LDAP
	// attribute holds a set, and a document whose shape changed with the data
	// would make a second value look like a type change in the diff.
	//
	// A value that is not valid printable UTF-8 is written with YAML's own
	// `!!binary` tag rather than passed off as a string.
	//
	// **Nothing reads this back.** Alder imports LDIF, which is the format with
	// a specification and a changetype; this is for looking at. It is bounded
	// rather than streamed for the same reason the outline is: a tree cannot be
	// nested until its last entry has arrived.
	//
	// Corresponds with GET /export/yaml (the `ExportYaml` operationId).
	ExportYaml(ctx context.Context, params *ExportYamlParams, reqEditors ...RequestEditorFn) (*http.Response, error)

	// ParseLdifWithBody Parse an LDIF document into change records
	//
	// Parses and validates without applying anything. The response is the
	// list of changes the document describes, each rendered back as LDIF, for
	// the user to review and confirm one at a time or all at once.
	//
	// URL-valued attributes (`attr:< url`) are refused: following one from a
	// process holding a privileged bind would be file disclosure and request
	// forgery. So are LDAP controls, which would change what the server does
	// with a record.
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with POST /import/ldif (the `ParseLdif` operationId).
	ParseLdifWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// ParseLdif Parse an LDIF document into change records
	//
	// Parses and validates without applying anything. The response is the
	// list of changes the document describes, each rendered back as LDIF, for
	// the user to review and confirm one at a time or all at once.
	//
	// URL-valued attributes (`attr:< url`) are refused: following one from a
	// process holding a privileged bind would be file disclosure and request
	// forgery. So are LDAP controls, which would change what the server does
	// with a record.
	//
	// Takes a body of the `application/json` content type.
	//
	// Corresponds with POST /import/ldif (the `ParseLdif` operationId).
	ParseLdif(ctx context.Context, body ParseLdifJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)

	// InventoryValuesWithBody What values one attribute holds, and how many entries carry each
	//
	// The question that finds a team name spelled "platfrm" with four people
	// on it, a cost centre nobody has used since a reorganisation, or the
	// accounts still pointing at a decommissioned site.
	//
	// It is a tally over a **bounded** search, so it never claims to describe
	// the directory — only the entries it examined. Every number is scoped
	// that way and `truncated` says the bound was reached. A tally that
	// quietly summarises a truncated result set is not a weaker answer than
	// the truth; it is a confident wrong one.
	//
	// Sensitive attributes are refused before the search runs rather than
	// filtered out of the result, which is the difference between never
	// reading a password and reading every password and choosing not to say.
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with POST /inventory (the `InventoryValues` operationId).
	InventoryValuesWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// InventoryValues What values one attribute holds, and how many entries carry each
	//
	// The question that finds a team name spelled "platfrm" with four people
	// on it, a cost centre nobody has used since a reorganisation, or the
	// accounts still pointing at a decommissioned site.
	//
	// It is a tally over a **bounded** search, so it never claims to describe
	// the directory — only the entries it examined. Every number is scoped
	// that way and `truncated` says the bound was reached. A tally that
	// quietly summarises a truncated result set is not a weaker answer than
	// the truth; it is a confident wrong one.
	//
	// Sensitive attributes are refused before the search runs rather than
	// filtered out of the result, which is the difference between never
	// reading a password and reading every password and choosing not to say.
	//
	// Takes a body of the `application/json` content type.
	//
	// Corresponds with POST /inventory (the `InventoryValues` operationId).
	InventoryValues(ctx context.Context, body InventoryValuesJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)

	// ExpandMembers Who is in this group, following nested groups
	//
	// The entry view lists a group's `member` values, which is the whole
	// answer for a flat group and no answer at all for a nested one:
	// `cn=everyone` in the test harness lists five members and contains no
	// people, because all five are themselves groups.
	//
	// This walks that structure and says, for each person reached, which
	// chain of groups brought them in.
	//
	// What it finds on the way is reported rather than dropped: a group that
	// contains itself, a member DN whose entry cannot be read — the dangling
	// reference the referenced-by panel exists to prevent, seen from the
	// other side — and membership values that are not DNs at all, since
	// `memberUid` holds a login name and `memberURL` a search.
	//
	// Corresponds with GET /members (the `ExpandMembers` operationId).
	ExpandMembers(ctx context.Context, params *ExpandMembersParams, reqEditors ...RequestEditorFn) (*http.Response, error)

	// BuildPackageWithBody Build a change package from changes
	//
	// Turns changes into an `alder-change-package` document (1.11): a portable
	// description of intended directory and schema changes that can be carried
	// to another environment and validated there.
	//
	// The package holds intent, not execution. Every `baseline` is dropped,
	// every expectation is dropped, and a change to a schema entry is recorded
	// as what it means -- this element, this operation, this definition --
	// rather than as the modification this server happens to need, because the
	// entry and attribute that hold schema differ between products.
	//
	// What cannot be packaged is never dropped silently. A password change
	// needs its secret to mean anything, so it is refused, and the package
	// records the omission with a machine-readable reason.
	//
	// Dependencies are derived from the changes themselves -- an object class
	// after the attribute types it names, an entry after the class it uses and
	// after its parent -- and written into the document. Array order carries no
	// meaning; the graph does.
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with POST /packages/build (the `BuildPackage` operationId).
	BuildPackageWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// BuildPackage Build a change package from changes
	//
	// Turns changes into an `alder-change-package` document (1.11): a portable
	// description of intended directory and schema changes that can be carried
	// to another environment and validated there.
	//
	// The package holds intent, not execution. Every `baseline` is dropped,
	// every expectation is dropped, and a change to a schema entry is recorded
	// as what it means -- this element, this operation, this definition --
	// rather than as the modification this server happens to need, because the
	// entry and attribute that hold schema differ between products.
	//
	// What cannot be packaged is never dropped silently. A password change
	// needs its secret to mean anything, so it is refused, and the package
	// records the omission with a machine-readable reason.
	//
	// Dependencies are derived from the changes themselves -- an object class
	// after the attribute types it names, an entry after the class it uses and
	// after its parent -- and written into the document. Array order carries no
	// meaning; the graph does.
	//
	// Takes a body of the `application/json` content type.
	//
	// Corresponds with POST /packages/build (the `BuildPackage` operationId).
	BuildPackage(ctx context.Context, body BuildPackageJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)

	// InspectPackageWithBody Read a change package and check everything that needs no directory
	//
	// Reads a package strictly and reports what it holds: its identity, its
	// provenance, its assumptions, its changes and its dependency order.
	//
	// Everything that can be checked without a directory is checked here --
	// the format and version, unknown fields, trailing content, duplicate
	// identifiers, missing dependencies, self-dependencies, cycles, malformed
	// DNs, schema definitions that do not parse, secrets, and the checksum.
	// Nothing is executed and no directory is read, so no session is needed:
	// this is the check a pipeline runs before it has credentials.
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with POST /packages/inspect (the `InspectPackage` operationId).
	InspectPackageWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// InspectPackage Read a change package and check everything that needs no directory
	//
	// Reads a package strictly and reports what it holds: its identity, its
	// provenance, its assumptions, its changes and its dependency order.
	//
	// Everything that can be checked without a directory is checked here --
	// the format and version, unknown fields, trailing content, duplicate
	// identifiers, missing dependencies, self-dependencies, cycles, malformed
	// DNs, schema definitions that do not parse, secrets, and the checksum.
	// Nothing is executed and no directory is read, so no session is needed:
	// this is the check a pipeline runs before it has credentials.
	//
	// Takes a body of the `application/json` content type.
	//
	// Corresponds with POST /packages/inspect (the `InspectPackage` operationId).
	InspectPackage(ctx context.Context, body InspectPackageJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)

	// ValidatePackageWithBody Validate a change package against the current directory
	//
	// Reads the package, then asks this directory what its intent means here:
	// which changes are ready, which the target already satisfies, which
	// conflict with what is there now, which name something that is missing,
	// and which this server cannot do at all.
	//
	// Validation is not a plan. It prepares the ordinary change requests a
	// plan would take and stops; the plan reads the directory again, issues
	// the baselines an apply checks, and remains the only thing that decides
	// what is written. Nothing here writes, and a validation is not something
	// to keep: carry the package to the next environment and validate it
	// there.
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with POST /packages/validate (the `ValidatePackage` operationId).
	ValidatePackageWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// ValidatePackage Validate a change package against the current directory
	//
	// Reads the package, then asks this directory what its intent means here:
	// which changes are ready, which the target already satisfies, which
	// conflict with what is there now, which name something that is missing,
	// and which this server cannot do at all.
	//
	// Validation is not a plan. It prepares the ordinary change requests a
	// plan would take and stops; the plan reads the directory again, issues
	// the baselines an apply checks, and remains the only thing that decides
	// what is written. Nothing here writes, and a validation is not something
	// to keep: carry the package to the next environment and validate it
	// there.
	//
	// Takes a body of the `application/json` content type.
	//
	// Corresponds with POST /packages/validate (the `ValidatePackage` operationId).
	ValidatePackage(ctx context.Context, body ValidatePackageJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)

	// PlanChangesWithBody What a set of changes would do, without doing it
	//
	// Answers the question one level above the preview: given these proposed
	// changes and the directory as it is, which are additions, which are
	// modifications, which would do nothing at all, and which cannot be
	// applied as written.
	//
	// Nothing is written. Every entry named is read, each change is
	// classified, and the records that would run come back in `items[].record`
	// — the same values `POST /changeset/apply` takes, not a description of
	// them. That is what stops a plan from promising one thing and an apply
	// doing another.
	//
	// `action` is a stable identifier and is what a client should switch on.
	// `reason` is prose for a person and may be reworded in any release.
	//
	// Each applicable item carries a `baseline`: an opaque token binding two
	// things — the exact operation in `record`, and the state of the directory
	// that operation was planned against. Hand it back on the corresponding
	// change when applying. The server re-reads the entry and refuses the
	// whole set before anything runs: `400 plan_mismatch` if a change is not
	// the operation its baseline was issued for, `409 conflict` with
	// `cause: plan_stale` if the directory has moved since. Neither is silently replanned. The token
	// holds no attribute values; a sensitive attribute contributes only
	// whether it is set and how many values it has, and a password change
	// binds the entry and the fact of the change, never the password.
	// Baselines are meaningless to any other Alder process and do not survive
	// a restart.
	//
	// The input is either `changes` or `ldif`, never both.
	//
	// **`ldif` with `mode: changes`** (the default) plans every record as the
	// exact operation it states. A record with no `changetype` is an add,
	// which is what `ldapadd` does with it. An add of an entry that already
	// exists is a conflict (`entry_exists`); it is never turned into a
	// modification nobody asked for.
	//
	// **`ldif` with `mode: desired`** reads the document as the state entries
	// should be in. Only content records — no `changetype` — are accepted; a
	// record with one is refused with `400 ldif_mode_mismatch`, because a
	// document mixing "this entry looks like this" with "do this" has no
	// single meaning. Each record is reconciled: created if the entry is
	// absent, turned into the modification of the attributes it names if
	// present, reported `unchanged` if it already matches. Attributes a record
	// does not name are left alone. **An entry the document does not mention
	// is not deleted**; there is no mode in which absence means deletion.
	//
	// `impact` reports facts, not a risk score: which target area each change
	// touches (ordinary data, schema, server configuration), memberships
	// gained and lost, deletions grouped into the branches they remove, and —
	// for deletions and renames — the entries that name the affected DN and
	// how many of those references the plan would leave dangling. Reference
	// search runs as the session's own bind, so it reports what that bind can
	// see; see `impact.references`.
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with POST /plan (the `PlanChanges` operationId).
	PlanChangesWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// PlanChanges What a set of changes would do, without doing it
	//
	// Answers the question one level above the preview: given these proposed
	// changes and the directory as it is, which are additions, which are
	// modifications, which would do nothing at all, and which cannot be
	// applied as written.
	//
	// Nothing is written. Every entry named is read, each change is
	// classified, and the records that would run come back in `items[].record`
	// — the same values `POST /changeset/apply` takes, not a description of
	// them. That is what stops a plan from promising one thing and an apply
	// doing another.
	//
	// `action` is a stable identifier and is what a client should switch on.
	// `reason` is prose for a person and may be reworded in any release.
	//
	// Each applicable item carries a `baseline`: an opaque token binding two
	// things — the exact operation in `record`, and the state of the directory
	// that operation was planned against. Hand it back on the corresponding
	// change when applying. The server re-reads the entry and refuses the
	// whole set before anything runs: `400 plan_mismatch` if a change is not
	// the operation its baseline was issued for, `409 conflict` with
	// `cause: plan_stale` if the directory has moved since. Neither is silently replanned. The token
	// holds no attribute values; a sensitive attribute contributes only
	// whether it is set and how many values it has, and a password change
	// binds the entry and the fact of the change, never the password.
	// Baselines are meaningless to any other Alder process and do not survive
	// a restart.
	//
	// The input is either `changes` or `ldif`, never both.
	//
	// **`ldif` with `mode: changes`** (the default) plans every record as the
	// exact operation it states. A record with no `changetype` is an add,
	// which is what `ldapadd` does with it. An add of an entry that already
	// exists is a conflict (`entry_exists`); it is never turned into a
	// modification nobody asked for.
	//
	// **`ldif` with `mode: desired`** reads the document as the state entries
	// should be in. Only content records — no `changetype` — are accepted; a
	// record with one is refused with `400 ldif_mode_mismatch`, because a
	// document mixing "this entry looks like this" with "do this" has no
	// single meaning. Each record is reconciled: created if the entry is
	// absent, turned into the modification of the attributes it names if
	// present, reported `unchanged` if it already matches. Attributes a record
	// does not name are left alone. **An entry the document does not mention
	// is not deleted**; there is no mode in which absence means deletion.
	//
	// `impact` reports facts, not a risk score: which target area each change
	// touches (ordinary data, schema, server configuration), memberships
	// gained and lost, deletions grouped into the branches they remove, and —
	// for deletions and renames — the entries that name the affected DN and
	// how many of those references the plan would leave dangling. Reference
	// search runs as the session's own bind, so it reports what that bind can
	// see; see `impact.references`.
	//
	// Takes a body of the `application/json` content type.
	//
	// Corresponds with POST /plan (the `PlanChanges` operationId).
	PlanChanges(ctx context.Context, body PlanChangesJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)

	// PreflightWithBody Report what of an artifact would carry across to this directory
	//
	// Reads a change package, a schema snapshot or a data snapshot -- told
	// apart by its own `format` and `kind`, never by a file name -- and reports,
	// finding by finding, what this directory already holds, what it could take,
	// what needs something done first, what contradicts it, what it cannot
	// represent, and what could not be seen.
	//
	// A preflight is not a plan and not a migration. It reads the directory and
	// never writes to it; it never rewrites a DN, maps an attribute or object
	// class, or changes the artifact; and it produces no baseline and no
	// prepared change. Package validation is its first step for a package, and
	// its answer is carried on every finding about an item.
	//
	// `overall` is derived from the findings: `incompatible` when any finding
	// that blocks portability is incompatible or unsupported, `incomplete` when
	// otherwise something could not be decided, `compatible_with_prerequisites`
	// when something has to be done first, and `compatible` only when none of
	// these hold. Access control and server configuration are never evaluated,
	// and `notEvaluated` says so on every report.
	//
	// A document that is none of the three is refused with
	// `preflight_artifact_unsupported`; an artifact that is malformed, forged
	// or from a newer version is refused with the code its own endpoints use.
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with POST /preflight (the `Preflight` operationId).
	PreflightWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// Preflight Report what of an artifact would carry across to this directory
	//
	// Reads a change package, a schema snapshot or a data snapshot -- told
	// apart by its own `format` and `kind`, never by a file name -- and reports,
	// finding by finding, what this directory already holds, what it could take,
	// what needs something done first, what contradicts it, what it cannot
	// represent, and what could not be seen.
	//
	// A preflight is not a plan and not a migration. It reads the directory and
	// never writes to it; it never rewrites a DN, maps an attribute or object
	// class, or changes the artifact; and it produces no baseline and no
	// prepared change. Package validation is its first step for a package, and
	// its answer is carried on every finding about an item.
	//
	// `overall` is derived from the findings: `incompatible` when any finding
	// that blocks portability is incompatible or unsupported, `incomplete` when
	// otherwise something could not be decided, `compatible_with_prerequisites`
	// when something has to be done first, and `compatible` only when none of
	// these hold. Access control and server configuration are never evaluated,
	// and `notEvaluated` says so on every report.
	//
	// A document that is none of the three is refused with
	// `preflight_artifact_unsupported`; an artifact that is malformed, forged
	// or from a newer version is refused with the code its own endpoints use.
	//
	// Takes a body of the `application/json` content type.
	//
	// Corresponds with POST /preflight (the `Preflight` operationId).
	Preflight(ctx context.Context, body PreflightJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)

	// InspectRecoveryWithBody Validate a recovery bundle and turn it into changes to plan
	//
	// Validates an uploaded recovery bundle and returns its compensating
	// changes as ordinary change requests, in the order they would have to
	// run, each carrying the `expect` precondition that makes it a conflict
	// in a plan if the entry is no longer as the original apply left it.
	// Added in 1.9; see `docs/RECOVERY.md`.
	//
	// Nothing is applied, and there is no endpoint that applies a bundle.
	// The changes go to `POST /plan`, are reviewed, and are applied with
	// `POST /changeset/apply` like any other change. `drift` is that plan's
	// verdict on each change against the directory as it is now, for the
	// preview; the plan made afterwards is the one that counts.
	//
	// A bundle is untrusted input. It is refused when its format or version
	// is not one this Alder reads (`recovery_unsupported_version` for another
	// version), when any field is malformed or unknown, when a sensitive
	// attribute appears with values, when a compensation is a password
	// change (`recovery_invalid`), or when a checksum is present and does
	// not match (`recovery_checksum_mismatch`).
	//
	// `originMatches` compares the vendor and naming contexts the bundle
	// records with this session's directory. Neither is proof of identity:
	// two directories can announce the same.
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with POST /recovery/inspect (the `InspectRecovery` operationId).
	InspectRecoveryWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// InspectRecovery Validate a recovery bundle and turn it into changes to plan
	//
	// Validates an uploaded recovery bundle and returns its compensating
	// changes as ordinary change requests, in the order they would have to
	// run, each carrying the `expect` precondition that makes it a conflict
	// in a plan if the entry is no longer as the original apply left it.
	// Added in 1.9; see `docs/RECOVERY.md`.
	//
	// Nothing is applied, and there is no endpoint that applies a bundle.
	// The changes go to `POST /plan`, are reviewed, and are applied with
	// `POST /changeset/apply` like any other change. `drift` is that plan's
	// verdict on each change against the directory as it is now, for the
	// preview; the plan made afterwards is the one that counts.
	//
	// A bundle is untrusted input. It is refused when its format or version
	// is not one this Alder reads (`recovery_unsupported_version` for another
	// version), when any field is malformed or unknown, when a sensitive
	// attribute appears with values, when a compensation is a password
	// change (`recovery_invalid`), or when a checksum is present and does
	// not match (`recovery_checksum_mismatch`).
	//
	// `originMatches` compares the vendor and naming contexts the bundle
	// records with this session's directory. Neither is proof of identity:
	// two directories can announce the same.
	//
	// Takes a body of the `application/json` content type.
	//
	// Corresponds with POST /recovery/inspect (the `InspectRecovery` operationId).
	InspectRecovery(ctx context.Context, body InspectRecoveryJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)

	// ListReferences The entries that name this one, and the attribute each names it by
	//
	// Answers "which groups is this in, and what else points at it" — and,
	// unlike the filter on the entry itself, says *how* each one points.
	//
	// That is the part a link cannot give you. A search returns the entries
	// matching the filter but not which term matched, and removing a
	// reference means modifying a named attribute on the referencing entry:
	// without knowing whether a group holds this DN in `member` or in `owner`,
	// there is nothing safe to offer.
	//
	// It is one bounded search. The reference attributes are requested and
	// matched here rather than in the browser, so a group's five hundred
	// members are compared where they already are instead of being sent
	// across to be compared and discarded.
	//
	// Corresponds with GET /references (the `ListReferences` operationId).
	ListReferences(ctx context.Context, params *ListReferencesParams, reqEditors ...RequestEditorFn) (*http.Response, error)

	// Resolve What could this input be — an entry, or a search
	//
	// Backs one box that takes a DN, an RFC 4515 filter, or a name.
	//
	// It parses and never searches. `internal/dn` and `internal/filter` are
	// the authorities on what these strings are, and a regex in the browser
	// would drift from them the first time either grew a case — but probing
	// the directory to see whether an entry exists would turn every keystroke
	// into a read, to answer a question the operator is about to answer by
	// pressing enter.
	//
	// The three kinds overlap. `cn=platform` is a valid one-component DN
	// *and* almost certainly a request to find something called platform, so
	// an ambiguous input returns both destinations and the operator picks.
	// Guessing would be wrong about half the time.
	//
	// Corresponds with GET /resolve (the `Resolve` operationId).
	Resolve(ctx context.Context, params *ResolveParams, reqEditors ...RequestEditorFn) (*http.Response, error)

	// GetSchema The whole schema, indexed for browsing
	//
	// Corresponds with GET /schema (the `GetSchema` operationId).
	GetSchema(ctx context.Context, reqEditors ...RequestEditorFn) (*http.Response, error)

	// GetAttributeType One attribute type, with its cross-links resolved
	//
	// Corresponds with GET /schema/attributetypes/{name} (the `GetAttributeType` operationId).
	GetAttributeType(ctx context.Context, name string, reqEditors ...RequestEditorFn) (*http.Response, error)

	// BuildSchemaChangeWithBody Build the modification that installs, replaces or removes a definition
	//
	// Renders a definition and returns the change that would apply it, without
	// applying anything.
	//
	// A schema definition is a value of an attribute on an ordinary entry, so
	// the change this returns is an ordinary modify, and it goes on to the
	// same preview, the same confirmation and the same changeset as every
	// other write. There is still exactly one path that writes.
	//
	// The definition text is built here rather than in the browser, for the
	// same reason the LDIF preview is: what the person confirms and what the
	// directory receives have to be the same bytes.
	//
	// A replace or a delete needs the value the server actually stores, which
	// is not always the value the schema browser displays — a server keeping
	// its schema in configuration prefixes each stored definition with its
	// load order and strips that prefix from what it publishes. This reads the
	// target to find it, which is why the request names an OID rather than
	// carrying a definition to remove.
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with POST /schema/change (the `BuildSchemaChange` operationId).
	BuildSchemaChangeWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// BuildSchemaChange Build the modification that installs, replaces or removes a definition
	//
	// Renders a definition and returns the change that would apply it, without
	// applying anything.
	//
	// A schema definition is a value of an attribute on an ordinary entry, so
	// the change this returns is an ordinary modify, and it goes on to the
	// same preview, the same confirmation and the same changeset as every
	// other write. There is still exactly one path that writes.
	//
	// The definition text is built here rather than in the browser, for the
	// same reason the LDIF preview is: what the person confirms and what the
	// directory receives have to be the same bytes.
	//
	// A replace or a delete needs the value the server actually stores, which
	// is not always the value the schema browser displays — a server keeping
	// its schema in configuration prefixes each stored definition with its
	// load order and strips that prefix from what it publishes. This reads the
	// target to find it, which is why the request names an OID rather than
	// carrying a definition to remove.
	//
	// Takes a body of the `application/json` content type.
	//
	// Corresponds with POST /schema/change (the `BuildSchemaChange` operationId).
	BuildSchemaChange(ctx context.Context, body BuildSchemaChangeJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)

	// GetObjectClass One object class, with its cross-links resolved
	//
	// Corresponds with GET /schema/objectclasses/{name} (the `GetObjectClass` operationId).
	GetObjectClass(ctx context.Context, name string, reqEditors ...RequestEditorFn) (*http.Response, error)

	// GetRequirements What an entry of these object classes must and may hold
	//
	// The MUST and MAY sets for a set of object classes, with the schema's
	// opinion about each attribute — syntax, single-valuedness, description,
	// which control to offer.
	//
	// It exists so a creation form is generated from the schema rather than
	// from a template. Without it the form has attribute names and nothing
	// else, which is how creating an entry ended up with a plain text box for
	// every field while editing the same entry a moment later offered a
	// Boolean control, an entry picker and the attribute's description.
	//
	// Corresponds with GET /schema/requirements (the `GetRequirements` operationId).
	GetRequirements(ctx context.Context, params *GetRequirementsParams, reqEditors ...RequestEditorFn) (*http.Response, error)

	// SearchWithBody Search the directory
	//
	// Every search is paged and bounded. `filter` is a raw RFC 4515 string
	// which is parsed, not interpolated: a value containing filter
	// metacharacters cannot escape into the filter structure.
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with POST /search (the `Search` operationId).
	SearchWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// Search Search the directory
	//
	// Every search is paged and bounded. `filter` is a raw RFC 4515 string
	// which is parsed, not interpolated: a value containing filter
	// metacharacters cannot escape into the filter structure.
	//
	// Takes a body of the `application/json` content type.
	//
	// Corresponds with POST /search (the `Search` operationId).
	Search(ctx context.Context, body SearchJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)

	// DeleteSession Disconnect
	//
	// Closes the connection and discards the credentials.
	//
	// Corresponds with DELETE /session (the `DeleteSession` operationId).
	DeleteSession(ctx context.Context, reqEditors ...RequestEditorFn) (*http.Response, error)

	// GetSession Describe the current session
	//
	// Corresponds with GET /session (the `GetSession` operationId).
	GetSession(ctx context.Context, reqEditors ...RequestEditorFn) (*http.Response, error)

	// CreateSessionWithBody Connect and bind to a directory
	//
	// Opens a connection, binds, and reads the RootDSE. On success the
	// response sets an httpOnly, Secure, SameSite=Strict cookie naming an
	// in-memory session. The bind password is held in that in-memory session
	// and is never written to disk, never placed in a token, and never
	// returned by any endpoint.
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with POST /session (the `CreateSession` operationId).
	CreateSessionWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// CreateSession Connect and bind to a directory
	//
	// Opens a connection, binds, and reads the RootDSE. On success the
	// response sets an httpOnly, Secure, SameSite=Strict cookie naming an
	// in-memory session. The bind password is held in that in-memory session
	// and is never written to disk, never placed in a token, and never
	// returned by any endpoint.
	//
	// Takes a body of the `application/json` content type.
	//
	// Corresponds with POST /session (the `CreateSession` operationId).
	CreateSession(ctx context.Context, body CreateSessionJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)

	// CaptureSnapshotWithBody Capture a subtree as a versioned snapshot
	//
	// Reads the subtree as the session's own identity and returns it as an
	// Alder snapshot: a versioned, canonical JSON document, safe to keep in a
	// repository. See `docs/SNAPSHOTS.md`.
	//
	// - Entries, attribute names and values are in one defined order, so two
	//   captures of an unchanged subtree differ only in `createdAt`, and have
	//   the same `checksum`.
	// - Operational attributes are left out unless
	//   `operationalAttributes` is set. Each entry's stable identity
	//   (`entryUUID` or `nsUniqueId`) is recorded as its `id` either way.
	// - Sensitive attributes are recorded as a `withheld` count, never as a
	//   value or anything derived from one.
	// - A capture that cannot finish -- more than 50,000 entries, a search
	//   limit, a failure part way -- is an error. There is no partial
	//   snapshot.
	// - A data snapshot's base must be ordinary data: a base inside the schema
	//   or the server's configuration is refused with
	//   `snapshot_scope_unsupported`.
	//
	// With `kind: schema` (1.10) the published subschema is captured instead,
	// and `base`, `scope`, `filter` and `operationalAttributes` are not used.
	// Attribute types and object classes are captured for comparison; LDAP
	// syntaxes, matching rules, matching rule uses, DIT content rules and name
	// forms as context. A definition that does not parse is kept verbatim and
	// makes the snapshot `partial`. Server configuration is never captured, and
	// there is no `kind: config`.
	//
	// Nothing is kept on the server. The snapshot is returned and belongs to
	// the caller.
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with POST /snapshots/capture (the `CaptureSnapshot` operationId).
	CaptureSnapshotWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// CaptureSnapshot Capture a subtree as a versioned snapshot
	//
	// Reads the subtree as the session's own identity and returns it as an
	// Alder snapshot: a versioned, canonical JSON document, safe to keep in a
	// repository. See `docs/SNAPSHOTS.md`.
	//
	// - Entries, attribute names and values are in one defined order, so two
	//   captures of an unchanged subtree differ only in `createdAt`, and have
	//   the same `checksum`.
	// - Operational attributes are left out unless
	//   `operationalAttributes` is set. Each entry's stable identity
	//   (`entryUUID` or `nsUniqueId`) is recorded as its `id` either way.
	// - Sensitive attributes are recorded as a `withheld` count, never as a
	//   value or anything derived from one.
	// - A capture that cannot finish -- more than 50,000 entries, a search
	//   limit, a failure part way -- is an error. There is no partial
	//   snapshot.
	// - A data snapshot's base must be ordinary data: a base inside the schema
	//   or the server's configuration is refused with
	//   `snapshot_scope_unsupported`.
	//
	// With `kind: schema` (1.10) the published subschema is captured instead,
	// and `base`, `scope`, `filter` and `operationalAttributes` are not used.
	// Attribute types and object classes are captured for comparison; LDAP
	// syntaxes, matching rules, matching rule uses, DIT content rules and name
	// forms as context. A definition that does not parse is kept verbatim and
	// makes the snapshot `partial`. Server configuration is never captured, and
	// there is no `kind: config`.
	//
	// Nothing is kept on the server. The snapshot is returned and belongs to
	// the caller.
	//
	// Takes a body of the `application/json` content type.
	//
	// Corresponds with POST /snapshots/capture (the `CaptureSnapshot` operationId).
	CaptureSnapshot(ctx context.Context, body CaptureSnapshotJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)

	// InspectSnapshotWithBody Validate a snapshot and describe it
	//
	// Validates an uploaded snapshot exactly as a comparison would and
	// describes what it covers. Read-only: nothing is kept and nothing is
	// applied.
	//
	// A snapshot is refused when its format or version is not one this Alder
	// reads (`snapshot_unsupported_version` for a newer version), when any DN,
	// value or field is malformed or unknown (`snapshot_invalid`), when a
	// sensitive attribute carries a value, or when a checksum is present and
	// does not match (`snapshot_checksum_mismatch`). A snapshot without a
	// checksum is accepted and described as `integrity: unverified`.
	//
	// A schema snapshot (1.10) is checked as strictly: every OID, no OID twice,
	// every parsed field equal to what its definition text parses to, counts
	// and coverage true, and no inheritance cycle.
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with POST /snapshots/inspect (the `InspectSnapshot` operationId).
	InspectSnapshotWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// InspectSnapshot Validate a snapshot and describe it
	//
	// Validates an uploaded snapshot exactly as a comparison would and
	// describes what it covers. Read-only: nothing is kept and nothing is
	// applied.
	//
	// A snapshot is refused when its format or version is not one this Alder
	// reads (`snapshot_unsupported_version` for a newer version), when any DN,
	// value or field is malformed or unknown (`snapshot_invalid`), when a
	// sensitive attribute carries a value, or when a checksum is present and
	// does not match (`snapshot_checksum_mismatch`). A snapshot without a
	// checksum is accepted and described as `integrity: unverified`.
	//
	// A schema snapshot (1.10) is checked as strictly: every OID, no OID twice,
	// every parsed field equal to what its definition text parses to, counts
	// and coverage true, and no inheritance cycle.
	//
	// Takes a body of the `application/json` content type.
	//
	// Corresponds with POST /snapshots/inspect (the `InspectSnapshot` operationId).
	InspectSnapshot(ctx context.Context, body InspectSnapshotJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)

	// GetSourceOffer Where to get the source of this running instance
	//
	// AGPL-3.0 section 13: someone using a modified Alder over a network is
	// entitled to its Corresponding Source. The offer is served rather than
	// written in a README because the obligation runs to the person using the
	// running instance, and the user of a modified instance has no reason to
	// know where its source went.
	//
	// **This is the one endpoint that answers without a session,** and it has
	// no 401 for that reason: an offer only the already-connected can read
	// would not discharge the obligation. It is described here so that the
	// contract in this document is the whole contract -- it was served outside
	// it until 1.0, which made `openapi.yaml` quietly incomplete about the one
	// endpoint a licence requires.
	//
	// Corresponds with GET /source (the `GetSourceOffer` operationId).
	GetSourceOffer(ctx context.Context, reqEditors ...RequestEditorFn) (*http.Response, error)

	// ListChildren List the children of an entry
	//
	// With no `dn`, returns the naming contexts the server holds, which are
	// the roots of the tree. With a `dn`, returns that entry's immediate
	// children. This is the lazy-loading tree browser's only endpoint.
	//
	// Corresponds with GET /tree (the `ListChildren` operationId).
	ListChildren(ctx context.Context, params *ListChildrenParams, reqEditors ...RequestEditorFn) (*http.Response, error)

	// ListObjectViews The object-aware views this directory supports
	//
	// Users, groups and organizational units, expressed as saved searches
	// derived from the schema the connected server published.
	//
	// A view is offered only where the server defines a class to anchor it
	// on, so a directory holding no person class has no Users view rather
	// than an empty one. The anchors are standards-track classes — RFC 4519,
	// RFC 4524, RFC 2307 — never vendor names, and a site class inheriting
	// from one of them is matched without being named, because an entry
	// carries its superclasses in its own `objectClass` attribute.
	//
	// The filter is built here and goes back to `/search` unmodified. The
	// columns are the attributes the anchor classes actually permit, so a
	// directory without `inetOrgPerson` gets no mail column instead of an
	// empty one.
	//
	// Corresponds with GET /views (the `ListObjectViews` operationId).
	ListObjectViews(ctx context.Context, reqEditors ...RequestEditorFn) (*http.Response, error)
}

// ApplyChangeWithBody Apply a change
//
// Applies one change. With the `baseline` a plan returned, the server first
// re-reads the entry and refuses the change if it is not the operation the
// plan was issued for (`400 plan_mismatch`) or the directory has moved
// since (`409 conflict`, `cause: plan_stale`). A secret in the change -- a
// new password, a sensitive attribute value -- is part of that operation:
// a different value than the one planned is a mismatch.
//
// Without a `baseline` the change is applied as it always was. The
// interface always plans first and always sends one.
//
// With `recovery=true` the response carries a recovery bundle for the
// change once it has succeeded: the compensating change derived from the
// entry as it was read immediately before the change ran. See
// `docs/RECOVERY.md`. A bundle is never applied from here or anywhere
// else; it becomes ordinary changes through `POST /recovery/inspect`.
//
// Takes any type of body and a specified content type.
//
// Corresponds with POST /changes/apply (the `ApplyChange` operationId).
func (c *Client) ApplyChangeWithBody(ctx context.Context, params *ApplyChangeParams, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewApplyChangeRequestWithBody(c.Server, params, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// ApplyChange Apply a change
//
// Applies one change. With the `baseline` a plan returned, the server first
// re-reads the entry and refuses the change if it is not the operation the
// plan was issued for (`400 plan_mismatch`) or the directory has moved
// since (`409 conflict`, `cause: plan_stale`). A secret in the change -- a
// new password, a sensitive attribute value -- is part of that operation:
// a different value than the one planned is a mismatch.
//
// Without a `baseline` the change is applied as it always was. The
// interface always plans first and always sends one.
//
// With `recovery=true` the response carries a recovery bundle for the
// change once it has succeeded: the compensating change derived from the
// entry as it was read immediately before the change ran. See
// `docs/RECOVERY.md`. A bundle is never applied from here or anywhere
// else; it becomes ordinary changes through `POST /recovery/inspect`.
//
// Takes a body of the `application/json` content type.
//
// Corresponds with POST /changes/apply (the `ApplyChange` operationId).
func (c *Client) ApplyChange(ctx context.Context, params *ApplyChangeParams, body ApplyChangeJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewApplyChangeRequest(c.Server, params, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// PreviewChangeWithBody Render a change as LDIF and as an Ansible task
//
// Renders without applying and without reading the directory.
//
// Deprecated since 1.6, and kept for 1.x clients. The interface no longer
// calls it: its confirmation dialog plans the change with `POST /plan`,
// which returns the same rendering inside the plan item together with
// what the change would do against the directory as it is, and a token
// that `/changes/apply` holds the change to. A rendering alone says
// nothing about whether the change still applies.
//
// Takes any type of body and a specified content type.
//
// Corresponds with POST /changes/preview (the `PreviewChange` operationId).
// Deprecated: this operation has been marked as deprecated upstream, but no `x-deprecated-reason` was set
func (c *Client) PreviewChangeWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewPreviewChangeRequestWithBody(c.Server, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// PreviewChange Render a change as LDIF and as an Ansible task
//
// Renders without applying and without reading the directory.
//
// Deprecated since 1.6, and kept for 1.x clients. The interface no longer
// calls it: its confirmation dialog plans the change with `POST /plan`,
// which returns the same rendering inside the plan item together with
// what the change would do against the directory as it is, and a token
// that `/changes/apply` holds the change to. A rendering alone says
// nothing about whether the change still applies.
//
// Takes a body of the `application/json` content type.
//
// Corresponds with POST /changes/preview (the `PreviewChange` operationId).
// Deprecated: this operation has been marked as deprecated upstream, but no `x-deprecated-reason` was set
func (c *Client) PreviewChange(ctx context.Context, body PreviewChangeJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewPreviewChangeRequest(c.Server, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// ApplyChangesetWithBody Apply several changes in order
//
// Applies each change in the order given and stops at the first failure.
//
// A directory has no transaction spanning entries, so a changeset is not
// atomic and does not pretend to be. The response says exactly which
// changes were applied and which was not, so the caller can fix the one
// that failed and resume from there rather than starting again and
// re-applying what already succeeded.
//
// With `recovery: true` the result carries a recovery bundle covering
// exactly the changes that were applied -- on a run that stopped partway,
// only those before the failure -- whose compensations run in the reverse
// order. See `docs/RECOVERY.md`.
//
// Takes any type of body and a specified content type.
//
// Corresponds with POST /changeset/apply (the `ApplyChangeset` operationId).
func (c *Client) ApplyChangesetWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewApplyChangesetRequestWithBody(c.Server, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// ApplyChangeset Apply several changes in order
//
// Applies each change in the order given and stops at the first failure.
//
// A directory has no transaction spanning entries, so a changeset is not
// atomic and does not pretend to be. The response says exactly which
// changes were applied and which was not, so the caller can fix the one
// that failed and resume from there rather than starting again and
// re-applying what already succeeded.
//
// With `recovery: true` the result carries a recovery bundle covering
// exactly the changes that were applied -- on a run that stopped partway,
// only those before the failure -- whose compensations run in the reverse
// order. See `docs/RECOVERY.md`.
//
// Takes a body of the `application/json` content type.
//
// Corresponds with POST /changeset/apply (the `ApplyChangeset` operationId).
func (c *Client) ApplyChangeset(ctx context.Context, body ApplyChangesetJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewApplyChangesetRequest(c.Server, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// PreviewChangesetWithBody Render several changes as one LDIF document and one playbook
//
// A changeset is an ordered list of changes reviewed and applied together.
// It is held by the client, not the server: Alder keeps no per-session
// state beyond the connection itself, so this endpoint is a pure
// rendering of whatever it is given.
//
// The order is the caller's and is preserved exactly. Alder does not sort
// the changes to make them work -- an entry created before its parent is
// reported as a warning so it can be reordered, because guessing the
// intended order silently is worse than saying what is wrong.
//
// Takes any type of body and a specified content type.
//
// Corresponds with POST /changeset/preview (the `PreviewChangeset` operationId).
func (c *Client) PreviewChangesetWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewPreviewChangesetRequestWithBody(c.Server, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// PreviewChangeset Render several changes as one LDIF document and one playbook
//
// A changeset is an ordered list of changes reviewed and applied together.
// It is held by the client, not the server: Alder keeps no per-session
// state beyond the connection itself, so this endpoint is a pure
// rendering of whatever it is given.
//
// The order is the caller's and is preserved exactly. Alder does not sort
// the changes to make them work -- an entry created before its parent is
// reported as a warning so it can be reordered, because guessing the
// intended order silently is worse than saying what is wrong.
//
// Takes a body of the `application/json` content type.
//
// Corresponds with POST /changeset/preview (the `PreviewChangeset` operationId).
func (c *Client) PreviewChangeset(ctx context.Context, body PreviewChangesetJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewPreviewChangesetRequest(c.Server, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// CompareEntries What differs between two entries
//
// Answers "why does this account work and that one not".
//
// A naive diff answers it badly in three ways, and avoiding those is most
// of what this endpoint is. It compares what is *present*, so an attribute
// one entry's classes require and it does not hold is invisible — and that
// absence is frequently the whole answer. It compares bytes, so two DNs
// naming the same entry in different case read as a difference the
// directory does not agree with. And its instinct is to show both sides of
// everything, which for `userPassword` is what rule 6 forbids.
//
// So each side is annotated from its *own* object classes, which the two
// entries need not share; DN-valued attributes are compared as DNs; and a
// sensitive attribute is compared on presence alone.
//
// Corresponds with GET /compare (the `CompareEntries` operationId).
func (c *Client) CompareEntries(ctx context.Context, params *CompareEntriesParams, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewCompareEntriesRequest(c.Server, params)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// CountEntries Count the entries under a base, up to a limit
//
// A subtree count, bounded like every other search. It asks the server for
// no attributes at all, so the cost is the search rather than the values.
//
// `truncated` is the important half of the answer: it means the count
// reached the limit and the real number is higher. The UI reports "at
// least N" in that case rather than a number that is simply wrong, which
// is why this is an action somebody asks for rather than something a
// landing page does to every naming context on load.
//
// Corresponds with GET /count (the `CountEntries` operationId).
func (c *Client) CountEntries(ctx context.Context, params *CountEntriesParams, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewCountEntriesRequest(c.Server, params)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// DiffStatesWithBody Compare two directory states
//
// Compares `source` with `target`, each a snapshot or the live directory
// this session is bound to. The direction is explicit: `added` means in
// the target and not in the source. See `docs/SNAPSHOTS.md`.
//
//   - Values compare by the equality rule both sides recorded for the
//     attribute where Alder models it, and byte for byte otherwise; the
//     attributes compared by bytes are listed.
//   - A rename is only reported when both sides share an entry's stable
//     identity. Otherwise a moved entry is removed and added.
//   - What could not be seen is `unknown`, never `removed` or `added`: an
//     entry beyond a live search's bound, outside the other side's scope, or
//     an attribute the live directory will not show. Any of those makes the
//     comparison `complete: false`, with `reasons`.
//   - A live side reads as the session's identity. An entry the access
//     rules hide is indistinguishable from one that does not exist; see the
//     limits in `docs/SNAPSHOTS.md`.
//
// **Two snapshots need no session.** Comparing two documents the caller
// sent reads no directory, so the request is answered without a session
// and opens none. It is still bounded like any request -- the request body
// limit, the in-flight limit and the request timeout -- and its snapshots
// are read exactly as strictly. A request with a live side needs a
// session; without one it is refused with `401` before either snapshot is
// read.
//
// **Schema (1.10).** Two schema snapshots, or a schema snapshot and the
// live schema (`live: {kind: schema}`, or `live: {}` beside a schema
// snapshot), compare definitions instead of entries, and the response has
// `kind: schema` and a `schema` section. Identity is the OID; definitions
// compare by meaning, not text; a difference only in X- extensions is
// `metadata_only`; what did not parse is `unknown`. `counts` summarises
// both element kinds, with `metadata_only` counted as unchanged. A schema
// and a data side cannot be compared.
//
// When the source is live, each item carries a `candidate`: change
// requests that would move the live directory toward the target. They are
// input for `POST /plan`, never applied from here. Deleting an entry is a
// `destructive` candidate, is never selected for the caller, and is
// withheld entirely when the comparison is partial. A missing entry in the
// target is never, by itself, a reason to delete.
//
// Takes any type of body and a specified content type.
//
// Corresponds with POST /diff (the `DiffStates` operationId).
func (c *Client) DiffStatesWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewDiffStatesRequestWithBody(c.Server, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// DiffStates Compare two directory states
//
// Compares `source` with `target`, each a snapshot or the live directory
// this session is bound to. The direction is explicit: `added` means in
// the target and not in the source. See `docs/SNAPSHOTS.md`.
//
//   - Values compare by the equality rule both sides recorded for the
//     attribute where Alder models it, and byte for byte otherwise; the
//     attributes compared by bytes are listed.
//   - A rename is only reported when both sides share an entry's stable
//     identity. Otherwise a moved entry is removed and added.
//   - What could not be seen is `unknown`, never `removed` or `added`: an
//     entry beyond a live search's bound, outside the other side's scope, or
//     an attribute the live directory will not show. Any of those makes the
//     comparison `complete: false`, with `reasons`.
//   - A live side reads as the session's identity. An entry the access
//     rules hide is indistinguishable from one that does not exist; see the
//     limits in `docs/SNAPSHOTS.md`.
//
// **Two snapshots need no session.** Comparing two documents the caller
// sent reads no directory, so the request is answered without a session
// and opens none. It is still bounded like any request -- the request body
// limit, the in-flight limit and the request timeout -- and its snapshots
// are read exactly as strictly. A request with a live side needs a
// session; without one it is refused with `401` before either snapshot is
// read.
//
// **Schema (1.10).** Two schema snapshots, or a schema snapshot and the
// live schema (`live: {kind: schema}`, or `live: {}` beside a schema
// snapshot), compare definitions instead of entries, and the response has
// `kind: schema` and a `schema` section. Identity is the OID; definitions
// compare by meaning, not text; a difference only in X- extensions is
// `metadata_only`; what did not parse is `unknown`. `counts` summarises
// both element kinds, with `metadata_only` counted as unchanged. A schema
// and a data side cannot be compared.
//
// When the source is live, each item carries a `candidate`: change
// requests that would move the live directory toward the target. They are
// input for `POST /plan`, never applied from here. Deleting an entry is a
// `destructive` candidate, is never selected for the caller, and is
// withheld entirely when the comparison is partial. A missing entry in the
// target is never, by itself, a reason to delete.
//
// Takes a body of the `application/json` content type.
//
// Corresponds with POST /diff (the `DiffStates` operationId).
func (c *Client) DiffStates(ctx context.Context, body DiffStatesJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewDiffStatesRequest(c.Server, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// GetEntry Read one entry
//
// Returns the entry with every attribute annotated from the schema: its
// syntax, whether it is single-valued, whether the server owns it, and
// which input control the editor should offer. Also returns the MUST and
// MAY sets computed from the entry's object classes.
//
// Corresponds with GET /entry (the `GetEntry` operationId).
func (c *Client) GetEntry(ctx context.Context, params *GetEntryParams, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewGetEntryRequest(c.Server, params)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// ExportAnsible Export an entry or a subtree as a playbook that enforces it
//
// A playbook that makes a directory match what is here, rather than one
// that merely reports on it.
//
// This is a separate path from `/export/ldif` on purpose. The obvious
// alternative was `format=ansible` on that operation, which would leave
// the URL saying "ldif" while returning YAML — and the two differ in more
// than serialisation. An LDIF export is a transcription of entries; this
// is an assertion about what they should be, and it is ordered parent
// first because `ldap_entry` cannot create a child under a parent that
// does not exist yet.
//
// Each entry becomes two tasks: `community.general.ldap_entry` to create
// it if it is missing, and `community.general.ldap_attrs` with
// `state: exact` to bring the listed attributes to exactly these values.
// `ldap_entry` with `state: present` alone would report success against
// an entry that exists with entirely different attributes, which is the
// failure this endpoint exists to avoid.
//
// Attributes the directory owns — operational, `NO-USER-MODIFICATION` —
// are omitted, because enforcing them would produce a task that fails on
// every run against a server doing its job. Sensitive attributes are
// omitted with no option to include them: a playbook is a file destined
// for a repository.
//
// Corresponds with GET /export/ansible (the `ExportAnsible` operationId).
func (c *Client) ExportAnsible(ctx context.Context, params *ExportAnsibleParams, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewExportAnsibleRequest(c.Server, params)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// ExportLdif Export an entry or a subtree as LDIF
//
// Corresponds with GET /export/ldif (the `ExportLdif` operationId).
func (c *Client) ExportLdif(ctx context.Context, params *ExportLdifParams, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewExportLdifRequest(c.Server, params)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// ExportOutline The shape of a subtree, as a tree
//
// The same entries an LDIF export would give, drawn as the hierarchy they
// form rather than listed one after another.
//
// Asked for by an operator testing Alder: an LDIF export is flat, and a
// flat list of three hundred records does not tell you the shape of what
// you exported.
//
// **This is not LDIF and cannot be applied.** It could not be: RFC 2849
// gives a leading space its own meaning -- it continues the line above --
// so a tree drawn with indentation would stop being a document you can
// import. Rather than a format that is almost LDIF and quietly broken,
// this is plainly something else, and says so on its first line.
//
// Bounded rather than streamed, unlike the LDIF export: a tree cannot be
// drawn until its last entry has arrived, because the entry that decides
// whether a node is a leaf may be the final one to come back.
//
// Corresponds with GET /export/outline (the `ExportOutline` operationId).
func (c *Client) ExportOutline(ctx context.Context, params *ExportOutlineParams, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewExportOutlineRequest(c.Server, params)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// ExportYaml The subtree as nested YAML
//
// The entries and what they hold, nested as the tree they form, in a
// format an editor folds and colours. Asked for so that a subtree can be
// opened in an editor and read: LDIF is flat and YAML is not.
//
// Every attribute is a list, even where the schema says one value. An LDAP
// attribute holds a set, and a document whose shape changed with the data
// would make a second value look like a type change in the diff.
//
// A value that is not valid printable UTF-8 is written with YAML's own
// `!!binary` tag rather than passed off as a string.
//
// **Nothing reads this back.** Alder imports LDIF, which is the format with
// a specification and a changetype; this is for looking at. It is bounded
// rather than streamed for the same reason the outline is: a tree cannot be
// nested until its last entry has arrived.
//
// Corresponds with GET /export/yaml (the `ExportYaml` operationId).
func (c *Client) ExportYaml(ctx context.Context, params *ExportYamlParams, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewExportYamlRequest(c.Server, params)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// ParseLdifWithBody Parse an LDIF document into change records
//
// Parses and validates without applying anything. The response is the
// list of changes the document describes, each rendered back as LDIF, for
// the user to review and confirm one at a time or all at once.
//
// URL-valued attributes (`attr:< url`) are refused: following one from a
// process holding a privileged bind would be file disclosure and request
// forgery. So are LDAP controls, which would change what the server does
// with a record.
//
// Takes any type of body and a specified content type.
//
// Corresponds with POST /import/ldif (the `ParseLdif` operationId).
func (c *Client) ParseLdifWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewParseLdifRequestWithBody(c.Server, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// ParseLdif Parse an LDIF document into change records
//
// Parses and validates without applying anything. The response is the
// list of changes the document describes, each rendered back as LDIF, for
// the user to review and confirm one at a time or all at once.
//
// URL-valued attributes (`attr:< url`) are refused: following one from a
// process holding a privileged bind would be file disclosure and request
// forgery. So are LDAP controls, which would change what the server does
// with a record.
//
// Takes a body of the `application/json` content type.
//
// Corresponds with POST /import/ldif (the `ParseLdif` operationId).
func (c *Client) ParseLdif(ctx context.Context, body ParseLdifJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewParseLdifRequest(c.Server, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// InventoryValuesWithBody What values one attribute holds, and how many entries carry each
//
// The question that finds a team name spelled "platfrm" with four people
// on it, a cost centre nobody has used since a reorganisation, or the
// accounts still pointing at a decommissioned site.
//
// It is a tally over a **bounded** search, so it never claims to describe
// the directory — only the entries it examined. Every number is scoped
// that way and `truncated` says the bound was reached. A tally that
// quietly summarises a truncated result set is not a weaker answer than
// the truth; it is a confident wrong one.
//
// Sensitive attributes are refused before the search runs rather than
// filtered out of the result, which is the difference between never
// reading a password and reading every password and choosing not to say.
//
// Takes any type of body and a specified content type.
//
// Corresponds with POST /inventory (the `InventoryValues` operationId).
func (c *Client) InventoryValuesWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewInventoryValuesRequestWithBody(c.Server, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// InventoryValues What values one attribute holds, and how many entries carry each
//
// The question that finds a team name spelled "platfrm" with four people
// on it, a cost centre nobody has used since a reorganisation, or the
// accounts still pointing at a decommissioned site.
//
// It is a tally over a **bounded** search, so it never claims to describe
// the directory — only the entries it examined. Every number is scoped
// that way and `truncated` says the bound was reached. A tally that
// quietly summarises a truncated result set is not a weaker answer than
// the truth; it is a confident wrong one.
//
// Sensitive attributes are refused before the search runs rather than
// filtered out of the result, which is the difference between never
// reading a password and reading every password and choosing not to say.
//
// Takes a body of the `application/json` content type.
//
// Corresponds with POST /inventory (the `InventoryValues` operationId).
func (c *Client) InventoryValues(ctx context.Context, body InventoryValuesJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewInventoryValuesRequest(c.Server, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// ExpandMembers Who is in this group, following nested groups
//
// The entry view lists a group's `member` values, which is the whole
// answer for a flat group and no answer at all for a nested one:
// `cn=everyone` in the test harness lists five members and contains no
// people, because all five are themselves groups.
//
// This walks that structure and says, for each person reached, which
// chain of groups brought them in.
//
// What it finds on the way is reported rather than dropped: a group that
// contains itself, a member DN whose entry cannot be read — the dangling
// reference the referenced-by panel exists to prevent, seen from the
// other side — and membership values that are not DNs at all, since
// `memberUid` holds a login name and `memberURL` a search.
//
// Corresponds with GET /members (the `ExpandMembers` operationId).
func (c *Client) ExpandMembers(ctx context.Context, params *ExpandMembersParams, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewExpandMembersRequest(c.Server, params)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// BuildPackageWithBody Build a change package from changes
//
// Turns changes into an `alder-change-package` document (1.11): a portable
// description of intended directory and schema changes that can be carried
// to another environment and validated there.
//
// The package holds intent, not execution. Every `baseline` is dropped,
// every expectation is dropped, and a change to a schema entry is recorded
// as what it means -- this element, this operation, this definition --
// rather than as the modification this server happens to need, because the
// entry and attribute that hold schema differ between products.
//
// What cannot be packaged is never dropped silently. A password change
// needs its secret to mean anything, so it is refused, and the package
// records the omission with a machine-readable reason.
//
// Dependencies are derived from the changes themselves -- an object class
// after the attribute types it names, an entry after the class it uses and
// after its parent -- and written into the document. Array order carries no
// meaning; the graph does.
//
// Takes any type of body and a specified content type.
//
// Corresponds with POST /packages/build (the `BuildPackage` operationId).
func (c *Client) BuildPackageWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewBuildPackageRequestWithBody(c.Server, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// BuildPackage Build a change package from changes
//
// Turns changes into an `alder-change-package` document (1.11): a portable
// description of intended directory and schema changes that can be carried
// to another environment and validated there.
//
// The package holds intent, not execution. Every `baseline` is dropped,
// every expectation is dropped, and a change to a schema entry is recorded
// as what it means -- this element, this operation, this definition --
// rather than as the modification this server happens to need, because the
// entry and attribute that hold schema differ between products.
//
// What cannot be packaged is never dropped silently. A password change
// needs its secret to mean anything, so it is refused, and the package
// records the omission with a machine-readable reason.
//
// Dependencies are derived from the changes themselves -- an object class
// after the attribute types it names, an entry after the class it uses and
// after its parent -- and written into the document. Array order carries no
// meaning; the graph does.
//
// Takes a body of the `application/json` content type.
//
// Corresponds with POST /packages/build (the `BuildPackage` operationId).
func (c *Client) BuildPackage(ctx context.Context, body BuildPackageJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewBuildPackageRequest(c.Server, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// InspectPackageWithBody Read a change package and check everything that needs no directory
//
// Reads a package strictly and reports what it holds: its identity, its
// provenance, its assumptions, its changes and its dependency order.
//
// Everything that can be checked without a directory is checked here --
// the format and version, unknown fields, trailing content, duplicate
// identifiers, missing dependencies, self-dependencies, cycles, malformed
// DNs, schema definitions that do not parse, secrets, and the checksum.
// Nothing is executed and no directory is read, so no session is needed:
// this is the check a pipeline runs before it has credentials.
//
// Takes any type of body and a specified content type.
//
// Corresponds with POST /packages/inspect (the `InspectPackage` operationId).
func (c *Client) InspectPackageWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewInspectPackageRequestWithBody(c.Server, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// InspectPackage Read a change package and check everything that needs no directory
//
// Reads a package strictly and reports what it holds: its identity, its
// provenance, its assumptions, its changes and its dependency order.
//
// Everything that can be checked without a directory is checked here --
// the format and version, unknown fields, trailing content, duplicate
// identifiers, missing dependencies, self-dependencies, cycles, malformed
// DNs, schema definitions that do not parse, secrets, and the checksum.
// Nothing is executed and no directory is read, so no session is needed:
// this is the check a pipeline runs before it has credentials.
//
// Takes a body of the `application/json` content type.
//
// Corresponds with POST /packages/inspect (the `InspectPackage` operationId).
func (c *Client) InspectPackage(ctx context.Context, body InspectPackageJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewInspectPackageRequest(c.Server, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// ValidatePackageWithBody Validate a change package against the current directory
//
// Reads the package, then asks this directory what its intent means here:
// which changes are ready, which the target already satisfies, which
// conflict with what is there now, which name something that is missing,
// and which this server cannot do at all.
//
// Validation is not a plan. It prepares the ordinary change requests a
// plan would take and stops; the plan reads the directory again, issues
// the baselines an apply checks, and remains the only thing that decides
// what is written. Nothing here writes, and a validation is not something
// to keep: carry the package to the next environment and validate it
// there.
//
// Takes any type of body and a specified content type.
//
// Corresponds with POST /packages/validate (the `ValidatePackage` operationId).
func (c *Client) ValidatePackageWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewValidatePackageRequestWithBody(c.Server, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// ValidatePackage Validate a change package against the current directory
//
// Reads the package, then asks this directory what its intent means here:
// which changes are ready, which the target already satisfies, which
// conflict with what is there now, which name something that is missing,
// and which this server cannot do at all.
//
// Validation is not a plan. It prepares the ordinary change requests a
// plan would take and stops; the plan reads the directory again, issues
// the baselines an apply checks, and remains the only thing that decides
// what is written. Nothing here writes, and a validation is not something
// to keep: carry the package to the next environment and validate it
// there.
//
// Takes a body of the `application/json` content type.
//
// Corresponds with POST /packages/validate (the `ValidatePackage` operationId).
func (c *Client) ValidatePackage(ctx context.Context, body ValidatePackageJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewValidatePackageRequest(c.Server, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// PlanChangesWithBody What a set of changes would do, without doing it
//
// Answers the question one level above the preview: given these proposed
// changes and the directory as it is, which are additions, which are
// modifications, which would do nothing at all, and which cannot be
// applied as written.
//
// Nothing is written. Every entry named is read, each change is
// classified, and the records that would run come back in `items[].record`
// — the same values `POST /changeset/apply` takes, not a description of
// them. That is what stops a plan from promising one thing and an apply
// doing another.
//
// `action` is a stable identifier and is what a client should switch on.
// `reason` is prose for a person and may be reworded in any release.
//
// Each applicable item carries a `baseline`: an opaque token binding two
// things — the exact operation in `record`, and the state of the directory
// that operation was planned against. Hand it back on the corresponding
// change when applying. The server re-reads the entry and refuses the
// whole set before anything runs: `400 plan_mismatch` if a change is not
// the operation its baseline was issued for, `409 conflict` with
// `cause: plan_stale` if the directory has moved since. Neither is silently replanned. The token
// holds no attribute values; a sensitive attribute contributes only
// whether it is set and how many values it has, and a password change
// binds the entry and the fact of the change, never the password.
// Baselines are meaningless to any other Alder process and do not survive
// a restart.
//
// The input is either `changes` or `ldif`, never both.
//
// **`ldif` with `mode: changes`** (the default) plans every record as the
// exact operation it states. A record with no `changetype` is an add,
// which is what `ldapadd` does with it. An add of an entry that already
// exists is a conflict (`entry_exists`); it is never turned into a
// modification nobody asked for.
//
// **`ldif` with `mode: desired`** reads the document as the state entries
// should be in. Only content records — no `changetype` — are accepted; a
// record with one is refused with `400 ldif_mode_mismatch`, because a
// document mixing "this entry looks like this" with "do this" has no
// single meaning. Each record is reconciled: created if the entry is
// absent, turned into the modification of the attributes it names if
// present, reported `unchanged` if it already matches. Attributes a record
// does not name are left alone. **An entry the document does not mention
// is not deleted**; there is no mode in which absence means deletion.
//
// `impact` reports facts, not a risk score: which target area each change
// touches (ordinary data, schema, server configuration), memberships
// gained and lost, deletions grouped into the branches they remove, and —
// for deletions and renames — the entries that name the affected DN and
// how many of those references the plan would leave dangling. Reference
// search runs as the session's own bind, so it reports what that bind can
// see; see `impact.references`.
//
// Takes any type of body and a specified content type.
//
// Corresponds with POST /plan (the `PlanChanges` operationId).
func (c *Client) PlanChangesWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewPlanChangesRequestWithBody(c.Server, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// PlanChanges What a set of changes would do, without doing it
//
// Answers the question one level above the preview: given these proposed
// changes and the directory as it is, which are additions, which are
// modifications, which would do nothing at all, and which cannot be
// applied as written.
//
// Nothing is written. Every entry named is read, each change is
// classified, and the records that would run come back in `items[].record`
// — the same values `POST /changeset/apply` takes, not a description of
// them. That is what stops a plan from promising one thing and an apply
// doing another.
//
// `action` is a stable identifier and is what a client should switch on.
// `reason` is prose for a person and may be reworded in any release.
//
// Each applicable item carries a `baseline`: an opaque token binding two
// things — the exact operation in `record`, and the state of the directory
// that operation was planned against. Hand it back on the corresponding
// change when applying. The server re-reads the entry and refuses the
// whole set before anything runs: `400 plan_mismatch` if a change is not
// the operation its baseline was issued for, `409 conflict` with
// `cause: plan_stale` if the directory has moved since. Neither is silently replanned. The token
// holds no attribute values; a sensitive attribute contributes only
// whether it is set and how many values it has, and a password change
// binds the entry and the fact of the change, never the password.
// Baselines are meaningless to any other Alder process and do not survive
// a restart.
//
// The input is either `changes` or `ldif`, never both.
//
// **`ldif` with `mode: changes`** (the default) plans every record as the
// exact operation it states. A record with no `changetype` is an add,
// which is what `ldapadd` does with it. An add of an entry that already
// exists is a conflict (`entry_exists`); it is never turned into a
// modification nobody asked for.
//
// **`ldif` with `mode: desired`** reads the document as the state entries
// should be in. Only content records — no `changetype` — are accepted; a
// record with one is refused with `400 ldif_mode_mismatch`, because a
// document mixing "this entry looks like this" with "do this" has no
// single meaning. Each record is reconciled: created if the entry is
// absent, turned into the modification of the attributes it names if
// present, reported `unchanged` if it already matches. Attributes a record
// does not name are left alone. **An entry the document does not mention
// is not deleted**; there is no mode in which absence means deletion.
//
// `impact` reports facts, not a risk score: which target area each change
// touches (ordinary data, schema, server configuration), memberships
// gained and lost, deletions grouped into the branches they remove, and —
// for deletions and renames — the entries that name the affected DN and
// how many of those references the plan would leave dangling. Reference
// search runs as the session's own bind, so it reports what that bind can
// see; see `impact.references`.
//
// Takes a body of the `application/json` content type.
//
// Corresponds with POST /plan (the `PlanChanges` operationId).
func (c *Client) PlanChanges(ctx context.Context, body PlanChangesJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewPlanChangesRequest(c.Server, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// PreflightWithBody Report what of an artifact would carry across to this directory
//
// Reads a change package, a schema snapshot or a data snapshot -- told
// apart by its own `format` and `kind`, never by a file name -- and reports,
// finding by finding, what this directory already holds, what it could take,
// what needs something done first, what contradicts it, what it cannot
// represent, and what could not be seen.
//
// A preflight is not a plan and not a migration. It reads the directory and
// never writes to it; it never rewrites a DN, maps an attribute or object
// class, or changes the artifact; and it produces no baseline and no
// prepared change. Package validation is its first step for a package, and
// its answer is carried on every finding about an item.
//
// `overall` is derived from the findings: `incompatible` when any finding
// that blocks portability is incompatible or unsupported, `incomplete` when
// otherwise something could not be decided, `compatible_with_prerequisites`
// when something has to be done first, and `compatible` only when none of
// these hold. Access control and server configuration are never evaluated,
// and `notEvaluated` says so on every report.
//
// A document that is none of the three is refused with
// `preflight_artifact_unsupported`; an artifact that is malformed, forged
// or from a newer version is refused with the code its own endpoints use.
//
// Takes any type of body and a specified content type.
//
// Corresponds with POST /preflight (the `Preflight` operationId).
func (c *Client) PreflightWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewPreflightRequestWithBody(c.Server, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// Preflight Report what of an artifact would carry across to this directory
//
// Reads a change package, a schema snapshot or a data snapshot -- told
// apart by its own `format` and `kind`, never by a file name -- and reports,
// finding by finding, what this directory already holds, what it could take,
// what needs something done first, what contradicts it, what it cannot
// represent, and what could not be seen.
//
// A preflight is not a plan and not a migration. It reads the directory and
// never writes to it; it never rewrites a DN, maps an attribute or object
// class, or changes the artifact; and it produces no baseline and no
// prepared change. Package validation is its first step for a package, and
// its answer is carried on every finding about an item.
//
// `overall` is derived from the findings: `incompatible` when any finding
// that blocks portability is incompatible or unsupported, `incomplete` when
// otherwise something could not be decided, `compatible_with_prerequisites`
// when something has to be done first, and `compatible` only when none of
// these hold. Access control and server configuration are never evaluated,
// and `notEvaluated` says so on every report.
//
// A document that is none of the three is refused with
// `preflight_artifact_unsupported`; an artifact that is malformed, forged
// or from a newer version is refused with the code its own endpoints use.
//
// Takes a body of the `application/json` content type.
//
// Corresponds with POST /preflight (the `Preflight` operationId).
func (c *Client) Preflight(ctx context.Context, body PreflightJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewPreflightRequest(c.Server, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// InspectRecoveryWithBody Validate a recovery bundle and turn it into changes to plan
//
// Validates an uploaded recovery bundle and returns its compensating
// changes as ordinary change requests, in the order they would have to
// run, each carrying the `expect` precondition that makes it a conflict
// in a plan if the entry is no longer as the original apply left it.
// Added in 1.9; see `docs/RECOVERY.md`.
//
// Nothing is applied, and there is no endpoint that applies a bundle.
// The changes go to `POST /plan`, are reviewed, and are applied with
// `POST /changeset/apply` like any other change. `drift` is that plan's
// verdict on each change against the directory as it is now, for the
// preview; the plan made afterwards is the one that counts.
//
// A bundle is untrusted input. It is refused when its format or version
// is not one this Alder reads (`recovery_unsupported_version` for another
// version), when any field is malformed or unknown, when a sensitive
// attribute appears with values, when a compensation is a password
// change (`recovery_invalid`), or when a checksum is present and does
// not match (`recovery_checksum_mismatch`).
//
// `originMatches` compares the vendor and naming contexts the bundle
// records with this session's directory. Neither is proof of identity:
// two directories can announce the same.
//
// Takes any type of body and a specified content type.
//
// Corresponds with POST /recovery/inspect (the `InspectRecovery` operationId).
func (c *Client) InspectRecoveryWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewInspectRecoveryRequestWithBody(c.Server, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// InspectRecovery Validate a recovery bundle and turn it into changes to plan
//
// Validates an uploaded recovery bundle and returns its compensating
// changes as ordinary change requests, in the order they would have to
// run, each carrying the `expect` precondition that makes it a conflict
// in a plan if the entry is no longer as the original apply left it.
// Added in 1.9; see `docs/RECOVERY.md`.
//
// Nothing is applied, and there is no endpoint that applies a bundle.
// The changes go to `POST /plan`, are reviewed, and are applied with
// `POST /changeset/apply` like any other change. `drift` is that plan's
// verdict on each change against the directory as it is now, for the
// preview; the plan made afterwards is the one that counts.
//
// A bundle is untrusted input. It is refused when its format or version
// is not one this Alder reads (`recovery_unsupported_version` for another
// version), when any field is malformed or unknown, when a sensitive
// attribute appears with values, when a compensation is a password
// change (`recovery_invalid`), or when a checksum is present and does
// not match (`recovery_checksum_mismatch`).
//
// `originMatches` compares the vendor and naming contexts the bundle
// records with this session's directory. Neither is proof of identity:
// two directories can announce the same.
//
// Takes a body of the `application/json` content type.
//
// Corresponds with POST /recovery/inspect (the `InspectRecovery` operationId).
func (c *Client) InspectRecovery(ctx context.Context, body InspectRecoveryJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewInspectRecoveryRequest(c.Server, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// ListReferences The entries that name this one, and the attribute each names it by
//
// Answers "which groups is this in, and what else points at it" — and,
// unlike the filter on the entry itself, says *how* each one points.
//
// That is the part a link cannot give you. A search returns the entries
// matching the filter but not which term matched, and removing a
// reference means modifying a named attribute on the referencing entry:
// without knowing whether a group holds this DN in `member` or in `owner`,
// there is nothing safe to offer.
//
// It is one bounded search. The reference attributes are requested and
// matched here rather than in the browser, so a group's five hundred
// members are compared where they already are instead of being sent
// across to be compared and discarded.
//
// Corresponds with GET /references (the `ListReferences` operationId).
func (c *Client) ListReferences(ctx context.Context, params *ListReferencesParams, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewListReferencesRequest(c.Server, params)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// Resolve What could this input be — an entry, or a search
//
// Backs one box that takes a DN, an RFC 4515 filter, or a name.
//
// It parses and never searches. `internal/dn` and `internal/filter` are
// the authorities on what these strings are, and a regex in the browser
// would drift from them the first time either grew a case — but probing
// the directory to see whether an entry exists would turn every keystroke
// into a read, to answer a question the operator is about to answer by
// pressing enter.
//
// The three kinds overlap. `cn=platform` is a valid one-component DN
// *and* almost certainly a request to find something called platform, so
// an ambiguous input returns both destinations and the operator picks.
// Guessing would be wrong about half the time.
//
// Corresponds with GET /resolve (the `Resolve` operationId).
func (c *Client) Resolve(ctx context.Context, params *ResolveParams, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewResolveRequest(c.Server, params)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// GetSchema The whole schema, indexed for browsing
//
// Corresponds with GET /schema (the `GetSchema` operationId).
func (c *Client) GetSchema(ctx context.Context, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewGetSchemaRequest(c.Server)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// GetAttributeType One attribute type, with its cross-links resolved
//
// Corresponds with GET /schema/attributetypes/{name} (the `GetAttributeType` operationId).
func (c *Client) GetAttributeType(ctx context.Context, name string, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewGetAttributeTypeRequest(c.Server, name)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// BuildSchemaChangeWithBody Build the modification that installs, replaces or removes a definition
//
// Renders a definition and returns the change that would apply it, without
// applying anything.
//
// A schema definition is a value of an attribute on an ordinary entry, so
// the change this returns is an ordinary modify, and it goes on to the
// same preview, the same confirmation and the same changeset as every
// other write. There is still exactly one path that writes.
//
// The definition text is built here rather than in the browser, for the
// same reason the LDIF preview is: what the person confirms and what the
// directory receives have to be the same bytes.
//
// A replace or a delete needs the value the server actually stores, which
// is not always the value the schema browser displays — a server keeping
// its schema in configuration prefixes each stored definition with its
// load order and strips that prefix from what it publishes. This reads the
// target to find it, which is why the request names an OID rather than
// carrying a definition to remove.
//
// Takes any type of body and a specified content type.
//
// Corresponds with POST /schema/change (the `BuildSchemaChange` operationId).
func (c *Client) BuildSchemaChangeWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewBuildSchemaChangeRequestWithBody(c.Server, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// BuildSchemaChange Build the modification that installs, replaces or removes a definition
//
// Renders a definition and returns the change that would apply it, without
// applying anything.
//
// A schema definition is a value of an attribute on an ordinary entry, so
// the change this returns is an ordinary modify, and it goes on to the
// same preview, the same confirmation and the same changeset as every
// other write. There is still exactly one path that writes.
//
// The definition text is built here rather than in the browser, for the
// same reason the LDIF preview is: what the person confirms and what the
// directory receives have to be the same bytes.
//
// A replace or a delete needs the value the server actually stores, which
// is not always the value the schema browser displays — a server keeping
// its schema in configuration prefixes each stored definition with its
// load order and strips that prefix from what it publishes. This reads the
// target to find it, which is why the request names an OID rather than
// carrying a definition to remove.
//
// Takes a body of the `application/json` content type.
//
// Corresponds with POST /schema/change (the `BuildSchemaChange` operationId).
func (c *Client) BuildSchemaChange(ctx context.Context, body BuildSchemaChangeJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewBuildSchemaChangeRequest(c.Server, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// GetObjectClass One object class, with its cross-links resolved
//
// Corresponds with GET /schema/objectclasses/{name} (the `GetObjectClass` operationId).
func (c *Client) GetObjectClass(ctx context.Context, name string, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewGetObjectClassRequest(c.Server, name)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// GetRequirements What an entry of these object classes must and may hold
//
// The MUST and MAY sets for a set of object classes, with the schema's
// opinion about each attribute — syntax, single-valuedness, description,
// which control to offer.
//
// It exists so a creation form is generated from the schema rather than
// from a template. Without it the form has attribute names and nothing
// else, which is how creating an entry ended up with a plain text box for
// every field while editing the same entry a moment later offered a
// Boolean control, an entry picker and the attribute's description.
//
// Corresponds with GET /schema/requirements (the `GetRequirements` operationId).
func (c *Client) GetRequirements(ctx context.Context, params *GetRequirementsParams, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewGetRequirementsRequest(c.Server, params)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// SearchWithBody Search the directory
//
// Every search is paged and bounded. `filter` is a raw RFC 4515 string
// which is parsed, not interpolated: a value containing filter
// metacharacters cannot escape into the filter structure.
//
// Takes any type of body and a specified content type.
//
// Corresponds with POST /search (the `Search` operationId).
func (c *Client) SearchWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewSearchRequestWithBody(c.Server, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// Search Search the directory
//
// Every search is paged and bounded. `filter` is a raw RFC 4515 string
// which is parsed, not interpolated: a value containing filter
// metacharacters cannot escape into the filter structure.
//
// Takes a body of the `application/json` content type.
//
// Corresponds with POST /search (the `Search` operationId).
func (c *Client) Search(ctx context.Context, body SearchJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewSearchRequest(c.Server, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// DeleteSession Disconnect
//
// Closes the connection and discards the credentials.
//
// Corresponds with DELETE /session (the `DeleteSession` operationId).
func (c *Client) DeleteSession(ctx context.Context, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewDeleteSessionRequest(c.Server)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// GetSession Describe the current session
//
// Corresponds with GET /session (the `GetSession` operationId).
func (c *Client) GetSession(ctx context.Context, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewGetSessionRequest(c.Server)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// CreateSessionWithBody Connect and bind to a directory
//
// Opens a connection, binds, and reads the RootDSE. On success the
// response sets an httpOnly, Secure, SameSite=Strict cookie naming an
// in-memory session. The bind password is held in that in-memory session
// and is never written to disk, never placed in a token, and never
// returned by any endpoint.
//
// Takes any type of body and a specified content type.
//
// Corresponds with POST /session (the `CreateSession` operationId).
func (c *Client) CreateSessionWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewCreateSessionRequestWithBody(c.Server, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// CreateSession Connect and bind to a directory
//
// Opens a connection, binds, and reads the RootDSE. On success the
// response sets an httpOnly, Secure, SameSite=Strict cookie naming an
// in-memory session. The bind password is held in that in-memory session
// and is never written to disk, never placed in a token, and never
// returned by any endpoint.
//
// Takes a body of the `application/json` content type.
//
// Corresponds with POST /session (the `CreateSession` operationId).
func (c *Client) CreateSession(ctx context.Context, body CreateSessionJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewCreateSessionRequest(c.Server, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// CaptureSnapshotWithBody Capture a subtree as a versioned snapshot
//
// Reads the subtree as the session's own identity and returns it as an
// Alder snapshot: a versioned, canonical JSON document, safe to keep in a
// repository. See `docs/SNAPSHOTS.md`.
//
//   - Entries, attribute names and values are in one defined order, so two
//     captures of an unchanged subtree differ only in `createdAt`, and have
//     the same `checksum`.
//   - Operational attributes are left out unless
//     `operationalAttributes` is set. Each entry's stable identity
//     (`entryUUID` or `nsUniqueId`) is recorded as its `id` either way.
//   - Sensitive attributes are recorded as a `withheld` count, never as a
//     value or anything derived from one.
//   - A capture that cannot finish -- more than 50,000 entries, a search
//     limit, a failure part way -- is an error. There is no partial
//     snapshot.
//   - A data snapshot's base must be ordinary data: a base inside the schema
//     or the server's configuration is refused with
//     `snapshot_scope_unsupported`.
//
// With `kind: schema` (1.10) the published subschema is captured instead,
// and `base`, `scope`, `filter` and `operationalAttributes` are not used.
// Attribute types and object classes are captured for comparison; LDAP
// syntaxes, matching rules, matching rule uses, DIT content rules and name
// forms as context. A definition that does not parse is kept verbatim and
// makes the snapshot `partial`. Server configuration is never captured, and
// there is no `kind: config`.
//
// Nothing is kept on the server. The snapshot is returned and belongs to
// the caller.
//
// Takes any type of body and a specified content type.
//
// Corresponds with POST /snapshots/capture (the `CaptureSnapshot` operationId).
func (c *Client) CaptureSnapshotWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewCaptureSnapshotRequestWithBody(c.Server, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// CaptureSnapshot Capture a subtree as a versioned snapshot
//
// Reads the subtree as the session's own identity and returns it as an
// Alder snapshot: a versioned, canonical JSON document, safe to keep in a
// repository. See `docs/SNAPSHOTS.md`.
//
//   - Entries, attribute names and values are in one defined order, so two
//     captures of an unchanged subtree differ only in `createdAt`, and have
//     the same `checksum`.
//   - Operational attributes are left out unless
//     `operationalAttributes` is set. Each entry's stable identity
//     (`entryUUID` or `nsUniqueId`) is recorded as its `id` either way.
//   - Sensitive attributes are recorded as a `withheld` count, never as a
//     value or anything derived from one.
//   - A capture that cannot finish -- more than 50,000 entries, a search
//     limit, a failure part way -- is an error. There is no partial
//     snapshot.
//   - A data snapshot's base must be ordinary data: a base inside the schema
//     or the server's configuration is refused with
//     `snapshot_scope_unsupported`.
//
// With `kind: schema` (1.10) the published subschema is captured instead,
// and `base`, `scope`, `filter` and `operationalAttributes` are not used.
// Attribute types and object classes are captured for comparison; LDAP
// syntaxes, matching rules, matching rule uses, DIT content rules and name
// forms as context. A definition that does not parse is kept verbatim and
// makes the snapshot `partial`. Server configuration is never captured, and
// there is no `kind: config`.
//
// Nothing is kept on the server. The snapshot is returned and belongs to
// the caller.
//
// Takes a body of the `application/json` content type.
//
// Corresponds with POST /snapshots/capture (the `CaptureSnapshot` operationId).
func (c *Client) CaptureSnapshot(ctx context.Context, body CaptureSnapshotJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewCaptureSnapshotRequest(c.Server, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// InspectSnapshotWithBody Validate a snapshot and describe it
//
// Validates an uploaded snapshot exactly as a comparison would and
// describes what it covers. Read-only: nothing is kept and nothing is
// applied.
//
// A snapshot is refused when its format or version is not one this Alder
// reads (`snapshot_unsupported_version` for a newer version), when any DN,
// value or field is malformed or unknown (`snapshot_invalid`), when a
// sensitive attribute carries a value, or when a checksum is present and
// does not match (`snapshot_checksum_mismatch`). A snapshot without a
// checksum is accepted and described as `integrity: unverified`.
//
// A schema snapshot (1.10) is checked as strictly: every OID, no OID twice,
// every parsed field equal to what its definition text parses to, counts
// and coverage true, and no inheritance cycle.
//
// Takes any type of body and a specified content type.
//
// Corresponds with POST /snapshots/inspect (the `InspectSnapshot` operationId).
func (c *Client) InspectSnapshotWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewInspectSnapshotRequestWithBody(c.Server, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// InspectSnapshot Validate a snapshot and describe it
//
// Validates an uploaded snapshot exactly as a comparison would and
// describes what it covers. Read-only: nothing is kept and nothing is
// applied.
//
// A snapshot is refused when its format or version is not one this Alder
// reads (`snapshot_unsupported_version` for a newer version), when any DN,
// value or field is malformed or unknown (`snapshot_invalid`), when a
// sensitive attribute carries a value, or when a checksum is present and
// does not match (`snapshot_checksum_mismatch`). A snapshot without a
// checksum is accepted and described as `integrity: unverified`.
//
// A schema snapshot (1.10) is checked as strictly: every OID, no OID twice,
// every parsed field equal to what its definition text parses to, counts
// and coverage true, and no inheritance cycle.
//
// Takes a body of the `application/json` content type.
//
// Corresponds with POST /snapshots/inspect (the `InspectSnapshot` operationId).
func (c *Client) InspectSnapshot(ctx context.Context, body InspectSnapshotJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewInspectSnapshotRequest(c.Server, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// GetSourceOffer Where to get the source of this running instance
//
// AGPL-3.0 section 13: someone using a modified Alder over a network is
// entitled to its Corresponding Source. The offer is served rather than
// written in a README because the obligation runs to the person using the
// running instance, and the user of a modified instance has no reason to
// know where its source went.
//
// **This is the one endpoint that answers without a session,** and it has
// no 401 for that reason: an offer only the already-connected can read
// would not discharge the obligation. It is described here so that the
// contract in this document is the whole contract -- it was served outside
// it until 1.0, which made `openapi.yaml` quietly incomplete about the one
// endpoint a licence requires.
//
// Corresponds with GET /source (the `GetSourceOffer` operationId).
func (c *Client) GetSourceOffer(ctx context.Context, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewGetSourceOfferRequest(c.Server)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// ListChildren List the children of an entry
//
// With no `dn`, returns the naming contexts the server holds, which are
// the roots of the tree. With a `dn`, returns that entry's immediate
// children. This is the lazy-loading tree browser's only endpoint.
//
// Corresponds with GET /tree (the `ListChildren` operationId).
func (c *Client) ListChildren(ctx context.Context, params *ListChildrenParams, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewListChildrenRequest(c.Server, params)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// ListObjectViews The object-aware views this directory supports
//
// Users, groups and organizational units, expressed as saved searches
// derived from the schema the connected server published.
//
// A view is offered only where the server defines a class to anchor it
// on, so a directory holding no person class has no Users view rather
// than an empty one. The anchors are standards-track classes — RFC 4519,
// RFC 4524, RFC 2307 — never vendor names, and a site class inheriting
// from one of them is matched without being named, because an entry
// carries its superclasses in its own `objectClass` attribute.
//
// The filter is built here and goes back to `/search` unmodified. The
// columns are the attributes the anchor classes actually permit, so a
// directory without `inetOrgPerson` gets no mail column instead of an
// empty one.
//
// Corresponds with GET /views (the `ListObjectViews` operationId).
func (c *Client) ListObjectViews(ctx context.Context, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewListObjectViewsRequest(c.Server)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// NewApplyChangeRequest calls the generic ApplyChange builder with application/json body
func NewApplyChangeRequest(server string, params *ApplyChangeParams, body ApplyChangeJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewApplyChangeRequestWithBody(server, params, "application/json", bodyReader)
}

// NewApplyChangeRequestWithBody constructs an http.Request for the ApplyChange method, with any body, and a specified content type
func NewApplyChangeRequestWithBody(server string, params *ApplyChangeParams, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/changes/apply")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	if params != nil {
		// queryValues collects non-styled parameters (passthrough, JSON)
		// that are safe to round-trip through url.Values.Encode().
		queryValues := queryURL.Query()
		// rawQueryFragments collects pre-encoded query fragments from
		// styled parameters, preserving literal commas as delimiters
		// per the OpenAPI spec (e.g. "color=blue,black,brown").
		var rawQueryFragments []string

		if params.Recovery != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "recovery", *params.Recovery, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "boolean", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if encoded := queryValues.Encode(); encoded != "" {
			rawQueryFragments = append(rawQueryFragments, encoded)
		}
		queryURL.RawQuery = strings.Join(rawQueryFragments, "&")
	}

	req, err := http.NewRequest(http.MethodPost, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	return req, nil
}

// NewPreviewChangeRequest calls the generic PreviewChange builder with application/json body
func NewPreviewChangeRequest(server string, body PreviewChangeJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewPreviewChangeRequestWithBody(server, "application/json", bodyReader)
}

// NewPreviewChangeRequestWithBody constructs an http.Request for the PreviewChange method, with any body, and a specified content type
func NewPreviewChangeRequestWithBody(server string, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/changes/preview")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPost, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	return req, nil
}

// NewApplyChangesetRequest calls the generic ApplyChangeset builder with application/json body
func NewApplyChangesetRequest(server string, body ApplyChangesetJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewApplyChangesetRequestWithBody(server, "application/json", bodyReader)
}

// NewApplyChangesetRequestWithBody constructs an http.Request for the ApplyChangeset method, with any body, and a specified content type
func NewApplyChangesetRequestWithBody(server string, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/changeset/apply")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPost, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	return req, nil
}

// NewPreviewChangesetRequest calls the generic PreviewChangeset builder with application/json body
func NewPreviewChangesetRequest(server string, body PreviewChangesetJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewPreviewChangesetRequestWithBody(server, "application/json", bodyReader)
}

// NewPreviewChangesetRequestWithBody constructs an http.Request for the PreviewChangeset method, with any body, and a specified content type
func NewPreviewChangesetRequestWithBody(server string, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/changeset/preview")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPost, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	return req, nil
}

// NewCompareEntriesRequest constructs an http.Request for the CompareEntries method
func NewCompareEntriesRequest(server string, params *CompareEntriesParams) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/compare")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	if params != nil {
		// queryValues collects non-styled parameters (passthrough, JSON)
		// that are safe to round-trip through url.Values.Encode().
		queryValues := queryURL.Query()
		// rawQueryFragments collects pre-encoded query fragments from
		// styled parameters, preserving literal commas as delimiters
		// per the OpenAPI spec (e.g. "color=blue,black,brown").
		var rawQueryFragments []string

		if queryFrag, err := runtime.StyleParamWithOptions("form", true, "left", params.Left, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
			return nil, err
		} else {
			for _, qp := range strings.Split(queryFrag, "&") {
				rawQueryFragments = append(rawQueryFragments, qp)
			}
		}

		if queryFrag, err := runtime.StyleParamWithOptions("form", true, "right", params.Right, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
			return nil, err
		} else {
			for _, qp := range strings.Split(queryFrag, "&") {
				rawQueryFragments = append(rawQueryFragments, qp)
			}
		}

		if params.IncludeOperational != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "includeOperational", *params.IncludeOperational, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "boolean", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.Limit != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "limit", *params.Limit, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "integer", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if encoded := queryValues.Encode(); encoded != "" {
			rawQueryFragments = append(rawQueryFragments, encoded)
		}
		queryURL.RawQuery = strings.Join(rawQueryFragments, "&")
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewCountEntriesRequest constructs an http.Request for the CountEntries method
func NewCountEntriesRequest(server string, params *CountEntriesParams) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/count")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	if params != nil {
		// queryValues collects non-styled parameters (passthrough, JSON)
		// that are safe to round-trip through url.Values.Encode().
		queryValues := queryURL.Query()
		// rawQueryFragments collects pre-encoded query fragments from
		// styled parameters, preserving literal commas as delimiters
		// per the OpenAPI spec (e.g. "color=blue,black,brown").
		var rawQueryFragments []string

		if queryFrag, err := runtime.StyleParamWithOptions("form", true, "dn", params.Dn, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
			return nil, err
		} else {
			for _, qp := range strings.Split(queryFrag, "&") {
				rawQueryFragments = append(rawQueryFragments, qp)
			}
		}

		if params.Limit != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "limit", *params.Limit, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "integer", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if encoded := queryValues.Encode(); encoded != "" {
			rawQueryFragments = append(rawQueryFragments, encoded)
		}
		queryURL.RawQuery = strings.Join(rawQueryFragments, "&")
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewDiffStatesRequest calls the generic DiffStates builder with application/json body
func NewDiffStatesRequest(server string, body DiffStatesJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewDiffStatesRequestWithBody(server, "application/json", bodyReader)
}

// NewDiffStatesRequestWithBody constructs an http.Request for the DiffStates method, with any body, and a specified content type
func NewDiffStatesRequestWithBody(server string, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/diff")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPost, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	return req, nil
}

// NewGetEntryRequest constructs an http.Request for the GetEntry method
func NewGetEntryRequest(server string, params *GetEntryParams) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/entry")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	if params != nil {
		// queryValues collects non-styled parameters (passthrough, JSON)
		// that are safe to round-trip through url.Values.Encode().
		queryValues := queryURL.Query()
		// rawQueryFragments collects pre-encoded query fragments from
		// styled parameters, preserving literal commas as delimiters
		// per the OpenAPI spec (e.g. "color=blue,black,brown").
		var rawQueryFragments []string

		if queryFrag, err := runtime.StyleParamWithOptions("form", true, "dn", params.Dn, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
			return nil, err
		} else {
			for _, qp := range strings.Split(queryFrag, "&") {
				rawQueryFragments = append(rawQueryFragments, qp)
			}
		}

		if params.IncludeOperational != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "includeOperational", *params.IncludeOperational, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "boolean", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if encoded := queryValues.Encode(); encoded != "" {
			rawQueryFragments = append(rawQueryFragments, encoded)
		}
		queryURL.RawQuery = strings.Join(rawQueryFragments, "&")
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewExportAnsibleRequest constructs an http.Request for the ExportAnsible method
func NewExportAnsibleRequest(server string, params *ExportAnsibleParams) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/export/ansible")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	if params != nil {
		// queryValues collects non-styled parameters (passthrough, JSON)
		// that are safe to round-trip through url.Values.Encode().
		queryValues := queryURL.Query()
		// rawQueryFragments collects pre-encoded query fragments from
		// styled parameters, preserving literal commas as delimiters
		// per the OpenAPI spec (e.g. "color=blue,black,brown").
		var rawQueryFragments []string

		if queryFrag, err := runtime.StyleParamWithOptions("form", true, "dn", params.Dn, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
			return nil, err
		} else {
			for _, qp := range strings.Split(queryFrag, "&") {
				rawQueryFragments = append(rawQueryFragments, qp)
			}
		}

		if params.Scope != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "scope", *params.Scope, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.Filter != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "filter", *params.Filter, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.Limit != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "limit", *params.Limit, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "integer", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if encoded := queryValues.Encode(); encoded != "" {
			rawQueryFragments = append(rawQueryFragments, encoded)
		}
		queryURL.RawQuery = strings.Join(rawQueryFragments, "&")
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewExportLdifRequest constructs an http.Request for the ExportLdif method
func NewExportLdifRequest(server string, params *ExportLdifParams) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/export/ldif")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	if params != nil {
		// queryValues collects non-styled parameters (passthrough, JSON)
		// that are safe to round-trip through url.Values.Encode().
		queryValues := queryURL.Query()
		// rawQueryFragments collects pre-encoded query fragments from
		// styled parameters, preserving literal commas as delimiters
		// per the OpenAPI spec (e.g. "color=blue,black,brown").
		var rawQueryFragments []string

		if queryFrag, err := runtime.StyleParamWithOptions("form", true, "dn", params.Dn, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
			return nil, err
		} else {
			for _, qp := range strings.Split(queryFrag, "&") {
				rawQueryFragments = append(rawQueryFragments, qp)
			}
		}

		if params.Scope != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "scope", *params.Scope, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.IncludeOperational != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "includeOperational", *params.IncludeOperational, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "boolean", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.IncludeSensitive != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "includeSensitive", *params.IncludeSensitive, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "boolean", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.Filter != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "filter", *params.Filter, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.Limit != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "limit", *params.Limit, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "integer", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if encoded := queryValues.Encode(); encoded != "" {
			rawQueryFragments = append(rawQueryFragments, encoded)
		}
		queryURL.RawQuery = strings.Join(rawQueryFragments, "&")
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewExportOutlineRequest constructs an http.Request for the ExportOutline method
func NewExportOutlineRequest(server string, params *ExportOutlineParams) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/export/outline")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	if params != nil {
		// queryValues collects non-styled parameters (passthrough, JSON)
		// that are safe to round-trip through url.Values.Encode().
		queryValues := queryURL.Query()
		// rawQueryFragments collects pre-encoded query fragments from
		// styled parameters, preserving literal commas as delimiters
		// per the OpenAPI spec (e.g. "color=blue,black,brown").
		var rawQueryFragments []string

		if queryFrag, err := runtime.StyleParamWithOptions("form", true, "dn", params.Dn, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
			return nil, err
		} else {
			for _, qp := range strings.Split(queryFrag, "&") {
				rawQueryFragments = append(rawQueryFragments, qp)
			}
		}

		if params.Scope != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "scope", *params.Scope, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.Filter != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "filter", *params.Filter, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.Limit != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "limit", *params.Limit, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "integer", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if encoded := queryValues.Encode(); encoded != "" {
			rawQueryFragments = append(rawQueryFragments, encoded)
		}
		queryURL.RawQuery = strings.Join(rawQueryFragments, "&")
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewExportYamlRequest constructs an http.Request for the ExportYaml method
func NewExportYamlRequest(server string, params *ExportYamlParams) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/export/yaml")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	if params != nil {
		// queryValues collects non-styled parameters (passthrough, JSON)
		// that are safe to round-trip through url.Values.Encode().
		queryValues := queryURL.Query()
		// rawQueryFragments collects pre-encoded query fragments from
		// styled parameters, preserving literal commas as delimiters
		// per the OpenAPI spec (e.g. "color=blue,black,brown").
		var rawQueryFragments []string

		if queryFrag, err := runtime.StyleParamWithOptions("form", true, "dn", params.Dn, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
			return nil, err
		} else {
			for _, qp := range strings.Split(queryFrag, "&") {
				rawQueryFragments = append(rawQueryFragments, qp)
			}
		}

		if params.Scope != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "scope", *params.Scope, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.Filter != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "filter", *params.Filter, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.IncludeOperational != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "includeOperational", *params.IncludeOperational, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "boolean", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.IncludeSensitive != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "includeSensitive", *params.IncludeSensitive, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "boolean", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.Limit != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "limit", *params.Limit, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "integer", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if encoded := queryValues.Encode(); encoded != "" {
			rawQueryFragments = append(rawQueryFragments, encoded)
		}
		queryURL.RawQuery = strings.Join(rawQueryFragments, "&")
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewParseLdifRequest calls the generic ParseLdif builder with application/json body
func NewParseLdifRequest(server string, body ParseLdifJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewParseLdifRequestWithBody(server, "application/json", bodyReader)
}

// NewParseLdifRequestWithBody constructs an http.Request for the ParseLdif method, with any body, and a specified content type
func NewParseLdifRequestWithBody(server string, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/import/ldif")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPost, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	return req, nil
}

// NewInventoryValuesRequest calls the generic InventoryValues builder with application/json body
func NewInventoryValuesRequest(server string, body InventoryValuesJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewInventoryValuesRequestWithBody(server, "application/json", bodyReader)
}

// NewInventoryValuesRequestWithBody constructs an http.Request for the InventoryValues method, with any body, and a specified content type
func NewInventoryValuesRequestWithBody(server string, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/inventory")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPost, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	return req, nil
}

// NewExpandMembersRequest constructs an http.Request for the ExpandMembers method
func NewExpandMembersRequest(server string, params *ExpandMembersParams) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/members")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	if params != nil {
		// queryValues collects non-styled parameters (passthrough, JSON)
		// that are safe to round-trip through url.Values.Encode().
		queryValues := queryURL.Query()
		// rawQueryFragments collects pre-encoded query fragments from
		// styled parameters, preserving literal commas as delimiters
		// per the OpenAPI spec (e.g. "color=blue,black,brown").
		var rawQueryFragments []string

		if queryFrag, err := runtime.StyleParamWithOptions("form", true, "dn", params.Dn, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
			return nil, err
		} else {
			for _, qp := range strings.Split(queryFrag, "&") {
				rawQueryFragments = append(rawQueryFragments, qp)
			}
		}

		if params.Limit != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "limit", *params.Limit, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "integer", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if encoded := queryValues.Encode(); encoded != "" {
			rawQueryFragments = append(rawQueryFragments, encoded)
		}
		queryURL.RawQuery = strings.Join(rawQueryFragments, "&")
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewBuildPackageRequest calls the generic BuildPackage builder with application/json body
func NewBuildPackageRequest(server string, body BuildPackageJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewBuildPackageRequestWithBody(server, "application/json", bodyReader)
}

// NewBuildPackageRequestWithBody constructs an http.Request for the BuildPackage method, with any body, and a specified content type
func NewBuildPackageRequestWithBody(server string, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/packages/build")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPost, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	return req, nil
}

// NewInspectPackageRequest calls the generic InspectPackage builder with application/json body
func NewInspectPackageRequest(server string, body InspectPackageJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewInspectPackageRequestWithBody(server, "application/json", bodyReader)
}

// NewInspectPackageRequestWithBody constructs an http.Request for the InspectPackage method, with any body, and a specified content type
func NewInspectPackageRequestWithBody(server string, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/packages/inspect")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPost, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	return req, nil
}

// NewValidatePackageRequest calls the generic ValidatePackage builder with application/json body
func NewValidatePackageRequest(server string, body ValidatePackageJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewValidatePackageRequestWithBody(server, "application/json", bodyReader)
}

// NewValidatePackageRequestWithBody constructs an http.Request for the ValidatePackage method, with any body, and a specified content type
func NewValidatePackageRequestWithBody(server string, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/packages/validate")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPost, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	return req, nil
}

// NewPlanChangesRequest calls the generic PlanChanges builder with application/json body
func NewPlanChangesRequest(server string, body PlanChangesJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewPlanChangesRequestWithBody(server, "application/json", bodyReader)
}

// NewPlanChangesRequestWithBody constructs an http.Request for the PlanChanges method, with any body, and a specified content type
func NewPlanChangesRequestWithBody(server string, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/plan")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPost, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	return req, nil
}

// NewPreflightRequest calls the generic Preflight builder with application/json body
func NewPreflightRequest(server string, body PreflightJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewPreflightRequestWithBody(server, "application/json", bodyReader)
}

// NewPreflightRequestWithBody constructs an http.Request for the Preflight method, with any body, and a specified content type
func NewPreflightRequestWithBody(server string, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/preflight")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPost, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	return req, nil
}

// NewInspectRecoveryRequest calls the generic InspectRecovery builder with application/json body
func NewInspectRecoveryRequest(server string, body InspectRecoveryJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewInspectRecoveryRequestWithBody(server, "application/json", bodyReader)
}

// NewInspectRecoveryRequestWithBody constructs an http.Request for the InspectRecovery method, with any body, and a specified content type
func NewInspectRecoveryRequestWithBody(server string, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/recovery/inspect")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPost, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	return req, nil
}

// NewListReferencesRequest constructs an http.Request for the ListReferences method
func NewListReferencesRequest(server string, params *ListReferencesParams) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/references")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	if params != nil {
		// queryValues collects non-styled parameters (passthrough, JSON)
		// that are safe to round-trip through url.Values.Encode().
		queryValues := queryURL.Query()
		// rawQueryFragments collects pre-encoded query fragments from
		// styled parameters, preserving literal commas as delimiters
		// per the OpenAPI spec (e.g. "color=blue,black,brown").
		var rawQueryFragments []string

		if queryFrag, err := runtime.StyleParamWithOptions("form", true, "dn", params.Dn, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
			return nil, err
		} else {
			for _, qp := range strings.Split(queryFrag, "&") {
				rawQueryFragments = append(rawQueryFragments, qp)
			}
		}

		if params.Limit != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "limit", *params.Limit, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "integer", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if encoded := queryValues.Encode(); encoded != "" {
			rawQueryFragments = append(rawQueryFragments, encoded)
		}
		queryURL.RawQuery = strings.Join(rawQueryFragments, "&")
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewResolveRequest constructs an http.Request for the Resolve method
func NewResolveRequest(server string, params *ResolveParams) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/resolve")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	if params != nil {
		// queryValues collects non-styled parameters (passthrough, JSON)
		// that are safe to round-trip through url.Values.Encode().
		queryValues := queryURL.Query()
		// rawQueryFragments collects pre-encoded query fragments from
		// styled parameters, preserving literal commas as delimiters
		// per the OpenAPI spec (e.g. "color=blue,black,brown").
		var rawQueryFragments []string

		if queryFrag, err := runtime.StyleParamWithOptions("form", true, "q", params.Q, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
			return nil, err
		} else {
			for _, qp := range strings.Split(queryFrag, "&") {
				rawQueryFragments = append(rawQueryFragments, qp)
			}
		}

		if encoded := queryValues.Encode(); encoded != "" {
			rawQueryFragments = append(rawQueryFragments, encoded)
		}
		queryURL.RawQuery = strings.Join(rawQueryFragments, "&")
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewGetSchemaRequest constructs an http.Request for the GetSchema method
func NewGetSchemaRequest(server string) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/schema")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewGetAttributeTypeRequest constructs an http.Request for the GetAttributeType method
func NewGetAttributeTypeRequest(server string, name string) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "name", name, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "string", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/schema/attributetypes/%s", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewBuildSchemaChangeRequest calls the generic BuildSchemaChange builder with application/json body
func NewBuildSchemaChangeRequest(server string, body BuildSchemaChangeJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewBuildSchemaChangeRequestWithBody(server, "application/json", bodyReader)
}

// NewBuildSchemaChangeRequestWithBody constructs an http.Request for the BuildSchemaChange method, with any body, and a specified content type
func NewBuildSchemaChangeRequestWithBody(server string, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/schema/change")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPost, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	return req, nil
}

// NewGetObjectClassRequest constructs an http.Request for the GetObjectClass method
func NewGetObjectClassRequest(server string, name string) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "name", name, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "string", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/schema/objectclasses/%s", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewGetRequirementsRequest constructs an http.Request for the GetRequirements method
func NewGetRequirementsRequest(server string, params *GetRequirementsParams) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/schema/requirements")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	if params != nil {
		// queryValues collects non-styled parameters (passthrough, JSON)
		// that are safe to round-trip through url.Values.Encode().
		queryValues := queryURL.Query()
		// rawQueryFragments collects pre-encoded query fragments from
		// styled parameters, preserving literal commas as delimiters
		// per the OpenAPI spec (e.g. "color=blue,black,brown").
		var rawQueryFragments []string

		if params.Class != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "class", params.Class, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "array", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if encoded := queryValues.Encode(); encoded != "" {
			rawQueryFragments = append(rawQueryFragments, encoded)
		}
		queryURL.RawQuery = strings.Join(rawQueryFragments, "&")
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewSearchRequest calls the generic Search builder with application/json body
func NewSearchRequest(server string, body SearchJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewSearchRequestWithBody(server, "application/json", bodyReader)
}

// NewSearchRequestWithBody constructs an http.Request for the Search method, with any body, and a specified content type
func NewSearchRequestWithBody(server string, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/search")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPost, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	return req, nil
}

// NewDeleteSessionRequest constructs an http.Request for the DeleteSession method
func NewDeleteSessionRequest(server string) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/session")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodDelete, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewGetSessionRequest constructs an http.Request for the GetSession method
func NewGetSessionRequest(server string) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/session")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewCreateSessionRequest calls the generic CreateSession builder with application/json body
func NewCreateSessionRequest(server string, body CreateSessionJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewCreateSessionRequestWithBody(server, "application/json", bodyReader)
}

// NewCreateSessionRequestWithBody constructs an http.Request for the CreateSession method, with any body, and a specified content type
func NewCreateSessionRequestWithBody(server string, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/session")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPost, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	return req, nil
}

// NewCaptureSnapshotRequest calls the generic CaptureSnapshot builder with application/json body
func NewCaptureSnapshotRequest(server string, body CaptureSnapshotJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewCaptureSnapshotRequestWithBody(server, "application/json", bodyReader)
}

// NewCaptureSnapshotRequestWithBody constructs an http.Request for the CaptureSnapshot method, with any body, and a specified content type
func NewCaptureSnapshotRequestWithBody(server string, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/snapshots/capture")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPost, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	return req, nil
}

// NewInspectSnapshotRequest calls the generic InspectSnapshot builder with application/json body
func NewInspectSnapshotRequest(server string, body InspectSnapshotJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewInspectSnapshotRequestWithBody(server, "application/json", bodyReader)
}

// NewInspectSnapshotRequestWithBody constructs an http.Request for the InspectSnapshot method, with any body, and a specified content type
func NewInspectSnapshotRequestWithBody(server string, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/snapshots/inspect")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPost, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	return req, nil
}

// NewGetSourceOfferRequest constructs an http.Request for the GetSourceOffer method
func NewGetSourceOfferRequest(server string) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/source")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewListChildrenRequest constructs an http.Request for the ListChildren method
func NewListChildrenRequest(server string, params *ListChildrenParams) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/tree")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	if params != nil {
		// queryValues collects non-styled parameters (passthrough, JSON)
		// that are safe to round-trip through url.Values.Encode().
		queryValues := queryURL.Query()
		// rawQueryFragments collects pre-encoded query fragments from
		// styled parameters, preserving literal commas as delimiters
		// per the OpenAPI spec (e.g. "color=blue,black,brown").
		var rawQueryFragments []string

		if params.Dn != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "dn", *params.Dn, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.Limit != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "limit", *params.Limit, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "integer", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.Cookie != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "cookie", *params.Cookie, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if encoded := queryValues.Encode(); encoded != "" {
			rawQueryFragments = append(rawQueryFragments, encoded)
		}
		queryURL.RawQuery = strings.Join(rawQueryFragments, "&")
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewListObjectViewsRequest constructs an http.Request for the ListObjectViews method
func NewListObjectViewsRequest(server string) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/views")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

func (c *Client) applyEditors(ctx context.Context, req *http.Request, additionalEditors []RequestEditorFn) error {
	for _, r := range c.RequestEditors {
		if err := r(ctx, req); err != nil {
			return err
		}
	}
	for _, r := range additionalEditors {
		if err := r(ctx, req); err != nil {
			return err
		}
	}
	return nil
}

// ClientWithResponses builds on ClientInterface to offer response payloads
type ClientWithResponses struct {
	ClientInterface
}

// NewClientWithResponses creates a new ClientWithResponses, which wraps
// Client with return type handling
func NewClientWithResponses(server string, opts ...ClientOption) (*ClientWithResponses, error) {
	client, err := NewClient(server, opts...)
	if err != nil {
		return nil, err
	}
	return &ClientWithResponses{client}, nil
}

// WithBaseURL overrides the baseURL.
func WithBaseURL(baseURL string) ClientOption {
	return func(c *Client) error {
		newBaseURL, err := url.Parse(baseURL)
		if err != nil {
			return err
		}
		c.Server = newBaseURL.String()
		return nil
	}
}

// ClientWithResponsesInterface is the interface specification for the client with responses above.
type ClientWithResponsesInterface interface {

	// ApplyChangeWithBodyWithResponse Apply a change
	//
	// Applies one change. With the `baseline` a plan returned, the server first
	// re-reads the entry and refuses the change if it is not the operation the
	// plan was issued for (`400 plan_mismatch`) or the directory has moved
	// since (`409 conflict`, `cause: plan_stale`). A secret in the change -- a
	// new password, a sensitive attribute value -- is part of that operation:
	// a different value than the one planned is a mismatch.
	//
	// Without a `baseline` the change is applied as it always was. The
	// interface always plans first and always sends one.
	//
	// With `recovery=true` the response carries a recovery bundle for the
	// change once it has succeeded: the compensating change derived from the
	// entry as it was read immediately before the change ran. See
	// `docs/RECOVERY.md`. A bundle is never applied from here or anywhere
	// else; it becomes ordinary changes through `POST /recovery/inspect`.
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /changes/apply (the `ApplyChange` operationId).
	ApplyChangeWithBodyWithResponse(ctx context.Context, params *ApplyChangeParams, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*ApplyChangeReply, error)

	// ApplyChangeWithResponse Apply a change
	//
	// Applies one change. With the `baseline` a plan returned, the server first
	// re-reads the entry and refuses the change if it is not the operation the
	// plan was issued for (`400 plan_mismatch`) or the directory has moved
	// since (`409 conflict`, `cause: plan_stale`). A secret in the change -- a
	// new password, a sensitive attribute value -- is part of that operation:
	// a different value than the one planned is a mismatch.
	//
	// Without a `baseline` the change is applied as it always was. The
	// interface always plans first and always sends one.
	//
	// With `recovery=true` the response carries a recovery bundle for the
	// change once it has succeeded: the compensating change derived from the
	// entry as it was read immediately before the change ran. See
	// `docs/RECOVERY.md`. A bundle is never applied from here or anywhere
	// else; it becomes ordinary changes through `POST /recovery/inspect`.
	//
	// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /changes/apply (the `ApplyChange` operationId).
	ApplyChangeWithResponse(ctx context.Context, params *ApplyChangeParams, body ApplyChangeJSONRequestBody, reqEditors ...RequestEditorFn) (*ApplyChangeReply, error)

	// PreviewChangeWithBodyWithResponse Render a change as LDIF and as an Ansible task
	//
	// Renders without applying and without reading the directory.
	//
	// Deprecated since 1.6, and kept for 1.x clients. The interface no longer
	// calls it: its confirmation dialog plans the change with `POST /plan`,
	// which returns the same rendering inside the plan item together with
	// what the change would do against the directory as it is, and a token
	// that `/changes/apply` holds the change to. A rendering alone says
	// nothing about whether the change still applies.
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /changes/preview (the `PreviewChange` operationId).
	//
	// Deprecated: this operation has been marked as deprecated upstream, but no `x-deprecated-reason` was set
	PreviewChangeWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*PreviewChangeReply, error)

	// PreviewChangeWithResponse Render a change as LDIF and as an Ansible task
	//
	// Renders without applying and without reading the directory.
	//
	// Deprecated since 1.6, and kept for 1.x clients. The interface no longer
	// calls it: its confirmation dialog plans the change with `POST /plan`,
	// which returns the same rendering inside the plan item together with
	// what the change would do against the directory as it is, and a token
	// that `/changes/apply` holds the change to. A rendering alone says
	// nothing about whether the change still applies.
	//
	// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /changes/preview (the `PreviewChange` operationId).
	//
	// Deprecated: this operation has been marked as deprecated upstream, but no `x-deprecated-reason` was set
	PreviewChangeWithResponse(ctx context.Context, body PreviewChangeJSONRequestBody, reqEditors ...RequestEditorFn) (*PreviewChangeReply, error)

	// ApplyChangesetWithBodyWithResponse Apply several changes in order
	//
	// Applies each change in the order given and stops at the first failure.
	//
	// A directory has no transaction spanning entries, so a changeset is not
	// atomic and does not pretend to be. The response says exactly which
	// changes were applied and which was not, so the caller can fix the one
	// that failed and resume from there rather than starting again and
	// re-applying what already succeeded.
	//
	// With `recovery: true` the result carries a recovery bundle covering
	// exactly the changes that were applied -- on a run that stopped partway,
	// only those before the failure -- whose compensations run in the reverse
	// order. See `docs/RECOVERY.md`.
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /changeset/apply (the `ApplyChangeset` operationId).
	ApplyChangesetWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*ApplyChangesetReply, error)

	// ApplyChangesetWithResponse Apply several changes in order
	//
	// Applies each change in the order given and stops at the first failure.
	//
	// A directory has no transaction spanning entries, so a changeset is not
	// atomic and does not pretend to be. The response says exactly which
	// changes were applied and which was not, so the caller can fix the one
	// that failed and resume from there rather than starting again and
	// re-applying what already succeeded.
	//
	// With `recovery: true` the result carries a recovery bundle covering
	// exactly the changes that were applied -- on a run that stopped partway,
	// only those before the failure -- whose compensations run in the reverse
	// order. See `docs/RECOVERY.md`.
	//
	// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /changeset/apply (the `ApplyChangeset` operationId).
	ApplyChangesetWithResponse(ctx context.Context, body ApplyChangesetJSONRequestBody, reqEditors ...RequestEditorFn) (*ApplyChangesetReply, error)

	// PreviewChangesetWithBodyWithResponse Render several changes as one LDIF document and one playbook
	//
	// A changeset is an ordered list of changes reviewed and applied together.
	// It is held by the client, not the server: Alder keeps no per-session
	// state beyond the connection itself, so this endpoint is a pure
	// rendering of whatever it is given.
	//
	// The order is the caller's and is preserved exactly. Alder does not sort
	// the changes to make them work -- an entry created before its parent is
	// reported as a warning so it can be reordered, because guessing the
	// intended order silently is worse than saying what is wrong.
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /changeset/preview (the `PreviewChangeset` operationId).
	PreviewChangesetWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*PreviewChangesetReply, error)

	// PreviewChangesetWithResponse Render several changes as one LDIF document and one playbook
	//
	// A changeset is an ordered list of changes reviewed and applied together.
	// It is held by the client, not the server: Alder keeps no per-session
	// state beyond the connection itself, so this endpoint is a pure
	// rendering of whatever it is given.
	//
	// The order is the caller's and is preserved exactly. Alder does not sort
	// the changes to make them work -- an entry created before its parent is
	// reported as a warning so it can be reordered, because guessing the
	// intended order silently is worse than saying what is wrong.
	//
	// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /changeset/preview (the `PreviewChangeset` operationId).
	PreviewChangesetWithResponse(ctx context.Context, body PreviewChangesetJSONRequestBody, reqEditors ...RequestEditorFn) (*PreviewChangesetReply, error)

	// CompareEntriesWithResponse What differs between two entries
	//
	// Answers "why does this account work and that one not".
	//
	// A naive diff answers it badly in three ways, and avoiding those is most
	// of what this endpoint is. It compares what is *present*, so an attribute
	// one entry's classes require and it does not hold is invisible — and that
	// absence is frequently the whole answer. It compares bytes, so two DNs
	// naming the same entry in different case read as a difference the
	// directory does not agree with. And its instinct is to show both sides of
	// everything, which for `userPassword` is what rule 6 forbids.
	//
	// So each side is annotated from its *own* object classes, which the two
	// entries need not share; DN-valued attributes are compared as DNs; and a
	// sensitive attribute is compared on presence alone.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /compare (the `CompareEntries` operationId).
	CompareEntriesWithResponse(ctx context.Context, params *CompareEntriesParams, reqEditors ...RequestEditorFn) (*CompareEntriesReply, error)

	// CountEntriesWithResponse Count the entries under a base, up to a limit
	//
	// A subtree count, bounded like every other search. It asks the server for
	// no attributes at all, so the cost is the search rather than the values.
	//
	// `truncated` is the important half of the answer: it means the count
	// reached the limit and the real number is higher. The UI reports "at
	// least N" in that case rather than a number that is simply wrong, which
	// is why this is an action somebody asks for rather than something a
	// landing page does to every naming context on load.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /count (the `CountEntries` operationId).
	CountEntriesWithResponse(ctx context.Context, params *CountEntriesParams, reqEditors ...RequestEditorFn) (*CountEntriesReply, error)

	// DiffStatesWithBodyWithResponse Compare two directory states
	//
	// Compares `source` with `target`, each a snapshot or the live directory
	// this session is bound to. The direction is explicit: `added` means in
	// the target and not in the source. See `docs/SNAPSHOTS.md`.
	//
	// - Values compare by the equality rule both sides recorded for the
	//   attribute where Alder models it, and byte for byte otherwise; the
	//   attributes compared by bytes are listed.
	// - A rename is only reported when both sides share an entry's stable
	//   identity. Otherwise a moved entry is removed and added.
	// - What could not be seen is `unknown`, never `removed` or `added`: an
	//   entry beyond a live search's bound, outside the other side's scope, or
	//   an attribute the live directory will not show. Any of those makes the
	//   comparison `complete: false`, with `reasons`.
	// - A live side reads as the session's identity. An entry the access
	//   rules hide is indistinguishable from one that does not exist; see the
	//   limits in `docs/SNAPSHOTS.md`.
	//
	// **Two snapshots need no session.** Comparing two documents the caller
	// sent reads no directory, so the request is answered without a session
	// and opens none. It is still bounded like any request -- the request body
	// limit, the in-flight limit and the request timeout -- and its snapshots
	// are read exactly as strictly. A request with a live side needs a
	// session; without one it is refused with `401` before either snapshot is
	// read.
	//
	// **Schema (1.10).** Two schema snapshots, or a schema snapshot and the
	// live schema (`live: {kind: schema}`, or `live: {}` beside a schema
	// snapshot), compare definitions instead of entries, and the response has
	// `kind: schema` and a `schema` section. Identity is the OID; definitions
	// compare by meaning, not text; a difference only in X- extensions is
	// `metadata_only`; what did not parse is `unknown`. `counts` summarises
	// both element kinds, with `metadata_only` counted as unchanged. A schema
	// and a data side cannot be compared.
	//
	// When the source is live, each item carries a `candidate`: change
	// requests that would move the live directory toward the target. They are
	// input for `POST /plan`, never applied from here. Deleting an entry is a
	// `destructive` candidate, is never selected for the caller, and is
	// withheld entirely when the comparison is partial. A missing entry in the
	// target is never, by itself, a reason to delete.
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /diff (the `DiffStates` operationId).
	DiffStatesWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*DiffStatesReply, error)

	// DiffStatesWithResponse Compare two directory states
	//
	// Compares `source` with `target`, each a snapshot or the live directory
	// this session is bound to. The direction is explicit: `added` means in
	// the target and not in the source. See `docs/SNAPSHOTS.md`.
	//
	// - Values compare by the equality rule both sides recorded for the
	//   attribute where Alder models it, and byte for byte otherwise; the
	//   attributes compared by bytes are listed.
	// - A rename is only reported when both sides share an entry's stable
	//   identity. Otherwise a moved entry is removed and added.
	// - What could not be seen is `unknown`, never `removed` or `added`: an
	//   entry beyond a live search's bound, outside the other side's scope, or
	//   an attribute the live directory will not show. Any of those makes the
	//   comparison `complete: false`, with `reasons`.
	// - A live side reads as the session's identity. An entry the access
	//   rules hide is indistinguishable from one that does not exist; see the
	//   limits in `docs/SNAPSHOTS.md`.
	//
	// **Two snapshots need no session.** Comparing two documents the caller
	// sent reads no directory, so the request is answered without a session
	// and opens none. It is still bounded like any request -- the request body
	// limit, the in-flight limit and the request timeout -- and its snapshots
	// are read exactly as strictly. A request with a live side needs a
	// session; without one it is refused with `401` before either snapshot is
	// read.
	//
	// **Schema (1.10).** Two schema snapshots, or a schema snapshot and the
	// live schema (`live: {kind: schema}`, or `live: {}` beside a schema
	// snapshot), compare definitions instead of entries, and the response has
	// `kind: schema` and a `schema` section. Identity is the OID; definitions
	// compare by meaning, not text; a difference only in X- extensions is
	// `metadata_only`; what did not parse is `unknown`. `counts` summarises
	// both element kinds, with `metadata_only` counted as unchanged. A schema
	// and a data side cannot be compared.
	//
	// When the source is live, each item carries a `candidate`: change
	// requests that would move the live directory toward the target. They are
	// input for `POST /plan`, never applied from here. Deleting an entry is a
	// `destructive` candidate, is never selected for the caller, and is
	// withheld entirely when the comparison is partial. A missing entry in the
	// target is never, by itself, a reason to delete.
	//
	// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /diff (the `DiffStates` operationId).
	DiffStatesWithResponse(ctx context.Context, body DiffStatesJSONRequestBody, reqEditors ...RequestEditorFn) (*DiffStatesReply, error)

	// GetEntryWithResponse Read one entry
	//
	// Returns the entry with every attribute annotated from the schema: its
	// syntax, whether it is single-valued, whether the server owns it, and
	// which input control the editor should offer. Also returns the MUST and
	// MAY sets computed from the entry's object classes.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /entry (the `GetEntry` operationId).
	GetEntryWithResponse(ctx context.Context, params *GetEntryParams, reqEditors ...RequestEditorFn) (*GetEntryReply, error)

	// ExportAnsibleWithResponse Export an entry or a subtree as a playbook that enforces it
	//
	// A playbook that makes a directory match what is here, rather than one
	// that merely reports on it.
	//
	// This is a separate path from `/export/ldif` on purpose. The obvious
	// alternative was `format=ansible` on that operation, which would leave
	// the URL saying "ldif" while returning YAML — and the two differ in more
	// than serialisation. An LDIF export is a transcription of entries; this
	// is an assertion about what they should be, and it is ordered parent
	// first because `ldap_entry` cannot create a child under a parent that
	// does not exist yet.
	//
	// Each entry becomes two tasks: `community.general.ldap_entry` to create
	// it if it is missing, and `community.general.ldap_attrs` with
	// `state: exact` to bring the listed attributes to exactly these values.
	// `ldap_entry` with `state: present` alone would report success against
	// an entry that exists with entirely different attributes, which is the
	// failure this endpoint exists to avoid.
	//
	// Attributes the directory owns — operational, `NO-USER-MODIFICATION` —
	// are omitted, because enforcing them would produce a task that fails on
	// every run against a server doing its job. Sensitive attributes are
	// omitted with no option to include them: a playbook is a file destined
	// for a repository.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /export/ansible (the `ExportAnsible` operationId).
	ExportAnsibleWithResponse(ctx context.Context, params *ExportAnsibleParams, reqEditors ...RequestEditorFn) (*ExportAnsibleReply, error)

	// ExportLdifWithResponse Export an entry or a subtree as LDIF
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /export/ldif (the `ExportLdif` operationId).
	ExportLdifWithResponse(ctx context.Context, params *ExportLdifParams, reqEditors ...RequestEditorFn) (*ExportLdifReply, error)

	// ExportOutlineWithResponse The shape of a subtree, as a tree
	//
	// The same entries an LDIF export would give, drawn as the hierarchy they
	// form rather than listed one after another.
	//
	// Asked for by an operator testing Alder: an LDIF export is flat, and a
	// flat list of three hundred records does not tell you the shape of what
	// you exported.
	//
	// **This is not LDIF and cannot be applied.** It could not be: RFC 2849
	// gives a leading space its own meaning -- it continues the line above --
	// so a tree drawn with indentation would stop being a document you can
	// import. Rather than a format that is almost LDIF and quietly broken,
	// this is plainly something else, and says so on its first line.
	//
	// Bounded rather than streamed, unlike the LDIF export: a tree cannot be
	// drawn until its last entry has arrived, because the entry that decides
	// whether a node is a leaf may be the final one to come back.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /export/outline (the `ExportOutline` operationId).
	ExportOutlineWithResponse(ctx context.Context, params *ExportOutlineParams, reqEditors ...RequestEditorFn) (*ExportOutlineReply, error)

	// ExportYamlWithResponse The subtree as nested YAML
	//
	// The entries and what they hold, nested as the tree they form, in a
	// format an editor folds and colours. Asked for so that a subtree can be
	// opened in an editor and read: LDIF is flat and YAML is not.
	//
	// Every attribute is a list, even where the schema says one value. An LDAP
	// attribute holds a set, and a document whose shape changed with the data
	// would make a second value look like a type change in the diff.
	//
	// A value that is not valid printable UTF-8 is written with YAML's own
	// `!!binary` tag rather than passed off as a string.
	//
	// **Nothing reads this back.** Alder imports LDIF, which is the format with
	// a specification and a changetype; this is for looking at. It is bounded
	// rather than streamed for the same reason the outline is: a tree cannot be
	// nested until its last entry has arrived.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /export/yaml (the `ExportYaml` operationId).
	ExportYamlWithResponse(ctx context.Context, params *ExportYamlParams, reqEditors ...RequestEditorFn) (*ExportYamlReply, error)

	// ParseLdifWithBodyWithResponse Parse an LDIF document into change records
	//
	// Parses and validates without applying anything. The response is the
	// list of changes the document describes, each rendered back as LDIF, for
	// the user to review and confirm one at a time or all at once.
	//
	// URL-valued attributes (`attr:< url`) are refused: following one from a
	// process holding a privileged bind would be file disclosure and request
	// forgery. So are LDAP controls, which would change what the server does
	// with a record.
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /import/ldif (the `ParseLdif` operationId).
	ParseLdifWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*ParseLdifReply, error)

	// ParseLdifWithResponse Parse an LDIF document into change records
	//
	// Parses and validates without applying anything. The response is the
	// list of changes the document describes, each rendered back as LDIF, for
	// the user to review and confirm one at a time or all at once.
	//
	// URL-valued attributes (`attr:< url`) are refused: following one from a
	// process holding a privileged bind would be file disclosure and request
	// forgery. So are LDAP controls, which would change what the server does
	// with a record.
	//
	// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /import/ldif (the `ParseLdif` operationId).
	ParseLdifWithResponse(ctx context.Context, body ParseLdifJSONRequestBody, reqEditors ...RequestEditorFn) (*ParseLdifReply, error)

	// InventoryValuesWithBodyWithResponse What values one attribute holds, and how many entries carry each
	//
	// The question that finds a team name spelled "platfrm" with four people
	// on it, a cost centre nobody has used since a reorganisation, or the
	// accounts still pointing at a decommissioned site.
	//
	// It is a tally over a **bounded** search, so it never claims to describe
	// the directory — only the entries it examined. Every number is scoped
	// that way and `truncated` says the bound was reached. A tally that
	// quietly summarises a truncated result set is not a weaker answer than
	// the truth; it is a confident wrong one.
	//
	// Sensitive attributes are refused before the search runs rather than
	// filtered out of the result, which is the difference between never
	// reading a password and reading every password and choosing not to say.
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /inventory (the `InventoryValues` operationId).
	InventoryValuesWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*InventoryValuesReply, error)

	// InventoryValuesWithResponse What values one attribute holds, and how many entries carry each
	//
	// The question that finds a team name spelled "platfrm" with four people
	// on it, a cost centre nobody has used since a reorganisation, or the
	// accounts still pointing at a decommissioned site.
	//
	// It is a tally over a **bounded** search, so it never claims to describe
	// the directory — only the entries it examined. Every number is scoped
	// that way and `truncated` says the bound was reached. A tally that
	// quietly summarises a truncated result set is not a weaker answer than
	// the truth; it is a confident wrong one.
	//
	// Sensitive attributes are refused before the search runs rather than
	// filtered out of the result, which is the difference between never
	// reading a password and reading every password and choosing not to say.
	//
	// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /inventory (the `InventoryValues` operationId).
	InventoryValuesWithResponse(ctx context.Context, body InventoryValuesJSONRequestBody, reqEditors ...RequestEditorFn) (*InventoryValuesReply, error)

	// ExpandMembersWithResponse Who is in this group, following nested groups
	//
	// The entry view lists a group's `member` values, which is the whole
	// answer for a flat group and no answer at all for a nested one:
	// `cn=everyone` in the test harness lists five members and contains no
	// people, because all five are themselves groups.
	//
	// This walks that structure and says, for each person reached, which
	// chain of groups brought them in.
	//
	// What it finds on the way is reported rather than dropped: a group that
	// contains itself, a member DN whose entry cannot be read — the dangling
	// reference the referenced-by panel exists to prevent, seen from the
	// other side — and membership values that are not DNs at all, since
	// `memberUid` holds a login name and `memberURL` a search.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /members (the `ExpandMembers` operationId).
	ExpandMembersWithResponse(ctx context.Context, params *ExpandMembersParams, reqEditors ...RequestEditorFn) (*ExpandMembersReply, error)

	// BuildPackageWithBodyWithResponse Build a change package from changes
	//
	// Turns changes into an `alder-change-package` document (1.11): a portable
	// description of intended directory and schema changes that can be carried
	// to another environment and validated there.
	//
	// The package holds intent, not execution. Every `baseline` is dropped,
	// every expectation is dropped, and a change to a schema entry is recorded
	// as what it means -- this element, this operation, this definition --
	// rather than as the modification this server happens to need, because the
	// entry and attribute that hold schema differ between products.
	//
	// What cannot be packaged is never dropped silently. A password change
	// needs its secret to mean anything, so it is refused, and the package
	// records the omission with a machine-readable reason.
	//
	// Dependencies are derived from the changes themselves -- an object class
	// after the attribute types it names, an entry after the class it uses and
	// after its parent -- and written into the document. Array order carries no
	// meaning; the graph does.
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /packages/build (the `BuildPackage` operationId).
	BuildPackageWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*BuildPackageReply, error)

	// BuildPackageWithResponse Build a change package from changes
	//
	// Turns changes into an `alder-change-package` document (1.11): a portable
	// description of intended directory and schema changes that can be carried
	// to another environment and validated there.
	//
	// The package holds intent, not execution. Every `baseline` is dropped,
	// every expectation is dropped, and a change to a schema entry is recorded
	// as what it means -- this element, this operation, this definition --
	// rather than as the modification this server happens to need, because the
	// entry and attribute that hold schema differ between products.
	//
	// What cannot be packaged is never dropped silently. A password change
	// needs its secret to mean anything, so it is refused, and the package
	// records the omission with a machine-readable reason.
	//
	// Dependencies are derived from the changes themselves -- an object class
	// after the attribute types it names, an entry after the class it uses and
	// after its parent -- and written into the document. Array order carries no
	// meaning; the graph does.
	//
	// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /packages/build (the `BuildPackage` operationId).
	BuildPackageWithResponse(ctx context.Context, body BuildPackageJSONRequestBody, reqEditors ...RequestEditorFn) (*BuildPackageReply, error)

	// InspectPackageWithBodyWithResponse Read a change package and check everything that needs no directory
	//
	// Reads a package strictly and reports what it holds: its identity, its
	// provenance, its assumptions, its changes and its dependency order.
	//
	// Everything that can be checked without a directory is checked here --
	// the format and version, unknown fields, trailing content, duplicate
	// identifiers, missing dependencies, self-dependencies, cycles, malformed
	// DNs, schema definitions that do not parse, secrets, and the checksum.
	// Nothing is executed and no directory is read, so no session is needed:
	// this is the check a pipeline runs before it has credentials.
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /packages/inspect (the `InspectPackage` operationId).
	InspectPackageWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*InspectPackageReply, error)

	// InspectPackageWithResponse Read a change package and check everything that needs no directory
	//
	// Reads a package strictly and reports what it holds: its identity, its
	// provenance, its assumptions, its changes and its dependency order.
	//
	// Everything that can be checked without a directory is checked here --
	// the format and version, unknown fields, trailing content, duplicate
	// identifiers, missing dependencies, self-dependencies, cycles, malformed
	// DNs, schema definitions that do not parse, secrets, and the checksum.
	// Nothing is executed and no directory is read, so no session is needed:
	// this is the check a pipeline runs before it has credentials.
	//
	// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /packages/inspect (the `InspectPackage` operationId).
	InspectPackageWithResponse(ctx context.Context, body InspectPackageJSONRequestBody, reqEditors ...RequestEditorFn) (*InspectPackageReply, error)

	// ValidatePackageWithBodyWithResponse Validate a change package against the current directory
	//
	// Reads the package, then asks this directory what its intent means here:
	// which changes are ready, which the target already satisfies, which
	// conflict with what is there now, which name something that is missing,
	// and which this server cannot do at all.
	//
	// Validation is not a plan. It prepares the ordinary change requests a
	// plan would take and stops; the plan reads the directory again, issues
	// the baselines an apply checks, and remains the only thing that decides
	// what is written. Nothing here writes, and a validation is not something
	// to keep: carry the package to the next environment and validate it
	// there.
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /packages/validate (the `ValidatePackage` operationId).
	ValidatePackageWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*ValidatePackageReply, error)

	// ValidatePackageWithResponse Validate a change package against the current directory
	//
	// Reads the package, then asks this directory what its intent means here:
	// which changes are ready, which the target already satisfies, which
	// conflict with what is there now, which name something that is missing,
	// and which this server cannot do at all.
	//
	// Validation is not a plan. It prepares the ordinary change requests a
	// plan would take and stops; the plan reads the directory again, issues
	// the baselines an apply checks, and remains the only thing that decides
	// what is written. Nothing here writes, and a validation is not something
	// to keep: carry the package to the next environment and validate it
	// there.
	//
	// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /packages/validate (the `ValidatePackage` operationId).
	ValidatePackageWithResponse(ctx context.Context, body ValidatePackageJSONRequestBody, reqEditors ...RequestEditorFn) (*ValidatePackageReply, error)

	// PlanChangesWithBodyWithResponse What a set of changes would do, without doing it
	//
	// Answers the question one level above the preview: given these proposed
	// changes and the directory as it is, which are additions, which are
	// modifications, which would do nothing at all, and which cannot be
	// applied as written.
	//
	// Nothing is written. Every entry named is read, each change is
	// classified, and the records that would run come back in `items[].record`
	// — the same values `POST /changeset/apply` takes, not a description of
	// them. That is what stops a plan from promising one thing and an apply
	// doing another.
	//
	// `action` is a stable identifier and is what a client should switch on.
	// `reason` is prose for a person and may be reworded in any release.
	//
	// Each applicable item carries a `baseline`: an opaque token binding two
	// things — the exact operation in `record`, and the state of the directory
	// that operation was planned against. Hand it back on the corresponding
	// change when applying. The server re-reads the entry and refuses the
	// whole set before anything runs: `400 plan_mismatch` if a change is not
	// the operation its baseline was issued for, `409 conflict` with
	// `cause: plan_stale` if the directory has moved since. Neither is silently replanned. The token
	// holds no attribute values; a sensitive attribute contributes only
	// whether it is set and how many values it has, and a password change
	// binds the entry and the fact of the change, never the password.
	// Baselines are meaningless to any other Alder process and do not survive
	// a restart.
	//
	// The input is either `changes` or `ldif`, never both.
	//
	// **`ldif` with `mode: changes`** (the default) plans every record as the
	// exact operation it states. A record with no `changetype` is an add,
	// which is what `ldapadd` does with it. An add of an entry that already
	// exists is a conflict (`entry_exists`); it is never turned into a
	// modification nobody asked for.
	//
	// **`ldif` with `mode: desired`** reads the document as the state entries
	// should be in. Only content records — no `changetype` — are accepted; a
	// record with one is refused with `400 ldif_mode_mismatch`, because a
	// document mixing "this entry looks like this" with "do this" has no
	// single meaning. Each record is reconciled: created if the entry is
	// absent, turned into the modification of the attributes it names if
	// present, reported `unchanged` if it already matches. Attributes a record
	// does not name are left alone. **An entry the document does not mention
	// is not deleted**; there is no mode in which absence means deletion.
	//
	// `impact` reports facts, not a risk score: which target area each change
	// touches (ordinary data, schema, server configuration), memberships
	// gained and lost, deletions grouped into the branches they remove, and —
	// for deletions and renames — the entries that name the affected DN and
	// how many of those references the plan would leave dangling. Reference
	// search runs as the session's own bind, so it reports what that bind can
	// see; see `impact.references`.
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /plan (the `PlanChanges` operationId).
	PlanChangesWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*PlanChangesReply, error)

	// PlanChangesWithResponse What a set of changes would do, without doing it
	//
	// Answers the question one level above the preview: given these proposed
	// changes and the directory as it is, which are additions, which are
	// modifications, which would do nothing at all, and which cannot be
	// applied as written.
	//
	// Nothing is written. Every entry named is read, each change is
	// classified, and the records that would run come back in `items[].record`
	// — the same values `POST /changeset/apply` takes, not a description of
	// them. That is what stops a plan from promising one thing and an apply
	// doing another.
	//
	// `action` is a stable identifier and is what a client should switch on.
	// `reason` is prose for a person and may be reworded in any release.
	//
	// Each applicable item carries a `baseline`: an opaque token binding two
	// things — the exact operation in `record`, and the state of the directory
	// that operation was planned against. Hand it back on the corresponding
	// change when applying. The server re-reads the entry and refuses the
	// whole set before anything runs: `400 plan_mismatch` if a change is not
	// the operation its baseline was issued for, `409 conflict` with
	// `cause: plan_stale` if the directory has moved since. Neither is silently replanned. The token
	// holds no attribute values; a sensitive attribute contributes only
	// whether it is set and how many values it has, and a password change
	// binds the entry and the fact of the change, never the password.
	// Baselines are meaningless to any other Alder process and do not survive
	// a restart.
	//
	// The input is either `changes` or `ldif`, never both.
	//
	// **`ldif` with `mode: changes`** (the default) plans every record as the
	// exact operation it states. A record with no `changetype` is an add,
	// which is what `ldapadd` does with it. An add of an entry that already
	// exists is a conflict (`entry_exists`); it is never turned into a
	// modification nobody asked for.
	//
	// **`ldif` with `mode: desired`** reads the document as the state entries
	// should be in. Only content records — no `changetype` — are accepted; a
	// record with one is refused with `400 ldif_mode_mismatch`, because a
	// document mixing "this entry looks like this" with "do this" has no
	// single meaning. Each record is reconciled: created if the entry is
	// absent, turned into the modification of the attributes it names if
	// present, reported `unchanged` if it already matches. Attributes a record
	// does not name are left alone. **An entry the document does not mention
	// is not deleted**; there is no mode in which absence means deletion.
	//
	// `impact` reports facts, not a risk score: which target area each change
	// touches (ordinary data, schema, server configuration), memberships
	// gained and lost, deletions grouped into the branches they remove, and —
	// for deletions and renames — the entries that name the affected DN and
	// how many of those references the plan would leave dangling. Reference
	// search runs as the session's own bind, so it reports what that bind can
	// see; see `impact.references`.
	//
	// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /plan (the `PlanChanges` operationId).
	PlanChangesWithResponse(ctx context.Context, body PlanChangesJSONRequestBody, reqEditors ...RequestEditorFn) (*PlanChangesReply, error)

	// PreflightWithBodyWithResponse Report what of an artifact would carry across to this directory
	//
	// Reads a change package, a schema snapshot or a data snapshot -- told
	// apart by its own `format` and `kind`, never by a file name -- and reports,
	// finding by finding, what this directory already holds, what it could take,
	// what needs something done first, what contradicts it, what it cannot
	// represent, and what could not be seen.
	//
	// A preflight is not a plan and not a migration. It reads the directory and
	// never writes to it; it never rewrites a DN, maps an attribute or object
	// class, or changes the artifact; and it produces no baseline and no
	// prepared change. Package validation is its first step for a package, and
	// its answer is carried on every finding about an item.
	//
	// `overall` is derived from the findings: `incompatible` when any finding
	// that blocks portability is incompatible or unsupported, `incomplete` when
	// otherwise something could not be decided, `compatible_with_prerequisites`
	// when something has to be done first, and `compatible` only when none of
	// these hold. Access control and server configuration are never evaluated,
	// and `notEvaluated` says so on every report.
	//
	// A document that is none of the three is refused with
	// `preflight_artifact_unsupported`; an artifact that is malformed, forged
	// or from a newer version is refused with the code its own endpoints use.
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /preflight (the `Preflight` operationId).
	PreflightWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*PreflightReply, error)

	// PreflightWithResponse Report what of an artifact would carry across to this directory
	//
	// Reads a change package, a schema snapshot or a data snapshot -- told
	// apart by its own `format` and `kind`, never by a file name -- and reports,
	// finding by finding, what this directory already holds, what it could take,
	// what needs something done first, what contradicts it, what it cannot
	// represent, and what could not be seen.
	//
	// A preflight is not a plan and not a migration. It reads the directory and
	// never writes to it; it never rewrites a DN, maps an attribute or object
	// class, or changes the artifact; and it produces no baseline and no
	// prepared change. Package validation is its first step for a package, and
	// its answer is carried on every finding about an item.
	//
	// `overall` is derived from the findings: `incompatible` when any finding
	// that blocks portability is incompatible or unsupported, `incomplete` when
	// otherwise something could not be decided, `compatible_with_prerequisites`
	// when something has to be done first, and `compatible` only when none of
	// these hold. Access control and server configuration are never evaluated,
	// and `notEvaluated` says so on every report.
	//
	// A document that is none of the three is refused with
	// `preflight_artifact_unsupported`; an artifact that is malformed, forged
	// or from a newer version is refused with the code its own endpoints use.
	//
	// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /preflight (the `Preflight` operationId).
	PreflightWithResponse(ctx context.Context, body PreflightJSONRequestBody, reqEditors ...RequestEditorFn) (*PreflightReply, error)

	// InspectRecoveryWithBodyWithResponse Validate a recovery bundle and turn it into changes to plan
	//
	// Validates an uploaded recovery bundle and returns its compensating
	// changes as ordinary change requests, in the order they would have to
	// run, each carrying the `expect` precondition that makes it a conflict
	// in a plan if the entry is no longer as the original apply left it.
	// Added in 1.9; see `docs/RECOVERY.md`.
	//
	// Nothing is applied, and there is no endpoint that applies a bundle.
	// The changes go to `POST /plan`, are reviewed, and are applied with
	// `POST /changeset/apply` like any other change. `drift` is that plan's
	// verdict on each change against the directory as it is now, for the
	// preview; the plan made afterwards is the one that counts.
	//
	// A bundle is untrusted input. It is refused when its format or version
	// is not one this Alder reads (`recovery_unsupported_version` for another
	// version), when any field is malformed or unknown, when a sensitive
	// attribute appears with values, when a compensation is a password
	// change (`recovery_invalid`), or when a checksum is present and does
	// not match (`recovery_checksum_mismatch`).
	//
	// `originMatches` compares the vendor and naming contexts the bundle
	// records with this session's directory. Neither is proof of identity:
	// two directories can announce the same.
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /recovery/inspect (the `InspectRecovery` operationId).
	InspectRecoveryWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*InspectRecoveryReply, error)

	// InspectRecoveryWithResponse Validate a recovery bundle and turn it into changes to plan
	//
	// Validates an uploaded recovery bundle and returns its compensating
	// changes as ordinary change requests, in the order they would have to
	// run, each carrying the `expect` precondition that makes it a conflict
	// in a plan if the entry is no longer as the original apply left it.
	// Added in 1.9; see `docs/RECOVERY.md`.
	//
	// Nothing is applied, and there is no endpoint that applies a bundle.
	// The changes go to `POST /plan`, are reviewed, and are applied with
	// `POST /changeset/apply` like any other change. `drift` is that plan's
	// verdict on each change against the directory as it is now, for the
	// preview; the plan made afterwards is the one that counts.
	//
	// A bundle is untrusted input. It is refused when its format or version
	// is not one this Alder reads (`recovery_unsupported_version` for another
	// version), when any field is malformed or unknown, when a sensitive
	// attribute appears with values, when a compensation is a password
	// change (`recovery_invalid`), or when a checksum is present and does
	// not match (`recovery_checksum_mismatch`).
	//
	// `originMatches` compares the vendor and naming contexts the bundle
	// records with this session's directory. Neither is proof of identity:
	// two directories can announce the same.
	//
	// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /recovery/inspect (the `InspectRecovery` operationId).
	InspectRecoveryWithResponse(ctx context.Context, body InspectRecoveryJSONRequestBody, reqEditors ...RequestEditorFn) (*InspectRecoveryReply, error)

	// ListReferencesWithResponse The entries that name this one, and the attribute each names it by
	//
	// Answers "which groups is this in, and what else points at it" — and,
	// unlike the filter on the entry itself, says *how* each one points.
	//
	// That is the part a link cannot give you. A search returns the entries
	// matching the filter but not which term matched, and removing a
	// reference means modifying a named attribute on the referencing entry:
	// without knowing whether a group holds this DN in `member` or in `owner`,
	// there is nothing safe to offer.
	//
	// It is one bounded search. The reference attributes are requested and
	// matched here rather than in the browser, so a group's five hundred
	// members are compared where they already are instead of being sent
	// across to be compared and discarded.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /references (the `ListReferences` operationId).
	ListReferencesWithResponse(ctx context.Context, params *ListReferencesParams, reqEditors ...RequestEditorFn) (*ListReferencesReply, error)

	// ResolveWithResponse What could this input be — an entry, or a search
	//
	// Backs one box that takes a DN, an RFC 4515 filter, or a name.
	//
	// It parses and never searches. `internal/dn` and `internal/filter` are
	// the authorities on what these strings are, and a regex in the browser
	// would drift from them the first time either grew a case — but probing
	// the directory to see whether an entry exists would turn every keystroke
	// into a read, to answer a question the operator is about to answer by
	// pressing enter.
	//
	// The three kinds overlap. `cn=platform` is a valid one-component DN
	// *and* almost certainly a request to find something called platform, so
	// an ambiguous input returns both destinations and the operator picks.
	// Guessing would be wrong about half the time.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /resolve (the `Resolve` operationId).
	ResolveWithResponse(ctx context.Context, params *ResolveParams, reqEditors ...RequestEditorFn) (*ResolveReply, error)

	// GetSchemaWithResponse The whole schema, indexed for browsing
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /schema (the `GetSchema` operationId).
	GetSchemaWithResponse(ctx context.Context, reqEditors ...RequestEditorFn) (*GetSchemaReply, error)

	// GetAttributeTypeWithResponse One attribute type, with its cross-links resolved
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /schema/attributetypes/{name} (the `GetAttributeType` operationId).
	GetAttributeTypeWithResponse(ctx context.Context, name string, reqEditors ...RequestEditorFn) (*GetAttributeTypeReply, error)

	// BuildSchemaChangeWithBodyWithResponse Build the modification that installs, replaces or removes a definition
	//
	// Renders a definition and returns the change that would apply it, without
	// applying anything.
	//
	// A schema definition is a value of an attribute on an ordinary entry, so
	// the change this returns is an ordinary modify, and it goes on to the
	// same preview, the same confirmation and the same changeset as every
	// other write. There is still exactly one path that writes.
	//
	// The definition text is built here rather than in the browser, for the
	// same reason the LDIF preview is: what the person confirms and what the
	// directory receives have to be the same bytes.
	//
	// A replace or a delete needs the value the server actually stores, which
	// is not always the value the schema browser displays — a server keeping
	// its schema in configuration prefixes each stored definition with its
	// load order and strips that prefix from what it publishes. This reads the
	// target to find it, which is why the request names an OID rather than
	// carrying a definition to remove.
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /schema/change (the `BuildSchemaChange` operationId).
	BuildSchemaChangeWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*BuildSchemaChangeReply, error)

	// BuildSchemaChangeWithResponse Build the modification that installs, replaces or removes a definition
	//
	// Renders a definition and returns the change that would apply it, without
	// applying anything.
	//
	// A schema definition is a value of an attribute on an ordinary entry, so
	// the change this returns is an ordinary modify, and it goes on to the
	// same preview, the same confirmation and the same changeset as every
	// other write. There is still exactly one path that writes.
	//
	// The definition text is built here rather than in the browser, for the
	// same reason the LDIF preview is: what the person confirms and what the
	// directory receives have to be the same bytes.
	//
	// A replace or a delete needs the value the server actually stores, which
	// is not always the value the schema browser displays — a server keeping
	// its schema in configuration prefixes each stored definition with its
	// load order and strips that prefix from what it publishes. This reads the
	// target to find it, which is why the request names an OID rather than
	// carrying a definition to remove.
	//
	// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /schema/change (the `BuildSchemaChange` operationId).
	BuildSchemaChangeWithResponse(ctx context.Context, body BuildSchemaChangeJSONRequestBody, reqEditors ...RequestEditorFn) (*BuildSchemaChangeReply, error)

	// GetObjectClassWithResponse One object class, with its cross-links resolved
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /schema/objectclasses/{name} (the `GetObjectClass` operationId).
	GetObjectClassWithResponse(ctx context.Context, name string, reqEditors ...RequestEditorFn) (*GetObjectClassReply, error)

	// GetRequirementsWithResponse What an entry of these object classes must and may hold
	//
	// The MUST and MAY sets for a set of object classes, with the schema's
	// opinion about each attribute — syntax, single-valuedness, description,
	// which control to offer.
	//
	// It exists so a creation form is generated from the schema rather than
	// from a template. Without it the form has attribute names and nothing
	// else, which is how creating an entry ended up with a plain text box for
	// every field while editing the same entry a moment later offered a
	// Boolean control, an entry picker and the attribute's description.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /schema/requirements (the `GetRequirements` operationId).
	GetRequirementsWithResponse(ctx context.Context, params *GetRequirementsParams, reqEditors ...RequestEditorFn) (*GetRequirementsReply, error)

	// SearchWithBodyWithResponse Search the directory
	//
	// Every search is paged and bounded. `filter` is a raw RFC 4515 string
	// which is parsed, not interpolated: a value containing filter
	// metacharacters cannot escape into the filter structure.
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /search (the `Search` operationId).
	SearchWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*SearchReply, error)

	// SearchWithResponse Search the directory
	//
	// Every search is paged and bounded. `filter` is a raw RFC 4515 string
	// which is parsed, not interpolated: a value containing filter
	// metacharacters cannot escape into the filter structure.
	//
	// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /search (the `Search` operationId).
	SearchWithResponse(ctx context.Context, body SearchJSONRequestBody, reqEditors ...RequestEditorFn) (*SearchReply, error)

	// DeleteSessionWithResponse Disconnect
	//
	// Closes the connection and discards the credentials.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with DELETE /session (the `DeleteSession` operationId).
	DeleteSessionWithResponse(ctx context.Context, reqEditors ...RequestEditorFn) (*DeleteSessionReply, error)

	// GetSessionWithResponse Describe the current session
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /session (the `GetSession` operationId).
	GetSessionWithResponse(ctx context.Context, reqEditors ...RequestEditorFn) (*GetSessionReply, error)

	// CreateSessionWithBodyWithResponse Connect and bind to a directory
	//
	// Opens a connection, binds, and reads the RootDSE. On success the
	// response sets an httpOnly, Secure, SameSite=Strict cookie naming an
	// in-memory session. The bind password is held in that in-memory session
	// and is never written to disk, never placed in a token, and never
	// returned by any endpoint.
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /session (the `CreateSession` operationId).
	CreateSessionWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*CreateSessionReply, error)

	// CreateSessionWithResponse Connect and bind to a directory
	//
	// Opens a connection, binds, and reads the RootDSE. On success the
	// response sets an httpOnly, Secure, SameSite=Strict cookie naming an
	// in-memory session. The bind password is held in that in-memory session
	// and is never written to disk, never placed in a token, and never
	// returned by any endpoint.
	//
	// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /session (the `CreateSession` operationId).
	CreateSessionWithResponse(ctx context.Context, body CreateSessionJSONRequestBody, reqEditors ...RequestEditorFn) (*CreateSessionReply, error)

	// CaptureSnapshotWithBodyWithResponse Capture a subtree as a versioned snapshot
	//
	// Reads the subtree as the session's own identity and returns it as an
	// Alder snapshot: a versioned, canonical JSON document, safe to keep in a
	// repository. See `docs/SNAPSHOTS.md`.
	//
	// - Entries, attribute names and values are in one defined order, so two
	//   captures of an unchanged subtree differ only in `createdAt`, and have
	//   the same `checksum`.
	// - Operational attributes are left out unless
	//   `operationalAttributes` is set. Each entry's stable identity
	//   (`entryUUID` or `nsUniqueId`) is recorded as its `id` either way.
	// - Sensitive attributes are recorded as a `withheld` count, never as a
	//   value or anything derived from one.
	// - A capture that cannot finish -- more than 50,000 entries, a search
	//   limit, a failure part way -- is an error. There is no partial
	//   snapshot.
	// - A data snapshot's base must be ordinary data: a base inside the schema
	//   or the server's configuration is refused with
	//   `snapshot_scope_unsupported`.
	//
	// With `kind: schema` (1.10) the published subschema is captured instead,
	// and `base`, `scope`, `filter` and `operationalAttributes` are not used.
	// Attribute types and object classes are captured for comparison; LDAP
	// syntaxes, matching rules, matching rule uses, DIT content rules and name
	// forms as context. A definition that does not parse is kept verbatim and
	// makes the snapshot `partial`. Server configuration is never captured, and
	// there is no `kind: config`.
	//
	// Nothing is kept on the server. The snapshot is returned and belongs to
	// the caller.
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /snapshots/capture (the `CaptureSnapshot` operationId).
	CaptureSnapshotWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*CaptureSnapshotReply, error)

	// CaptureSnapshotWithResponse Capture a subtree as a versioned snapshot
	//
	// Reads the subtree as the session's own identity and returns it as an
	// Alder snapshot: a versioned, canonical JSON document, safe to keep in a
	// repository. See `docs/SNAPSHOTS.md`.
	//
	// - Entries, attribute names and values are in one defined order, so two
	//   captures of an unchanged subtree differ only in `createdAt`, and have
	//   the same `checksum`.
	// - Operational attributes are left out unless
	//   `operationalAttributes` is set. Each entry's stable identity
	//   (`entryUUID` or `nsUniqueId`) is recorded as its `id` either way.
	// - Sensitive attributes are recorded as a `withheld` count, never as a
	//   value or anything derived from one.
	// - A capture that cannot finish -- more than 50,000 entries, a search
	//   limit, a failure part way -- is an error. There is no partial
	//   snapshot.
	// - A data snapshot's base must be ordinary data: a base inside the schema
	//   or the server's configuration is refused with
	//   `snapshot_scope_unsupported`.
	//
	// With `kind: schema` (1.10) the published subschema is captured instead,
	// and `base`, `scope`, `filter` and `operationalAttributes` are not used.
	// Attribute types and object classes are captured for comparison; LDAP
	// syntaxes, matching rules, matching rule uses, DIT content rules and name
	// forms as context. A definition that does not parse is kept verbatim and
	// makes the snapshot `partial`. Server configuration is never captured, and
	// there is no `kind: config`.
	//
	// Nothing is kept on the server. The snapshot is returned and belongs to
	// the caller.
	//
	// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /snapshots/capture (the `CaptureSnapshot` operationId).
	CaptureSnapshotWithResponse(ctx context.Context, body CaptureSnapshotJSONRequestBody, reqEditors ...RequestEditorFn) (*CaptureSnapshotReply, error)

	// InspectSnapshotWithBodyWithResponse Validate a snapshot and describe it
	//
	// Validates an uploaded snapshot exactly as a comparison would and
	// describes what it covers. Read-only: nothing is kept and nothing is
	// applied.
	//
	// A snapshot is refused when its format or version is not one this Alder
	// reads (`snapshot_unsupported_version` for a newer version), when any DN,
	// value or field is malformed or unknown (`snapshot_invalid`), when a
	// sensitive attribute carries a value, or when a checksum is present and
	// does not match (`snapshot_checksum_mismatch`). A snapshot without a
	// checksum is accepted and described as `integrity: unverified`.
	//
	// A schema snapshot (1.10) is checked as strictly: every OID, no OID twice,
	// every parsed field equal to what its definition text parses to, counts
	// and coverage true, and no inheritance cycle.
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /snapshots/inspect (the `InspectSnapshot` operationId).
	InspectSnapshotWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*InspectSnapshotReply, error)

	// InspectSnapshotWithResponse Validate a snapshot and describe it
	//
	// Validates an uploaded snapshot exactly as a comparison would and
	// describes what it covers. Read-only: nothing is kept and nothing is
	// applied.
	//
	// A snapshot is refused when its format or version is not one this Alder
	// reads (`snapshot_unsupported_version` for a newer version), when any DN,
	// value or field is malformed or unknown (`snapshot_invalid`), when a
	// sensitive attribute carries a value, or when a checksum is present and
	// does not match (`snapshot_checksum_mismatch`). A snapshot without a
	// checksum is accepted and described as `integrity: unverified`.
	//
	// A schema snapshot (1.10) is checked as strictly: every OID, no OID twice,
	// every parsed field equal to what its definition text parses to, counts
	// and coverage true, and no inheritance cycle.
	//
	// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /snapshots/inspect (the `InspectSnapshot` operationId).
	InspectSnapshotWithResponse(ctx context.Context, body InspectSnapshotJSONRequestBody, reqEditors ...RequestEditorFn) (*InspectSnapshotReply, error)

	// GetSourceOfferWithResponse Where to get the source of this running instance
	//
	// AGPL-3.0 section 13: someone using a modified Alder over a network is
	// entitled to its Corresponding Source. The offer is served rather than
	// written in a README because the obligation runs to the person using the
	// running instance, and the user of a modified instance has no reason to
	// know where its source went.
	//
	// **This is the one endpoint that answers without a session,** and it has
	// no 401 for that reason: an offer only the already-connected can read
	// would not discharge the obligation. It is described here so that the
	// contract in this document is the whole contract -- it was served outside
	// it until 1.0, which made `openapi.yaml` quietly incomplete about the one
	// endpoint a licence requires.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /source (the `GetSourceOffer` operationId).
	GetSourceOfferWithResponse(ctx context.Context, reqEditors ...RequestEditorFn) (*GetSourceOfferReply, error)

	// ListChildrenWithResponse List the children of an entry
	//
	// With no `dn`, returns the naming contexts the server holds, which are
	// the roots of the tree. With a `dn`, returns that entry's immediate
	// children. This is the lazy-loading tree browser's only endpoint.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /tree (the `ListChildren` operationId).
	ListChildrenWithResponse(ctx context.Context, params *ListChildrenParams, reqEditors ...RequestEditorFn) (*ListChildrenReply, error)

	// ListObjectViewsWithResponse The object-aware views this directory supports
	//
	// Users, groups and organizational units, expressed as saved searches
	// derived from the schema the connected server published.
	//
	// A view is offered only where the server defines a class to anchor it
	// on, so a directory holding no person class has no Users view rather
	// than an empty one. The anchors are standards-track classes — RFC 4519,
	// RFC 4524, RFC 2307 — never vendor names, and a site class inheriting
	// from one of them is matched without being named, because an entry
	// carries its superclasses in its own `objectClass` attribute.
	//
	// The filter is built here and goes back to `/search` unmodified. The
	// columns are the attributes the anchor classes actually permit, so a
	// directory without `inetOrgPerson` gets no mail column instead of an
	// empty one.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /views (the `ListObjectViews` operationId).
	ListObjectViewsWithResponse(ctx context.Context, reqEditors ...RequestEditorFn) (*ListObjectViewsReply, error)
}

type ApplyChangeReply struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *ApplyResult
	// JSON400 the response for an HTTP 400 `application/json` response
	JSON400 *BadRequest
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *Forbidden
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
	// JSON409 the response for an HTTP 409 `application/json` response
	JSON409 *Error
	// JSON422 the response for an HTTP 422 `application/json` response
	JSON422 *Error
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r ApplyChangeReply) GetJSON200() *ApplyResult {
	return r.JSON200
}

// GetJSON400 returns the response for an HTTP 400 `application/json` response
func (r ApplyChangeReply) GetJSON400() *BadRequest {
	return r.JSON400
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r ApplyChangeReply) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r ApplyChangeReply) GetJSON403() *Forbidden {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r ApplyChangeReply) GetJSON404() *NotFound {
	return r.JSON404
}

// GetJSON409 returns the response for an HTTP 409 `application/json` response
func (r ApplyChangeReply) GetJSON409() *Error {
	return r.JSON409
}

// GetJSON422 returns the response for an HTTP 422 `application/json` response
func (r ApplyChangeReply) GetJSON422() *Error {
	return r.JSON422
}

// GetBody returns the raw response body bytes
func (r ApplyChangeReply) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r ApplyChangeReply) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r ApplyChangeReply) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r ApplyChangeReply) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type PreviewChangeReply struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *ChangePreview
	// JSON400 the response for an HTTP 400 `application/json` response
	JSON400 *BadRequest
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r PreviewChangeReply) GetJSON200() *ChangePreview {
	return r.JSON200
}

// GetJSON400 returns the response for an HTTP 400 `application/json` response
func (r PreviewChangeReply) GetJSON400() *BadRequest {
	return r.JSON400
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r PreviewChangeReply) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetBody returns the raw response body bytes
func (r PreviewChangeReply) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r PreviewChangeReply) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r PreviewChangeReply) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r PreviewChangeReply) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type ApplyChangesetReply struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *ChangesetResult
	// JSON400 the response for an HTTP 400 `application/json` response
	JSON400 *BadRequest
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *Forbidden
	// JSON409 the response for an HTTP 409 `application/json` response
	JSON409 *Error
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r ApplyChangesetReply) GetJSON200() *ChangesetResult {
	return r.JSON200
}

// GetJSON400 returns the response for an HTTP 400 `application/json` response
func (r ApplyChangesetReply) GetJSON400() *BadRequest {
	return r.JSON400
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r ApplyChangesetReply) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r ApplyChangesetReply) GetJSON403() *Forbidden {
	return r.JSON403
}

// GetJSON409 returns the response for an HTTP 409 `application/json` response
func (r ApplyChangesetReply) GetJSON409() *Error {
	return r.JSON409
}

// GetBody returns the raw response body bytes
func (r ApplyChangesetReply) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r ApplyChangesetReply) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r ApplyChangesetReply) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r ApplyChangesetReply) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type PreviewChangesetReply struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *ChangesetPreview
	// JSON400 the response for an HTTP 400 `application/json` response
	JSON400 *BadRequest
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r PreviewChangesetReply) GetJSON200() *ChangesetPreview {
	return r.JSON200
}

// GetJSON400 returns the response for an HTTP 400 `application/json` response
func (r PreviewChangesetReply) GetJSON400() *BadRequest {
	return r.JSON400
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r PreviewChangesetReply) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetBody returns the raw response body bytes
func (r PreviewChangesetReply) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r PreviewChangesetReply) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r PreviewChangesetReply) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r PreviewChangesetReply) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type CompareEntriesReply struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *EntryComparison
	// JSON400 the response for an HTTP 400 `application/json` response
	JSON400 *BadRequest
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r CompareEntriesReply) GetJSON200() *EntryComparison {
	return r.JSON200
}

// GetJSON400 returns the response for an HTTP 400 `application/json` response
func (r CompareEntriesReply) GetJSON400() *BadRequest {
	return r.JSON400
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r CompareEntriesReply) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r CompareEntriesReply) GetJSON404() *NotFound {
	return r.JSON404
}

// GetBody returns the raw response body bytes
func (r CompareEntriesReply) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r CompareEntriesReply) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r CompareEntriesReply) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r CompareEntriesReply) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type CountEntriesReply struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *CountResult
	// JSON400 the response for an HTTP 400 `application/json` response
	JSON400 *BadRequest
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *Forbidden
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r CountEntriesReply) GetJSON200() *CountResult {
	return r.JSON200
}

// GetJSON400 returns the response for an HTTP 400 `application/json` response
func (r CountEntriesReply) GetJSON400() *BadRequest {
	return r.JSON400
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r CountEntriesReply) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r CountEntriesReply) GetJSON403() *Forbidden {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r CountEntriesReply) GetJSON404() *NotFound {
	return r.JSON404
}

// GetBody returns the raw response body bytes
func (r CountEntriesReply) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r CountEntriesReply) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r CountEntriesReply) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r CountEntriesReply) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type DiffStatesReply struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *Diff
	// JSON400 the response for an HTTP 400 `application/json` response
	JSON400 *BadRequest
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r DiffStatesReply) GetJSON200() *Diff {
	return r.JSON200
}

// GetJSON400 returns the response for an HTTP 400 `application/json` response
func (r DiffStatesReply) GetJSON400() *BadRequest {
	return r.JSON400
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r DiffStatesReply) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetBody returns the raw response body bytes
func (r DiffStatesReply) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r DiffStatesReply) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r DiffStatesReply) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r DiffStatesReply) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type GetEntryReply struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *EntryView
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *Forbidden
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r GetEntryReply) GetJSON200() *EntryView {
	return r.JSON200
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r GetEntryReply) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r GetEntryReply) GetJSON403() *Forbidden {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r GetEntryReply) GetJSON404() *NotFound {
	return r.JSON404
}

// GetBody returns the raw response body bytes
func (r GetEntryReply) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r GetEntryReply) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r GetEntryReply) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r GetEntryReply) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type ExportAnsibleReply struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON400 the response for an HTTP 400 `application/json` response
	JSON400 *BadRequest
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
}

// GetJSON400 returns the response for an HTTP 400 `application/json` response
func (r ExportAnsibleReply) GetJSON400() *BadRequest {
	return r.JSON400
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r ExportAnsibleReply) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r ExportAnsibleReply) GetJSON404() *NotFound {
	return r.JSON404
}

// GetBody returns the raw response body bytes
func (r ExportAnsibleReply) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r ExportAnsibleReply) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r ExportAnsibleReply) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r ExportAnsibleReply) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type ExportLdifReply struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r ExportLdifReply) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r ExportLdifReply) GetJSON404() *NotFound {
	return r.JSON404
}

// GetBody returns the raw response body bytes
func (r ExportLdifReply) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r ExportLdifReply) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r ExportLdifReply) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r ExportLdifReply) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type ExportOutlineReply struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON400 the response for an HTTP 400 `application/json` response
	JSON400 *BadRequest
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
}

// GetJSON400 returns the response for an HTTP 400 `application/json` response
func (r ExportOutlineReply) GetJSON400() *BadRequest {
	return r.JSON400
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r ExportOutlineReply) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r ExportOutlineReply) GetJSON404() *NotFound {
	return r.JSON404
}

// GetBody returns the raw response body bytes
func (r ExportOutlineReply) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r ExportOutlineReply) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r ExportOutlineReply) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r ExportOutlineReply) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type ExportYamlReply struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON400 the response for an HTTP 400 `application/json` response
	JSON400 *BadRequest
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
}

// GetJSON400 returns the response for an HTTP 400 `application/json` response
func (r ExportYamlReply) GetJSON400() *BadRequest {
	return r.JSON400
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r ExportYamlReply) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r ExportYamlReply) GetJSON404() *NotFound {
	return r.JSON404
}

// GetBody returns the raw response body bytes
func (r ExportYamlReply) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r ExportYamlReply) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r ExportYamlReply) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r ExportYamlReply) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type ParseLdifReply struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *ImportResult
	// JSON400 the response for an HTTP 400 `application/json` response
	JSON400 *BadRequest
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r ParseLdifReply) GetJSON200() *ImportResult {
	return r.JSON200
}

// GetJSON400 returns the response for an HTTP 400 `application/json` response
func (r ParseLdifReply) GetJSON400() *BadRequest {
	return r.JSON400
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r ParseLdifReply) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetBody returns the raw response body bytes
func (r ParseLdifReply) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r ParseLdifReply) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r ParseLdifReply) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r ParseLdifReply) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type InventoryValuesReply struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *InventoryResponse
	// JSON400 the response for an HTTP 400 `application/json` response
	JSON400 *BadRequest
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r InventoryValuesReply) GetJSON200() *InventoryResponse {
	return r.JSON200
}

// GetJSON400 returns the response for an HTTP 400 `application/json` response
func (r InventoryValuesReply) GetJSON400() *BadRequest {
	return r.JSON400
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r InventoryValuesReply) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetBody returns the raw response body bytes
func (r InventoryValuesReply) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r InventoryValuesReply) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r InventoryValuesReply) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r InventoryValuesReply) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type ExpandMembersReply struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *MemberList
	// JSON400 the response for an HTTP 400 `application/json` response
	JSON400 *BadRequest
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r ExpandMembersReply) GetJSON200() *MemberList {
	return r.JSON200
}

// GetJSON400 returns the response for an HTTP 400 `application/json` response
func (r ExpandMembersReply) GetJSON400() *BadRequest {
	return r.JSON400
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r ExpandMembersReply) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r ExpandMembersReply) GetJSON404() *NotFound {
	return r.JSON404
}

// GetBody returns the raw response body bytes
func (r ExpandMembersReply) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r ExpandMembersReply) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r ExpandMembersReply) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r ExpandMembersReply) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type BuildPackageReply struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *ChangePackage
	// JSON400 the response for an HTTP 400 `application/json` response
	JSON400 *BadRequest
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r BuildPackageReply) GetJSON200() *ChangePackage {
	return r.JSON200
}

// GetJSON400 returns the response for an HTTP 400 `application/json` response
func (r BuildPackageReply) GetJSON400() *BadRequest {
	return r.JSON400
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r BuildPackageReply) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetBody returns the raw response body bytes
func (r BuildPackageReply) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r BuildPackageReply) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r BuildPackageReply) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r BuildPackageReply) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type InspectPackageReply struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *PackageInspection
	// JSON400 the response for an HTTP 400 `application/json` response
	JSON400 *BadRequest
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r InspectPackageReply) GetJSON200() *PackageInspection {
	return r.JSON200
}

// GetJSON400 returns the response for an HTTP 400 `application/json` response
func (r InspectPackageReply) GetJSON400() *BadRequest {
	return r.JSON400
}

// GetBody returns the raw response body bytes
func (r InspectPackageReply) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r InspectPackageReply) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r InspectPackageReply) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r InspectPackageReply) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type ValidatePackageReply struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *PackageValidation
	// JSON400 the response for an HTTP 400 `application/json` response
	JSON400 *BadRequest
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r ValidatePackageReply) GetJSON200() *PackageValidation {
	return r.JSON200
}

// GetJSON400 returns the response for an HTTP 400 `application/json` response
func (r ValidatePackageReply) GetJSON400() *BadRequest {
	return r.JSON400
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r ValidatePackageReply) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetBody returns the raw response body bytes
func (r ValidatePackageReply) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r ValidatePackageReply) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r ValidatePackageReply) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r ValidatePackageReply) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type PlanChangesReply struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *Plan
	// JSON400 the response for an HTTP 400 `application/json` response
	JSON400 *Error
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *Forbidden
	// JSON502 the response for an HTTP 502 `application/json` response
	JSON502 *Error
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r PlanChangesReply) GetJSON200() *Plan {
	return r.JSON200
}

// GetJSON400 returns the response for an HTTP 400 `application/json` response
func (r PlanChangesReply) GetJSON400() *Error {
	return r.JSON400
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r PlanChangesReply) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r PlanChangesReply) GetJSON403() *Forbidden {
	return r.JSON403
}

// GetJSON502 returns the response for an HTTP 502 `application/json` response
func (r PlanChangesReply) GetJSON502() *Error {
	return r.JSON502
}

// GetBody returns the raw response body bytes
func (r PlanChangesReply) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r PlanChangesReply) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r PlanChangesReply) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r PlanChangesReply) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type PreflightReply struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *PreflightReport
	// JSON400 the response for an HTTP 400 `application/json` response
	JSON400 *BadRequest
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r PreflightReply) GetJSON200() *PreflightReport {
	return r.JSON200
}

// GetJSON400 returns the response for an HTTP 400 `application/json` response
func (r PreflightReply) GetJSON400() *BadRequest {
	return r.JSON400
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r PreflightReply) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetBody returns the raw response body bytes
func (r PreflightReply) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r PreflightReply) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r PreflightReply) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r PreflightReply) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type InspectRecoveryReply struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *RecoveryInspection
	// JSON400 the response for an HTTP 400 `application/json` response
	JSON400 *BadRequest
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r InspectRecoveryReply) GetJSON200() *RecoveryInspection {
	return r.JSON200
}

// GetJSON400 returns the response for an HTTP 400 `application/json` response
func (r InspectRecoveryReply) GetJSON400() *BadRequest {
	return r.JSON400
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r InspectRecoveryReply) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetBody returns the raw response body bytes
func (r InspectRecoveryReply) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r InspectRecoveryReply) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r InspectRecoveryReply) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r InspectRecoveryReply) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type ListReferencesReply struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *ReferenceList
	// JSON400 the response for an HTTP 400 `application/json` response
	JSON400 *BadRequest
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r ListReferencesReply) GetJSON200() *ReferenceList {
	return r.JSON200
}

// GetJSON400 returns the response for an HTTP 400 `application/json` response
func (r ListReferencesReply) GetJSON400() *BadRequest {
	return r.JSON400
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r ListReferencesReply) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetBody returns the raw response body bytes
func (r ListReferencesReply) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r ListReferencesReply) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r ListReferencesReply) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r ListReferencesReply) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type ResolveReply struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *ResolveResult
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r ResolveReply) GetJSON200() *ResolveResult {
	return r.JSON200
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r ResolveReply) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetBody returns the raw response body bytes
func (r ResolveReply) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r ResolveReply) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r ResolveReply) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r ResolveReply) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type GetSchemaReply struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *SchemaView
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r GetSchemaReply) GetJSON200() *SchemaView {
	return r.JSON200
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r GetSchemaReply) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetBody returns the raw response body bytes
func (r GetSchemaReply) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r GetSchemaReply) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r GetSchemaReply) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r GetSchemaReply) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type GetAttributeTypeReply struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *AttributeTypeDetail
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r GetAttributeTypeReply) GetJSON200() *AttributeTypeDetail {
	return r.JSON200
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r GetAttributeTypeReply) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r GetAttributeTypeReply) GetJSON404() *NotFound {
	return r.JSON404
}

// GetBody returns the raw response body bytes
func (r GetAttributeTypeReply) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r GetAttributeTypeReply) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r GetAttributeTypeReply) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r GetAttributeTypeReply) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type BuildSchemaChangeReply struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *SchemaChangeBuild
	// JSON400 the response for an HTTP 400 `application/json` response
	JSON400 *BadRequest
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r BuildSchemaChangeReply) GetJSON200() *SchemaChangeBuild {
	return r.JSON200
}

// GetJSON400 returns the response for an HTTP 400 `application/json` response
func (r BuildSchemaChangeReply) GetJSON400() *BadRequest {
	return r.JSON400
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r BuildSchemaChangeReply) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetBody returns the raw response body bytes
func (r BuildSchemaChangeReply) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r BuildSchemaChangeReply) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r BuildSchemaChangeReply) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r BuildSchemaChangeReply) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type GetObjectClassReply struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *ObjectClassDetail
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r GetObjectClassReply) GetJSON200() *ObjectClassDetail {
	return r.JSON200
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r GetObjectClassReply) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r GetObjectClassReply) GetJSON404() *NotFound {
	return r.JSON404
}

// GetBody returns the raw response body bytes
func (r GetObjectClassReply) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r GetObjectClassReply) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r GetObjectClassReply) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r GetObjectClassReply) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type GetRequirementsReply struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *RequirementsView
	// JSON400 the response for an HTTP 400 `application/json` response
	JSON400 *BadRequest
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r GetRequirementsReply) GetJSON200() *RequirementsView {
	return r.JSON200
}

// GetJSON400 returns the response for an HTTP 400 `application/json` response
func (r GetRequirementsReply) GetJSON400() *BadRequest {
	return r.JSON400
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r GetRequirementsReply) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetBody returns the raw response body bytes
func (r GetRequirementsReply) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r GetRequirementsReply) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r GetRequirementsReply) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r GetRequirementsReply) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type SearchReply struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *SearchResponse
	// JSON400 the response for an HTTP 400 `application/json` response
	JSON400 *BadRequest
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *Forbidden
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r SearchReply) GetJSON200() *SearchResponse {
	return r.JSON200
}

// GetJSON400 returns the response for an HTTP 400 `application/json` response
func (r SearchReply) GetJSON400() *BadRequest {
	return r.JSON400
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r SearchReply) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r SearchReply) GetJSON403() *Forbidden {
	return r.JSON403
}

// GetBody returns the raw response body bytes
func (r SearchReply) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r SearchReply) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r SearchReply) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r SearchReply) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type DeleteSessionReply struct {
	Body         []byte
	HTTPResponse *http.Response
}

// GetBody returns the raw response body bytes
func (r DeleteSessionReply) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r DeleteSessionReply) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r DeleteSessionReply) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r DeleteSessionReply) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type GetSessionReply struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *SessionInfo
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r GetSessionReply) GetJSON200() *SessionInfo {
	return r.JSON200
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r GetSessionReply) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetBody returns the raw response body bytes
func (r GetSessionReply) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r GetSessionReply) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r GetSessionReply) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r GetSessionReply) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type CreateSessionReply struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON201 the response for an HTTP 201 `application/json` response
	JSON201 *SessionInfo
	// JSON400 the response for an HTTP 400 `application/json` response
	JSON400 *BadRequest
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *Error
	// JSON502 the response for an HTTP 502 `application/json` response
	JSON502 *Error
}

// GetJSON201 returns the response for an HTTP 201 `application/json` response
func (r CreateSessionReply) GetJSON201() *SessionInfo {
	return r.JSON201
}

// GetJSON400 returns the response for an HTTP 400 `application/json` response
func (r CreateSessionReply) GetJSON400() *BadRequest {
	return r.JSON400
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r CreateSessionReply) GetJSON403() *Error {
	return r.JSON403
}

// GetJSON502 returns the response for an HTTP 502 `application/json` response
func (r CreateSessionReply) GetJSON502() *Error {
	return r.JSON502
}

// GetBody returns the raw response body bytes
func (r CreateSessionReply) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r CreateSessionReply) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r CreateSessionReply) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r CreateSessionReply) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type CaptureSnapshotReply struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *CaptureSnapshot200JSONResponseBody
	// JSON400 the response for an HTTP 400 `application/json` response
	JSON400 *BadRequest
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r CaptureSnapshotReply) GetJSON200() *CaptureSnapshot200JSONResponseBody {
	return r.JSON200
}

// GetJSON400 returns the response for an HTTP 400 `application/json` response
func (r CaptureSnapshotReply) GetJSON400() *BadRequest {
	return r.JSON400
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r CaptureSnapshotReply) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r CaptureSnapshotReply) GetJSON404() *NotFound {
	return r.JSON404
}

// GetBody returns the raw response body bytes
func (r CaptureSnapshotReply) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r CaptureSnapshotReply) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r CaptureSnapshotReply) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r CaptureSnapshotReply) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type InspectSnapshotReply struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *SnapshotInspection
	// JSON400 the response for an HTTP 400 `application/json` response
	JSON400 *BadRequest
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r InspectSnapshotReply) GetJSON200() *SnapshotInspection {
	return r.JSON200
}

// GetJSON400 returns the response for an HTTP 400 `application/json` response
func (r InspectSnapshotReply) GetJSON400() *BadRequest {
	return r.JSON400
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r InspectSnapshotReply) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetBody returns the raw response body bytes
func (r InspectSnapshotReply) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r InspectSnapshotReply) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r InspectSnapshotReply) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r InspectSnapshotReply) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type GetSourceOfferReply struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *SourceOffer
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r GetSourceOfferReply) GetJSON200() *SourceOffer {
	return r.JSON200
}

// GetBody returns the raw response body bytes
func (r GetSourceOfferReply) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r GetSourceOfferReply) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r GetSourceOfferReply) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r GetSourceOfferReply) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type ListChildrenReply struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *TreePage
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *Forbidden
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r ListChildrenReply) GetJSON200() *TreePage {
	return r.JSON200
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r ListChildrenReply) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r ListChildrenReply) GetJSON403() *Forbidden {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r ListChildrenReply) GetJSON404() *NotFound {
	return r.JSON404
}

// GetBody returns the raw response body bytes
func (r ListChildrenReply) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r ListChildrenReply) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r ListChildrenReply) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r ListChildrenReply) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type ListObjectViewsReply struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *ObjectViewList
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r ListObjectViewsReply) GetJSON200() *ObjectViewList {
	return r.JSON200
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r ListObjectViewsReply) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetBody returns the raw response body bytes
func (r ListObjectViewsReply) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r ListObjectViewsReply) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r ListObjectViewsReply) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r ListObjectViewsReply) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

// ApplyChangeWithBodyWithResponse Apply a change
//
// Applies one change. With the `baseline` a plan returned, the server first
// re-reads the entry and refuses the change if it is not the operation the
// plan was issued for (`400 plan_mismatch`) or the directory has moved
// since (`409 conflict`, `cause: plan_stale`). A secret in the change -- a
// new password, a sensitive attribute value -- is part of that operation:
// a different value than the one planned is a mismatch.
//
// Without a `baseline` the change is applied as it always was. The
// interface always plans first and always sends one.
//
// With `recovery=true` the response carries a recovery bundle for the
// change once it has succeeded: the compensating change derived from the
// entry as it was read immediately before the change ran. See
// `docs/RECOVERY.md`. A bundle is never applied from here or anywhere
// else; it becomes ordinary changes through `POST /recovery/inspect`.
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /changes/apply (the `ApplyChange` operationId).
func (c *ClientWithResponses) ApplyChangeWithBodyWithResponse(ctx context.Context, params *ApplyChangeParams, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*ApplyChangeReply, error) {
	rsp, err := c.ApplyChangeWithBody(ctx, params, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseApplyChangeReply(rsp)
}

// ApplyChangeWithResponse Apply a change
//
// Applies one change. With the `baseline` a plan returned, the server first
// re-reads the entry and refuses the change if it is not the operation the
// plan was issued for (`400 plan_mismatch`) or the directory has moved
// since (`409 conflict`, `cause: plan_stale`). A secret in the change -- a
// new password, a sensitive attribute value -- is part of that operation:
// a different value than the one planned is a mismatch.
//
// Without a `baseline` the change is applied as it always was. The
// interface always plans first and always sends one.
//
// With `recovery=true` the response carries a recovery bundle for the
// change once it has succeeded: the compensating change derived from the
// entry as it was read immediately before the change ran. See
// `docs/RECOVERY.md`. A bundle is never applied from here or anywhere
// else; it becomes ordinary changes through `POST /recovery/inspect`.
//
// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /changes/apply (the `ApplyChange` operationId).
func (c *ClientWithResponses) ApplyChangeWithResponse(ctx context.Context, params *ApplyChangeParams, body ApplyChangeJSONRequestBody, reqEditors ...RequestEditorFn) (*ApplyChangeReply, error) {
	rsp, err := c.ApplyChange(ctx, params, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseApplyChangeReply(rsp)
}

// PreviewChangeWithBodyWithResponse Render a change as LDIF and as an Ansible task
//
// Renders without applying and without reading the directory.
//
// Deprecated since 1.6, and kept for 1.x clients. The interface no longer
// calls it: its confirmation dialog plans the change with `POST /plan`,
// which returns the same rendering inside the plan item together with
// what the change would do against the directory as it is, and a token
// that `/changes/apply` holds the change to. A rendering alone says
// nothing about whether the change still applies.
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /changes/preview (the `PreviewChange` operationId).
//
// Deprecated: this operation has been marked as deprecated upstream, but no `x-deprecated-reason` was set
func (c *ClientWithResponses) PreviewChangeWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*PreviewChangeReply, error) {
	rsp, err := c.PreviewChangeWithBody(ctx, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParsePreviewChangeReply(rsp)
}

// PreviewChangeWithResponse Render a change as LDIF and as an Ansible task
//
// Renders without applying and without reading the directory.
//
// Deprecated since 1.6, and kept for 1.x clients. The interface no longer
// calls it: its confirmation dialog plans the change with `POST /plan`,
// which returns the same rendering inside the plan item together with
// what the change would do against the directory as it is, and a token
// that `/changes/apply` holds the change to. A rendering alone says
// nothing about whether the change still applies.
//
// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /changes/preview (the `PreviewChange` operationId).
// Deprecated: this operation has been marked as deprecated upstream, but no `x-deprecated-reason` was set
func (c *ClientWithResponses) PreviewChangeWithResponse(ctx context.Context, body PreviewChangeJSONRequestBody, reqEditors ...RequestEditorFn) (*PreviewChangeReply, error) {
	rsp, err := c.PreviewChange(ctx, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParsePreviewChangeReply(rsp)
}

// ApplyChangesetWithBodyWithResponse Apply several changes in order
//
// Applies each change in the order given and stops at the first failure.
//
// A directory has no transaction spanning entries, so a changeset is not
// atomic and does not pretend to be. The response says exactly which
// changes were applied and which was not, so the caller can fix the one
// that failed and resume from there rather than starting again and
// re-applying what already succeeded.
//
// With `recovery: true` the result carries a recovery bundle covering
// exactly the changes that were applied -- on a run that stopped partway,
// only those before the failure -- whose compensations run in the reverse
// order. See `docs/RECOVERY.md`.
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /changeset/apply (the `ApplyChangeset` operationId).
func (c *ClientWithResponses) ApplyChangesetWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*ApplyChangesetReply, error) {
	rsp, err := c.ApplyChangesetWithBody(ctx, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseApplyChangesetReply(rsp)
}

// ApplyChangesetWithResponse Apply several changes in order
//
// Applies each change in the order given and stops at the first failure.
//
// A directory has no transaction spanning entries, so a changeset is not
// atomic and does not pretend to be. The response says exactly which
// changes were applied and which was not, so the caller can fix the one
// that failed and resume from there rather than starting again and
// re-applying what already succeeded.
//
// With `recovery: true` the result carries a recovery bundle covering
// exactly the changes that were applied -- on a run that stopped partway,
// only those before the failure -- whose compensations run in the reverse
// order. See `docs/RECOVERY.md`.
//
// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /changeset/apply (the `ApplyChangeset` operationId).
func (c *ClientWithResponses) ApplyChangesetWithResponse(ctx context.Context, body ApplyChangesetJSONRequestBody, reqEditors ...RequestEditorFn) (*ApplyChangesetReply, error) {
	rsp, err := c.ApplyChangeset(ctx, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseApplyChangesetReply(rsp)
}

// PreviewChangesetWithBodyWithResponse Render several changes as one LDIF document and one playbook
//
// A changeset is an ordered list of changes reviewed and applied together.
// It is held by the client, not the server: Alder keeps no per-session
// state beyond the connection itself, so this endpoint is a pure
// rendering of whatever it is given.
//
// The order is the caller's and is preserved exactly. Alder does not sort
// the changes to make them work -- an entry created before its parent is
// reported as a warning so it can be reordered, because guessing the
// intended order silently is worse than saying what is wrong.
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /changeset/preview (the `PreviewChangeset` operationId).
func (c *ClientWithResponses) PreviewChangesetWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*PreviewChangesetReply, error) {
	rsp, err := c.PreviewChangesetWithBody(ctx, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParsePreviewChangesetReply(rsp)
}

// PreviewChangesetWithResponse Render several changes as one LDIF document and one playbook
//
// A changeset is an ordered list of changes reviewed and applied together.
// It is held by the client, not the server: Alder keeps no per-session
// state beyond the connection itself, so this endpoint is a pure
// rendering of whatever it is given.
//
// The order is the caller's and is preserved exactly. Alder does not sort
// the changes to make them work -- an entry created before its parent is
// reported as a warning so it can be reordered, because guessing the
// intended order silently is worse than saying what is wrong.
//
// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /changeset/preview (the `PreviewChangeset` operationId).
func (c *ClientWithResponses) PreviewChangesetWithResponse(ctx context.Context, body PreviewChangesetJSONRequestBody, reqEditors ...RequestEditorFn) (*PreviewChangesetReply, error) {
	rsp, err := c.PreviewChangeset(ctx, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParsePreviewChangesetReply(rsp)
}

// CompareEntriesWithResponse What differs between two entries
//
// Answers "why does this account work and that one not".
//
// A naive diff answers it badly in three ways, and avoiding those is most
// of what this endpoint is. It compares what is *present*, so an attribute
// one entry's classes require and it does not hold is invisible — and that
// absence is frequently the whole answer. It compares bytes, so two DNs
// naming the same entry in different case read as a difference the
// directory does not agree with. And its instinct is to show both sides of
// everything, which for `userPassword` is what rule 6 forbids.
//
// So each side is annotated from its *own* object classes, which the two
// entries need not share; DN-valued attributes are compared as DNs; and a
// sensitive attribute is compared on presence alone.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /compare (the `CompareEntries` operationId).
func (c *ClientWithResponses) CompareEntriesWithResponse(ctx context.Context, params *CompareEntriesParams, reqEditors ...RequestEditorFn) (*CompareEntriesReply, error) {
	rsp, err := c.CompareEntries(ctx, params, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseCompareEntriesReply(rsp)
}

// CountEntriesWithResponse Count the entries under a base, up to a limit
//
// A subtree count, bounded like every other search. It asks the server for
// no attributes at all, so the cost is the search rather than the values.
//
// `truncated` is the important half of the answer: it means the count
// reached the limit and the real number is higher. The UI reports "at
// least N" in that case rather than a number that is simply wrong, which
// is why this is an action somebody asks for rather than something a
// landing page does to every naming context on load.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /count (the `CountEntries` operationId).
func (c *ClientWithResponses) CountEntriesWithResponse(ctx context.Context, params *CountEntriesParams, reqEditors ...RequestEditorFn) (*CountEntriesReply, error) {
	rsp, err := c.CountEntries(ctx, params, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseCountEntriesReply(rsp)
}

// DiffStatesWithBodyWithResponse Compare two directory states
//
// Compares `source` with `target`, each a snapshot or the live directory
// this session is bound to. The direction is explicit: `added` means in
// the target and not in the source. See `docs/SNAPSHOTS.md`.
//
//   - Values compare by the equality rule both sides recorded for the
//     attribute where Alder models it, and byte for byte otherwise; the
//     attributes compared by bytes are listed.
//   - A rename is only reported when both sides share an entry's stable
//     identity. Otherwise a moved entry is removed and added.
//   - What could not be seen is `unknown`, never `removed` or `added`: an
//     entry beyond a live search's bound, outside the other side's scope, or
//     an attribute the live directory will not show. Any of those makes the
//     comparison `complete: false`, with `reasons`.
//   - A live side reads as the session's identity. An entry the access
//     rules hide is indistinguishable from one that does not exist; see the
//     limits in `docs/SNAPSHOTS.md`.
//
// **Two snapshots need no session.** Comparing two documents the caller
// sent reads no directory, so the request is answered without a session
// and opens none. It is still bounded like any request -- the request body
// limit, the in-flight limit and the request timeout -- and its snapshots
// are read exactly as strictly. A request with a live side needs a
// session; without one it is refused with `401` before either snapshot is
// read.
//
// **Schema (1.10).** Two schema snapshots, or a schema snapshot and the
// live schema (`live: {kind: schema}`, or `live: {}` beside a schema
// snapshot), compare definitions instead of entries, and the response has
// `kind: schema` and a `schema` section. Identity is the OID; definitions
// compare by meaning, not text; a difference only in X- extensions is
// `metadata_only`; what did not parse is `unknown`. `counts` summarises
// both element kinds, with `metadata_only` counted as unchanged. A schema
// and a data side cannot be compared.
//
// When the source is live, each item carries a `candidate`: change
// requests that would move the live directory toward the target. They are
// input for `POST /plan`, never applied from here. Deleting an entry is a
// `destructive` candidate, is never selected for the caller, and is
// withheld entirely when the comparison is partial. A missing entry in the
// target is never, by itself, a reason to delete.
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /diff (the `DiffStates` operationId).
func (c *ClientWithResponses) DiffStatesWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*DiffStatesReply, error) {
	rsp, err := c.DiffStatesWithBody(ctx, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseDiffStatesReply(rsp)
}

// DiffStatesWithResponse Compare two directory states
//
// Compares `source` with `target`, each a snapshot or the live directory
// this session is bound to. The direction is explicit: `added` means in
// the target and not in the source. See `docs/SNAPSHOTS.md`.
//
//   - Values compare by the equality rule both sides recorded for the
//     attribute where Alder models it, and byte for byte otherwise; the
//     attributes compared by bytes are listed.
//   - A rename is only reported when both sides share an entry's stable
//     identity. Otherwise a moved entry is removed and added.
//   - What could not be seen is `unknown`, never `removed` or `added`: an
//     entry beyond a live search's bound, outside the other side's scope, or
//     an attribute the live directory will not show. Any of those makes the
//     comparison `complete: false`, with `reasons`.
//   - A live side reads as the session's identity. An entry the access
//     rules hide is indistinguishable from one that does not exist; see the
//     limits in `docs/SNAPSHOTS.md`.
//
// **Two snapshots need no session.** Comparing two documents the caller
// sent reads no directory, so the request is answered without a session
// and opens none. It is still bounded like any request -- the request body
// limit, the in-flight limit and the request timeout -- and its snapshots
// are read exactly as strictly. A request with a live side needs a
// session; without one it is refused with `401` before either snapshot is
// read.
//
// **Schema (1.10).** Two schema snapshots, or a schema snapshot and the
// live schema (`live: {kind: schema}`, or `live: {}` beside a schema
// snapshot), compare definitions instead of entries, and the response has
// `kind: schema` and a `schema` section. Identity is the OID; definitions
// compare by meaning, not text; a difference only in X- extensions is
// `metadata_only`; what did not parse is `unknown`. `counts` summarises
// both element kinds, with `metadata_only` counted as unchanged. A schema
// and a data side cannot be compared.
//
// When the source is live, each item carries a `candidate`: change
// requests that would move the live directory toward the target. They are
// input for `POST /plan`, never applied from here. Deleting an entry is a
// `destructive` candidate, is never selected for the caller, and is
// withheld entirely when the comparison is partial. A missing entry in the
// target is never, by itself, a reason to delete.
//
// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /diff (the `DiffStates` operationId).
func (c *ClientWithResponses) DiffStatesWithResponse(ctx context.Context, body DiffStatesJSONRequestBody, reqEditors ...RequestEditorFn) (*DiffStatesReply, error) {
	rsp, err := c.DiffStates(ctx, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseDiffStatesReply(rsp)
}

// GetEntryWithResponse Read one entry
//
// Returns the entry with every attribute annotated from the schema: its
// syntax, whether it is single-valued, whether the server owns it, and
// which input control the editor should offer. Also returns the MUST and
// MAY sets computed from the entry's object classes.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /entry (the `GetEntry` operationId).
func (c *ClientWithResponses) GetEntryWithResponse(ctx context.Context, params *GetEntryParams, reqEditors ...RequestEditorFn) (*GetEntryReply, error) {
	rsp, err := c.GetEntry(ctx, params, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseGetEntryReply(rsp)
}

// ExportAnsibleWithResponse Export an entry or a subtree as a playbook that enforces it
//
// A playbook that makes a directory match what is here, rather than one
// that merely reports on it.
//
// This is a separate path from `/export/ldif` on purpose. The obvious
// alternative was `format=ansible` on that operation, which would leave
// the URL saying "ldif" while returning YAML — and the two differ in more
// than serialisation. An LDIF export is a transcription of entries; this
// is an assertion about what they should be, and it is ordered parent
// first because `ldap_entry` cannot create a child under a parent that
// does not exist yet.
//
// Each entry becomes two tasks: `community.general.ldap_entry` to create
// it if it is missing, and `community.general.ldap_attrs` with
// `state: exact` to bring the listed attributes to exactly these values.
// `ldap_entry` with `state: present` alone would report success against
// an entry that exists with entirely different attributes, which is the
// failure this endpoint exists to avoid.
//
// Attributes the directory owns — operational, `NO-USER-MODIFICATION` —
// are omitted, because enforcing them would produce a task that fails on
// every run against a server doing its job. Sensitive attributes are
// omitted with no option to include them: a playbook is a file destined
// for a repository.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /export/ansible (the `ExportAnsible` operationId).
func (c *ClientWithResponses) ExportAnsibleWithResponse(ctx context.Context, params *ExportAnsibleParams, reqEditors ...RequestEditorFn) (*ExportAnsibleReply, error) {
	rsp, err := c.ExportAnsible(ctx, params, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseExportAnsibleReply(rsp)
}

// ExportLdifWithResponse Export an entry or a subtree as LDIF
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /export/ldif (the `ExportLdif` operationId).
func (c *ClientWithResponses) ExportLdifWithResponse(ctx context.Context, params *ExportLdifParams, reqEditors ...RequestEditorFn) (*ExportLdifReply, error) {
	rsp, err := c.ExportLdif(ctx, params, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseExportLdifReply(rsp)
}

// ExportOutlineWithResponse The shape of a subtree, as a tree
//
// The same entries an LDIF export would give, drawn as the hierarchy they
// form rather than listed one after another.
//
// Asked for by an operator testing Alder: an LDIF export is flat, and a
// flat list of three hundred records does not tell you the shape of what
// you exported.
//
// **This is not LDIF and cannot be applied.** It could not be: RFC 2849
// gives a leading space its own meaning -- it continues the line above --
// so a tree drawn with indentation would stop being a document you can
// import. Rather than a format that is almost LDIF and quietly broken,
// this is plainly something else, and says so on its first line.
//
// Bounded rather than streamed, unlike the LDIF export: a tree cannot be
// drawn until its last entry has arrived, because the entry that decides
// whether a node is a leaf may be the final one to come back.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /export/outline (the `ExportOutline` operationId).
func (c *ClientWithResponses) ExportOutlineWithResponse(ctx context.Context, params *ExportOutlineParams, reqEditors ...RequestEditorFn) (*ExportOutlineReply, error) {
	rsp, err := c.ExportOutline(ctx, params, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseExportOutlineReply(rsp)
}

// ExportYamlWithResponse The subtree as nested YAML
//
// The entries and what they hold, nested as the tree they form, in a
// format an editor folds and colours. Asked for so that a subtree can be
// opened in an editor and read: LDIF is flat and YAML is not.
//
// Every attribute is a list, even where the schema says one value. An LDAP
// attribute holds a set, and a document whose shape changed with the data
// would make a second value look like a type change in the diff.
//
// A value that is not valid printable UTF-8 is written with YAML's own
// `!!binary` tag rather than passed off as a string.
//
// **Nothing reads this back.** Alder imports LDIF, which is the format with
// a specification and a changetype; this is for looking at. It is bounded
// rather than streamed for the same reason the outline is: a tree cannot be
// nested until its last entry has arrived.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /export/yaml (the `ExportYaml` operationId).
func (c *ClientWithResponses) ExportYamlWithResponse(ctx context.Context, params *ExportYamlParams, reqEditors ...RequestEditorFn) (*ExportYamlReply, error) {
	rsp, err := c.ExportYaml(ctx, params, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseExportYamlReply(rsp)
}

// ParseLdifWithBodyWithResponse Parse an LDIF document into change records
//
// Parses and validates without applying anything. The response is the
// list of changes the document describes, each rendered back as LDIF, for
// the user to review and confirm one at a time or all at once.
//
// URL-valued attributes (`attr:< url`) are refused: following one from a
// process holding a privileged bind would be file disclosure and request
// forgery. So are LDAP controls, which would change what the server does
// with a record.
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /import/ldif (the `ParseLdif` operationId).
func (c *ClientWithResponses) ParseLdifWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*ParseLdifReply, error) {
	rsp, err := c.ParseLdifWithBody(ctx, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseParseLdifReply(rsp)
}

// ParseLdifWithResponse Parse an LDIF document into change records
//
// Parses and validates without applying anything. The response is the
// list of changes the document describes, each rendered back as LDIF, for
// the user to review and confirm one at a time or all at once.
//
// URL-valued attributes (`attr:< url`) are refused: following one from a
// process holding a privileged bind would be file disclosure and request
// forgery. So are LDAP controls, which would change what the server does
// with a record.
//
// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /import/ldif (the `ParseLdif` operationId).
func (c *ClientWithResponses) ParseLdifWithResponse(ctx context.Context, body ParseLdifJSONRequestBody, reqEditors ...RequestEditorFn) (*ParseLdifReply, error) {
	rsp, err := c.ParseLdif(ctx, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseParseLdifReply(rsp)
}

// InventoryValuesWithBodyWithResponse What values one attribute holds, and how many entries carry each
//
// The question that finds a team name spelled "platfrm" with four people
// on it, a cost centre nobody has used since a reorganisation, or the
// accounts still pointing at a decommissioned site.
//
// It is a tally over a **bounded** search, so it never claims to describe
// the directory — only the entries it examined. Every number is scoped
// that way and `truncated` says the bound was reached. A tally that
// quietly summarises a truncated result set is not a weaker answer than
// the truth; it is a confident wrong one.
//
// Sensitive attributes are refused before the search runs rather than
// filtered out of the result, which is the difference between never
// reading a password and reading every password and choosing not to say.
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /inventory (the `InventoryValues` operationId).
func (c *ClientWithResponses) InventoryValuesWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*InventoryValuesReply, error) {
	rsp, err := c.InventoryValuesWithBody(ctx, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseInventoryValuesReply(rsp)
}

// InventoryValuesWithResponse What values one attribute holds, and how many entries carry each
//
// The question that finds a team name spelled "platfrm" with four people
// on it, a cost centre nobody has used since a reorganisation, or the
// accounts still pointing at a decommissioned site.
//
// It is a tally over a **bounded** search, so it never claims to describe
// the directory — only the entries it examined. Every number is scoped
// that way and `truncated` says the bound was reached. A tally that
// quietly summarises a truncated result set is not a weaker answer than
// the truth; it is a confident wrong one.
//
// Sensitive attributes are refused before the search runs rather than
// filtered out of the result, which is the difference between never
// reading a password and reading every password and choosing not to say.
//
// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /inventory (the `InventoryValues` operationId).
func (c *ClientWithResponses) InventoryValuesWithResponse(ctx context.Context, body InventoryValuesJSONRequestBody, reqEditors ...RequestEditorFn) (*InventoryValuesReply, error) {
	rsp, err := c.InventoryValues(ctx, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseInventoryValuesReply(rsp)
}

// ExpandMembersWithResponse Who is in this group, following nested groups
//
// The entry view lists a group's `member` values, which is the whole
// answer for a flat group and no answer at all for a nested one:
// `cn=everyone` in the test harness lists five members and contains no
// people, because all five are themselves groups.
//
// This walks that structure and says, for each person reached, which
// chain of groups brought them in.
//
// What it finds on the way is reported rather than dropped: a group that
// contains itself, a member DN whose entry cannot be read — the dangling
// reference the referenced-by panel exists to prevent, seen from the
// other side — and membership values that are not DNs at all, since
// `memberUid` holds a login name and `memberURL` a search.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /members (the `ExpandMembers` operationId).
func (c *ClientWithResponses) ExpandMembersWithResponse(ctx context.Context, params *ExpandMembersParams, reqEditors ...RequestEditorFn) (*ExpandMembersReply, error) {
	rsp, err := c.ExpandMembers(ctx, params, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseExpandMembersReply(rsp)
}

// BuildPackageWithBodyWithResponse Build a change package from changes
//
// Turns changes into an `alder-change-package` document (1.11): a portable
// description of intended directory and schema changes that can be carried
// to another environment and validated there.
//
// The package holds intent, not execution. Every `baseline` is dropped,
// every expectation is dropped, and a change to a schema entry is recorded
// as what it means -- this element, this operation, this definition --
// rather than as the modification this server happens to need, because the
// entry and attribute that hold schema differ between products.
//
// What cannot be packaged is never dropped silently. A password change
// needs its secret to mean anything, so it is refused, and the package
// records the omission with a machine-readable reason.
//
// Dependencies are derived from the changes themselves -- an object class
// after the attribute types it names, an entry after the class it uses and
// after its parent -- and written into the document. Array order carries no
// meaning; the graph does.
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /packages/build (the `BuildPackage` operationId).
func (c *ClientWithResponses) BuildPackageWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*BuildPackageReply, error) {
	rsp, err := c.BuildPackageWithBody(ctx, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseBuildPackageReply(rsp)
}

// BuildPackageWithResponse Build a change package from changes
//
// Turns changes into an `alder-change-package` document (1.11): a portable
// description of intended directory and schema changes that can be carried
// to another environment and validated there.
//
// The package holds intent, not execution. Every `baseline` is dropped,
// every expectation is dropped, and a change to a schema entry is recorded
// as what it means -- this element, this operation, this definition --
// rather than as the modification this server happens to need, because the
// entry and attribute that hold schema differ between products.
//
// What cannot be packaged is never dropped silently. A password change
// needs its secret to mean anything, so it is refused, and the package
// records the omission with a machine-readable reason.
//
// Dependencies are derived from the changes themselves -- an object class
// after the attribute types it names, an entry after the class it uses and
// after its parent -- and written into the document. Array order carries no
// meaning; the graph does.
//
// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /packages/build (the `BuildPackage` operationId).
func (c *ClientWithResponses) BuildPackageWithResponse(ctx context.Context, body BuildPackageJSONRequestBody, reqEditors ...RequestEditorFn) (*BuildPackageReply, error) {
	rsp, err := c.BuildPackage(ctx, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseBuildPackageReply(rsp)
}

// InspectPackageWithBodyWithResponse Read a change package and check everything that needs no directory
//
// Reads a package strictly and reports what it holds: its identity, its
// provenance, its assumptions, its changes and its dependency order.
//
// Everything that can be checked without a directory is checked here --
// the format and version, unknown fields, trailing content, duplicate
// identifiers, missing dependencies, self-dependencies, cycles, malformed
// DNs, schema definitions that do not parse, secrets, and the checksum.
// Nothing is executed and no directory is read, so no session is needed:
// this is the check a pipeline runs before it has credentials.
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /packages/inspect (the `InspectPackage` operationId).
func (c *ClientWithResponses) InspectPackageWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*InspectPackageReply, error) {
	rsp, err := c.InspectPackageWithBody(ctx, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseInspectPackageReply(rsp)
}

// InspectPackageWithResponse Read a change package and check everything that needs no directory
//
// Reads a package strictly and reports what it holds: its identity, its
// provenance, its assumptions, its changes and its dependency order.
//
// Everything that can be checked without a directory is checked here --
// the format and version, unknown fields, trailing content, duplicate
// identifiers, missing dependencies, self-dependencies, cycles, malformed
// DNs, schema definitions that do not parse, secrets, and the checksum.
// Nothing is executed and no directory is read, so no session is needed:
// this is the check a pipeline runs before it has credentials.
//
// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /packages/inspect (the `InspectPackage` operationId).
func (c *ClientWithResponses) InspectPackageWithResponse(ctx context.Context, body InspectPackageJSONRequestBody, reqEditors ...RequestEditorFn) (*InspectPackageReply, error) {
	rsp, err := c.InspectPackage(ctx, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseInspectPackageReply(rsp)
}

// ValidatePackageWithBodyWithResponse Validate a change package against the current directory
//
// Reads the package, then asks this directory what its intent means here:
// which changes are ready, which the target already satisfies, which
// conflict with what is there now, which name something that is missing,
// and which this server cannot do at all.
//
// Validation is not a plan. It prepares the ordinary change requests a
// plan would take and stops; the plan reads the directory again, issues
// the baselines an apply checks, and remains the only thing that decides
// what is written. Nothing here writes, and a validation is not something
// to keep: carry the package to the next environment and validate it
// there.
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /packages/validate (the `ValidatePackage` operationId).
func (c *ClientWithResponses) ValidatePackageWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*ValidatePackageReply, error) {
	rsp, err := c.ValidatePackageWithBody(ctx, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseValidatePackageReply(rsp)
}

// ValidatePackageWithResponse Validate a change package against the current directory
//
// Reads the package, then asks this directory what its intent means here:
// which changes are ready, which the target already satisfies, which
// conflict with what is there now, which name something that is missing,
// and which this server cannot do at all.
//
// Validation is not a plan. It prepares the ordinary change requests a
// plan would take and stops; the plan reads the directory again, issues
// the baselines an apply checks, and remains the only thing that decides
// what is written. Nothing here writes, and a validation is not something
// to keep: carry the package to the next environment and validate it
// there.
//
// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /packages/validate (the `ValidatePackage` operationId).
func (c *ClientWithResponses) ValidatePackageWithResponse(ctx context.Context, body ValidatePackageJSONRequestBody, reqEditors ...RequestEditorFn) (*ValidatePackageReply, error) {
	rsp, err := c.ValidatePackage(ctx, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseValidatePackageReply(rsp)
}

// PlanChangesWithBodyWithResponse What a set of changes would do, without doing it
//
// Answers the question one level above the preview: given these proposed
// changes and the directory as it is, which are additions, which are
// modifications, which would do nothing at all, and which cannot be
// applied as written.
//
// Nothing is written. Every entry named is read, each change is
// classified, and the records that would run come back in `items[].record`
// — the same values `POST /changeset/apply` takes, not a description of
// them. That is what stops a plan from promising one thing and an apply
// doing another.
//
// `action` is a stable identifier and is what a client should switch on.
// `reason` is prose for a person and may be reworded in any release.
//
// Each applicable item carries a `baseline`: an opaque token binding two
// things — the exact operation in `record`, and the state of the directory
// that operation was planned against. Hand it back on the corresponding
// change when applying. The server re-reads the entry and refuses the
// whole set before anything runs: `400 plan_mismatch` if a change is not
// the operation its baseline was issued for, `409 conflict` with
// `cause: plan_stale` if the directory has moved since. Neither is silently replanned. The token
// holds no attribute values; a sensitive attribute contributes only
// whether it is set and how many values it has, and a password change
// binds the entry and the fact of the change, never the password.
// Baselines are meaningless to any other Alder process and do not survive
// a restart.
//
// The input is either `changes` or `ldif`, never both.
//
// **`ldif` with `mode: changes`** (the default) plans every record as the
// exact operation it states. A record with no `changetype` is an add,
// which is what `ldapadd` does with it. An add of an entry that already
// exists is a conflict (`entry_exists`); it is never turned into a
// modification nobody asked for.
//
// **`ldif` with `mode: desired`** reads the document as the state entries
// should be in. Only content records — no `changetype` — are accepted; a
// record with one is refused with `400 ldif_mode_mismatch`, because a
// document mixing "this entry looks like this" with "do this" has no
// single meaning. Each record is reconciled: created if the entry is
// absent, turned into the modification of the attributes it names if
// present, reported `unchanged` if it already matches. Attributes a record
// does not name are left alone. **An entry the document does not mention
// is not deleted**; there is no mode in which absence means deletion.
//
// `impact` reports facts, not a risk score: which target area each change
// touches (ordinary data, schema, server configuration), memberships
// gained and lost, deletions grouped into the branches they remove, and —
// for deletions and renames — the entries that name the affected DN and
// how many of those references the plan would leave dangling. Reference
// search runs as the session's own bind, so it reports what that bind can
// see; see `impact.references`.
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /plan (the `PlanChanges` operationId).
func (c *ClientWithResponses) PlanChangesWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*PlanChangesReply, error) {
	rsp, err := c.PlanChangesWithBody(ctx, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParsePlanChangesReply(rsp)
}

// PlanChangesWithResponse What a set of changes would do, without doing it
//
// Answers the question one level above the preview: given these proposed
// changes and the directory as it is, which are additions, which are
// modifications, which would do nothing at all, and which cannot be
// applied as written.
//
// Nothing is written. Every entry named is read, each change is
// classified, and the records that would run come back in `items[].record`
// — the same values `POST /changeset/apply` takes, not a description of
// them. That is what stops a plan from promising one thing and an apply
// doing another.
//
// `action` is a stable identifier and is what a client should switch on.
// `reason` is prose for a person and may be reworded in any release.
//
// Each applicable item carries a `baseline`: an opaque token binding two
// things — the exact operation in `record`, and the state of the directory
// that operation was planned against. Hand it back on the corresponding
// change when applying. The server re-reads the entry and refuses the
// whole set before anything runs: `400 plan_mismatch` if a change is not
// the operation its baseline was issued for, `409 conflict` with
// `cause: plan_stale` if the directory has moved since. Neither is silently replanned. The token
// holds no attribute values; a sensitive attribute contributes only
// whether it is set and how many values it has, and a password change
// binds the entry and the fact of the change, never the password.
// Baselines are meaningless to any other Alder process and do not survive
// a restart.
//
// The input is either `changes` or `ldif`, never both.
//
// **`ldif` with `mode: changes`** (the default) plans every record as the
// exact operation it states. A record with no `changetype` is an add,
// which is what `ldapadd` does with it. An add of an entry that already
// exists is a conflict (`entry_exists`); it is never turned into a
// modification nobody asked for.
//
// **`ldif` with `mode: desired`** reads the document as the state entries
// should be in. Only content records — no `changetype` — are accepted; a
// record with one is refused with `400 ldif_mode_mismatch`, because a
// document mixing "this entry looks like this" with "do this" has no
// single meaning. Each record is reconciled: created if the entry is
// absent, turned into the modification of the attributes it names if
// present, reported `unchanged` if it already matches. Attributes a record
// does not name are left alone. **An entry the document does not mention
// is not deleted**; there is no mode in which absence means deletion.
//
// `impact` reports facts, not a risk score: which target area each change
// touches (ordinary data, schema, server configuration), memberships
// gained and lost, deletions grouped into the branches they remove, and —
// for deletions and renames — the entries that name the affected DN and
// how many of those references the plan would leave dangling. Reference
// search runs as the session's own bind, so it reports what that bind can
// see; see `impact.references`.
//
// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /plan (the `PlanChanges` operationId).
func (c *ClientWithResponses) PlanChangesWithResponse(ctx context.Context, body PlanChangesJSONRequestBody, reqEditors ...RequestEditorFn) (*PlanChangesReply, error) {
	rsp, err := c.PlanChanges(ctx, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParsePlanChangesReply(rsp)
}

// PreflightWithBodyWithResponse Report what of an artifact would carry across to this directory
//
// Reads a change package, a schema snapshot or a data snapshot -- told
// apart by its own `format` and `kind`, never by a file name -- and reports,
// finding by finding, what this directory already holds, what it could take,
// what needs something done first, what contradicts it, what it cannot
// represent, and what could not be seen.
//
// A preflight is not a plan and not a migration. It reads the directory and
// never writes to it; it never rewrites a DN, maps an attribute or object
// class, or changes the artifact; and it produces no baseline and no
// prepared change. Package validation is its first step for a package, and
// its answer is carried on every finding about an item.
//
// `overall` is derived from the findings: `incompatible` when any finding
// that blocks portability is incompatible or unsupported, `incomplete` when
// otherwise something could not be decided, `compatible_with_prerequisites`
// when something has to be done first, and `compatible` only when none of
// these hold. Access control and server configuration are never evaluated,
// and `notEvaluated` says so on every report.
//
// A document that is none of the three is refused with
// `preflight_artifact_unsupported`; an artifact that is malformed, forged
// or from a newer version is refused with the code its own endpoints use.
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /preflight (the `Preflight` operationId).
func (c *ClientWithResponses) PreflightWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*PreflightReply, error) {
	rsp, err := c.PreflightWithBody(ctx, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParsePreflightReply(rsp)
}

// PreflightWithResponse Report what of an artifact would carry across to this directory
//
// Reads a change package, a schema snapshot or a data snapshot -- told
// apart by its own `format` and `kind`, never by a file name -- and reports,
// finding by finding, what this directory already holds, what it could take,
// what needs something done first, what contradicts it, what it cannot
// represent, and what could not be seen.
//
// A preflight is not a plan and not a migration. It reads the directory and
// never writes to it; it never rewrites a DN, maps an attribute or object
// class, or changes the artifact; and it produces no baseline and no
// prepared change. Package validation is its first step for a package, and
// its answer is carried on every finding about an item.
//
// `overall` is derived from the findings: `incompatible` when any finding
// that blocks portability is incompatible or unsupported, `incomplete` when
// otherwise something could not be decided, `compatible_with_prerequisites`
// when something has to be done first, and `compatible` only when none of
// these hold. Access control and server configuration are never evaluated,
// and `notEvaluated` says so on every report.
//
// A document that is none of the three is refused with
// `preflight_artifact_unsupported`; an artifact that is malformed, forged
// or from a newer version is refused with the code its own endpoints use.
//
// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /preflight (the `Preflight` operationId).
func (c *ClientWithResponses) PreflightWithResponse(ctx context.Context, body PreflightJSONRequestBody, reqEditors ...RequestEditorFn) (*PreflightReply, error) {
	rsp, err := c.Preflight(ctx, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParsePreflightReply(rsp)
}

// InspectRecoveryWithBodyWithResponse Validate a recovery bundle and turn it into changes to plan
//
// Validates an uploaded recovery bundle and returns its compensating
// changes as ordinary change requests, in the order they would have to
// run, each carrying the `expect` precondition that makes it a conflict
// in a plan if the entry is no longer as the original apply left it.
// Added in 1.9; see `docs/RECOVERY.md`.
//
// Nothing is applied, and there is no endpoint that applies a bundle.
// The changes go to `POST /plan`, are reviewed, and are applied with
// `POST /changeset/apply` like any other change. `drift` is that plan's
// verdict on each change against the directory as it is now, for the
// preview; the plan made afterwards is the one that counts.
//
// A bundle is untrusted input. It is refused when its format or version
// is not one this Alder reads (`recovery_unsupported_version` for another
// version), when any field is malformed or unknown, when a sensitive
// attribute appears with values, when a compensation is a password
// change (`recovery_invalid`), or when a checksum is present and does
// not match (`recovery_checksum_mismatch`).
//
// `originMatches` compares the vendor and naming contexts the bundle
// records with this session's directory. Neither is proof of identity:
// two directories can announce the same.
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /recovery/inspect (the `InspectRecovery` operationId).
func (c *ClientWithResponses) InspectRecoveryWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*InspectRecoveryReply, error) {
	rsp, err := c.InspectRecoveryWithBody(ctx, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseInspectRecoveryReply(rsp)
}

// InspectRecoveryWithResponse Validate a recovery bundle and turn it into changes to plan
//
// Validates an uploaded recovery bundle and returns its compensating
// changes as ordinary change requests, in the order they would have to
// run, each carrying the `expect` precondition that makes it a conflict
// in a plan if the entry is no longer as the original apply left it.
// Added in 1.9; see `docs/RECOVERY.md`.
//
// Nothing is applied, and there is no endpoint that applies a bundle.
// The changes go to `POST /plan`, are reviewed, and are applied with
// `POST /changeset/apply` like any other change. `drift` is that plan's
// verdict on each change against the directory as it is now, for the
// preview; the plan made afterwards is the one that counts.
//
// A bundle is untrusted input. It is refused when its format or version
// is not one this Alder reads (`recovery_unsupported_version` for another
// version), when any field is malformed or unknown, when a sensitive
// attribute appears with values, when a compensation is a password
// change (`recovery_invalid`), or when a checksum is present and does
// not match (`recovery_checksum_mismatch`).
//
// `originMatches` compares the vendor and naming contexts the bundle
// records with this session's directory. Neither is proof of identity:
// two directories can announce the same.
//
// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /recovery/inspect (the `InspectRecovery` operationId).
func (c *ClientWithResponses) InspectRecoveryWithResponse(ctx context.Context, body InspectRecoveryJSONRequestBody, reqEditors ...RequestEditorFn) (*InspectRecoveryReply, error) {
	rsp, err := c.InspectRecovery(ctx, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseInspectRecoveryReply(rsp)
}

// ListReferencesWithResponse The entries that name this one, and the attribute each names it by
//
// Answers "which groups is this in, and what else points at it" — and,
// unlike the filter on the entry itself, says *how* each one points.
//
// That is the part a link cannot give you. A search returns the entries
// matching the filter but not which term matched, and removing a
// reference means modifying a named attribute on the referencing entry:
// without knowing whether a group holds this DN in `member` or in `owner`,
// there is nothing safe to offer.
//
// It is one bounded search. The reference attributes are requested and
// matched here rather than in the browser, so a group's five hundred
// members are compared where they already are instead of being sent
// across to be compared and discarded.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /references (the `ListReferences` operationId).
func (c *ClientWithResponses) ListReferencesWithResponse(ctx context.Context, params *ListReferencesParams, reqEditors ...RequestEditorFn) (*ListReferencesReply, error) {
	rsp, err := c.ListReferences(ctx, params, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseListReferencesReply(rsp)
}

// ResolveWithResponse What could this input be — an entry, or a search
//
// Backs one box that takes a DN, an RFC 4515 filter, or a name.
//
// It parses and never searches. `internal/dn` and `internal/filter` are
// the authorities on what these strings are, and a regex in the browser
// would drift from them the first time either grew a case — but probing
// the directory to see whether an entry exists would turn every keystroke
// into a read, to answer a question the operator is about to answer by
// pressing enter.
//
// The three kinds overlap. `cn=platform` is a valid one-component DN
// *and* almost certainly a request to find something called platform, so
// an ambiguous input returns both destinations and the operator picks.
// Guessing would be wrong about half the time.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /resolve (the `Resolve` operationId).
func (c *ClientWithResponses) ResolveWithResponse(ctx context.Context, params *ResolveParams, reqEditors ...RequestEditorFn) (*ResolveReply, error) {
	rsp, err := c.Resolve(ctx, params, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseResolveReply(rsp)
}

// GetSchemaWithResponse The whole schema, indexed for browsing
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /schema (the `GetSchema` operationId).
func (c *ClientWithResponses) GetSchemaWithResponse(ctx context.Context, reqEditors ...RequestEditorFn) (*GetSchemaReply, error) {
	rsp, err := c.GetSchema(ctx, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseGetSchemaReply(rsp)
}

// GetAttributeTypeWithResponse One attribute type, with its cross-links resolved
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /schema/attributetypes/{name} (the `GetAttributeType` operationId).
func (c *ClientWithResponses) GetAttributeTypeWithResponse(ctx context.Context, name string, reqEditors ...RequestEditorFn) (*GetAttributeTypeReply, error) {
	rsp, err := c.GetAttributeType(ctx, name, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseGetAttributeTypeReply(rsp)
}

// BuildSchemaChangeWithBodyWithResponse Build the modification that installs, replaces or removes a definition
//
// Renders a definition and returns the change that would apply it, without
// applying anything.
//
// A schema definition is a value of an attribute on an ordinary entry, so
// the change this returns is an ordinary modify, and it goes on to the
// same preview, the same confirmation and the same changeset as every
// other write. There is still exactly one path that writes.
//
// The definition text is built here rather than in the browser, for the
// same reason the LDIF preview is: what the person confirms and what the
// directory receives have to be the same bytes.
//
// A replace or a delete needs the value the server actually stores, which
// is not always the value the schema browser displays — a server keeping
// its schema in configuration prefixes each stored definition with its
// load order and strips that prefix from what it publishes. This reads the
// target to find it, which is why the request names an OID rather than
// carrying a definition to remove.
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /schema/change (the `BuildSchemaChange` operationId).
func (c *ClientWithResponses) BuildSchemaChangeWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*BuildSchemaChangeReply, error) {
	rsp, err := c.BuildSchemaChangeWithBody(ctx, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseBuildSchemaChangeReply(rsp)
}

// BuildSchemaChangeWithResponse Build the modification that installs, replaces or removes a definition
//
// Renders a definition and returns the change that would apply it, without
// applying anything.
//
// A schema definition is a value of an attribute on an ordinary entry, so
// the change this returns is an ordinary modify, and it goes on to the
// same preview, the same confirmation and the same changeset as every
// other write. There is still exactly one path that writes.
//
// The definition text is built here rather than in the browser, for the
// same reason the LDIF preview is: what the person confirms and what the
// directory receives have to be the same bytes.
//
// A replace or a delete needs the value the server actually stores, which
// is not always the value the schema browser displays — a server keeping
// its schema in configuration prefixes each stored definition with its
// load order and strips that prefix from what it publishes. This reads the
// target to find it, which is why the request names an OID rather than
// carrying a definition to remove.
//
// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /schema/change (the `BuildSchemaChange` operationId).
func (c *ClientWithResponses) BuildSchemaChangeWithResponse(ctx context.Context, body BuildSchemaChangeJSONRequestBody, reqEditors ...RequestEditorFn) (*BuildSchemaChangeReply, error) {
	rsp, err := c.BuildSchemaChange(ctx, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseBuildSchemaChangeReply(rsp)
}

// GetObjectClassWithResponse One object class, with its cross-links resolved
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /schema/objectclasses/{name} (the `GetObjectClass` operationId).
func (c *ClientWithResponses) GetObjectClassWithResponse(ctx context.Context, name string, reqEditors ...RequestEditorFn) (*GetObjectClassReply, error) {
	rsp, err := c.GetObjectClass(ctx, name, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseGetObjectClassReply(rsp)
}

// GetRequirementsWithResponse What an entry of these object classes must and may hold
//
// The MUST and MAY sets for a set of object classes, with the schema's
// opinion about each attribute — syntax, single-valuedness, description,
// which control to offer.
//
// It exists so a creation form is generated from the schema rather than
// from a template. Without it the form has attribute names and nothing
// else, which is how creating an entry ended up with a plain text box for
// every field while editing the same entry a moment later offered a
// Boolean control, an entry picker and the attribute's description.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /schema/requirements (the `GetRequirements` operationId).
func (c *ClientWithResponses) GetRequirementsWithResponse(ctx context.Context, params *GetRequirementsParams, reqEditors ...RequestEditorFn) (*GetRequirementsReply, error) {
	rsp, err := c.GetRequirements(ctx, params, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseGetRequirementsReply(rsp)
}

// SearchWithBodyWithResponse Search the directory
//
// Every search is paged and bounded. `filter` is a raw RFC 4515 string
// which is parsed, not interpolated: a value containing filter
// metacharacters cannot escape into the filter structure.
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /search (the `Search` operationId).
func (c *ClientWithResponses) SearchWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*SearchReply, error) {
	rsp, err := c.SearchWithBody(ctx, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseSearchReply(rsp)
}

// SearchWithResponse Search the directory
//
// Every search is paged and bounded. `filter` is a raw RFC 4515 string
// which is parsed, not interpolated: a value containing filter
// metacharacters cannot escape into the filter structure.
//
// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /search (the `Search` operationId).
func (c *ClientWithResponses) SearchWithResponse(ctx context.Context, body SearchJSONRequestBody, reqEditors ...RequestEditorFn) (*SearchReply, error) {
	rsp, err := c.Search(ctx, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseSearchReply(rsp)
}

// DeleteSessionWithResponse Disconnect
//
// Closes the connection and discards the credentials.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with DELETE /session (the `DeleteSession` operationId).
func (c *ClientWithResponses) DeleteSessionWithResponse(ctx context.Context, reqEditors ...RequestEditorFn) (*DeleteSessionReply, error) {
	rsp, err := c.DeleteSession(ctx, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseDeleteSessionReply(rsp)
}

// GetSessionWithResponse Describe the current session
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /session (the `GetSession` operationId).
func (c *ClientWithResponses) GetSessionWithResponse(ctx context.Context, reqEditors ...RequestEditorFn) (*GetSessionReply, error) {
	rsp, err := c.GetSession(ctx, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseGetSessionReply(rsp)
}

// CreateSessionWithBodyWithResponse Connect and bind to a directory
//
// Opens a connection, binds, and reads the RootDSE. On success the
// response sets an httpOnly, Secure, SameSite=Strict cookie naming an
// in-memory session. The bind password is held in that in-memory session
// and is never written to disk, never placed in a token, and never
// returned by any endpoint.
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /session (the `CreateSession` operationId).
func (c *ClientWithResponses) CreateSessionWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*CreateSessionReply, error) {
	rsp, err := c.CreateSessionWithBody(ctx, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseCreateSessionReply(rsp)
}

// CreateSessionWithResponse Connect and bind to a directory
//
// Opens a connection, binds, and reads the RootDSE. On success the
// response sets an httpOnly, Secure, SameSite=Strict cookie naming an
// in-memory session. The bind password is held in that in-memory session
// and is never written to disk, never placed in a token, and never
// returned by any endpoint.
//
// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /session (the `CreateSession` operationId).
func (c *ClientWithResponses) CreateSessionWithResponse(ctx context.Context, body CreateSessionJSONRequestBody, reqEditors ...RequestEditorFn) (*CreateSessionReply, error) {
	rsp, err := c.CreateSession(ctx, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseCreateSessionReply(rsp)
}

// CaptureSnapshotWithBodyWithResponse Capture a subtree as a versioned snapshot
//
// Reads the subtree as the session's own identity and returns it as an
// Alder snapshot: a versioned, canonical JSON document, safe to keep in a
// repository. See `docs/SNAPSHOTS.md`.
//
//   - Entries, attribute names and values are in one defined order, so two
//     captures of an unchanged subtree differ only in `createdAt`, and have
//     the same `checksum`.
//   - Operational attributes are left out unless
//     `operationalAttributes` is set. Each entry's stable identity
//     (`entryUUID` or `nsUniqueId`) is recorded as its `id` either way.
//   - Sensitive attributes are recorded as a `withheld` count, never as a
//     value or anything derived from one.
//   - A capture that cannot finish -- more than 50,000 entries, a search
//     limit, a failure part way -- is an error. There is no partial
//     snapshot.
//   - A data snapshot's base must be ordinary data: a base inside the schema
//     or the server's configuration is refused with
//     `snapshot_scope_unsupported`.
//
// With `kind: schema` (1.10) the published subschema is captured instead,
// and `base`, `scope`, `filter` and `operationalAttributes` are not used.
// Attribute types and object classes are captured for comparison; LDAP
// syntaxes, matching rules, matching rule uses, DIT content rules and name
// forms as context. A definition that does not parse is kept verbatim and
// makes the snapshot `partial`. Server configuration is never captured, and
// there is no `kind: config`.
//
// Nothing is kept on the server. The snapshot is returned and belongs to
// the caller.
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /snapshots/capture (the `CaptureSnapshot` operationId).
func (c *ClientWithResponses) CaptureSnapshotWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*CaptureSnapshotReply, error) {
	rsp, err := c.CaptureSnapshotWithBody(ctx, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseCaptureSnapshotReply(rsp)
}

// CaptureSnapshotWithResponse Capture a subtree as a versioned snapshot
//
// Reads the subtree as the session's own identity and returns it as an
// Alder snapshot: a versioned, canonical JSON document, safe to keep in a
// repository. See `docs/SNAPSHOTS.md`.
//
//   - Entries, attribute names and values are in one defined order, so two
//     captures of an unchanged subtree differ only in `createdAt`, and have
//     the same `checksum`.
//   - Operational attributes are left out unless
//     `operationalAttributes` is set. Each entry's stable identity
//     (`entryUUID` or `nsUniqueId`) is recorded as its `id` either way.
//   - Sensitive attributes are recorded as a `withheld` count, never as a
//     value or anything derived from one.
//   - A capture that cannot finish -- more than 50,000 entries, a search
//     limit, a failure part way -- is an error. There is no partial
//     snapshot.
//   - A data snapshot's base must be ordinary data: a base inside the schema
//     or the server's configuration is refused with
//     `snapshot_scope_unsupported`.
//
// With `kind: schema` (1.10) the published subschema is captured instead,
// and `base`, `scope`, `filter` and `operationalAttributes` are not used.
// Attribute types and object classes are captured for comparison; LDAP
// syntaxes, matching rules, matching rule uses, DIT content rules and name
// forms as context. A definition that does not parse is kept verbatim and
// makes the snapshot `partial`. Server configuration is never captured, and
// there is no `kind: config`.
//
// Nothing is kept on the server. The snapshot is returned and belongs to
// the caller.
//
// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /snapshots/capture (the `CaptureSnapshot` operationId).
func (c *ClientWithResponses) CaptureSnapshotWithResponse(ctx context.Context, body CaptureSnapshotJSONRequestBody, reqEditors ...RequestEditorFn) (*CaptureSnapshotReply, error) {
	rsp, err := c.CaptureSnapshot(ctx, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseCaptureSnapshotReply(rsp)
}

// InspectSnapshotWithBodyWithResponse Validate a snapshot and describe it
//
// Validates an uploaded snapshot exactly as a comparison would and
// describes what it covers. Read-only: nothing is kept and nothing is
// applied.
//
// A snapshot is refused when its format or version is not one this Alder
// reads (`snapshot_unsupported_version` for a newer version), when any DN,
// value or field is malformed or unknown (`snapshot_invalid`), when a
// sensitive attribute carries a value, or when a checksum is present and
// does not match (`snapshot_checksum_mismatch`). A snapshot without a
// checksum is accepted and described as `integrity: unverified`.
//
// A schema snapshot (1.10) is checked as strictly: every OID, no OID twice,
// every parsed field equal to what its definition text parses to, counts
// and coverage true, and no inheritance cycle.
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /snapshots/inspect (the `InspectSnapshot` operationId).
func (c *ClientWithResponses) InspectSnapshotWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*InspectSnapshotReply, error) {
	rsp, err := c.InspectSnapshotWithBody(ctx, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseInspectSnapshotReply(rsp)
}

// InspectSnapshotWithResponse Validate a snapshot and describe it
//
// Validates an uploaded snapshot exactly as a comparison would and
// describes what it covers. Read-only: nothing is kept and nothing is
// applied.
//
// A snapshot is refused when its format or version is not one this Alder
// reads (`snapshot_unsupported_version` for a newer version), when any DN,
// value or field is malformed or unknown (`snapshot_invalid`), when a
// sensitive attribute carries a value, or when a checksum is present and
// does not match (`snapshot_checksum_mismatch`). A snapshot without a
// checksum is accepted and described as `integrity: unverified`.
//
// A schema snapshot (1.10) is checked as strictly: every OID, no OID twice,
// every parsed field equal to what its definition text parses to, counts
// and coverage true, and no inheritance cycle.
//
// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /snapshots/inspect (the `InspectSnapshot` operationId).
func (c *ClientWithResponses) InspectSnapshotWithResponse(ctx context.Context, body InspectSnapshotJSONRequestBody, reqEditors ...RequestEditorFn) (*InspectSnapshotReply, error) {
	rsp, err := c.InspectSnapshot(ctx, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseInspectSnapshotReply(rsp)
}

// GetSourceOfferWithResponse Where to get the source of this running instance
//
// AGPL-3.0 section 13: someone using a modified Alder over a network is
// entitled to its Corresponding Source. The offer is served rather than
// written in a README because the obligation runs to the person using the
// running instance, and the user of a modified instance has no reason to
// know where its source went.
//
// **This is the one endpoint that answers without a session,** and it has
// no 401 for that reason: an offer only the already-connected can read
// would not discharge the obligation. It is described here so that the
// contract in this document is the whole contract -- it was served outside
// it until 1.0, which made `openapi.yaml` quietly incomplete about the one
// endpoint a licence requires.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /source (the `GetSourceOffer` operationId).
func (c *ClientWithResponses) GetSourceOfferWithResponse(ctx context.Context, reqEditors ...RequestEditorFn) (*GetSourceOfferReply, error) {
	rsp, err := c.GetSourceOffer(ctx, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseGetSourceOfferReply(rsp)
}

// ListChildrenWithResponse List the children of an entry
//
// With no `dn`, returns the naming contexts the server holds, which are
// the roots of the tree. With a `dn`, returns that entry's immediate
// children. This is the lazy-loading tree browser's only endpoint.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /tree (the `ListChildren` operationId).
func (c *ClientWithResponses) ListChildrenWithResponse(ctx context.Context, params *ListChildrenParams, reqEditors ...RequestEditorFn) (*ListChildrenReply, error) {
	rsp, err := c.ListChildren(ctx, params, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseListChildrenReply(rsp)
}

// ListObjectViewsWithResponse The object-aware views this directory supports
//
// Users, groups and organizational units, expressed as saved searches
// derived from the schema the connected server published.
//
// A view is offered only where the server defines a class to anchor it
// on, so a directory holding no person class has no Users view rather
// than an empty one. The anchors are standards-track classes — RFC 4519,
// RFC 4524, RFC 2307 — never vendor names, and a site class inheriting
// from one of them is matched without being named, because an entry
// carries its superclasses in its own `objectClass` attribute.
//
// The filter is built here and goes back to `/search` unmodified. The
// columns are the attributes the anchor classes actually permit, so a
// directory without `inetOrgPerson` gets no mail column instead of an
// empty one.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /views (the `ListObjectViews` operationId).
func (c *ClientWithResponses) ListObjectViewsWithResponse(ctx context.Context, reqEditors ...RequestEditorFn) (*ListObjectViewsReply, error) {
	rsp, err := c.ListObjectViews(ctx, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseListObjectViewsReply(rsp)
}

// ParseApplyChangeReply parses an HTTP response from a ApplyChangeWithResponse call
func ParseApplyChangeReply(rsp *http.Response) (*ApplyChangeReply, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &ApplyChangeReply{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest ApplyResult
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 400:
		var dest BadRequest
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON400 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest Forbidden
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 409:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON409 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 422:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON422 = &dest

	}

	return response, nil
}

// ParsePreviewChangeReply parses an HTTP response from a PreviewChangeWithResponse call
func ParsePreviewChangeReply(rsp *http.Response) (*PreviewChangeReply, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &PreviewChangeReply{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest ChangePreview
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 400:
		var dest BadRequest
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON400 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	}

	return response, nil
}

// ParseApplyChangesetReply parses an HTTP response from a ApplyChangesetWithResponse call
func ParseApplyChangesetReply(rsp *http.Response) (*ApplyChangesetReply, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &ApplyChangesetReply{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest ChangesetResult
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 400:
		var dest BadRequest
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON400 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest Forbidden
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 409:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON409 = &dest

	}

	return response, nil
}

// ParsePreviewChangesetReply parses an HTTP response from a PreviewChangesetWithResponse call
func ParsePreviewChangesetReply(rsp *http.Response) (*PreviewChangesetReply, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &PreviewChangesetReply{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest ChangesetPreview
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 400:
		var dest BadRequest
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON400 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	}

	return response, nil
}

// ParseCompareEntriesReply parses an HTTP response from a CompareEntriesWithResponse call
func ParseCompareEntriesReply(rsp *http.Response) (*CompareEntriesReply, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &CompareEntriesReply{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest EntryComparison
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 400:
		var dest BadRequest
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON400 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	}

	return response, nil
}

// ParseCountEntriesReply parses an HTTP response from a CountEntriesWithResponse call
func ParseCountEntriesReply(rsp *http.Response) (*CountEntriesReply, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &CountEntriesReply{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest CountResult
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 400:
		var dest BadRequest
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON400 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest Forbidden
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	}

	return response, nil
}

// ParseDiffStatesReply parses an HTTP response from a DiffStatesWithResponse call
func ParseDiffStatesReply(rsp *http.Response) (*DiffStatesReply, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &DiffStatesReply{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest Diff
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 400:
		var dest BadRequest
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON400 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	}

	return response, nil
}

// ParseGetEntryReply parses an HTTP response from a GetEntryWithResponse call
func ParseGetEntryReply(rsp *http.Response) (*GetEntryReply, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &GetEntryReply{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest EntryView
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest Forbidden
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	}

	return response, nil
}

// ParseExportAnsibleReply parses an HTTP response from a ExportAnsibleWithResponse call
func ParseExportAnsibleReply(rsp *http.Response) (*ExportAnsibleReply, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &ExportAnsibleReply{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 400:
		var dest BadRequest
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON400 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	}

	return response, nil
}

// ParseExportLdifReply parses an HTTP response from a ExportLdifWithResponse call
func ParseExportLdifReply(rsp *http.Response) (*ExportLdifReply, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &ExportLdifReply{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	}

	return response, nil
}

// ParseExportOutlineReply parses an HTTP response from a ExportOutlineWithResponse call
func ParseExportOutlineReply(rsp *http.Response) (*ExportOutlineReply, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &ExportOutlineReply{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 400:
		var dest BadRequest
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON400 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	}

	return response, nil
}

// ParseExportYamlReply parses an HTTP response from a ExportYamlWithResponse call
func ParseExportYamlReply(rsp *http.Response) (*ExportYamlReply, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &ExportYamlReply{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 400:
		var dest BadRequest
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON400 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	}

	return response, nil
}

// ParseParseLdifReply parses an HTTP response from a ParseLdifWithResponse call
func ParseParseLdifReply(rsp *http.Response) (*ParseLdifReply, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &ParseLdifReply{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest ImportResult
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 400:
		var dest BadRequest
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON400 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	}

	return response, nil
}

// ParseInventoryValuesReply parses an HTTP response from a InventoryValuesWithResponse call
func ParseInventoryValuesReply(rsp *http.Response) (*InventoryValuesReply, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &InventoryValuesReply{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest InventoryResponse
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 400:
		var dest BadRequest
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON400 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	}

	return response, nil
}

// ParseExpandMembersReply parses an HTTP response from a ExpandMembersWithResponse call
func ParseExpandMembersReply(rsp *http.Response) (*ExpandMembersReply, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &ExpandMembersReply{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest MemberList
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 400:
		var dest BadRequest
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON400 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	}

	return response, nil
}

// ParseBuildPackageReply parses an HTTP response from a BuildPackageWithResponse call
func ParseBuildPackageReply(rsp *http.Response) (*BuildPackageReply, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &BuildPackageReply{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest ChangePackage
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 400:
		var dest BadRequest
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON400 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	}

	return response, nil
}

// ParseInspectPackageReply parses an HTTP response from a InspectPackageWithResponse call
func ParseInspectPackageReply(rsp *http.Response) (*InspectPackageReply, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &InspectPackageReply{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest PackageInspection
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 400:
		var dest BadRequest
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON400 = &dest

	}

	return response, nil
}

// ParseValidatePackageReply parses an HTTP response from a ValidatePackageWithResponse call
func ParseValidatePackageReply(rsp *http.Response) (*ValidatePackageReply, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &ValidatePackageReply{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest PackageValidation
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 400:
		var dest BadRequest
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON400 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	}

	return response, nil
}

// ParsePlanChangesReply parses an HTTP response from a PlanChangesWithResponse call
func ParsePlanChangesReply(rsp *http.Response) (*PlanChangesReply, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &PlanChangesReply{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest Plan
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 400:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON400 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest Forbidden
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 502:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON502 = &dest

	}

	return response, nil
}

// ParsePreflightReply parses an HTTP response from a PreflightWithResponse call
func ParsePreflightReply(rsp *http.Response) (*PreflightReply, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &PreflightReply{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest PreflightReport
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 400:
		var dest BadRequest
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON400 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	}

	return response, nil
}

// ParseInspectRecoveryReply parses an HTTP response from a InspectRecoveryWithResponse call
func ParseInspectRecoveryReply(rsp *http.Response) (*InspectRecoveryReply, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &InspectRecoveryReply{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest RecoveryInspection
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 400:
		var dest BadRequest
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON400 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	}

	return response, nil
}

// ParseListReferencesReply parses an HTTP response from a ListReferencesWithResponse call
func ParseListReferencesReply(rsp *http.Response) (*ListReferencesReply, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &ListReferencesReply{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest ReferenceList
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 400:
		var dest BadRequest
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON400 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	}

	return response, nil
}

// ParseResolveReply parses an HTTP response from a ResolveWithResponse call
func ParseResolveReply(rsp *http.Response) (*ResolveReply, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &ResolveReply{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest ResolveResult
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	}

	return response, nil
}

// ParseGetSchemaReply parses an HTTP response from a GetSchemaWithResponse call
func ParseGetSchemaReply(rsp *http.Response) (*GetSchemaReply, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &GetSchemaReply{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest SchemaView
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	}

	return response, nil
}

// ParseGetAttributeTypeReply parses an HTTP response from a GetAttributeTypeWithResponse call
func ParseGetAttributeTypeReply(rsp *http.Response) (*GetAttributeTypeReply, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &GetAttributeTypeReply{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest AttributeTypeDetail
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	}

	return response, nil
}

// ParseBuildSchemaChangeReply parses an HTTP response from a BuildSchemaChangeWithResponse call
func ParseBuildSchemaChangeReply(rsp *http.Response) (*BuildSchemaChangeReply, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &BuildSchemaChangeReply{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest SchemaChangeBuild
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 400:
		var dest BadRequest
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON400 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	}

	return response, nil
}

// ParseGetObjectClassReply parses an HTTP response from a GetObjectClassWithResponse call
func ParseGetObjectClassReply(rsp *http.Response) (*GetObjectClassReply, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &GetObjectClassReply{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest ObjectClassDetail
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	}

	return response, nil
}

// ParseGetRequirementsReply parses an HTTP response from a GetRequirementsWithResponse call
func ParseGetRequirementsReply(rsp *http.Response) (*GetRequirementsReply, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &GetRequirementsReply{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest RequirementsView
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 400:
		var dest BadRequest
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON400 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	}

	return response, nil
}

// ParseSearchReply parses an HTTP response from a SearchWithResponse call
func ParseSearchReply(rsp *http.Response) (*SearchReply, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &SearchReply{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest SearchResponse
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 400:
		var dest BadRequest
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON400 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest Forbidden
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	}

	return response, nil
}

// ParseDeleteSessionReply parses an HTTP response from a DeleteSessionWithResponse call
func ParseDeleteSessionReply(rsp *http.Response) (*DeleteSessionReply, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &DeleteSessionReply{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	return response, nil
}

// ParseGetSessionReply parses an HTTP response from a GetSessionWithResponse call
func ParseGetSessionReply(rsp *http.Response) (*GetSessionReply, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &GetSessionReply{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest SessionInfo
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	}

	return response, nil
}

// ParseCreateSessionReply parses an HTTP response from a CreateSessionWithResponse call
func ParseCreateSessionReply(rsp *http.Response) (*CreateSessionReply, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &CreateSessionReply{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 201:
		var dest SessionInfo
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON201 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 400:
		var dest BadRequest
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON400 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 502:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON502 = &dest

	}

	return response, nil
}

// ParseCaptureSnapshotReply parses an HTTP response from a CaptureSnapshotWithResponse call
func ParseCaptureSnapshotReply(rsp *http.Response) (*CaptureSnapshotReply, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &CaptureSnapshotReply{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest CaptureSnapshot200JSONResponseBody
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 400:
		var dest BadRequest
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON400 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	}

	return response, nil
}

// ParseInspectSnapshotReply parses an HTTP response from a InspectSnapshotWithResponse call
func ParseInspectSnapshotReply(rsp *http.Response) (*InspectSnapshotReply, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &InspectSnapshotReply{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest SnapshotInspection
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 400:
		var dest BadRequest
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON400 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	}

	return response, nil
}

// ParseGetSourceOfferReply parses an HTTP response from a GetSourceOfferWithResponse call
func ParseGetSourceOfferReply(rsp *http.Response) (*GetSourceOfferReply, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &GetSourceOfferReply{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest SourceOffer
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	}

	return response, nil
}

// ParseListChildrenReply parses an HTTP response from a ListChildrenWithResponse call
func ParseListChildrenReply(rsp *http.Response) (*ListChildrenReply, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &ListChildrenReply{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest TreePage
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest Forbidden
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	}

	return response, nil
}

// ParseListObjectViewsReply parses an HTTP response from a ListObjectViewsWithResponse call
func ParseListObjectViewsReply(rsp *http.Response) (*ListObjectViewsReply, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &ListObjectViewsReply{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest ObjectViewList
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	}

	return response, nil
}

// ServerInterface represents all server handlers.
type ServerInterface interface {
	// ApplyChange Apply a change
	// (POST /changes/apply)
	ApplyChange(c *fiber.Ctx, params ApplyChangeParams) error
	// PreviewChange Render a change as LDIF and as an Ansible task
	// (POST /changes/preview)
	//
	// Deprecated: this operation has been marked as deprecated upstream, but no `x-deprecated-reason` was set
	PreviewChange(c *fiber.Ctx) error
	// ApplyChangeset Apply several changes in order
	// (POST /changeset/apply)
	ApplyChangeset(c *fiber.Ctx) error
	// PreviewChangeset Render several changes as one LDIF document and one playbook
	// (POST /changeset/preview)
	PreviewChangeset(c *fiber.Ctx) error
	// CompareEntries What differs between two entries
	// (GET /compare)
	CompareEntries(c *fiber.Ctx, params CompareEntriesParams) error
	// CountEntries Count the entries under a base, up to a limit
	// (GET /count)
	CountEntries(c *fiber.Ctx, params CountEntriesParams) error
	// DiffStates Compare two directory states
	// (POST /diff)
	DiffStates(c *fiber.Ctx) error
	// GetEntry Read one entry
	// (GET /entry)
	GetEntry(c *fiber.Ctx, params GetEntryParams) error
	// ExportAnsible Export an entry or a subtree as a playbook that enforces it
	// (GET /export/ansible)
	ExportAnsible(c *fiber.Ctx, params ExportAnsibleParams) error
	// ExportLdif Export an entry or a subtree as LDIF
	// (GET /export/ldif)
	ExportLdif(c *fiber.Ctx, params ExportLdifParams) error
	// ExportOutline The shape of a subtree, as a tree
	// (GET /export/outline)
	ExportOutline(c *fiber.Ctx, params ExportOutlineParams) error
	// ExportYaml The subtree as nested YAML
	// (GET /export/yaml)
	ExportYaml(c *fiber.Ctx, params ExportYamlParams) error
	// ParseLdif Parse an LDIF document into change records
	// (POST /import/ldif)
	ParseLdif(c *fiber.Ctx) error
	// InventoryValues What values one attribute holds, and how many entries carry each
	// (POST /inventory)
	InventoryValues(c *fiber.Ctx) error
	// ExpandMembers Who is in this group, following nested groups
	// (GET /members)
	ExpandMembers(c *fiber.Ctx, params ExpandMembersParams) error
	// BuildPackage Build a change package from changes
	// (POST /packages/build)
	BuildPackage(c *fiber.Ctx) error
	// InspectPackage Read a change package and check everything that needs no directory
	// (POST /packages/inspect)
	InspectPackage(c *fiber.Ctx) error
	// ValidatePackage Validate a change package against the current directory
	// (POST /packages/validate)
	ValidatePackage(c *fiber.Ctx) error
	// PlanChanges What a set of changes would do, without doing it
	// (POST /plan)
	PlanChanges(c *fiber.Ctx) error
	// Preflight Report what of an artifact would carry across to this directory
	// (POST /preflight)
	Preflight(c *fiber.Ctx) error
	// InspectRecovery Validate a recovery bundle and turn it into changes to plan
	// (POST /recovery/inspect)
	InspectRecovery(c *fiber.Ctx) error
	// ListReferences The entries that name this one, and the attribute each names it by
	// (GET /references)
	ListReferences(c *fiber.Ctx, params ListReferencesParams) error
	// Resolve What could this input be — an entry, or a search
	// (GET /resolve)
	Resolve(c *fiber.Ctx, params ResolveParams) error
	// GetSchema The whole schema, indexed for browsing
	// (GET /schema)
	GetSchema(c *fiber.Ctx) error
	// GetAttributeType One attribute type, with its cross-links resolved
	// (GET /schema/attributetypes/{name})
	GetAttributeType(c *fiber.Ctx, name string) error
	// BuildSchemaChange Build the modification that installs, replaces or removes a definition
	// (POST /schema/change)
	BuildSchemaChange(c *fiber.Ctx) error
	// GetObjectClass One object class, with its cross-links resolved
	// (GET /schema/objectclasses/{name})
	GetObjectClass(c *fiber.Ctx, name string) error
	// GetRequirements What an entry of these object classes must and may hold
	// (GET /schema/requirements)
	GetRequirements(c *fiber.Ctx, params GetRequirementsParams) error
	// Search Search the directory
	// (POST /search)
	Search(c *fiber.Ctx) error
	// DeleteSession Disconnect
	// (DELETE /session)
	DeleteSession(c *fiber.Ctx) error
	// GetSession Describe the current session
	// (GET /session)
	GetSession(c *fiber.Ctx) error
	// CreateSession Connect and bind to a directory
	// (POST /session)
	CreateSession(c *fiber.Ctx) error
	// CaptureSnapshot Capture a subtree as a versioned snapshot
	// (POST /snapshots/capture)
	CaptureSnapshot(c *fiber.Ctx) error
	// InspectSnapshot Validate a snapshot and describe it
	// (POST /snapshots/inspect)
	InspectSnapshot(c *fiber.Ctx) error
	// GetSourceOffer Where to get the source of this running instance
	// (GET /source)
	GetSourceOffer(c *fiber.Ctx) error
	// ListChildren List the children of an entry
	// (GET /tree)
	ListChildren(c *fiber.Ctx, params ListChildrenParams) error
	// ListObjectViews The object-aware views this directory supports
	// (GET /views)
	ListObjectViews(c *fiber.Ctx) error
}

// ServerInterfaceWrapper converts contexts to parameters.
type ServerInterfaceWrapper struct {
	Handler            ServerInterface
	HandlerMiddlewares []HandlerMiddlewareFunc
}

type MiddlewareFunc fiber.Handler
type HandlerMiddlewareFunc func(c *fiber.Ctx, next fiber.Handler) error

// ApplyChange operation middleware
func (siw *ServerInterfaceWrapper) ApplyChange(c *fiber.Ctx) error {

	var err error
	_ = err

	// Parameter object where we will unmarshal all parameters from the context
	var params ApplyChangeParams

	var query url.Values
	query, err = url.ParseQuery(string(c.Request().URI().QueryString()))
	if err != nil {
		return fiber.NewError(fiber.StatusBadRequest, fmt.Errorf("Invalid format for query string: %w", err).Error())
	}

	// ------------- Optional query parameter "recovery" -------------

	err = runtime.BindQueryParameterWithOptions("form", true, false, "recovery", query, &params.Recovery, runtime.BindQueryParameterOptions{Type: "boolean", Format: ""})
	if err != nil {
		return fiber.NewError(fiber.StatusBadRequest, fmt.Errorf("Invalid format for parameter recovery: %w", err).Error())
	}

	handler := func(c *fiber.Ctx) error {
		return siw.Handler.ApplyChange(c, params)
	}

	for i := len(siw.HandlerMiddlewares) - 1; i >= 0; i-- {
		m := siw.HandlerMiddlewares[i]
		next := handler
		handler = func(c *fiber.Ctx) error {
			return m(c, next)
		}
	}

	return handler(c)
}

// PreviewChange operation middleware
func (siw *ServerInterfaceWrapper) PreviewChange(c *fiber.Ctx) error {

	handler := func(c *fiber.Ctx) error {
		return siw.Handler.PreviewChange(c)
	}

	for i := len(siw.HandlerMiddlewares) - 1; i >= 0; i-- {
		m := siw.HandlerMiddlewares[i]
		next := handler
		handler = func(c *fiber.Ctx) error {
			return m(c, next)
		}
	}

	return handler(c)
}

// ApplyChangeset operation middleware
func (siw *ServerInterfaceWrapper) ApplyChangeset(c *fiber.Ctx) error {

	handler := func(c *fiber.Ctx) error {
		return siw.Handler.ApplyChangeset(c)
	}

	for i := len(siw.HandlerMiddlewares) - 1; i >= 0; i-- {
		m := siw.HandlerMiddlewares[i]
		next := handler
		handler = func(c *fiber.Ctx) error {
			return m(c, next)
		}
	}

	return handler(c)
}

// PreviewChangeset operation middleware
func (siw *ServerInterfaceWrapper) PreviewChangeset(c *fiber.Ctx) error {

	handler := func(c *fiber.Ctx) error {
		return siw.Handler.PreviewChangeset(c)
	}

	for i := len(siw.HandlerMiddlewares) - 1; i >= 0; i-- {
		m := siw.HandlerMiddlewares[i]
		next := handler
		handler = func(c *fiber.Ctx) error {
			return m(c, next)
		}
	}

	return handler(c)
}

// CompareEntries operation middleware
func (siw *ServerInterfaceWrapper) CompareEntries(c *fiber.Ctx) error {

	var err error
	_ = err

	// Parameter object where we will unmarshal all parameters from the context
	var params CompareEntriesParams

	var query url.Values
	query, err = url.ParseQuery(string(c.Request().URI().QueryString()))
	if err != nil {
		return fiber.NewError(fiber.StatusBadRequest, fmt.Errorf("Invalid format for query string: %w", err).Error())
	}

	// ------------- Required query parameter "left" -------------

	err = runtime.BindQueryParameterWithOptions("form", true, true, "left", query, &params.Left, runtime.BindQueryParameterOptions{Type: "string", Format: ""})
	if err != nil {
		return fiber.NewError(fiber.StatusBadRequest, fmt.Errorf("Invalid format for parameter left: %w", err).Error())
	}

	// ------------- Required query parameter "right" -------------

	err = runtime.BindQueryParameterWithOptions("form", true, true, "right", query, &params.Right, runtime.BindQueryParameterOptions{Type: "string", Format: ""})
	if err != nil {
		return fiber.NewError(fiber.StatusBadRequest, fmt.Errorf("Invalid format for parameter right: %w", err).Error())
	}

	// ------------- Optional query parameter "includeOperational" -------------

	err = runtime.BindQueryParameterWithOptions("form", true, false, "includeOperational", query, &params.IncludeOperational, runtime.BindQueryParameterOptions{Type: "boolean", Format: ""})
	if err != nil {
		return fiber.NewError(fiber.StatusBadRequest, fmt.Errorf("Invalid format for parameter includeOperational: %w", err).Error())
	}

	// ------------- Optional query parameter "limit" -------------

	err = runtime.BindQueryParameterWithOptions("form", true, false, "limit", query, &params.Limit, runtime.BindQueryParameterOptions{Type: "integer", Format: ""})
	if err != nil {
		return fiber.NewError(fiber.StatusBadRequest, fmt.Errorf("Invalid format for parameter limit: %w", err).Error())
	}

	handler := func(c *fiber.Ctx) error {
		return siw.Handler.CompareEntries(c, params)
	}

	for i := len(siw.HandlerMiddlewares) - 1; i >= 0; i-- {
		m := siw.HandlerMiddlewares[i]
		next := handler
		handler = func(c *fiber.Ctx) error {
			return m(c, next)
		}
	}

	return handler(c)
}

// CountEntries operation middleware
func (siw *ServerInterfaceWrapper) CountEntries(c *fiber.Ctx) error {

	var err error
	_ = err

	// Parameter object where we will unmarshal all parameters from the context
	var params CountEntriesParams

	var query url.Values
	query, err = url.ParseQuery(string(c.Request().URI().QueryString()))
	if err != nil {
		return fiber.NewError(fiber.StatusBadRequest, fmt.Errorf("Invalid format for query string: %w", err).Error())
	}

	// ------------- Required query parameter "dn" -------------

	err = runtime.BindQueryParameterWithOptions("form", true, true, "dn", query, &params.Dn, runtime.BindQueryParameterOptions{Type: "string", Format: ""})
	if err != nil {
		return fiber.NewError(fiber.StatusBadRequest, fmt.Errorf("Invalid format for parameter dn: %w", err).Error())
	}

	// ------------- Optional query parameter "limit" -------------

	err = runtime.BindQueryParameterWithOptions("form", true, false, "limit", query, &params.Limit, runtime.BindQueryParameterOptions{Type: "integer", Format: ""})
	if err != nil {
		return fiber.NewError(fiber.StatusBadRequest, fmt.Errorf("Invalid format for parameter limit: %w", err).Error())
	}

	handler := func(c *fiber.Ctx) error {
		return siw.Handler.CountEntries(c, params)
	}

	for i := len(siw.HandlerMiddlewares) - 1; i >= 0; i-- {
		m := siw.HandlerMiddlewares[i]
		next := handler
		handler = func(c *fiber.Ctx) error {
			return m(c, next)
		}
	}

	return handler(c)
}

// DiffStates operation middleware
func (siw *ServerInterfaceWrapper) DiffStates(c *fiber.Ctx) error {

	handler := func(c *fiber.Ctx) error {
		return siw.Handler.DiffStates(c)
	}

	for i := len(siw.HandlerMiddlewares) - 1; i >= 0; i-- {
		m := siw.HandlerMiddlewares[i]
		next := handler
		handler = func(c *fiber.Ctx) error {
			return m(c, next)
		}
	}

	return handler(c)
}

// GetEntry operation middleware
func (siw *ServerInterfaceWrapper) GetEntry(c *fiber.Ctx) error {

	var err error
	_ = err

	// Parameter object where we will unmarshal all parameters from the context
	var params GetEntryParams

	var query url.Values
	query, err = url.ParseQuery(string(c.Request().URI().QueryString()))
	if err != nil {
		return fiber.NewError(fiber.StatusBadRequest, fmt.Errorf("Invalid format for query string: %w", err).Error())
	}

	// ------------- Required query parameter "dn" -------------

	err = runtime.BindQueryParameterWithOptions("form", true, true, "dn", query, &params.Dn, runtime.BindQueryParameterOptions{Type: "string", Format: ""})
	if err != nil {
		return fiber.NewError(fiber.StatusBadRequest, fmt.Errorf("Invalid format for parameter dn: %w", err).Error())
	}

	// ------------- Optional query parameter "includeOperational" -------------

	err = runtime.BindQueryParameterWithOptions("form", true, false, "includeOperational", query, &params.IncludeOperational, runtime.BindQueryParameterOptions{Type: "boolean", Format: ""})
	if err != nil {
		return fiber.NewError(fiber.StatusBadRequest, fmt.Errorf("Invalid format for parameter includeOperational: %w", err).Error())
	}

	handler := func(c *fiber.Ctx) error {
		return siw.Handler.GetEntry(c, params)
	}

	for i := len(siw.HandlerMiddlewares) - 1; i >= 0; i-- {
		m := siw.HandlerMiddlewares[i]
		next := handler
		handler = func(c *fiber.Ctx) error {
			return m(c, next)
		}
	}

	return handler(c)
}

// ExportAnsible operation middleware
func (siw *ServerInterfaceWrapper) ExportAnsible(c *fiber.Ctx) error {

	var err error
	_ = err

	// Parameter object where we will unmarshal all parameters from the context
	var params ExportAnsibleParams

	var query url.Values
	query, err = url.ParseQuery(string(c.Request().URI().QueryString()))
	if err != nil {
		return fiber.NewError(fiber.StatusBadRequest, fmt.Errorf("Invalid format for query string: %w", err).Error())
	}

	// ------------- Required query parameter "dn" -------------

	err = runtime.BindQueryParameterWithOptions("form", true, true, "dn", query, &params.Dn, runtime.BindQueryParameterOptions{Type: "string", Format: ""})
	if err != nil {
		return fiber.NewError(fiber.StatusBadRequest, fmt.Errorf("Invalid format for parameter dn: %w", err).Error())
	}

	// ------------- Optional query parameter "scope" -------------

	err = runtime.BindQueryParameterWithOptions("form", true, false, "scope", query, &params.Scope, runtime.BindQueryParameterOptions{Type: "string", Format: ""})
	if err != nil {
		return fiber.NewError(fiber.StatusBadRequest, fmt.Errorf("Invalid format for parameter scope: %w", err).Error())
	}

	// ------------- Optional query parameter "filter" -------------

	err = runtime.BindQueryParameterWithOptions("form", true, false, "filter", query, &params.Filter, runtime.BindQueryParameterOptions{Type: "string", Format: ""})
	if err != nil {
		return fiber.NewError(fiber.StatusBadRequest, fmt.Errorf("Invalid format for parameter filter: %w", err).Error())
	}

	// ------------- Optional query parameter "limit" -------------

	err = runtime.BindQueryParameterWithOptions("form", true, false, "limit", query, &params.Limit, runtime.BindQueryParameterOptions{Type: "integer", Format: ""})
	if err != nil {
		return fiber.NewError(fiber.StatusBadRequest, fmt.Errorf("Invalid format for parameter limit: %w", err).Error())
	}

	handler := func(c *fiber.Ctx) error {
		return siw.Handler.ExportAnsible(c, params)
	}

	for i := len(siw.HandlerMiddlewares) - 1; i >= 0; i-- {
		m := siw.HandlerMiddlewares[i]
		next := handler
		handler = func(c *fiber.Ctx) error {
			return m(c, next)
		}
	}

	return handler(c)
}

// ExportLdif operation middleware
func (siw *ServerInterfaceWrapper) ExportLdif(c *fiber.Ctx) error {

	var err error
	_ = err

	// Parameter object where we will unmarshal all parameters from the context
	var params ExportLdifParams

	var query url.Values
	query, err = url.ParseQuery(string(c.Request().URI().QueryString()))
	if err != nil {
		return fiber.NewError(fiber.StatusBadRequest, fmt.Errorf("Invalid format for query string: %w", err).Error())
	}

	// ------------- Required query parameter "dn" -------------

	err = runtime.BindQueryParameterWithOptions("form", true, true, "dn", query, &params.Dn, runtime.BindQueryParameterOptions{Type: "string", Format: ""})
	if err != nil {
		return fiber.NewError(fiber.StatusBadRequest, fmt.Errorf("Invalid format for parameter dn: %w", err).Error())
	}

	// ------------- Optional query parameter "scope" -------------

	err = runtime.BindQueryParameterWithOptions("form", true, false, "scope", query, &params.Scope, runtime.BindQueryParameterOptions{Type: "string", Format: ""})
	if err != nil {
		return fiber.NewError(fiber.StatusBadRequest, fmt.Errorf("Invalid format for parameter scope: %w", err).Error())
	}

	// ------------- Optional query parameter "includeOperational" -------------

	err = runtime.BindQueryParameterWithOptions("form", true, false, "includeOperational", query, &params.IncludeOperational, runtime.BindQueryParameterOptions{Type: "boolean", Format: ""})
	if err != nil {
		return fiber.NewError(fiber.StatusBadRequest, fmt.Errorf("Invalid format for parameter includeOperational: %w", err).Error())
	}

	// ------------- Optional query parameter "includeSensitive" -------------

	err = runtime.BindQueryParameterWithOptions("form", true, false, "includeSensitive", query, &params.IncludeSensitive, runtime.BindQueryParameterOptions{Type: "boolean", Format: ""})
	if err != nil {
		return fiber.NewError(fiber.StatusBadRequest, fmt.Errorf("Invalid format for parameter includeSensitive: %w", err).Error())
	}

	// ------------- Optional query parameter "filter" -------------

	err = runtime.BindQueryParameterWithOptions("form", true, false, "filter", query, &params.Filter, runtime.BindQueryParameterOptions{Type: "string", Format: ""})
	if err != nil {
		return fiber.NewError(fiber.StatusBadRequest, fmt.Errorf("Invalid format for parameter filter: %w", err).Error())
	}

	// ------------- Optional query parameter "limit" -------------

	err = runtime.BindQueryParameterWithOptions("form", true, false, "limit", query, &params.Limit, runtime.BindQueryParameterOptions{Type: "integer", Format: ""})
	if err != nil {
		return fiber.NewError(fiber.StatusBadRequest, fmt.Errorf("Invalid format for parameter limit: %w", err).Error())
	}

	handler := func(c *fiber.Ctx) error {
		return siw.Handler.ExportLdif(c, params)
	}

	for i := len(siw.HandlerMiddlewares) - 1; i >= 0; i-- {
		m := siw.HandlerMiddlewares[i]
		next := handler
		handler = func(c *fiber.Ctx) error {
			return m(c, next)
		}
	}

	return handler(c)
}

// ExportOutline operation middleware
func (siw *ServerInterfaceWrapper) ExportOutline(c *fiber.Ctx) error {

	var err error
	_ = err

	// Parameter object where we will unmarshal all parameters from the context
	var params ExportOutlineParams

	var query url.Values
	query, err = url.ParseQuery(string(c.Request().URI().QueryString()))
	if err != nil {
		return fiber.NewError(fiber.StatusBadRequest, fmt.Errorf("Invalid format for query string: %w", err).Error())
	}

	// ------------- Required query parameter "dn" -------------

	err = runtime.BindQueryParameterWithOptions("form", true, true, "dn", query, &params.Dn, runtime.BindQueryParameterOptions{Type: "string", Format: ""})
	if err != nil {
		return fiber.NewError(fiber.StatusBadRequest, fmt.Errorf("Invalid format for parameter dn: %w", err).Error())
	}

	// ------------- Optional query parameter "scope" -------------

	err = runtime.BindQueryParameterWithOptions("form", true, false, "scope", query, &params.Scope, runtime.BindQueryParameterOptions{Type: "string", Format: ""})
	if err != nil {
		return fiber.NewError(fiber.StatusBadRequest, fmt.Errorf("Invalid format for parameter scope: %w", err).Error())
	}

	// ------------- Optional query parameter "filter" -------------

	err = runtime.BindQueryParameterWithOptions("form", true, false, "filter", query, &params.Filter, runtime.BindQueryParameterOptions{Type: "string", Format: ""})
	if err != nil {
		return fiber.NewError(fiber.StatusBadRequest, fmt.Errorf("Invalid format for parameter filter: %w", err).Error())
	}

	// ------------- Optional query parameter "limit" -------------

	err = runtime.BindQueryParameterWithOptions("form", true, false, "limit", query, &params.Limit, runtime.BindQueryParameterOptions{Type: "integer", Format: ""})
	if err != nil {
		return fiber.NewError(fiber.StatusBadRequest, fmt.Errorf("Invalid format for parameter limit: %w", err).Error())
	}

	handler := func(c *fiber.Ctx) error {
		return siw.Handler.ExportOutline(c, params)
	}

	for i := len(siw.HandlerMiddlewares) - 1; i >= 0; i-- {
		m := siw.HandlerMiddlewares[i]
		next := handler
		handler = func(c *fiber.Ctx) error {
			return m(c, next)
		}
	}

	return handler(c)
}

// ExportYaml operation middleware
func (siw *ServerInterfaceWrapper) ExportYaml(c *fiber.Ctx) error {

	var err error
	_ = err

	// Parameter object where we will unmarshal all parameters from the context
	var params ExportYamlParams

	var query url.Values
	query, err = url.ParseQuery(string(c.Request().URI().QueryString()))
	if err != nil {
		return fiber.NewError(fiber.StatusBadRequest, fmt.Errorf("Invalid format for query string: %w", err).Error())
	}

	// ------------- Required query parameter "dn" -------------

	err = runtime.BindQueryParameterWithOptions("form", true, true, "dn", query, &params.Dn, runtime.BindQueryParameterOptions{Type: "string", Format: ""})
	if err != nil {
		return fiber.NewError(fiber.StatusBadRequest, fmt.Errorf("Invalid format for parameter dn: %w", err).Error())
	}

	// ------------- Optional query parameter "scope" -------------

	err = runtime.BindQueryParameterWithOptions("form", true, false, "scope", query, &params.Scope, runtime.BindQueryParameterOptions{Type: "string", Format: ""})
	if err != nil {
		return fiber.NewError(fiber.StatusBadRequest, fmt.Errorf("Invalid format for parameter scope: %w", err).Error())
	}

	// ------------- Optional query parameter "filter" -------------

	err = runtime.BindQueryParameterWithOptions("form", true, false, "filter", query, &params.Filter, runtime.BindQueryParameterOptions{Type: "string", Format: ""})
	if err != nil {
		return fiber.NewError(fiber.StatusBadRequest, fmt.Errorf("Invalid format for parameter filter: %w", err).Error())
	}

	// ------------- Optional query parameter "includeOperational" -------------

	err = runtime.BindQueryParameterWithOptions("form", true, false, "includeOperational", query, &params.IncludeOperational, runtime.BindQueryParameterOptions{Type: "boolean", Format: ""})
	if err != nil {
		return fiber.NewError(fiber.StatusBadRequest, fmt.Errorf("Invalid format for parameter includeOperational: %w", err).Error())
	}

	// ------------- Optional query parameter "includeSensitive" -------------

	err = runtime.BindQueryParameterWithOptions("form", true, false, "includeSensitive", query, &params.IncludeSensitive, runtime.BindQueryParameterOptions{Type: "boolean", Format: ""})
	if err != nil {
		return fiber.NewError(fiber.StatusBadRequest, fmt.Errorf("Invalid format for parameter includeSensitive: %w", err).Error())
	}

	// ------------- Optional query parameter "limit" -------------

	err = runtime.BindQueryParameterWithOptions("form", true, false, "limit", query, &params.Limit, runtime.BindQueryParameterOptions{Type: "integer", Format: ""})
	if err != nil {
		return fiber.NewError(fiber.StatusBadRequest, fmt.Errorf("Invalid format for parameter limit: %w", err).Error())
	}

	handler := func(c *fiber.Ctx) error {
		return siw.Handler.ExportYaml(c, params)
	}

	for i := len(siw.HandlerMiddlewares) - 1; i >= 0; i-- {
		m := siw.HandlerMiddlewares[i]
		next := handler
		handler = func(c *fiber.Ctx) error {
			return m(c, next)
		}
	}

	return handler(c)
}

// ParseLdif operation middleware
func (siw *ServerInterfaceWrapper) ParseLdif(c *fiber.Ctx) error {

	handler := func(c *fiber.Ctx) error {
		return siw.Handler.ParseLdif(c)
	}

	for i := len(siw.HandlerMiddlewares) - 1; i >= 0; i-- {
		m := siw.HandlerMiddlewares[i]
		next := handler
		handler = func(c *fiber.Ctx) error {
			return m(c, next)
		}
	}

	return handler(c)
}

// InventoryValues operation middleware
func (siw *ServerInterfaceWrapper) InventoryValues(c *fiber.Ctx) error {

	handler := func(c *fiber.Ctx) error {
		return siw.Handler.InventoryValues(c)
	}

	for i := len(siw.HandlerMiddlewares) - 1; i >= 0; i-- {
		m := siw.HandlerMiddlewares[i]
		next := handler
		handler = func(c *fiber.Ctx) error {
			return m(c, next)
		}
	}

	return handler(c)
}

// ExpandMembers operation middleware
func (siw *ServerInterfaceWrapper) ExpandMembers(c *fiber.Ctx) error {

	var err error
	_ = err

	// Parameter object where we will unmarshal all parameters from the context
	var params ExpandMembersParams

	var query url.Values
	query, err = url.ParseQuery(string(c.Request().URI().QueryString()))
	if err != nil {
		return fiber.NewError(fiber.StatusBadRequest, fmt.Errorf("Invalid format for query string: %w", err).Error())
	}

	// ------------- Required query parameter "dn" -------------

	err = runtime.BindQueryParameterWithOptions("form", true, true, "dn", query, &params.Dn, runtime.BindQueryParameterOptions{Type: "string", Format: ""})
	if err != nil {
		return fiber.NewError(fiber.StatusBadRequest, fmt.Errorf("Invalid format for parameter dn: %w", err).Error())
	}

	// ------------- Optional query parameter "limit" -------------

	err = runtime.BindQueryParameterWithOptions("form", true, false, "limit", query, &params.Limit, runtime.BindQueryParameterOptions{Type: "integer", Format: ""})
	if err != nil {
		return fiber.NewError(fiber.StatusBadRequest, fmt.Errorf("Invalid format for parameter limit: %w", err).Error())
	}

	handler := func(c *fiber.Ctx) error {
		return siw.Handler.ExpandMembers(c, params)
	}

	for i := len(siw.HandlerMiddlewares) - 1; i >= 0; i-- {
		m := siw.HandlerMiddlewares[i]
		next := handler
		handler = func(c *fiber.Ctx) error {
			return m(c, next)
		}
	}

	return handler(c)
}

// BuildPackage operation middleware
func (siw *ServerInterfaceWrapper) BuildPackage(c *fiber.Ctx) error {

	handler := func(c *fiber.Ctx) error {
		return siw.Handler.BuildPackage(c)
	}

	for i := len(siw.HandlerMiddlewares) - 1; i >= 0; i-- {
		m := siw.HandlerMiddlewares[i]
		next := handler
		handler = func(c *fiber.Ctx) error {
			return m(c, next)
		}
	}

	return handler(c)
}

// InspectPackage operation middleware
func (siw *ServerInterfaceWrapper) InspectPackage(c *fiber.Ctx) error {

	handler := func(c *fiber.Ctx) error {
		return siw.Handler.InspectPackage(c)
	}

	for i := len(siw.HandlerMiddlewares) - 1; i >= 0; i-- {
		m := siw.HandlerMiddlewares[i]
		next := handler
		handler = func(c *fiber.Ctx) error {
			return m(c, next)
		}
	}

	return handler(c)
}

// ValidatePackage operation middleware
func (siw *ServerInterfaceWrapper) ValidatePackage(c *fiber.Ctx) error {

	handler := func(c *fiber.Ctx) error {
		return siw.Handler.ValidatePackage(c)
	}

	for i := len(siw.HandlerMiddlewares) - 1; i >= 0; i-- {
		m := siw.HandlerMiddlewares[i]
		next := handler
		handler = func(c *fiber.Ctx) error {
			return m(c, next)
		}
	}

	return handler(c)
}

// PlanChanges operation middleware
func (siw *ServerInterfaceWrapper) PlanChanges(c *fiber.Ctx) error {

	handler := func(c *fiber.Ctx) error {
		return siw.Handler.PlanChanges(c)
	}

	for i := len(siw.HandlerMiddlewares) - 1; i >= 0; i-- {
		m := siw.HandlerMiddlewares[i]
		next := handler
		handler = func(c *fiber.Ctx) error {
			return m(c, next)
		}
	}

	return handler(c)
}

// Preflight operation middleware
func (siw *ServerInterfaceWrapper) Preflight(c *fiber.Ctx) error {

	handler := func(c *fiber.Ctx) error {
		return siw.Handler.Preflight(c)
	}

	for i := len(siw.HandlerMiddlewares) - 1; i >= 0; i-- {
		m := siw.HandlerMiddlewares[i]
		next := handler
		handler = func(c *fiber.Ctx) error {
			return m(c, next)
		}
	}

	return handler(c)
}

// InspectRecovery operation middleware
func (siw *ServerInterfaceWrapper) InspectRecovery(c *fiber.Ctx) error {

	handler := func(c *fiber.Ctx) error {
		return siw.Handler.InspectRecovery(c)
	}

	for i := len(siw.HandlerMiddlewares) - 1; i >= 0; i-- {
		m := siw.HandlerMiddlewares[i]
		next := handler
		handler = func(c *fiber.Ctx) error {
			return m(c, next)
		}
	}

	return handler(c)
}

// ListReferences operation middleware
func (siw *ServerInterfaceWrapper) ListReferences(c *fiber.Ctx) error {

	var err error
	_ = err

	// Parameter object where we will unmarshal all parameters from the context
	var params ListReferencesParams

	var query url.Values
	query, err = url.ParseQuery(string(c.Request().URI().QueryString()))
	if err != nil {
		return fiber.NewError(fiber.StatusBadRequest, fmt.Errorf("Invalid format for query string: %w", err).Error())
	}

	// ------------- Required query parameter "dn" -------------

	err = runtime.BindQueryParameterWithOptions("form", true, true, "dn", query, &params.Dn, runtime.BindQueryParameterOptions{Type: "string", Format: ""})
	if err != nil {
		return fiber.NewError(fiber.StatusBadRequest, fmt.Errorf("Invalid format for parameter dn: %w", err).Error())
	}

	// ------------- Optional query parameter "limit" -------------

	err = runtime.BindQueryParameterWithOptions("form", true, false, "limit", query, &params.Limit, runtime.BindQueryParameterOptions{Type: "integer", Format: ""})
	if err != nil {
		return fiber.NewError(fiber.StatusBadRequest, fmt.Errorf("Invalid format for parameter limit: %w", err).Error())
	}

	handler := func(c *fiber.Ctx) error {
		return siw.Handler.ListReferences(c, params)
	}

	for i := len(siw.HandlerMiddlewares) - 1; i >= 0; i-- {
		m := siw.HandlerMiddlewares[i]
		next := handler
		handler = func(c *fiber.Ctx) error {
			return m(c, next)
		}
	}

	return handler(c)
}

// Resolve operation middleware
func (siw *ServerInterfaceWrapper) Resolve(c *fiber.Ctx) error {

	var err error
	_ = err

	// Parameter object where we will unmarshal all parameters from the context
	var params ResolveParams

	var query url.Values
	query, err = url.ParseQuery(string(c.Request().URI().QueryString()))
	if err != nil {
		return fiber.NewError(fiber.StatusBadRequest, fmt.Errorf("Invalid format for query string: %w", err).Error())
	}

	// ------------- Required query parameter "q" -------------

	err = runtime.BindQueryParameterWithOptions("form", true, true, "q", query, &params.Q, runtime.BindQueryParameterOptions{Type: "string", Format: ""})
	if err != nil {
		return fiber.NewError(fiber.StatusBadRequest, fmt.Errorf("Invalid format for parameter q: %w", err).Error())
	}

	handler := func(c *fiber.Ctx) error {
		return siw.Handler.Resolve(c, params)
	}

	for i := len(siw.HandlerMiddlewares) - 1; i >= 0; i-- {
		m := siw.HandlerMiddlewares[i]
		next := handler
		handler = func(c *fiber.Ctx) error {
			return m(c, next)
		}
	}

	return handler(c)
}

// GetSchema operation middleware
func (siw *ServerInterfaceWrapper) GetSchema(c *fiber.Ctx) error {

	handler := func(c *fiber.Ctx) error {
		return siw.Handler.GetSchema(c)
	}

	for i := len(siw.HandlerMiddlewares) - 1; i >= 0; i-- {
		m := siw.HandlerMiddlewares[i]
		next := handler
		handler = func(c *fiber.Ctx) error {
			return m(c, next)
		}
	}

	return handler(c)
}

// GetAttributeType operation middleware
func (siw *ServerInterfaceWrapper) GetAttributeType(c *fiber.Ctx) error {

	var err error
	_ = err

	// ------------- Path parameter "name" -------------
	var name string

	err = runtime.BindStyledParameterWithOptions("simple", "name", c.Params("name"), &name, runtime.BindStyledParameterOptions{ParamLocation: runtime.ParamLocationPath, Explode: false, Required: true, Type: "string", Format: ""})
	if err != nil {
		return fiber.NewError(fiber.StatusBadRequest, fmt.Errorf("Invalid format for parameter name: %w", err).Error())
	}

	handler := func(c *fiber.Ctx) error {
		return siw.Handler.GetAttributeType(c, name)
	}

	for i := len(siw.HandlerMiddlewares) - 1; i >= 0; i-- {
		m := siw.HandlerMiddlewares[i]
		next := handler
		handler = func(c *fiber.Ctx) error {
			return m(c, next)
		}
	}

	return handler(c)
}

// BuildSchemaChange operation middleware
func (siw *ServerInterfaceWrapper) BuildSchemaChange(c *fiber.Ctx) error {

	handler := func(c *fiber.Ctx) error {
		return siw.Handler.BuildSchemaChange(c)
	}

	for i := len(siw.HandlerMiddlewares) - 1; i >= 0; i-- {
		m := siw.HandlerMiddlewares[i]
		next := handler
		handler = func(c *fiber.Ctx) error {
			return m(c, next)
		}
	}

	return handler(c)
}

// GetObjectClass operation middleware
func (siw *ServerInterfaceWrapper) GetObjectClass(c *fiber.Ctx) error {

	var err error
	_ = err

	// ------------- Path parameter "name" -------------
	var name string

	err = runtime.BindStyledParameterWithOptions("simple", "name", c.Params("name"), &name, runtime.BindStyledParameterOptions{ParamLocation: runtime.ParamLocationPath, Explode: false, Required: true, Type: "string", Format: ""})
	if err != nil {
		return fiber.NewError(fiber.StatusBadRequest, fmt.Errorf("Invalid format for parameter name: %w", err).Error())
	}

	handler := func(c *fiber.Ctx) error {
		return siw.Handler.GetObjectClass(c, name)
	}

	for i := len(siw.HandlerMiddlewares) - 1; i >= 0; i-- {
		m := siw.HandlerMiddlewares[i]
		next := handler
		handler = func(c *fiber.Ctx) error {
			return m(c, next)
		}
	}

	return handler(c)
}

// GetRequirements operation middleware
func (siw *ServerInterfaceWrapper) GetRequirements(c *fiber.Ctx) error {

	var err error
	_ = err

	// Parameter object where we will unmarshal all parameters from the context
	var params GetRequirementsParams

	var query url.Values
	query, err = url.ParseQuery(string(c.Request().URI().QueryString()))
	if err != nil {
		return fiber.NewError(fiber.StatusBadRequest, fmt.Errorf("Invalid format for query string: %w", err).Error())
	}

	// ------------- Required query parameter "class" -------------

	err = runtime.BindQueryParameterWithOptions("form", true, true, "class", query, &params.Class, runtime.BindQueryParameterOptions{Type: "array", Format: ""})
	if err != nil {
		return fiber.NewError(fiber.StatusBadRequest, fmt.Errorf("Invalid format for parameter class: %w", err).Error())
	}

	handler := func(c *fiber.Ctx) error {
		return siw.Handler.GetRequirements(c, params)
	}

	for i := len(siw.HandlerMiddlewares) - 1; i >= 0; i-- {
		m := siw.HandlerMiddlewares[i]
		next := handler
		handler = func(c *fiber.Ctx) error {
			return m(c, next)
		}
	}

	return handler(c)
}

// Search operation middleware
func (siw *ServerInterfaceWrapper) Search(c *fiber.Ctx) error {

	handler := func(c *fiber.Ctx) error {
		return siw.Handler.Search(c)
	}

	for i := len(siw.HandlerMiddlewares) - 1; i >= 0; i-- {
		m := siw.HandlerMiddlewares[i]
		next := handler
		handler = func(c *fiber.Ctx) error {
			return m(c, next)
		}
	}

	return handler(c)
}

// DeleteSession operation middleware
func (siw *ServerInterfaceWrapper) DeleteSession(c *fiber.Ctx) error {

	handler := func(c *fiber.Ctx) error {
		return siw.Handler.DeleteSession(c)
	}

	for i := len(siw.HandlerMiddlewares) - 1; i >= 0; i-- {
		m := siw.HandlerMiddlewares[i]
		next := handler
		handler = func(c *fiber.Ctx) error {
			return m(c, next)
		}
	}

	return handler(c)
}

// GetSession operation middleware
func (siw *ServerInterfaceWrapper) GetSession(c *fiber.Ctx) error {

	handler := func(c *fiber.Ctx) error {
		return siw.Handler.GetSession(c)
	}

	for i := len(siw.HandlerMiddlewares) - 1; i >= 0; i-- {
		m := siw.HandlerMiddlewares[i]
		next := handler
		handler = func(c *fiber.Ctx) error {
			return m(c, next)
		}
	}

	return handler(c)
}

// CreateSession operation middleware
func (siw *ServerInterfaceWrapper) CreateSession(c *fiber.Ctx) error {

	handler := func(c *fiber.Ctx) error {
		return siw.Handler.CreateSession(c)
	}

	for i := len(siw.HandlerMiddlewares) - 1; i >= 0; i-- {
		m := siw.HandlerMiddlewares[i]
		next := handler
		handler = func(c *fiber.Ctx) error {
			return m(c, next)
		}
	}

	return handler(c)
}

// CaptureSnapshot operation middleware
func (siw *ServerInterfaceWrapper) CaptureSnapshot(c *fiber.Ctx) error {

	handler := func(c *fiber.Ctx) error {
		return siw.Handler.CaptureSnapshot(c)
	}

	for i := len(siw.HandlerMiddlewares) - 1; i >= 0; i-- {
		m := siw.HandlerMiddlewares[i]
		next := handler
		handler = func(c *fiber.Ctx) error {
			return m(c, next)
		}
	}

	return handler(c)
}

// InspectSnapshot operation middleware
func (siw *ServerInterfaceWrapper) InspectSnapshot(c *fiber.Ctx) error {

	handler := func(c *fiber.Ctx) error {
		return siw.Handler.InspectSnapshot(c)
	}

	for i := len(siw.HandlerMiddlewares) - 1; i >= 0; i-- {
		m := siw.HandlerMiddlewares[i]
		next := handler
		handler = func(c *fiber.Ctx) error {
			return m(c, next)
		}
	}

	return handler(c)
}

// GetSourceOffer operation middleware
func (siw *ServerInterfaceWrapper) GetSourceOffer(c *fiber.Ctx) error {

	handler := func(c *fiber.Ctx) error {
		return siw.Handler.GetSourceOffer(c)
	}

	for i := len(siw.HandlerMiddlewares) - 1; i >= 0; i-- {
		m := siw.HandlerMiddlewares[i]
		next := handler
		handler = func(c *fiber.Ctx) error {
			return m(c, next)
		}
	}

	return handler(c)
}

// ListChildren operation middleware
func (siw *ServerInterfaceWrapper) ListChildren(c *fiber.Ctx) error {

	var err error
	_ = err

	// Parameter object where we will unmarshal all parameters from the context
	var params ListChildrenParams

	var query url.Values
	query, err = url.ParseQuery(string(c.Request().URI().QueryString()))
	if err != nil {
		return fiber.NewError(fiber.StatusBadRequest, fmt.Errorf("Invalid format for query string: %w", err).Error())
	}

	// ------------- Optional query parameter "dn" -------------

	err = runtime.BindQueryParameterWithOptions("form", true, false, "dn", query, &params.Dn, runtime.BindQueryParameterOptions{Type: "string", Format: ""})
	if err != nil {
		return fiber.NewError(fiber.StatusBadRequest, fmt.Errorf("Invalid format for parameter dn: %w", err).Error())
	}

	// ------------- Optional query parameter "limit" -------------

	err = runtime.BindQueryParameterWithOptions("form", true, false, "limit", query, &params.Limit, runtime.BindQueryParameterOptions{Type: "integer", Format: ""})
	if err != nil {
		return fiber.NewError(fiber.StatusBadRequest, fmt.Errorf("Invalid format for parameter limit: %w", err).Error())
	}

	// ------------- Optional query parameter "cookie" -------------

	err = runtime.BindQueryParameterWithOptions("form", true, false, "cookie", query, &params.Cookie, runtime.BindQueryParameterOptions{Type: "string", Format: ""})
	if err != nil {
		return fiber.NewError(fiber.StatusBadRequest, fmt.Errorf("Invalid format for parameter cookie: %w", err).Error())
	}

	handler := func(c *fiber.Ctx) error {
		return siw.Handler.ListChildren(c, params)
	}

	for i := len(siw.HandlerMiddlewares) - 1; i >= 0; i-- {
		m := siw.HandlerMiddlewares[i]
		next := handler
		handler = func(c *fiber.Ctx) error {
			return m(c, next)
		}
	}

	return handler(c)
}

// ListObjectViews operation middleware
func (siw *ServerInterfaceWrapper) ListObjectViews(c *fiber.Ctx) error {

	handler := func(c *fiber.Ctx) error {
		return siw.Handler.ListObjectViews(c)
	}

	for i := len(siw.HandlerMiddlewares) - 1; i >= 0; i-- {
		m := siw.HandlerMiddlewares[i]
		next := handler
		handler = func(c *fiber.Ctx) error {
			return m(c, next)
		}
	}

	return handler(c)
}

// FiberServerOptions provides options for the Fiber server.
type FiberServerOptions struct {
	BaseURL            string
	Middlewares        []MiddlewareFunc
	HandlerMiddlewares []HandlerMiddlewareFunc
}

// RegisterHandlers creates http.Handler with routing matching OpenAPI spec.
func RegisterHandlers(router fiber.Router, si ServerInterface) {
	RegisterHandlersWithOptions(router, si, FiberServerOptions{})
}

// RegisterHandlersWithOptions creates http.Handler with additional options
func RegisterHandlersWithOptions(router fiber.Router, si ServerInterface, options FiberServerOptions) {
	wrapper := ServerInterfaceWrapper{
		Handler:            si,
		HandlerMiddlewares: options.HandlerMiddlewares,
	}

	for _, m := range options.Middlewares {
		router.Use(fiber.Handler(m))
	}

	router.Delete(options.BaseURL+"/session", wrapper.DeleteSession)

	router.Get(options.BaseURL+"/session", wrapper.GetSession)

	router.Post(options.BaseURL+"/session", wrapper.CreateSession)

	router.Get(options.BaseURL+"/tree", wrapper.ListChildren)

	router.Get(options.BaseURL+"/count", wrapper.CountEntries)

	router.Get(options.BaseURL+"/compare", wrapper.CompareEntries)

	router.Get(options.BaseURL+"/entry", wrapper.GetEntry)

	router.Post(options.BaseURL+"/search", wrapper.Search)

	router.Get(options.BaseURL+"/views", wrapper.ListObjectViews)

	router.Post(options.BaseURL+"/inventory", wrapper.InventoryValues)

	router.Get(options.BaseURL+"/members", wrapper.ExpandMembers)

	router.Get(options.BaseURL+"/references", wrapper.ListReferences)

	router.Get(options.BaseURL+"/resolve", wrapper.Resolve)

	router.Get(options.BaseURL+"/schema", wrapper.GetSchema)

	router.Get(options.BaseURL+"/schema/objectclasses/:name", wrapper.GetObjectClass)

	router.Get(options.BaseURL+"/schema/attributetypes/:name", wrapper.GetAttributeType)

	router.Get(options.BaseURL+"/schema/requirements", wrapper.GetRequirements)

	router.Post(options.BaseURL+"/schema/change", wrapper.BuildSchemaChange)

	router.Post(options.BaseURL+"/changes/preview", wrapper.PreviewChange)

	router.Post(options.BaseURL+"/changes/apply", wrapper.ApplyChange)

	router.Post(options.BaseURL+"/changeset/preview", wrapper.PreviewChangeset)

	router.Post(options.BaseURL+"/changeset/apply", wrapper.ApplyChangeset)

	router.Post(options.BaseURL+"/recovery/inspect", wrapper.InspectRecovery)

	router.Post(options.BaseURL+"/plan", wrapper.PlanChanges)

	router.Post(options.BaseURL+"/snapshots/capture", wrapper.CaptureSnapshot)

	router.Post(options.BaseURL+"/snapshots/inspect", wrapper.InspectSnapshot)

	router.Post(options.BaseURL+"/diff", wrapper.DiffStates)

	router.Post(options.BaseURL+"/packages/build", wrapper.BuildPackage)

	router.Post(options.BaseURL+"/packages/inspect", wrapper.InspectPackage)

	router.Post(options.BaseURL+"/packages/validate", wrapper.ValidatePackage)

	router.Post(options.BaseURL+"/preflight", wrapper.Preflight)

	router.Get(options.BaseURL+"/export/ldif", wrapper.ExportLdif)

	router.Get(options.BaseURL+"/export/ansible", wrapper.ExportAnsible)

	router.Get(options.BaseURL+"/export/outline", wrapper.ExportOutline)

	router.Get(options.BaseURL+"/export/yaml", wrapper.ExportYaml)

	router.Post(options.BaseURL+"/import/ldif", wrapper.ParseLdif)

	router.Get(options.BaseURL+"/source", wrapper.GetSourceOffer)

}

// Base64 encoded, compressed with deflate, json marshaled OpenAPI spec.
// Stored as a slice of fixed-width chunks rather than one concatenated
// const string: with thousands of chunks the chained `+` fold is several
// times slower for the Go compiler than parsing a slice literal.
var swaggerSpec = []string{
	"7P37chw3mjcI3wqW+0W4rSjSlu3ud5qK+YMW5W7uWKKClNwz8ZZDhaoEWRhlAdUJpKiaDkfsRewV7pV8",
	"8ZwAZFZmHXSwet59IzraYmUmEonDg+f4+/3jZOFXa++Mi+Hk/B8njQlr74LBP37U1Y35e2tChL8W3kXj",
	"8J96va7tQkfr3Tf/GbyD38JiaVYa/vX/a8zdyfnJ//lNbvobuhq+edY0vjn57bffJieVCYvGrqGRk/OT",
	"V0ujGnqZetBBrXR955uVqc5Ofpuc/OSbua0q436fjlS2MYvom41qzF0bTKXi0ii/Ng2+St35RsWlDWpu",
	"HXXwhY8/+dZVn79/L7wK7WKpjIvNRvlGVebOOgtXsSevnW7j0jf2v0z1u4xWY5QNynkVTAjWuwl0yka1",
	"1EGZ92vb4Bz+NuFX4cq6WK/rzY0JbY0dWzcwttGakLpJnY+btTk5P5l7Xxvt4PMq7PX2lOFwfBXU5Qul",
	"76JpcMYWS+3uzUQ9LO1iqSp7d2eagLOnVWOcXpmzk4m8JMTGunt4R13Zu+Lt+YLz0Wy//mVjgnFRPSyN",
	"K16Ly1gvFmYdTaXmbVQXdWUatfBtXSnno1p4d2eb1dQ94ChG+QxlXGUq1a7Ppm6of41Z+Hem2eBg1fX1",
	"3cn5/9w9fzf8xI+tq2pz8tuvw9uPblJzvGtCH+Qdf0l4ayoYuzN1UUH3rFOPz/4MA/j+9N6fci9hXZ3d",
	"6IfnJgR9b6C7IfrGVJcDE/e33nfrRWx1XW/UQq+Mil41JsSJDKyOtNAiPnH5Av4zdeVgV1VjAm5XfzZ1",
	"U3dR7OSV3ijsidIO7jQVv7R1MCualwdMJCyMc4WTMnXapbW19gH3mdIr7+6VjUEFO6+tu8dFtQpqrZuo",
	"/B11FpqZUCeDad7BS0Kw9y5g/3NjrlKNeWhsNHTl5vLFmboxa99E6+6pARaNpoLvtjwaCx2MCsZV9NxC",
	"1zUsfK+kz+WgTV2EDxpZUqFdrTStqN41XG9/b2Ebn5z/z7Q5cSf+mlry8/80iwgtXcTY2HkbzVO/WuvG",
	"spzpbvHa3MV9MoeeN9WtrXAZwXAO730tr1TFtYny+N+grFvUbWWqMzVrg2meQjfuQA6aJ3PrdLOZwSxX",
	"W1dnSsPqfPCq8Q9hgivPu7ERbOz98uhvClHHNmx/1ezBxuXS1NVMrYx2Qc19JJFvTVBLX9OJlD8c+m/p",
	"5MS5XvCLJmpuFroNBq7aAKsFlt07c6au8AetqA+wamFB+wenGg1LBRaMdmoW9MrMFHRIaXVX63s1N8FW",
	"QRvPTSam3IETHx1p0J9i1iMahMMOtQmzM6KTy90L3m/2CRc8COddd9hCO8GO34bCIRPKByTXBd2M4M90",
	"P7lIxSWyMKoZQcHk0keJcuAI0z6XuUlm6/YAPBeUtvxd0TJSWMZ4A6/Nd1+fq8zX2sPRg9D3AUB6HTT/",
	"dEL2EOEIgIkQAvQQ7BGgIet6E2xQXcdhMfaAEoDTjtGFEgkO/WcJBS5FuxggiPLZcQzxa0NnJNOIDIwk",
	"iBSw+Atrn80Tss1LjVvxOT9kxk4db+7JPvNcu413aDKKnbpYpn1qF6bTdbFsfvv1t///AA==",
}

// decodeSpec returns the embedded OpenAPI spec as raw JSON bytes,
// after base64-decoding and flate-decompressing the embedded blob.
func decodeSpec() ([]byte, error) {
	encoded := strings.Join(swaggerSpec, "")
	compressed, err := base64.StdEncoding.DecodeString(encoded)
	if err != nil {
		return nil, fmt.Errorf("error base64 decoding spec: %w", err)
	}
	zr := flate.NewReader(bytes.NewReader(compressed))
	var buf bytes.Buffer
	if _, err := buf.ReadFrom(zr); err != nil {
		return nil, fmt.Errorf("read flate: %w", err)
	}
	if err := zr.Close(); err != nil {
		return nil, fmt.Errorf("close flate reader: %w", err)
	}

	return buf.Bytes(), nil
}

var rawSpec = decodeSpecCached()

// a naive cache of the decoded OpenAPI spec
func decodeSpecCached() func() ([]byte, error) {
	data, err := decodeSpec()
	return func() ([]byte, error) {
		return data, err
	}
}

// Constructs a synthetic filesystem for resolving external references when loading openapi specifications.
func PathToRawSpec(pathToFile string) map[string]func() ([]byte, error) {
	res := make(map[string]func() ([]byte, error))
	if len(pathToFile) > 0 {
		res[pathToFile] = rawSpec
	}

	return res
}

// GetSpec returns the OpenAPI specification corresponding to the generated
// code in this file. External references in the spec are resolved through
// PathToRawSpec; externally-referenced files must be embedded in their
// corresponding Go packages (via the import-mapping feature). URL-based
// external refs are not supported.
func GetSpec() (swagger *openapi3.T, err error) {
	resolvePath := PathToRawSpec("")

	loader := openapi3.NewLoader()
	loader.IsExternalRefsAllowed = true
	loader.ReadFromURIFunc = func(loader *openapi3.Loader, url *url.URL) ([]byte, error) {
		pathToFile := url.String()
		pathToFile = path.Clean(pathToFile)
		getSpec, ok := resolvePath[pathToFile]
		if !ok {
			err1 := fmt.Errorf("path not found: %s", pathToFile)
			return nil, err1
		}
		return getSpec()
	}
	var specData []byte
	specData, err = rawSpec()
	if err != nil {
		return
	}
	swagger, err = loader.LoadFromData(specData)
	if err != nil {
		return
	}
	return
}

// GetSpecJSON returns the raw JSON bytes of the embedded OpenAPI
// specification: decompressed but not unmarshaled. External references
// are not resolved here; the bytes are the spec exactly as embedded by
// codegen. The result is cached at package init time, so repeated calls
// are cheap.
func GetSpecJSON() ([]byte, error) {
	return rawSpec()
}

// GetSwagger returns the OpenAPI specification corresponding to the
// generated code in this file.
//
// Deprecated: GetSwagger predates kin-openapi renaming openapi3.Swagger
// to openapi3.T. Use [GetSpec] instead. This wrapper is retained for
// backwards compatibility.
func GetSwagger() (*openapi3.T, error) {
	return GetSpec()
}
