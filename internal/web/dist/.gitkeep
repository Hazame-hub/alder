The Vite build writes into this directory and it is embedded into the binary.
Keeping the directory in git means "go build" works on a fresh clone before
"make web" has ever run; the build output itself is gitignored.
